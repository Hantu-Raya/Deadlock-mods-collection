diff --git a/hp_colors/panorama/scripts/anita_persist_loader.js b/hp_colors/panorama/scripts/anita_persist_loader.js
index 2db4898..52a50cf 100644
--- a/hp_colors/panorama/scripts/anita_persist_loader.js
+++ b/hp_colors/panorama/scripts/anita_persist_loader.js
@@ -1,643 +1,377 @@
-'use strict';
-(function () {
-
-  var TITLE = "HP Colors";
-  var STORAGE_NAMESPACE = "hp_colors";
-  var STORAGE_KEY = "anita_v1_hp_colors";
-  var PERSIST_DEBOUNCE_SEC = 0.35;
-  var HP_COMPACT_PERSIST_VERSION = 1;
-  var HP_PERSIST_ALIASES = {
-    hp_enabled: "e",
-    hp_mode: "m",
-    hp_low_threshold: "l",
-    hp_high_threshold: "h",
-    hp_bg_visible: "b",
-    hp_team_colors: "t",
-    hp_color_low: "cl",
-    hp_color_mid: "cm",
-    hp_color_high: "ch",
-    hp_counter_size: "s",
-    hp_counter_position: "p",
-    hp_text_color_mode: "tm",
-    hp_level_number_visible: "lnv",
-    hp_text_color_low: "tl",
-    hp_text_color_mid: "ti",
-    hp_text_color_high: "th",
-    hp_pulse_bpm: "bp",
-    hp_pulse_intensity: "pi",
-    hp_pulse_enabled: "pe",
-    hp_pulse_text_enabled: "pte",
-    hp_pulse_text_scale: "pts",
-    hp_pulse_text_position: "ptp",
-    hp_pulse_hide_bar: "phb",
-    hp_skip_buildings: "sb",
-    hp_pulse_threshold: "pt",
-    hp_friend_enabled: "fe",
-    hp_friend_pulse_enabled: "fpe",
-    hp_friend_pulse_bpm: "fpb",
-    hp_friend_pulse_intensity: "fpi",
-    hp_friend_pulse_threshold: "fpt",
-    hp_friend_color_low: "fcl",
-    hp_friend_color_mid: "fcm",
-    hp_friend_color_high: "fch",
-    hp_friend_pulse_color_enabled: "fpce",
-    hp_friend_pulse_color: "fpc",
-    hp_kill_zone_enabled: "kze",
-    hp_kill_zone_threshold: "kzt",
-    hp_kill_zone_color: "kzc",
-    hp_kill_zone_width: "kzw"
-  };
-  var HP_PERSIST_ALIAS_TO_ID = (function () {
-    var out = {};
-    for (var id in HP_PERSIST_ALIASES) {
-      if (Object.prototype.hasOwnProperty.call(HP_PERSIST_ALIASES, id)) {
-        out[HP_PERSIST_ALIASES[id]] = id;
-      }
-    }
-    return out;
-  })();
-
-  var bridgeConfig = null;
-  var currentValues = null;
-  var persistedValues = null;
-  var cachedEncoded = "";
-  var cachedRaw = "";
-  var cachedValues = null;
-  var persistToken = 0;
-  var bootstrapToken = 0;
-  var pendingBootstrapReason = "";
-
-
-
-  var AnitaBase64 = (function () {
-    var CHARS = "ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789-_";
-
-    function encode(str) {
-      var bytes = [];
-      for (var i = 0; i < str.length; i++) {
-        var code = str.charCodeAt(i);
-        if (code < 128) {
-          bytes.push(code);
-        } else if (code < 2048) {
-          bytes.push(0xC0 | (code >> 6));
-          bytes.push(0x80 | (code & 0x3F));
-        } else {
-          bytes.push(0xE0 | (code >> 12));
-          bytes.push(0x80 | ((code >> 6) & 0x3F));
-          bytes.push(0x80 | (code & 0x3F));
+"use strict";
+
+!function() {
+    var CHARS, TITLE = "HP Colors", STORAGE_KEY = "anita_v1_hp_colors", HP_PERSIST_ALIASES = {
+        hp_enabled: "e",
+        hp_mode: "m",
+        hp_low_threshold: "l",
+        hp_high_threshold: "h",
+        hp_bg_visible: "b",
+        hp_team_colors: "t",
+        hp_color_low: "cl",
+        hp_color_mid: "cm",
+        hp_color_high: "ch",
+        hp_counter_size: "s",
+        hp_counter_position: "p",
+        hp_text_color_mode: "tm",
+        hp_level_number_visible: "lnv",
+        hp_text_color_low: "tl",
+        hp_text_color_mid: "ti",
+        hp_text_color_high: "th",
+        hp_pulse_bpm: "bp",
+        hp_pulse_intensity: "pi",
+        hp_pulse_enabled: "pe",
+        hp_pulse_text_enabled: "pte",
+        hp_pulse_text_scale: "pts",
+        hp_pulse_text_position: "ptp",
+        hp_pulse_hide_bar: "phb",
+        hp_skip_buildings: "sb",
+        hp_pulse_threshold: "pt",
+        hp_friend_enabled: "fe",
+        hp_friend_pulse_enabled: "fpe",
+        hp_friend_pulse_bpm: "fpb",
+        hp_friend_pulse_intensity: "fpi",
+        hp_friend_pulse_threshold: "fpt",
+        hp_friend_color_low: "fcl",
+        hp_friend_color_mid: "fcm",
+        hp_friend_color_high: "fch",
+        hp_friend_pulse_color_enabled: "fpce",
+        hp_friend_pulse_color: "fpc",
+        hp_kill_zone_enabled: "kze",
+        hp_kill_zone_threshold: "kzt",
+        hp_kill_zone_color: "kzc",
+        hp_kill_zone_width: "kzw"
+    }, HP_PERSIST_ALIAS_TO_ID = function() {
+        var out = {};
+        for (var id in HP_PERSIST_ALIASES) Object.prototype.hasOwnProperty.call(HP_PERSIST_ALIASES, id) && (out[HP_PERSIST_ALIASES[id]] = id);
+        return out;
+    }(), bridgeConfig = null, currentValues = null, persistedValues = null, cachedEncoded = "", cachedRaw = "", cachedValues = null, persistToken = 0, bootstrapToken = 0, pendingBootstrapReason = "", AnitaBase64 = (CHARS = "ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789-_",
+    {
+        encode: function(str) {
+            for (var bytes = [], i = 0; i < str.length; i++) {
+                var code = str.charCodeAt(i);
+                code < 128 ? bytes.push(code) : code < 2048 ? (bytes.push(192 | code >> 6), bytes.push(128 | 63 & code)) : (bytes.push(224 | code >> 12),
+                bytes.push(128 | code >> 6 & 63), bytes.push(128 | 63 & code));
+            }
+            for (var out = "", j = 0; j < bytes.length; j += 3) {
+                var b0 = bytes[j], b1 = bytes[j + 1] || 0, b2 = bytes[j + 2] || 0;
+                out += CHARS[b0 >> 2], out += CHARS[(3 & b0) << 4 | b1 >> 4], out += j + 1 < bytes.length ? CHARS[(15 & b1) << 2 | b2 >> 6] : "",
+                out += j + 2 < bytes.length ? CHARS[63 & b2] : "";
+            }
+            return out;
+        },
+        decode: function(str) {
+            for (var lookup = {}, i = 0; i < 64; i++) lookup[CHARS[i]] = i;
+            function getVal(ch) {
+                if (void 0 === ch) return 0;
+                if (!Object.prototype.hasOwnProperty.call(lookup, ch)) throw new Error("Invalid base64url char: " + ch);
+                return lookup[ch];
+            }
+            for (var decodedBytes = [], j = 0; j < str.length; j += 4) {
+                var c0 = getVal(str[j]), c1 = getVal(str[j + 1]), c2 = void 0 !== str[j + 2] ? getVal(str[j + 2]) : 0, c3 = void 0 !== str[j + 3] ? getVal(str[j + 3]) : 0;
+                decodedBytes.push(c0 << 2 | c1 >> 4), void 0 !== str[j + 2] && decodedBytes.push((15 & c1) << 4 | c2 >> 2),
+                void 0 !== str[j + 3] && decodedBytes.push((3 & c2) << 6 | c3);
+            }
+            for (var out = "", k = 0; k < decodedBytes.length; k++) {
+                var b = decodedBytes[k];
+                if (b < 128) out += String.fromCharCode(b); else if (b < 224) out += String.fromCharCode((31 & b) << 6 | 63 & decodedBytes[++k]); else {
+                    var cont2 = decodedBytes[++k], cont3 = decodedBytes[++k];
+                    out += String.fromCharCode((15 & b) << 12 | (63 & cont2) << 6 | 63 & cont3);
+                }
+            }
+            return out;
         }
-      }
-
-      var out = "";
-      for (var j = 0; j < bytes.length; j += 3) {
-        var b0 = bytes[j];
-        var b1 = bytes[j + 1] || 0;
-        var b2 = bytes[j + 2] || 0;
-        out += CHARS[b0 >> 2];
-        out += CHARS[((b0 & 3) << 4) | (b1 >> 4)];
-        out += (j + 1 < bytes.length) ? CHARS[((b1 & 15) << 2) | (b2 >> 6)] : "";
-        out += (j + 2 < bytes.length) ? CHARS[b2 & 63] : "";
-      }
-      return out;
-    }
-
-    function decode(str) {
-      var lookup = {};
-      for (var i = 0; i < CHARS.length; i++) lookup[CHARS[i]] = i;
-
-      function getVal(ch) {
-        if (ch === undefined) return 0;
-        if (!Object.prototype.hasOwnProperty.call(lookup, ch)) {
-          throw new Error("Invalid base64url char: " + ch);
-        }
-        return lookup[ch];
-      }
-
-      var decodedBytes = [];
-      for (var j = 0; j < str.length; j += 4) {
-        var c0 = getVal(str[j]);
-        var c1 = getVal(str[j + 1]);
-        var c2 = str[j + 2] !== undefined ? getVal(str[j + 2]) : 0;
-        var c3 = str[j + 3] !== undefined ? getVal(str[j + 3]) : 0;
-        decodedBytes.push((c0 << 2) | (c1 >> 4));
-        if (str[j + 2] !== undefined) decodedBytes.push(((c1 & 15) << 4) | (c2 >> 2));
-        if (str[j + 3] !== undefined) decodedBytes.push(((c2 & 3) << 6) | c3);
-      }
-
-      var out = "";
-      for (var k = 0; k < decodedBytes.length; k++) {
-        var b = decodedBytes[k];
-        if (b < 128) {
-          out += String.fromCharCode(b);
-        } else if (b < 224) {
-          out += String.fromCharCode(((b & 31) << 6) | (decodedBytes[++k] & 63));
-        } else {
-          var cont2 = decodedBytes[++k];
-          var cont3 = decodedBytes[++k];
-          out += String.fromCharCode(((b & 15) << 12) | ((cont2 & 63) << 6) | (cont3 & 63));
+    });
+    function normalizeNamespace(storageNamespace) {
+        return String(storageNamespace || "").toLowerCase().replace(/[^a-z0-9_]+/g, "_").replace(/^_+|_+$/g, "");
+    }
+    function hasPersistentStorage() {
+        try {
+            return $.persistentStorage.setItem("_anita_probe", "1"), $.persistentStorage.removeItem("_anita_probe"),
+            storageSupportLogged || (storageSupportLogged = !0), !0;
+        } catch (e) {
+            return storageSupportLogged || (storageSupportLogged = !0), !1;
         }
-      }
-      return out;
     }
-
-    return {
-      encode: encode,
-      decode: decode
-    };
-  })();
-
-  function normalizeNamespace(storageNamespace) {
-    return String(storageNamespace || "")
-      .toLowerCase()
-      .replace(/[^a-z0-9_]+/g, "_")
-      .replace(/^_+|_+$/g, "");
-  }
-
-  function hasPersistentStorage() {
-    try {
-      // Probe with an actual write — Panorama native methods may not report
-      // typeof === "function" even when callable (QoL pattern: just call it).
-      $.persistentStorage.setItem("_anita_probe", "1");
-      $.persistentStorage.removeItem("_anita_probe");
-      if (!storageSupportLogged) {
-        storageSupportLogged = true;
-      }
-      return true;
-    } catch (e) {
-      if (!storageSupportLogged) {
-        storageSupportLogged = true;
-      }
-      return false;
-    }
-  }
-
-  function getRootPanel() {
-    var root = $.GetContextPanel();
-    while (root && root.GetParent && root.GetParent()) {
-      root = root.GetParent();
-    }
-    return root || null;
-  }
-
-  function cloneValues(values) {
-    var out = {};
-    for (var key in values) {
-      if (Object.prototype.hasOwnProperty.call(values, key)) {
-        out[key] = values[key];
-      }
-    }
-    return out;
-  }
-
-  function findElement(settingId) {
-    if (!bridgeConfig || !Array.isArray(bridgeConfig.elements)) return null;
-    for (var i = 0; i < bridgeConfig.elements.length; i++) {
-      var element = bridgeConfig.elements[i];
-      if (element && element.id === settingId) return element;
+    function cloneValues(values) {
+        var out = {};
+        for (var key in values) Object.prototype.hasOwnProperty.call(values, key) && (out[key] = values[key]);
+        return out;
     }
-    return null;
-  }
-
-  function sanitizeValue(element, value) {
-    if (!element) return value;
-
-    var fallback = element.defaultValue;
-    var type = String(element.type || "");
-
-    if (type === "positionpicker") {
-      var posX = 0;
-      var posY = 200;
-      var rawPos = value;
-
-      if (rawPos && typeof rawPos === "object") {
-        if (Array.isArray(rawPos)) {
-          if (rawPos.length > 0) posX = Number(rawPos[0]);
-          if (rawPos.length > 1) posY = Number(rawPos[1]);
-        } else {
-          if (Object.prototype.hasOwnProperty.call(rawPos, "x")) posX = Number(rawPos.x);
-          if (Object.prototype.hasOwnProperty.call(rawPos, "y")) posY = Number(rawPos.y);
-        }
-      } else if (typeof rawPos === "string") {
-        var parts = rawPos.match(/-?\d+(?:\.\d+)?/g);
-        if (parts && parts.length > 0) {
-          posX = Number(parts[0]);
-          if (parts.length > 1) posY = Number(parts[1]);
+    function findElement(settingId) {
+        if (!bridgeConfig || !Array.isArray(bridgeConfig.elements)) return null;
+        for (var i = 0; i < bridgeConfig.elements.length; i++) {
+            var element = bridgeConfig.elements[i];
+            if (element && element.id === settingId) return element;
         }
-      } else if (typeof rawPos === "number") {
-        posY = Number(rawPos);
-      }
-
-      if (!isFinite(posX)) posX = 0;
-      if (!isFinite(posY)) posY = 200;
-      if (posX < 0) posX = 0;
-      if (posY < 0) posY = 0;
-      if (posX > 400) posX = 400;
-      if (posY > 400) posY = 400;
-
-      return Math.round(posX) + "," + Math.round(posY);
-    }
-
-    if (type === "toggle") {
-      if (value === true || value === false) return value;
-      if (value === 1 || value === "1") return true;
-      if (value === 0 || value === "0") return false;
-      if (typeof value === "string") {
-        var lowered = value.toLowerCase();
-        if (lowered === "true") return true;
-        if (lowered === "false") return false;
-      }
-      return !!fallback;
-    }
-
-    if (type === "cycler") {
-      var count = Array.isArray(element.options) ? element.options.length : 0;
-      var nextIndex = Number(value);
-      if (!isFinite(nextIndex)) nextIndex = Number(fallback);
-      if (!isFinite(nextIndex)) nextIndex = 0;
-      nextIndex = Math.round(nextIndex);
-      if (nextIndex < 0) nextIndex = 0;
-      if (count > 0 && nextIndex >= count) {
-        var fallbackIndex = Number(fallback);
-        if (!isFinite(fallbackIndex) || fallbackIndex < 0 || fallbackIndex >= count) fallbackIndex = 0;
-        nextIndex = fallbackIndex;
-      }
-      return nextIndex;
-    }
-
-    if (type === "slider" || type === "stepper") {
-      var nextNumber = Number(value);
-      if (!isFinite(nextNumber)) nextNumber = Number(fallback);
-      if (!isFinite(nextNumber)) nextNumber = 0;
-      var step = Number(element.step);
-      if (!isFinite(step) || step === 0) step = 1;
-      var minV = isFinite(Number(element.min)) ? Number(element.min) : -Infinity;
-      var maxV = isFinite(Number(element.max)) ? Number(element.max) : Infinity;
-      if (nextNumber < minV) nextNumber = minV;
-      if (nextNumber > maxV) nextNumber = maxV;
-      if (Math.round(step) === step) return Math.round(nextNumber);
-      return parseFloat(nextNumber.toFixed(2));
-    }
-
-    if (type === "colorpicker") {
-      if (typeof value === "string" && value.length > 0) return value;
-      return (typeof fallback === "string" && fallback.length > 0) ? fallback : "#FFFFFF";
-    }
-
-    if (value !== undefined) return value;
-    return fallback;
-  }
-
-  function buildDefaultValues(config) {
-    var values = {};
-    var elements = (config && Array.isArray(config.elements)) ? config.elements : [];
-    for (var i = 0; i < elements.length; i++) {
-      var element = elements[i];
-      if (!element || !element.id || element.type === "button") continue;
-      values[element.id] = sanitizeValue(element, element.defaultValue);
-    }
-    return values;
-  }
-
-  function mergeWithDefaults(values) {
-    var merged = buildDefaultValues(bridgeConfig);
-    for (var key in values) {
-      if (!Object.prototype.hasOwnProperty.call(values, key)) continue;
-      var element = findElement(key);
-      if (!element) continue;
-      merged[key] = sanitizeValue(element, values[key]);
-    }
-    return merged;
-  }
-
-  function parseStoredPayload(raw, sourceLabel) {
-    var text = String(raw || "");
-    if (!text) return null;
-
-    var parsed = null;
-    try {
-      parsed = JSON.parse(text);
-    } catch (eParse) {
-      return null;
-    }
-
-    if (!parsed || typeof parsed !== "object" || !parsed.values || typeof parsed.values !== "object") {
-      return null;
-    }
-
-    if (bridgeConfig && parsed.v && Number(parsed.v) < Number(bridgeConfig.storageVersion)) {
-    }
-
-    var rawValues = parsed.values;
-    var values = {};
-    var useCompact = parsed.c === HP_COMPACT_PERSIST_VERSION || parsed.compact === true;
-    if (useCompact) {
-      var expanded = {};
-      for (var alias in rawValues) {
-        if (!Object.prototype.hasOwnProperty.call(rawValues, alias)) continue;
-        var fullId = HP_PERSIST_ALIAS_TO_ID[alias];
-        if (!fullId) continue;
-        expanded[fullId] = rawValues[alias];
-      }
-      values = mergeWithDefaults(expanded);
-    } else {
-      values = mergeWithDefaults(rawValues);
-    }
-    return {
-      raw: text,
-      values: values
-    };
-  }
-
-  function buildStoredPayload() {
-    if (!bridgeConfig) return "";
-    var sourceValues = persistedValues || currentValues || buildDefaultValues(bridgeConfig);
-    var defaults = buildDefaultValues(bridgeConfig);
-    var compactValues = {};
-    for (var key in sourceValues) {
-      if (!Object.prototype.hasOwnProperty.call(sourceValues, key)) continue;
-      if (!Object.prototype.hasOwnProperty.call(defaults, key)) continue;
-      if (sourceValues[key] === defaults[key]) continue;
-      compactValues[HP_PERSIST_ALIASES[key] || key] = sourceValues[key];
-    }
-    return JSON.stringify({
-      v: Math.max(1, Math.floor(Number(bridgeConfig.storageVersion) || 1)),
-      c: HP_COMPACT_PERSIST_VERSION,
-      values: compactValues
-    });
-  }
-
-  function writeSessionMirror(encoded) {
-    var root = getRootPanel();
-    if (!root) return;
-
-    try {
-      if (root.SetAttributeString) root.SetAttributeString(STORAGE_KEY, encoded);
-    } catch (eRoot) {}
-
-    try {
-      var hud = root.FindChildTraverse ? root.FindChildTraverse("Hud") : null;
-      if (hud && hud.SetAttributeString) hud.SetAttributeString(STORAGE_KEY, encoded);
-    } catch (eHud) {}
-  }
-
-  function cachePayload(raw, encoded, values, persisted) {
-    cachedRaw = String(raw || "");
-    cachedEncoded = String(encoded || "");
-    cachedValues = cloneValues(values || {});
-    currentValues = cloneValues(values || {});
-    persistedValues = cloneValues(persisted || values || {});
-    writeSessionMirror(cachedEncoded);
-  }
-
-  function readStoredPayload() {
-    if (cachedRaw && cachedEncoded && cachedValues) {
-      return {
-        raw: cachedRaw,
-        encoded: cachedEncoded,
-        values: cloneValues(cachedValues),
-        source: "cache"
-      };
-    }
-
-    if (!hasPersistentStorage()) {
-
-      var canReadConvar = typeof GameInterfaceAPI !== "undefined" &&
-        GameInterfaceAPI &&
-        typeof GameInterfaceAPI.GetSettingString === "function";
-      if (!canReadConvar) {
         return null;
-      }
-
-      var convarRaw = "";
-      try {
-        convarRaw = String(GameInterfaceAPI.GetSettingString("deadlock_hero_debuts_seen") || "");
-      } catch (eConvar) {
-        return null;
-      }
-
-      var tokenMatch = convarRaw.match(/\[ANITA-v1-hp_colors\]:([A-Za-z0-9_-]+)/);
-      if (!tokenMatch) {
-        return null;
-      }
-
-      var convarEncoded = tokenMatch[1];
-      var convarDecoded = "";
-      try {
-        convarDecoded = AnitaBase64.decode(convarEncoded);
-      } catch (eDecode) {
-        return null;
-      }
-
-      var convarParsed = parseStoredPayload(convarDecoded, "convar");
-      if (!convarParsed) {
-        return null;
-      }
-
-      cachePayload(convarParsed.raw, convarEncoded, convarParsed.values, convarParsed.values);
-      return {
-        raw: convarParsed.raw,
-        encoded: convarEncoded,
-        values: cloneValues(convarParsed.values),
-        source: "convar"
-      };
-    }
-
-    var encoded = "";
-    try {
-      encoded = String($.persistentStorage.getItem(STORAGE_KEY) || "");
-    } catch (eRead) {
-      return null;
-    }
-
-    if (!encoded) {
-      return null;
-    }
-
-    var raw = "";
-    try {
-      raw = AnitaBase64.decode(encoded);
-    } catch (eDecode) {
-      return null;
-    }
-
-    var parsed = parseStoredPayload(raw, "persistentStorage");
-    if (!parsed) return null;
-
-    cachePayload(parsed.raw, encoded, parsed.values, parsed.values);
-    return {
-      raw: parsed.raw,
-      encoded: encoded,
-      values: cloneValues(parsed.values),
-      source: "persistentStorage"
-    };
-  }
-
-  function persistCurrentState(reason, forceWrite) {
-    if (!bridgeConfig) return false;
-    if (!currentValues) currentValues = buildDefaultValues(bridgeConfig);
-
-    var raw = buildStoredPayload();
-    if (!raw) return false;
-
-    var encoded = "";
-    try {
-      encoded = AnitaBase64.encode(raw);
-    } catch (eEnc) {
-      return false;
-    }
-
-    if (!forceWrite && encoded === cachedEncoded) {
-      writeSessionMirror(encoded);
-      return false;
-    }
-
-    cachePayload(raw, encoded, currentValues, persistedValues);
-
-    if (!hasPersistentStorage()) {
-      return false;
-    }
-
-    try {
-      $.persistentStorage.setItem(STORAGE_KEY, encoded);
-      return true;
-    } catch (eWrite) {
-      return false;
-    }
-  }
-
-  function schedulePersist(reason, immediate) {
-    var token = ++persistToken;
-    $.Schedule(immediate ? 0.0 : PERSIST_DEBOUNCE_SEC, function () {
-      if (token !== persistToken) return;
-      persistCurrentState(reason, !!immediate);
-    });
-  }
-
-  function replayValues(values, reason) {
-    if (!values) return;
-
-    var count = 0;
-    for (var key in values) {
-      if (!Object.prototype.hasOwnProperty.call(values, key)) continue;
-      $.DispatchEvent("ClientUI_FireOutput", JSON.stringify({
-        magic_word: "ANITA_UPDATE",
-        mod_title: TITLE,
-        setting_id: key,
-        value: values[key],
-        update_source: "bridge_bootstrap",
-        skip_bridge_persist: true
-      }));
-      count += 1;
     }
-  }
-
-  function performBootstrap(reason) {
-    if (!bridgeConfig) {
-      pendingBootstrapReason = reason;
-      return;
+    function sanitizeValue(element, value) {
+        if (!element) return value;
+        var fallback = element.defaultValue, type = String(element.type || "");
+        if ("positionpicker" === type) {
+            var posX = 0, posY = 200, rawPos = value;
+            if (rawPos && "object" == typeof rawPos) Array.isArray(rawPos) ? (rawPos.length > 0 && (posX = Number(rawPos[0])),
+            rawPos.length > 1 && (posY = Number(rawPos[1]))) : (Object.prototype.hasOwnProperty.call(rawPos, "x") && (posX = Number(rawPos.x)),
+            Object.prototype.hasOwnProperty.call(rawPos, "y") && (posY = Number(rawPos.y))); else if ("string" == typeof rawPos) {
+                var parts = rawPos.match(/-?\d+(?:\.\d+)?/g);
+                parts && parts.length > 0 && (posX = Number(parts[0]), parts.length > 1 && (posY = Number(parts[1])));
+            } else "number" == typeof rawPos && (posY = Number(rawPos));
+            return isFinite(posX) || (posX = 0), isFinite(posY) || (posY = 200), posX < 0 && (posX = 0),
+            posY < 0 && (posY = 0), posX > 400 && (posX = 400), posY > 400 && (posY = 400),
+            Math.round(posX) + "," + Math.round(posY);
+        }
+        if ("toggle" === type) {
+            if (!0 === value || !1 === value) return value;
+            if (1 === value || "1" === value) return !0;
+            if (0 === value || "0" === value) return !1;
+            if ("string" == typeof value) {
+                var lowered = value.toLowerCase();
+                if ("true" === lowered) return !0;
+                if ("false" === lowered) return !1;
+            }
+            return !!fallback;
+        }
+        if ("cycler" === type) {
+            var count = Array.isArray(element.options) ? element.options.length : 0, nextIndex = Number(value);
+            if (isFinite(nextIndex) || (nextIndex = Number(fallback)), isFinite(nextIndex) || (nextIndex = 0),
+            (nextIndex = Math.round(nextIndex)) < 0 && (nextIndex = 0), count > 0 && nextIndex >= count) {
+                var fallbackIndex = Number(fallback);
+                (!isFinite(fallbackIndex) || fallbackIndex < 0 || fallbackIndex >= count) && (fallbackIndex = 0),
+                nextIndex = fallbackIndex;
+            }
+            return nextIndex;
+        }
+        if ("slider" === type || "stepper" === type) {
+            var nextNumber = Number(value);
+            isFinite(nextNumber) || (nextNumber = Number(fallback)), isFinite(nextNumber) || (nextNumber = 0);
+            var step = Number(element.step);
+            isFinite(step) && 0 !== step || (step = 1);
+            var minV = isFinite(Number(element.min)) ? Number(element.min) : -1 / 0, maxV = isFinite(Number(element.max)) ? Number(element.max) : 1 / 0;
+            return nextNumber < minV && (nextNumber = minV), nextNumber > maxV && (nextNumber = maxV),
+            Math.round(step) === step ? Math.round(nextNumber) : parseFloat(nextNumber.toFixed(2));
+        }
+        return "colorpicker" === type ? "string" == typeof value && value.length > 0 ? value : "string" == typeof fallback && fallback.length > 0 ? fallback : "#FFFFFF" : void 0 !== value ? value : fallback;
     }
-
-    var stored = readStoredPayload();
-    if (stored) {
-      currentValues = cloneValues(stored.values);
-      persistedValues = cloneValues(stored.values);
-      writeSessionMirror(stored.encoded);
-      replayValues(stored.values, reason + ":" + stored.source);
-      return;
+    function buildDefaultValues(config) {
+        for (var values = {}, elements = config && Array.isArray(config.elements) ? config.elements : [], i = 0; i < elements.length; i++) {
+            var element = elements[i];
+            element && element.id && "button" !== element.type && (values[element.id] = sanitizeValue(element, element.defaultValue));
+        }
+        return values;
     }
-
-    var sessionValues = currentValues || persistedValues;
-    if (sessionValues) {
-      replayValues(sessionValues, reason + ":session");
-      return;
+    function mergeWithDefaults(values) {
+        var merged = buildDefaultValues(bridgeConfig);
+        for (var key in values) if (Object.prototype.hasOwnProperty.call(values, key)) {
+            var element = findElement(key);
+            element && (merged[key] = sanitizeValue(element, values[key]));
+        }
+        return merged;
+    }
+    function parseStoredPayload(raw, sourceLabel) {
+        var text = String(raw || "");
+        if (!text) return null;
+        var parsed = null;
+        try {
+            parsed = JSON.parse(text);
+        } catch (eParse) {
+            return null;
+        }
+        if (!parsed || "object" != typeof parsed || !parsed.values || "object" != typeof parsed.values) return null;
+        bridgeConfig && parsed.v && (Number(parsed.v), Number(bridgeConfig.storageVersion));
+        var rawValues = parsed.values, values = {};
+        if (1 === parsed.c || !0 === parsed.compact) {
+            var expanded = {};
+            for (var alias in rawValues) if (Object.prototype.hasOwnProperty.call(rawValues, alias)) {
+                var fullId = HP_PERSIST_ALIAS_TO_ID[alias];
+                fullId && (expanded[fullId] = rawValues[alias]);
+            }
+            values = mergeWithDefaults(expanded);
+        } else values = mergeWithDefaults(rawValues);
+        return {
+            raw: text,
+            values: values
+        };
+    }
+    function writeSessionMirror(encoded) {
+        var root = function() {
+            for (var root = $.GetContextPanel(); root && root.GetParent && root.GetParent(); ) root = root.GetParent();
+            return root || null;
+        }();
+        if (root) {
+            try {
+                root.SetAttributeString && root.SetAttributeString(STORAGE_KEY, encoded);
+            } catch (eRoot) {}
+            try {
+                var hud = root.FindChildTraverse ? root.FindChildTraverse("Hud") : null;
+                hud && hud.SetAttributeString && hud.SetAttributeString(STORAGE_KEY, encoded);
+            } catch (eHud) {}
+        }
     }
-
-  }
-
-  function scheduleBootstrap(reason) {
-    pendingBootstrapReason = String(reason || "request");
-    var token = ++bootstrapToken;
-    $.Schedule(0.0, function () {
-      if (token !== bootstrapToken) return;
-      performBootstrap(pendingBootstrapReason);
-    });
-  }
-
-  function captureConfig(config) {
-    if (!config || String(config.title || "") !== TITLE) return false;
-    if (normalizeNamespace(config.storageNamespace) !== STORAGE_NAMESPACE) return false;
-
-    var nextConfig = {
-      title: TITLE,
-      storageNamespace: STORAGE_NAMESPACE,
-      storageVersion: Math.max(1, Math.floor(Number(config.storageVersion) || 1)),
-      elements: []
-    };
-
-    var sourceElements = Array.isArray(config.elements) ? config.elements : [];
-    for (var i = 0; i < sourceElements.length; i++) {
-      var source = sourceElements[i];
-      if (!source || !source.id) continue;
-      nextConfig.elements.push({
-        id: source.id,
-        type: source.type,
-        defaultValue: source.defaultValue,
-        options: Array.isArray(source.options) ? source.options.slice(0) : null,
-        step: source.step
-      });
+    function writeSharedSnapshot(values) {
+        try {
+            "undefined" != typeof GameUI && GameUI && GameUI.CustomUIConfig && (GameUI.CustomUIConfig().__hpColorsCfgRaw = JSON.stringify(values || {}));
+        } catch (eShare) {}
+    }
+    function cachePayload(raw, encoded, values, persisted) {
+        cachedRaw = String(raw || ""), cachedEncoded = String(encoded || ""), cachedValues = cloneValues(values || {}),
+        currentValues = cloneValues(values || {}), persistedValues = cloneValues(persisted || values || {}),
+        writeSessionMirror(cachedEncoded), writeSharedSnapshot(currentValues);
+    }
+    function persistCurrentState(reason, forceWrite) {
+        if (!bridgeConfig) return !1;
+        currentValues || (currentValues = buildDefaultValues(bridgeConfig));
+        var raw = function() {
+            if (!bridgeConfig) return "";
+            var sourceValues = persistedValues || currentValues || buildDefaultValues(bridgeConfig), defaults = buildDefaultValues(bridgeConfig), compactValues = {};
+            for (var key in sourceValues) Object.prototype.hasOwnProperty.call(sourceValues, key) && Object.prototype.hasOwnProperty.call(defaults, key) && sourceValues[key] !== defaults[key] && (compactValues[HP_PERSIST_ALIASES[key] || key] = sourceValues[key]);
+            return JSON.stringify({
+                v: Math.max(1, Math.floor(Number(bridgeConfig.storageVersion) || 1)),
+                c: 1,
+                values: compactValues
+            });
+        }();
+        if (!raw) return !1;
+        var encoded = "";
+        try {
+            encoded = AnitaBase64.encode(raw);
+        } catch (eEnc) {
+            return !1;
+        }
+        if (!forceWrite && encoded === cachedEncoded) return writeSessionMirror(encoded),
+        !1;
+        if (cachePayload(raw, encoded, currentValues, persistedValues), !hasPersistentStorage()) return !1;
+        try {
+            return $.persistentStorage.setItem(STORAGE_KEY, encoded), !0;
+        } catch (eWrite) {
+            return !1;
+        }
     }
-
-    bridgeConfig = nextConfig;
-    if (!currentValues) currentValues = buildDefaultValues(bridgeConfig);
-    if (!persistedValues) persistedValues = cloneValues(currentValues);
-    return true;
-  }
-
-  $.RegisterForUnhandledEvent("ClientUI_FireOutput", function (payload) {
-    try {
-      var data = (typeof payload === "string") ? JSON.parse(payload) : payload;
-      if (!data) return;
-
-      if (data.magic_word === "ANITA_REGISTER") {
-        if (captureConfig(data.config) && pendingBootstrapReason) {
-          scheduleBootstrap(pendingBootstrapReason);
+    function replayValues(values, reason) {
+        if (values) {
+            for (var key in values) Object.prototype.hasOwnProperty.call(values, key) && ($.DispatchEvent("ClientUI_FireOutput", JSON.stringify({
+                magic_word: "ANITA_UPDATE",
+                mod_title: TITLE,
+                setting_id: key,
+                value: values[key],
+                update_source: "bridge_bootstrap",
+                skip_bridge_persist: !0
+            })), 1);
         }
-        return;
-      }
-
-      if (data.magic_word === "ANITA_REQUEST_BOOTSTRAP") {
-        if (data.mod_title !== TITLE) return;
-        if (normalizeNamespace(data.storageNamespace) !== STORAGE_NAMESPACE) return;
-        scheduleBootstrap(String(data.reason || "request"));
-        return;
-      }
-
-      if (data.magic_word !== "ANITA_UPDATE" || data.mod_title !== TITLE) return;
-      if (!bridgeConfig || !data.setting_id) return;
-
-      var element = findElement(data.setting_id);
-      if (!element) return;
-
-      if (!currentValues) currentValues = buildDefaultValues(bridgeConfig);
-      if (!persistedValues) persistedValues = cloneValues(currentValues);
-
-      var updateSource = String(data.update_source || "ui_update");
-      var isResync = updateSource === "core_auto_resync" || updateSource === "ui_resync";
-
-      currentValues[data.setting_id] = sanitizeValue(element, data.value);
-      if (!isResync) {
-        persistedValues[data.setting_id] = sanitizeValue(element, data.value);
-      }
-
-      if (data.skip_bridge_persist || updateSource === "bridge_bootstrap" || isResync) {
-        return;
-      }
-
-      schedulePersist(String(data.update_source || "ui_update"), !!data.force_persist);
-    } catch (e) {
     }
-  });
-
-  hasPersistentStorage();
-
-})();
+    function performBootstrap(reason) {
+        if (bridgeConfig) {
+            var stored = function() {
+                if (cachedRaw && cachedEncoded && cachedValues) return {
+                    raw: cachedRaw,
+                    encoded: cachedEncoded,
+                    values: cloneValues(cachedValues),
+                    source: "cache"
+                };
+                if (!hasPersistentStorage()) {
+                    if ("undefined" == typeof GameInterfaceAPI || !GameInterfaceAPI || "function" != typeof GameInterfaceAPI.GetSettingString) return null;
+                    var convarRaw = "";
+                    try {
+                        convarRaw = String(GameInterfaceAPI.GetSettingString("deadlock_hero_debuts_seen") || "");
+                    } catch (eConvar) {
+                        return null;
+                    }
+                    var tokenMatch = convarRaw.match(/\[ANITA-v1-hp_colors\]:([A-Za-z0-9_-]+)/);
+                    if (!tokenMatch) return null;
+                    var convarEncoded = tokenMatch[1], convarDecoded = "";
+                    try {
+                        convarDecoded = AnitaBase64.decode(convarEncoded);
+                    } catch (eDecode) {
+                        return null;
+                    }
+                    var convarParsed = parseStoredPayload(convarDecoded);
+                    return convarParsed ? (cachePayload(convarParsed.raw, convarEncoded, convarParsed.values, convarParsed.values),
+                    {
+                        raw: convarParsed.raw,
+                        encoded: convarEncoded,
+                        values: cloneValues(convarParsed.values),
+                        source: "convar"
+                    }) : null;
+                }
+                var encoded = "";
+                try {
+                    encoded = String($.persistentStorage.getItem(STORAGE_KEY) || "");
+                } catch (eRead) {
+                    return null;
+                }
+                if (!encoded) return null;
+                var raw = "";
+                try {
+                    raw = AnitaBase64.decode(encoded);
+                } catch (eDecode) {
+                    return null;
+                }
+                var parsed = parseStoredPayload(raw);
+                return parsed ? (cachePayload(parsed.raw, encoded, parsed.values, parsed.values),
+                {
+                    raw: parsed.raw,
+                    encoded: encoded,
+                    values: cloneValues(parsed.values),
+                    source: "persistentStorage"
+                }) : null;
+            }();
+            if (stored) return currentValues = cloneValues(stored.values), persistedValues = cloneValues(stored.values),
+            writeSessionMirror(stored.encoded), void replayValues(stored.values, stored.source);
+            var sessionValues = currentValues || persistedValues;
+            sessionValues && replayValues(sessionValues);
+        } else pendingBootstrapReason = reason;
+    }
+    function scheduleBootstrap(reason) {
+        pendingBootstrapReason = String(reason || "request");
+        var token = ++bootstrapToken;
+        $.Schedule(0, function() {
+            token === bootstrapToken && performBootstrap(pendingBootstrapReason);
+        });
+    }
+    $.RegisterForUnhandledEvent("ClientUI_FireOutput", function(payload) {
+        try {
+            var data = "string" == typeof payload ? JSON.parse(payload) : payload;
+            if (!data) return;
+            if ("ANITA_REGISTER" === data.magic_word) return void (function(config) {
+                if (!config || String(config.title || "") !== TITLE) return !1;
+                if ("hp_colors" !== normalizeNamespace(config.storageNamespace)) return !1;
+                for (var nextConfig = {
+                    title: TITLE,
+                    storageNamespace: "hp_colors",
+                    storageVersion: Math.max(1, Math.floor(Number(config.storageVersion) || 1)),
+                    elements: []
+                }, sourceElements = Array.isArray(config.elements) ? config.elements : [], i = 0; i < sourceElements.length; i++) {
+                    var source = sourceElements[i];
+                    source && source.id && nextConfig.elements.push({
+                        id: source.id,
+                        type: source.type,
+                        defaultValue: source.defaultValue,
+                        options: Array.isArray(source.options) ? source.options.slice(0) : null,
+                        step: source.step
+                    });
+                }
+                return bridgeConfig = nextConfig, currentValues || (currentValues = buildDefaultValues(bridgeConfig)),
+                persistedValues || (persistedValues = cloneValues(currentValues)), !0;
+            }(data.config) && pendingBootstrapReason && scheduleBootstrap(pendingBootstrapReason));
+            if ("ANITA_REQUEST_BOOTSTRAP" === data.magic_word) {
+                if (data.mod_title !== TITLE) return;
+                if ("hp_colors" !== normalizeNamespace(data.storageNamespace)) return;
+                return void scheduleBootstrap(String(data.reason || "request"));
+            }
+            if ("ANITA_UPDATE" !== data.magic_word || data.mod_title !== TITLE) return;
+            if (!bridgeConfig || !data.setting_id) return;
+            var element = findElement(data.setting_id);
+            if (!element) return;
+            currentValues || (currentValues = buildDefaultValues(bridgeConfig)), persistedValues || (persistedValues = cloneValues(currentValues));
+            var updateSource = String(data.update_source || "ui_update"), isResync = "core_auto_resync" === updateSource || "ui_resync" === updateSource || "ui_refresh_after_apply" === updateSource;
+            if (currentValues[data.setting_id] = sanitizeValue(element, data.value), writeSharedSnapshot(currentValues),
+            isResync || (persistedValues[data.setting_id] = sanitizeValue(element, data.value)),
+            data.skip_bridge_persist || "bridge_bootstrap" === updateSource || isResync) return;
+            String(data.update_source || "ui_update"), immediate = !!data.force_persist, token = ++persistToken,
+            $.Schedule(immediate ? 0 : .35, function() {
+                token === persistToken && persistCurrentState(0, !!immediate);
+            });
+        } catch (e) {}
+        var immediate, token;
+    }), hasPersistentStorage();
+}();
\ No newline at end of file
diff --git a/hp_colors/panorama/scripts/anita_ui_core.js b/hp_colors/panorama/scripts/anita_ui_core.js
index 91fa2ab..1c2f64b 100644
--- a/hp_colors/panorama/scripts/anita_ui_core.js
+++ b/hp_colors/panorama/scripts/anita_ui_core.js
@@ -1,3211 +1,1704 @@
-
-
-
-(function () {
-  "use strict";
-
-  const CONFIG = {
-    VERSION: "2.2.3",
-
-    IDS: {
-      WINDOW: "AnitaUI_Window",
-      BACKDROP: "AnitaUI_Backdrop",
-      NAVBAR: "AnitaUI_NavBar",
-      CONTENT: "AnitaUI_ContentArea",
-      OVERLAY_BTN: "AnitaOverlayBtn",
-      HUD_ROOT: "Hud"
-    },
-    CLASSES: {
-      ESCAPE_MENU: "ShowEscapeMenu",
-      OPEN: "Open",
-      ACTIVE: "Active",
-      VISIBLE: "Visible",
-      CHECKED: "Checked",
-      ATTENTION: "Attention"
-    },
-    EVENTS: {
-      COMMS: "ClientUI_FireOutput",
-      MAGIC_WORD: "ANITA_REGISTER",
-      UPDATE: "ANITA_UPDATE"
-    },
-    UI: {
-      TAB_MAX_CHARS: 17,
-      MONITOR_INTERVAL: 0.05
-    }
-  };
-  const RENDER_REFRESH_DEBOUNCE_SEC = 0.05;
-  const MAX_CONVAR_VALUE_LEN = 400;
-  const HP_COMPACT_PERSIST_VERSION = 1;
-  const HP_PERSIST_ALIASES = {
-    hp_enabled: "e",
-    hp_mode: "m",
-    hp_low_threshold: "l",
-    hp_high_threshold: "h",
-    hp_bg_visible: "b",
-    hp_team_colors: "t",
-    hp_color_low: "cl",
-    hp_color_mid: "cm",
-    hp_color_high: "ch",
-    hp_counter_size: "s",
-    hp_counter_position: "p",
-    hp_text_color_mode: "tm",
-    hp_level_number_visible: "lnv",
-    hp_text_color_low: "tl",
-    hp_text_color_mid: "ti",
-    hp_text_color_high: "th",
-    hp_pulse_bpm: "bp",
-    hp_pulse_intensity: "pi",
-    hp_pulse_enabled: "pe",
-    hp_pulse_text_enabled: "pte",
-    hp_pulse_text_scale: "pts",
-    hp_pulse_text_position: "ptp",
-    hp_pulse_hide_bar: "phb",
-    hp_skip_buildings: "sb",
-    hp_pulse_threshold: "pt",
-    hp_friend_enabled: "fe",
-    hp_friend_pulse_enabled: "fpe",
-    hp_friend_pulse_bpm: "fpb",
-    hp_friend_pulse_intensity: "fpi",
-    hp_friend_pulse_threshold: "fpt",
-    hp_friend_color_low: "fcl",
-    hp_friend_color_mid: "fcm",
-    hp_friend_color_high: "fch",
-    hp_friend_pulse_color_enabled: "fpce",
-    hp_friend_pulse_color: "fpc",
-    hp_kill_zone_enabled: "kze",
-    hp_kill_zone_threshold: "kzt",
-    hp_kill_zone_color: "kzc",
-    hp_kill_zone_width: "kzw"
-  };
-  const HP_PERSIST_ALIAS_TO_ID = (function () {
-    var out = {};
-    for (var id in HP_PERSIST_ALIASES) {
-      if (Object.prototype.hasOwnProperty.call(HP_PERSIST_ALIASES, id)) {
-        out[HP_PERSIST_ALIASES[id]] = id;
-      }
-    }
-    return out;
-  })();// Base64url encode/decode — no btoa/atob in Deadlock Panorama
-  var AnitaBase64 = (function () {
-    var CHARS = "ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789-_";
-
-    function encode(str) {
-      var bytes = [];
-      for (var i = 0; i < str.length; i++) {
-        var code = str.charCodeAt(i);
-        if (code < 128) {
-          bytes.push(code);
-        } else if (code < 2048) {
-          bytes.push(0xC0 | (code >> 6));
-          bytes.push(0x80 | (code & 0x3F));
-        } else {
-          bytes.push(0xE0 | (code >> 12));
-          bytes.push(0x80 | ((code >> 6) & 0x3F));
-          bytes.push(0x80 | (code & 0x3F));
-        }
-      }
-      var out = "";
-      for (var j = 0; j < bytes.length; j += 3) {
-        var b0 = bytes[j], b1 = bytes[j + 1] || 0, b2 = bytes[j + 2] || 0;
-        out += CHARS[b0 >> 2];
-        out += CHARS[((b0 & 3) << 4) | (b1 >> 4)];
-        out += (j + 1 < bytes.length) ? CHARS[((b1 & 15) << 2) | (b2 >> 6)] : "";
-        out += (j + 2 < bytes.length) ? CHARS[b2 & 63] : "";
-      }
-      return out;
-    }
-
-    function decode(str) {
-      var lookup = {};
-      for (var i = 0; i < CHARS.length; i++) lookup[CHARS[i]] = i;
-      function getVal(ch) {
-        if (ch === undefined) return 0;
-        if (!Object.prototype.hasOwnProperty.call(lookup, ch)) {
-          throw new Error("Invalid base64url char: " + ch);
-        }
-        return lookup[ch];
-      }
-      var decodedBytes = [];
-      for (var j = 0; j < str.length; j += 4) {
-        var c0 = getVal(str[j]);
-        var c1 = getVal(str[j + 1]);
-        var c2 = str[j + 2] !== undefined ? getVal(str[j + 2]) : 0;
-        var c3 = str[j + 3] !== undefined ? getVal(str[j + 3]) : 0;
-        decodedBytes.push((c0 << 2) | (c1 >> 4));
-        if (str[j + 2] !== undefined) decodedBytes.push(((c1 & 15) << 4) | (c2 >> 2));
-        if (str[j + 3] !== undefined) decodedBytes.push(((c2 & 3) << 6) | c3);
-      }
-      var out = "";
-      for (var k = 0; k < decodedBytes.length; k++) {
-        var b = decodedBytes[k];
-        if (b < 128) {
-          out += String.fromCharCode(b);
-        } else if (b < 224) {
-          out += String.fromCharCode(((b & 31) << 6) | (decodedBytes[++k] & 63));
-        } else {
-          var cont2 = decodedBytes[++k], cont3 = decodedBytes[++k];
-          out += String.fromCharCode(((b & 15) << 12) | ((cont2 & 63) << 6) | (cont3 & 63));
-        }
-      }
-      return out;
-    }
-
-    return { encode: encode, decode: decode };
-  })();
-
-  function emitUpdate(modTitle, settingId, newValue, meta) {
-    var payload = {
-      magic_word: "ANITA_UPDATE",
-      mod_title: modTitle,
-      setting_id: settingId,
-      value: newValue
-    };
-    if (meta && typeof meta === "object") {
-      for (var key in meta) {
-        if (Object.prototype.hasOwnProperty.call(meta, key)) {
-          payload[key] = meta[key];
-        }
-      }
-    }
-    $.DispatchEvent("ClientUI_FireOutput", JSON.stringify(payload));
-  }
-
-  function runConsoleCommandBestEffort(commandText) {
-    var cmd = String(commandText || "").trim();
-    var didAny = false;
-    if (!cmd) return false;
-
-    try {
-      $.DispatchEvent("CitadelConCommand", cmd);
-      didAny = true;
-    } catch (e0) {}
-
-    try {
-      if (typeof GameInterfaceAPI !== "undefined" &&
-          GameInterfaceAPI &&
-          typeof GameInterfaceAPI.ConsoleCommand === "function") {
-        GameInterfaceAPI.ConsoleCommand(cmd);
-        didAny = true;
-      }
-    } catch (e1) {}
-
-    try {
-      $.DispatchEvent("ConsoleCommand", cmd);
-      didAny = true;
-    } catch (e2) {}
-
-    try {
-      $.DispatchEvent("GameUIRunCommand", cmd);
-      didAny = true;
-    } catch (e3) {}
-
-    return didAny;
-  }
-
-  const AnitaPersistence = {
-
-    normalizeNamespace: function (storageNamespace) {
-      return String(storageNamespace || "")
-        .toLowerCase()
-        .replace(/[^a-z0-9_]+/g, "_")
-        .replace(/^_+|_+$/g, "");
-    },
-
-    getVersion: function (config) {
-      var version = Number(config && config.storageVersion);
-      if (!isFinite(version) || version < 1) return 1;
-      return Math.floor(version);
-    },
-
-    hasPersistentConfig: function (config) {
-      return this.normalizeNamespace(config && config.storageNamespace).length > 0;
-    },
-
-    isHpColorsConfig: function (config) {
-      return !!config &&
-        String(config.title || "") === "HP Colors" &&
-        this.normalizeNamespace(config.storageNamespace) === "hp_colors";
-    },
-
-    CONVAR_KEY: "deadlock_hero_debuts_seen",
-    TOKEN_PREFIX: "ANITA-v1-",
-
-    // ns is safe to interpolate into regex: normalizeNamespace restricts output to [a-z0-9_]
-    getTokenRegex: function (ns) {
-      return new RegExp("\\[" + this.TOKEN_PREFIX + ns + "\\]:[A-Za-z0-9_-]+");
-    },
-
-    getCleanupRegex: function (ns) {
-      return new RegExp("\\[" + this.TOKEN_PREFIX + ns + "\\]:[A-Za-z0-9_-]*", "g");
-    },
-
-    canWriteConvarDirect: function () {
-      return typeof GameInterfaceAPI !== "undefined" &&
-        !!GameInterfaceAPI &&
-        (typeof GameInterfaceAPI.ConsoleCommand === "function" ||
-         typeof GameInterfaceAPI.SetSettingString === "function");
-    },
-
-    getStorageKey: function (config) {
-      var ns = this.normalizeNamespace(config && config.storageNamespace);
-      return ns ? "anita_v1_" + ns : "";
-    },
-
-    getRootPanel: function () {
-      var rootPanel = $.GetContextPanel();
-      while (rootPanel && rootPanel.GetParent && rootPanel.GetParent()) rootPanel = rootPanel.GetParent();
-      return rootPanel || null;
-    },
-
-    getSessionEncoded: function (config) {
-      var key = this.getStorageKey(config);
-      if (!key) return "";
-
-      var rootPanel = this.getRootPanel();
-      var rootEncoded = "";
-      var hudEncoded = "";
-
-      try {
-        if (rootPanel && rootPanel.GetAttributeString) {
-          rootEncoded = String(rootPanel.GetAttributeString(key, "") || "");
-        }
-      } catch (eRoot) {}
-
-      try {
-        var hudPanel = (rootPanel && rootPanel.FindChildTraverse) ? rootPanel.FindChildTraverse("Hud") : null;
-        if (hudPanel && hudPanel.GetAttributeString) {
-          hudEncoded = String(hudPanel.GetAttributeString(key, "") || "");
-        }
-      } catch (eHud) {}
-
-      return rootEncoded || hudEncoded || "";
-    },
-
-    hasPersistentStorage: function () {
-      try {
-        var supported = !!($ && $.persistentStorage &&
-          typeof $.persistentStorage.getItem === "function" &&
-          typeof $.persistentStorage.setItem === "function");
-        return supported;
-      } catch (eStorage) {
-        return false;
-      }
-    },
-
-    writeSessionMirror: function (config, encoded) {
-      var key = this.getStorageKey(config);
-      if (!key) return;
-
-      try {
-        var rootPanel = this.getRootPanel();
-        if (rootPanel && rootPanel.SetAttributeString) {
-          rootPanel.SetAttributeString(key, encoded);
-        }
-        var hudPanel = (rootPanel && rootPanel.FindChildTraverse) ? rootPanel.FindChildTraverse("Hud") : null;
-        if (hudPanel && hudPanel.SetAttributeString) {
-          hudPanel.SetAttributeString(key, encoded);
-        }
-      } catch (eSess) {
-      }
-    },
-
-    writeConvar: function (key, value) {
-      // Prefer SetSettingString (writes to settings file, survives restarts)
-      if (typeof GameInterfaceAPI !== "undefined" &&
-          GameInterfaceAPI &&
-          typeof GameInterfaceAPI.SetSettingString === "function") {
-        GameInterfaceAPI.SetSettingString(key, value);
-        this.log("writeConvar via SetSettingString key=" + key);
-      } else if (typeof GameInterfaceAPI !== "undefined" &&
-          GameInterfaceAPI &&
-          typeof GameInterfaceAPI.ConsoleCommand === "function") {
-        GameInterfaceAPI.ConsoleCommand(key + ' "' + value + '"');
-        this.log("writeConvar via ConsoleCommand key=" + key);
-      }
-    },
-
-    writeConvarBestEffort: function (key, value) {
-      var command = String(key || "") + ' "' + String(value || "") + '"';
-      if (this.canWriteConvarDirect()) {
-        try {
-          this.writeConvar(key, value);
-          return true;
-        } catch (e0) {
-          this.log("direct convar write failed key=" + key + " err=" + e0);
-        }
-      }
-
-      if (runConsoleCommandBestEffort(command)) {
-        this.log("writeConvar via command events key=" + key);
-        // Also attempt SetSettingString as a durable fallback after events path
-        try {
-          if (typeof GameInterfaceAPI !== "undefined" && GameInterfaceAPI &&
-              typeof GameInterfaceAPI.SetSettingString === "function") {
-            GameInterfaceAPI.SetSettingString(key, value);
-          }
-        } catch (eSS) {}
-        return true;
-      }
-      return false;
-    },
-
-    getElements: function (config) {
-      return (config && Array.isArray(config.elements)) ? config.elements : [];
-    },
-
-    warnPersistence: function (config, reason) {
-      var title = (config && config.title) ? String(config.title) : "unknown";
-      if (config && config.__anitaLastPersistWarning === reason) return;
-      if (config) config.__anitaLastPersistWarning = reason;
-    },
-
-    shouldPersistElement: function (element) {
-      return !!(element && element.id && element.type !== "button");
-    },
-
-    sanitizeValue: function (element, value) {
-      if (!element) return value;
-
-      var fallback = element.defaultValue;
-      var type = String(element.type || "");
-
-      if (type === "toggle") {
-        if (value === true || value === false) return value;
-        if (value === 1 || value === "1") return true;
-        if (value === 0 || value === "0") return false;
-        if (typeof value === "string") {
-          var lowered = value.toLowerCase();
-          if (lowered === "true") return true;
-          if (lowered === "false") return false;
-        }
-        return !!fallback;
-      }
-
-      if (type === "cycler") {
-        var count = Array.isArray(element.options) ? element.options.length : 0;
-        var nextIndex = Number(value);
-        if (!isFinite(nextIndex)) nextIndex = Number(fallback);
-        if (!isFinite(nextIndex)) nextIndex = 0;
-        nextIndex = Math.round(nextIndex);
-        if (nextIndex < 0) nextIndex = 0;
-        if (count > 0 && nextIndex >= count) {
-          var fallbackIndex = Number(fallback);
-          if (!isFinite(fallbackIndex) || fallbackIndex < 0 || fallbackIndex >= count) fallbackIndex = 0;
-          nextIndex = fallbackIndex;
-        }
-        return nextIndex;
-      }
-
-      if (type === "stepper" || type === "slider") {
-        var nextNumber = Number(value);
-        if (!isFinite(nextNumber)) nextNumber = Number(fallback);
-        if (!isFinite(nextNumber)) nextNumber = 0;
-        var min = Number(element.min);
-        var max = Number(element.max);
-        if (isFinite(min) && nextNumber < min) nextNumber = min;
-        if (isFinite(max) && nextNumber > max) nextNumber = max;
-        var step = Number(element.step);
-        if (!isFinite(step) || step === 0) step = 1;
-        if (Math.round(step) === step) {
-          nextNumber = Math.round(nextNumber);
-        } else {
-          nextNumber = parseFloat(nextNumber.toFixed(2));
-        }
-        if (isFinite(min) && nextNumber < min) nextNumber = min;
-        if (isFinite(max) && nextNumber > max) nextNumber = max;
-        return nextNumber;
-      }
-
-      if (type === "colorpicker") {
-        if (typeof value === "string" && value.length > 0) return value;
-        return (typeof fallback === "string" && fallback.length > 0) ? fallback : "#FFFFFF";
-      }
-
-      if (value !== undefined) return value;
-      return fallback;
-    },
-
-    ensureDefaults: function (config) {
-      var elements = this.getElements(config);
-      for (var i = 0; i < elements.length; i++) {
-        var element = elements[i];
-        if (!this.shouldPersistElement(element)) {
-          if (element.currentValue === undefined && element.defaultValue !== undefined) {
-            element.currentValue = element.defaultValue;
-          }
-          continue;
-        }
-        var sourceValue = (element.currentValue !== undefined) ? element.currentValue : element.defaultValue;
-        element.currentValue = this.sanitizeValue(element, sourceValue);
-      }
-    },
-
-    expandStoredValues: function (config, parsed) {
-      if (!parsed || typeof parsed !== "object" || !parsed.values || typeof parsed.values !== "object") {
-        return null;
-      }
-
-      var rawValues = parsed.values;
-      var expanded = {};
-      var isCompact = this.isHpColorsConfig(config) && (parsed.c === HP_COMPACT_PERSIST_VERSION || parsed.compact === true);
-
-      if (!isCompact) {
-        for (var key in rawValues) {
-          if (Object.prototype.hasOwnProperty.call(rawValues, key)) {
-            expanded[key] = rawValues[key];
-          }
-        }
-        return expanded;
-      }
-
-      for (var alias in rawValues) {
-        if (!Object.prototype.hasOwnProperty.call(rawValues, alias)) continue;
-        var fullId = HP_PERSIST_ALIAS_TO_ID[alias];
-        if (!fullId) continue;
-        expanded[fullId] = rawValues[alias];
-      }
-      return expanded;
-    },
-
-    parseStoredPayload: function (config, raw, sourceLabel) {
-      var text = String(raw || "");
-      if (!text) return null;
-
-      var parsed = null;
-      try {
-        parsed = JSON.parse(text);
-      } catch (e) {
-        return null;
-      }
-
-      if (!parsed || typeof parsed !== "object") {
-        return null;
-      }
-
-      var expandedValues = this.expandStoredValues(config, parsed);
-      if (!expandedValues) {
-        return null;
-      }
-
-      var values = {};
-      var elements = this.getElements(config);
-      for (var i = 0; i < elements.length; i++) {
-        var element = elements[i];
-        if (!this.shouldPersistElement(element)) continue;
-        if (!Object.prototype.hasOwnProperty.call(expandedValues, element.id)) continue;
-        values[element.id] = this.sanitizeValue(element, expandedValues[element.id]);
-      }
-
-      return {
-        raw: text,
-        values: values
-      };
-    },
-
-    readPersistentStoragePayload: function (config) {
-      var key = this.getStorageKey(config);
-      var storageAvailable = this.hasPersistentStorage();
-      if (!key || !storageAvailable) return null;
-
-      var encoded = "";
-      try {
-        encoded = String($.persistentStorage.getItem(key) || "");
-      } catch (eRead) {
-        return null;
-      }
-      if (!encoded) return null;
-
-      var raw = "";
-      try {
-        raw = AnitaBase64.decode(encoded);
-      } catch (eDecode) {
-        return null;
-      }
-
-      var parsed = this.parseStoredPayload(config, raw, "persistentStorage");
-      if (!parsed) {
-        return null;
-      }
-      return {
-        raw: parsed.raw,
-        encoded: encoded,
-        values: parsed.values,
-        source: "persistentStorage"
-      };
-    },
-
-    readConvarPayload: function (config) {
-      var ns = this.normalizeNamespace(config && config.storageNamespace);
-      if (!ns) return null;
-      if (typeof GameInterfaceAPI === "undefined" ||
-          !GameInterfaceAPI ||
-          typeof GameInterfaceAPI.GetSettingString !== "function") {
-        return null;
-      }
-
-      var convarRaw = "";
-      try {
-        convarRaw = String(GameInterfaceAPI.GetSettingString(this.CONVAR_KEY) || "");
-      } catch (eRead) {
-        return null;
-      }
-
-      var tokenMatch = convarRaw.match(new RegExp("\\[" + this.TOKEN_PREFIX + ns + "\\]:([A-Za-z0-9_-]+)"));
-      if (!tokenMatch) {
-        return null;
-      }
-
-      var encoded = String(tokenMatch[1] || "");
-      if (!encoded) return null;
-
-      var raw = "";
-      try {
-        raw = AnitaBase64.decode(encoded);
-      } catch (eDecode) {
-        return null;
-      }
-
-      var parsed = this.parseStoredPayload(config, raw, "convar");
-      if (!parsed) {
-        return null;
-      }
-      return {
-        raw: parsed.raw,
-        encoded: encoded,
-        values: parsed.values,
-        source: "convar"
-      };
-    },
-
-    readStoredPayload: function (config) {
-      var persisted = null;
-
-      try {
-        var sessionEncoded = this.getSessionEncoded(config);
-        if (sessionEncoded) {
-          var sessionRaw = AnitaBase64.decode(sessionEncoded);
-          persisted = this.parseStoredPayload(config, sessionRaw, "session");
-          if (persisted) {
-            return {
-              raw: persisted.raw,
-              encoded: sessionEncoded,
-              values: persisted.values,
-              source: "session"
-            };
-          }
-        }
-      } catch (eSess) {
-      }
-
-      persisted = this.readPersistentStoragePayload(config);
-      if (persisted) {
-        if (config && config.title === "HP Colors") {
-        }
-        return persisted;
-      }
-
-      persisted = this.readConvarPayload(config);
-      if (persisted) {
-        if (config && config.title === "HP Colors") {
-        }
-        return persisted;
-      }
-
-      return null;
-    },
-
-    applyResolvedValues: function (config, values) {
-      var elements = this.getElements(config);
-      for (var i = 0; i < elements.length; i++) {
-        var element = elements[i];
-        if (!this.shouldPersistElement(element)) {
-          if (element.currentValue === undefined && element.defaultValue !== undefined) {
-            element.currentValue = element.defaultValue;
-          }
-          continue;
-        }
-
-        var nextValue = Object.prototype.hasOwnProperty.call(values || {}, element.id)
-          ? values[element.id]
-          : element.defaultValue;
-        element.currentValue = this.sanitizeValue(element, nextValue);
-      }
-    },
-
-    hydrateConfig: function (config) {
-      this.ensureDefaults(config);
-      var hydrateSource = "defaults";
-      var ns = this.normalizeNamespace(config && config.storageNamespace);
-
-      if (!this.hasPersistentConfig(config)) {
-        config.__anitaLastPersistedRaw = "";
-        this.applyResolvedValues(config, {});
-        return;
-      }
-
-      var persisted = this.readStoredPayload(config);
-      if (persisted) {
-        hydrateSource = persisted.source;
-      }
-
-      if (persisted) {
-        this.applyResolvedValues(config, persisted.values);
-        if (persisted.encoded && hydrateSource !== "session") {
-          this.writeSessionMirror(config, persisted.encoded);
-        }
-      } else {
-        this.applyResolvedValues(config, {});
-      }
-
-      config.__anitaLastPersistedRaw = persisted ? persisted.raw : "";
-      if (config && config.title === "HP Colors") {
-      }
-    },
-
-    buildStoredPayload: function (config) {
-      var elements = this.getElements(config);
-      var values = {};
-      var isHpColors = this.isHpColorsConfig(config);
-      for (var i = 0; i < elements.length; i++) {
-        var element = elements[i];
-        if (!this.shouldPersistElement(element)) continue;
-        var value = this.sanitizeValue(
-          element,
-          element.currentValue !== undefined ? element.currentValue : element.defaultValue
-        );
-        element.currentValue = value;
-        if (isHpColors && value === this.sanitizeValue(element, element.defaultValue)) {
-          continue;
-        }
-        values[isHpColors ? (HP_PERSIST_ALIASES[element.id] || element.id) : element.id] = value;
-      }
-      if (isHpColors) {
-        return JSON.stringify({
-          v: this.getVersion(config),
-          c: HP_COMPACT_PERSIST_VERSION,
-          values: values
-        });
-      }
-      return JSON.stringify({
-        version: this.getVersion(config),
-        values: values
-      });
-    },
-
-    persistConfig: function (config, forceWrite) {
-      if (!this.hasPersistentConfig(config)) return false;
-
-      var raw = this.buildStoredPayload(config);
-      if (!raw) return false;
-      if (!forceWrite && raw === String(config.__anitaLastPersistedRaw || "")) {
-        return false;
-      }
-
-      var ns = this.normalizeNamespace(config.storageNamespace);
-      var encoded = "";
-      try {
-        encoded = AnitaBase64.encode(raw);
-      } catch (eEnc) {
-        return false;
-      }
-      var token = "[" + this.TOKEN_PREFIX + ns + "]:" + encoded;
-
-      try {
-        var current = "";
-        if (typeof GameInterfaceAPI !== "undefined" &&
-            GameInterfaceAPI &&
-            typeof GameInterfaceAPI.GetSettingString === "function") {
-          current = String(GameInterfaceAPI.GetSettingString(this.CONVAR_KEY) || "");
-        }
-        var cleaned = current.replace(this.getCleanupRegex(ns), "").replace(/,,+/g, ",").replace(/^,|,$/, "");
-        var finalValue = (cleaned ? cleaned + "," : "") + token;
-        if (finalValue.length > MAX_CONVAR_VALUE_LEN) {
-          // Do NOT write cleaned back — that would destroy the last valid token.
-          // Leave the convar untouched and skip only the new write.
-          this.warnPersistence(config, "convar skipped (payload_too_large)");
-        } else if (!this.writeConvarBestEffort(this.CONVAR_KEY, finalValue)) {
-          this.warnPersistence(config, "convar unavailable");
-        } else {
-        }
-      } catch (e) {
-      }
-
-      try {
-        if (this.hasPersistentStorage()) {
-          var storageKey = this.getStorageKey(config);
-          if (storageKey) {
-            $.persistentStorage.setItem(storageKey, encoded);
-          }
-        }
-      } catch (ePersist) {
-      }
-
-      this.writeSessionMirror(config, encoded);
-
-      config.__anitaLastPersistedRaw = raw;
-      return true;
-    },
-
-    requestBootstrap: function (config, reason) {
-      if (!this.hasPersistentConfig(config)) return;
-      var payload = {
-        magic_word: "ANITA_REQUEST_BOOTSTRAP",
-        mod_title: config.title,
-        storageNamespace: this.normalizeNamespace(config.storageNamespace),
-        reason: String(reason || "request")
-      };
-      $.DispatchEvent("ClientUI_FireOutput", JSON.stringify(payload));
-    },
-
-    applyUpdate: function (config, settingId, value) {
-      var elements = this.getElements(config);
-      for (var i = 0; i < elements.length; i++) {
-        var element = elements[i];
-        if (!element || element.id !== settingId) continue;
-        element.currentValue = this.sanitizeValue(element, value);
-        return true;
-      }
-      return false;
+!function() {
+    "use strict";
+    const CONFIG_VERSION = "2.2.3", CONFIG_IDS = {
+        WINDOW: "AnitaUI_Window",
+        BACKDROP: "AnitaUI_Backdrop",
+        NAVBAR: "AnitaUI_NavBar",
+        CONTENT: "AnitaUI_ContentArea",
+        OVERLAY_BTN: "AnitaOverlayBtn",
+        HUD_ROOT: "Hud"
+    }, CONFIG_CLASSES = {
+        ESCAPE_MENU: "ShowEscapeMenu",
+        OPEN: "Open",
+        ACTIVE: "Active",
+        VISIBLE: "Visible",
+        CHECKED: "Checked",
+        ATTENTION: "Attention"
+    }, CONFIG_UI = {
+        TAB_MAX_CHARS: 17,
+        MONITOR_INTERVAL: .05
+    }, HP_PERSIST_ALIASES = {
+        hp_enabled: "e",
+        hp_mode: "m",
+        hp_low_threshold: "l",
+        hp_high_threshold: "h",
+        hp_bg_visible: "b",
+        hp_team_colors: "t",
+        hp_color_low: "cl",
+        hp_color_mid: "cm",
+        hp_color_high: "ch",
+        hp_counter_size: "s",
+        hp_counter_position: "p",
+        hp_text_color_mode: "tm",
+        hp_level_number_visible: "lnv",
+        hp_text_color_low: "tl",
+        hp_text_color_mid: "ti",
+        hp_text_color_high: "th",
+        hp_pulse_bpm: "bp",
+        hp_pulse_intensity: "pi",
+        hp_pulse_enabled: "pe",
+        hp_pulse_text_enabled: "pte",
+        hp_pulse_text_scale: "pts",
+        hp_pulse_text_position: "ptp",
+        hp_pulse_hide_bar: "phb",
+        hp_skip_buildings: "sb",
+        hp_pulse_threshold: "pt",
+        hp_friend_enabled: "fe",
+        hp_friend_pulse_enabled: "fpe",
+        hp_friend_pulse_bpm: "fpb",
+        hp_friend_pulse_intensity: "fpi",
+        hp_friend_pulse_threshold: "fpt",
+        hp_friend_color_low: "fcl",
+        hp_friend_color_mid: "fcm",
+        hp_friend_color_high: "fch",
+        hp_friend_pulse_color_enabled: "fpce",
+        hp_friend_pulse_color: "fpc",
+        hp_kill_zone_enabled: "kze",
+        hp_kill_zone_threshold: "kzt",
+        hp_kill_zone_color: "kzc",
+        hp_kill_zone_width: "kzw"
+    }, HP_PERSIST_ALIAS_TO_ID = function() {
+        var out = {};
+        for (var id in HP_PERSIST_ALIASES) Object.prototype.hasOwnProperty.call(HP_PERSIST_ALIASES, id) && (out[HP_PERSIST_ALIASES[id]] = id);
+        return out;
+    }();
+    var CHARS, AnitaBase64 = (CHARS = "ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789-_",
+    {
+        encode: function(str) {
+            for (var bytes = [], i = 0; i < str.length; i++) {
+                var code = str.charCodeAt(i);
+                code < 128 ? bytes.push(code) : code < 2048 ? (bytes.push(192 | code >> 6), bytes.push(128 | 63 & code)) : (bytes.push(224 | code >> 12),
+                bytes.push(128 | code >> 6 & 63), bytes.push(128 | 63 & code));
+            }
+            for (var out = "", j = 0; j < bytes.length; j += 3) {
+                var b0 = bytes[j], b1 = bytes[j + 1] || 0, b2 = bytes[j + 2] || 0;
+                out += CHARS[b0 >> 2], out += CHARS[(3 & b0) << 4 | b1 >> 4], out += j + 1 < bytes.length ? CHARS[(15 & b1) << 2 | b2 >> 6] : "",
+                out += j + 2 < bytes.length ? CHARS[63 & b2] : "";
+            }
+            return out;
+        },
+        decode: function(str) {
+            for (var lookup = {}, i = 0; i < 64; i++) lookup[CHARS[i]] = i;
+            function getVal(ch) {
+                if (void 0 === ch) return 0;
+                if (!Object.prototype.hasOwnProperty.call(lookup, ch)) throw new Error("Invalid base64url char: " + ch);
+                return lookup[ch];
+            }
+            for (var decodedBytes = [], j = 0; j < str.length; j += 4) {
+                var c0 = getVal(str[j]), c1 = getVal(str[j + 1]), c2 = void 0 !== str[j + 2] ? getVal(str[j + 2]) : 0, c3 = void 0 !== str[j + 3] ? getVal(str[j + 3]) : 0;
+                decodedBytes.push(c0 << 2 | c1 >> 4), void 0 !== str[j + 2] && decodedBytes.push((15 & c1) << 4 | c2 >> 2),
+                void 0 !== str[j + 3] && decodedBytes.push((3 & c2) << 6 | c3);
+            }
+            for (var out = "", k = 0; k < decodedBytes.length; k++) {
+                var b = decodedBytes[k];
+                if (b < 128) out += String.fromCharCode(b); else if (b < 224) out += String.fromCharCode((31 & b) << 6 | 63 & decodedBytes[++k]); else {
+                    var cont2 = decodedBytes[++k], cont3 = decodedBytes[++k];
+                    out += String.fromCharCode((15 & b) << 12 | (63 & cont2) << 6 | 63 & cont3);
+                }
+            }
+            return out;
+        }
+    });
+    function emitUpdate(modTitle, settingId, newValue, meta) {
+        var payload = {
+            magic_word: "ANITA_UPDATE",
+            mod_title: modTitle,
+            setting_id: settingId,
+            value: newValue
+        };
+        if (meta && "object" == typeof meta) for (var key in meta) Object.prototype.hasOwnProperty.call(meta, key) && (payload[key] = meta[key]);
+        $.DispatchEvent("ClientUI_FireOutput", JSON.stringify(payload));
     }
-  };
-
-  const AnitaComponents = {
-    createToggle: function (parent, config, modTitle) {
-      const row = $.CreatePanel("Panel", parent, "");
-      row.AddClass("AnitaToggleRow");
-
-      const btn = $.CreatePanel("Button", row, "");
-      btn.AddClass("AnitaToggleBtn");
-
-      const lbl = $.CreatePanel("Label", row, "");
-      lbl.text = config.label || "Option";
-      lbl.AddClass("AnitaLabel");
-
-      const box = $.CreatePanel("Panel", row, "");
-      box.AddClass("AnitaCheckBox");
-
-      const tick = $.CreatePanel("Panel", box, "");
-      tick.AddClass("AnitaCheckMark");
-
-      let isOn = (config.currentValue !== undefined) ? config.currentValue : (config.defaultValue || false);
-
-      const updateState = (active) => row.SetHasClass("Checked", active);
-      updateState(isOn);
-
-      btn.SetPanelEvent("onactivate", () => {
-        isOn = !isOn;
-        updateState(isOn);
-
-        config.currentValue = isOn;
-
-        if (config.id) emitUpdate(modTitle, config.id, isOn);
-        if (config.onChange) config.onChange(isOn);
-
-        var ownerConfig = AnitaCore.findRegisteredMod(modTitle);
-        if (ownerConfig && AnitaRenderer.hasVisibilityDependents(ownerConfig, config.id)) {
-          AnitaCore.queueRenderRefresh(ownerConfig);
-        }
-      });
-
-      return row;
-    },
-
-    createStepper: function (parent, config, modTitle) {
-      const row = $.CreatePanel("Panel", parent, "");
-      row.AddClass("AnitaRow");
-      const lbl = $.CreatePanel("Label", row, "");
-      lbl.text = config.label || "Value";
-      lbl.AddClass("AnitaLabel");
-      const controls = $.CreatePanel("Panel", row, "");
-      controls.AddClass("AnitaStepperControls");
-      const btnM = $.CreatePanel("Button", controls, "");
-      btnM.AddClass("AnitaStepBtn");
-      $.CreatePanel("Label", btnM, "less").text = "-";
-      const input = $.CreatePanel("TextEntry", controls, "");
-      input.AddClass("AnitaStepInput");
-      const btnP = $.CreatePanel("Button", controls, "");
-      btnP.AddClass("AnitaStepBtn");
-      $.CreatePanel("Label", btnP, "").text = "+";
-
-      let val = (config.currentValue !== undefined) ? config.currentValue : (config.defaultValue || 0);
-      const step = config.step || 1;
-      const isFloat = !Number.isInteger(step);
-      input.text = isFloat ? val.toFixed(2) : val;
-
-      function update(newVal) {
-        if (isFloat) newVal = parseFloat(newVal.toFixed(2)); else newVal = Math.round(newVal);
-        val = newVal;
-        config.currentValue = val;
-        input.text = val.toString();
-        if (config.onChange) config.onChange(val);
-        if (config.id && modTitle) {
-          emitUpdate(modTitle, config.id, val);
-        }
-      }
-
-      input.SetPanelEvent("ontextentrychange", () => {
-        let v = parseFloat(input.text);
-        if (!isNaN(v)) {
-          val = v;
-          config.currentValue = v;
-        }
-      });
-
-      input.SetPanelEvent("oncancel", () => {
-        AnitaRenderer.toggle(false);
-      });
-
-      btnM.SetPanelEvent("onactivate", () => update(val - step));
-      btnP.SetPanelEvent("onactivate", () => update(val + step));
-
-      input.SetPanelEvent("oninputsubmit", () => {
-        update(val);
-        $.DispatchEvent("DropInputFocus", input);
-        AnitaRenderer.mainWindow.SetFocus();
-      });
-
-      input.SetPanelEvent("onfocusout", () => {
-        update(val);
-      });
-
-      return row;
-    },
-
-    createSlider: function (parent, config, modTitle) {
-      const row = $.CreatePanel("Panel", parent, "");
-      row.AddClass("AnitaRow");
-      row.AddClass("AnitaSliderRow");
-      row.style.width = "100%";
-
-      const lbl = $.CreatePanel("Label", row, "");
-      lbl.text = config.label || "Value";
-      lbl.AddClass("AnitaLabel");
-
-      const valueGroup = $.CreatePanel("Panel", row, "");
-      valueGroup.AddClass("AnitaSliderValueGroup");
-      valueGroup.AddClass("SliderValueGroup");
-      valueGroup.style.flowChildren = "right";
-      valueGroup.style.verticalAlign = "center";
-      valueGroup.style.horizontalAlign = "left";
-      valueGroup.style.width = "296px";
-
-      const sliderContainer = $.CreatePanel("Panel", valueGroup, "");
-      sliderContainer.AddClass("AnitaSliderContainer");
-      sliderContainer.AddClass("SliderContainer");
-      sliderContainer.style.width = "230px";
-      sliderContainer.style.height = "26px";
-      sliderContainer.style.padding = "0px";
-      sliderContainer.style.verticalAlign = "center";
-      sliderContainer.style.overflow = "noclip";
-
-      const slider = $.CreatePanel("Slider", sliderContainer, "", { direction: "horizontal" });
-      slider.AddClass("AnitaSlider");
-      slider.AddClass("HorizontalSlider");
-      slider.style.width = "100%";
-      slider.style.height = "100%";
-      slider.style.verticalAlign = "center";
-      slider.style.overflow = "noclip";
-
-      const valueLbl = $.CreatePanel("Label", valueGroup, "");
-      valueLbl.AddClass("AnitaSliderValue");
-
-      const rawMin = Number(config.min);
-      const rawMax = Number(config.max);
-      const rawStep = Number(config.step);
-      const min = isFinite(rawMin) ? rawMin : 0;
-      const max = isFinite(rawMax) ? rawMax : 100;
-      const step = isFinite(rawStep) && rawStep > 0 ? rawStep : 1;
-
-      let val = (config.currentValue !== undefined)
-        ? config.currentValue
-        : (config.defaultValue !== undefined ? config.defaultValue : min);
-      let isSyncing = false;
-      function normalize(nextVal) {
-        let next = Number(nextVal);
-        if (!isFinite(next)) next = Number(config.defaultValue);
-        if (!isFinite(next)) next = min;
-        if (next < min) next = min;
-        if (next > max) next = max;
-        if (Math.round(step) === step) {
-          next = Math.round(next);
-        } else {
-          next = parseFloat(next.toFixed(2));
-        }
-        return next;
-      }
-
-      function syncVisuals(nextVal, emitUpdateEvent) {
-        const normalized = normalize(nextVal);
-        val = normalized;
-        config.currentValue = normalized;
-        valueLbl.text = normalized.toString() + "%";
-
-        if (slider && slider.IsValid && slider.IsValid()) {
-          if (Number(slider.value) !== normalized) {
-            isSyncing = true;
+    const AnitaPersistence = {
+        normalizeNamespace: function(storageNamespace) {
+            return String(storageNamespace || "").toLowerCase().replace(/[^a-z0-9_]+/g, "_").replace(/^_+|_+$/g, "");
+        },
+        getVersion: function(config) {
+            var version = Number(config && config.storageVersion);
+            return !isFinite(version) || version < 1 ? 1 : Math.floor(version);
+        },
+        hasPersistentConfig: function(config) {
+            return this.normalizeNamespace(config && config.storageNamespace).length > 0;
+        },
+        isHpColorsConfig: function(config) {
+            return !!config && "HP Colors" === String(config.title || "") && "hp_colors" === this.normalizeNamespace(config.storageNamespace);
+        },
+        CONVAR_KEY: "deadlock_hero_debuts_seen",
+        TOKEN_PREFIX: "ANITA-v1-",
+        getTokenRegex: function(ns) {
+            return new RegExp("\\[" + this.TOKEN_PREFIX + ns + "\\]:[A-Za-z0-9_-]+");
+        },
+        getCleanupRegex: function(ns) {
+            return new RegExp("\\[" + this.TOKEN_PREFIX + ns + "\\]:[A-Za-z0-9_-]*", "g");
+        },
+        canWriteConvarDirect: function() {
+            return "undefined" != typeof GameInterfaceAPI && !!GameInterfaceAPI && ("function" == typeof GameInterfaceAPI.ConsoleCommand || "function" == typeof GameInterfaceAPI.SetSettingString);
+        },
+        getStorageKey: function(config) {
+            var ns = this.normalizeNamespace(config && config.storageNamespace);
+            return ns ? "anita_v1_" + ns : "";
+        },
+        getRootPanel: function() {
+            for (var rootPanel = $.GetContextPanel(); rootPanel && rootPanel.GetParent && rootPanel.GetParent(); ) rootPanel = rootPanel.GetParent();
+            return rootPanel || null;
+        },
+        getSessionEncoded: function(config) {
+            var key = this.getStorageKey(config);
+            if (!key) return "";
+            var rootPanel = this.getRootPanel(), rootEncoded = "", hudEncoded = "";
             try {
-              if (typeof slider.SetValueNoEvents === "function") {
-                slider.SetValueNoEvents(normalized);
-              } else {
-                slider.value = normalized;
-              }
-            } finally {
-              isSyncing = false;
+                rootPanel && rootPanel.GetAttributeString && (rootEncoded = String(rootPanel.GetAttributeString(key, "") || ""));
+            } catch (eRoot) {}
+            try {
+                var hudPanel = rootPanel && rootPanel.FindChildTraverse ? rootPanel.FindChildTraverse("Hud") : null;
+                hudPanel && hudPanel.GetAttributeString && (hudEncoded = String(hudPanel.GetAttributeString(key, "") || ""));
+            } catch (eHud) {}
+            return rootEncoded || hudEncoded || "";
+        },
+        hasPersistentStorage: function() {
+            try {
+                return !(!$ || !$.persistentStorage || "function" != typeof $.persistentStorage.getItem || "function" != typeof $.persistentStorage.setItem);
+            } catch (eStorage) {
+                return !1;
             }
-          }
-        }
-
-        if (emitUpdateEvent) {
-          if (config.onChange) config.onChange(normalized);
-          if (config.id && modTitle) {
-            emitUpdate(modTitle, config.id, normalized);
-          }
-        }
-      }
-
-      slider.min = min;
-      slider.max = max;
-      slider.increment = step;
-      slider.value = normalize(val);
-
-      if (typeof slider.SetShowDefaultValue === "function") {
-        slider.SetShowDefaultValue(false);
-      }
-      if (typeof slider.SetRequiresSelection === "function") {
-        slider.SetRequiresSelection(false);
-      }
-
-      syncVisuals(val, false);
-
-      slider.SetPanelEvent("onvaluechanged", function () {
-        if (isSyncing) return;
-        syncVisuals(slider.value, true);
-      });
-
-      slider.SetPanelEvent("oncancel", () => {
-        AnitaRenderer.toggle(false);
-      });
-
-      return row;
-    },
-
-    createButton: function (parent, config, modTitle) {
-      const btn = $.CreatePanel("Button", parent, "");
-      btn.AddClass("AnitaActionBtn");
-      const lbl = $.CreatePanel("Label", btn, "");
-      lbl.text = config.label || "Action";
-
-      btn.SetPanelEvent("onactivate", () => {
-        if (config.onClick) config.onClick();
-
-        if (config.id && modTitle) {
-          emitUpdate(modTitle, config.id, true);
-        }
-
-        btn.AddClass("Activated");
-        $.Schedule(0.1, () => btn.RemoveClass("Activated"));
-      });
-      return btn;
-    },
-
-    createCycler: function (parent, config, modTitle) {
-      const row = $.CreatePanel("Panel", parent, "");
-      row.AddClass("AnitaRow");
-
-      const lbl = $.CreatePanel("Label", row, "");
-      lbl.text = config.label || "Cycle";
-      lbl.AddClass("AnitaLabel");
-
-      const btn = $.CreatePanel("Button", row, "");
-      btn.AddClass("AnitaCyclerBtn");
-
-      const valLbl = $.CreatePanel("Label", btn, "");
-
-      const options = config.options || ["OFF", "ON"];
-
-      let idx = (config.currentValue !== undefined) ? config.currentValue : (config.defaultValue || 0);
-
-      if (idx < 0 || idx >= options.length) idx = 0;
-
-      const updateVisuals = () => {
-        valLbl.text = options[idx];
-      };
-
-      updateVisuals();
-
-      btn.SetPanelEvent("onactivate", () => {
-        idx = (idx + 1) % options.length;
-        updateVisuals();
-
-        config.currentValue = idx;
-
-        var ownerConfig = AnitaCore.findRegisteredMod(modTitle);
-        if (ownerConfig && AnitaRenderer.hasVisibilityDependents(ownerConfig, config.id)) {
-          AnitaCore.queueRenderRefresh(ownerConfig);
-        }
-
-        if (config.id && modTitle) {
-          emitUpdate(modTitle, config.id, idx);
-        }
-
-        if (config.onChange) config.onChange(idx, options[idx]);
-      });
-
-      return row;
-    },
-
-    createColorPicker: function (parent, config, modTitle) {
-      const row = $.CreatePanel("Panel", parent, "");
-      row.AddClass("AnitaRow");
-      row.AddClass("AnitaSliderRow");
-      row.style.overflow = "noclip";
-      row.style.width = "100%";
-
-      const lbl = $.CreatePanel("Label", row, "");
-      lbl.text = config.label || "Color";
-      lbl.AddClass("AnitaLabel");
-
-      let currentColor = normalizeHexColor((config.currentValue !== undefined) ? config.currentValue : (config.defaultValue || "#FF0000"));
-      let pickerBoxHue = 0;
-      let pickerBoxSat = 1;
-      let colorPopupPanel = null;
-      let colorBoxFrame = null;
-      let colorBoxPanel = null;
-      let colorBoxCursor = null;
-      let colorPickerSyncing = false;
-      let colorPickerPollGeneration = 0;
-      let colorDragging = false;
-      const hasGameUI = (typeof GameUI !== "undefined" && GameUI !== null);
-      let colorDragAnchorX = -1;
-      let colorDragAnchorY = -1;
-      let colorDragSource = "";
-      let popupPreview = null;
-      let popupHexLabel = null;
-      let popupMetaLabel = null;
-      let colorPreview = null;
-      let rowHexLabel = null;
-      let pickerHueSlider = null;
-      let pickerSatSlider = null;
-      let pickerSatTrack = null;
-      let pickerHueValue = null;
-      let pickerSatValue = null;
-
-      function clampByte(value) {
-        let next = Number(value);
-        if (!isFinite(next)) next = 0;
-        if (next < 0) next = 0;
-        if (next > 255) next = 255;
-        return Math.round(next);
-      }
-
-      function byteToHex(value) {
-        const hex = clampByte(value).toString(16).toUpperCase();
-        return hex.length < 2 ? "0" + hex : hex;
-      }
-
-      function rgbToHex(r, g, b) {
-        return "#" + byteToHex(r) + byteToHex(g) + byteToHex(b);
-      }
-
-      function hexToRgbLocal(colorCode) {
-        if (!colorCode) return [255, 255, 255];
-
-        const text = String(colorCode).trim();
-        if (text.charAt(0) === "#") {
-          let hex = text.slice(1);
-          if (hex.length === 3) {
-            hex = hex[0] + hex[0] + hex[1] + hex[1] + hex[2] + hex[2];
-          }
-          if (hex.length >= 6) {
-            return [
-              parseInt(hex.slice(0, 2), 16) || 0,
-              parseInt(hex.slice(2, 4), 16) || 0,
-              parseInt(hex.slice(4, 6), 16) || 0
-            ];
-          }
-        }
-
-        const match = text.match(/rgb\((\d+),\s*(\d+),\s*(\d+)\)/i);
-        if (match) {
-          return [
-            clampByte(match[1]),
-            clampByte(match[2]),
-            clampByte(match[3])
-          ];
-        }
-
-        return [255, 255, 255];
-      }
-
-      function hsvToRgb(h, s, v) {
-        var hue = Number(h);
-        var sat = Number(s);
-        var val = Number(v);
-
-        if (!isFinite(hue)) hue = 0;
-        if (!isFinite(sat)) sat = 0;
-        if (!isFinite(val)) val = 0;
-
-        hue = ((hue % 360) + 360) % 360;
-        sat = Math.max(0, Math.min(1, sat));
-        val = Math.max(0, Math.min(1, val));
-
-        var c = val * sat;
-        var x = c * (1 - Math.abs(((hue / 60) % 2) - 1));
-        var m = val - c;
-        var r = 0;
-        var g = 0;
-        var b = 0;
-
-        if (hue < 60) {
-          r = c; g = x; b = 0;
-        } else if (hue < 120) {
-          r = x; g = c; b = 0;
-        } else if (hue < 180) {
-          r = 0; g = c; b = x;
-        } else if (hue < 240) {
-          r = 0; g = x; b = c;
-        } else if (hue < 300) {
-          r = x; g = 0; b = c;
-        } else {
-          r = c; g = 0; b = x;
-        }
-
-        return [
-          clampByte((r + m) * 255),
-          clampByte((g + m) * 255),
-          clampByte((b + m) * 255)
-        ];
-      }
-
-      function hsvToHex(h, s, v) {
-        var rgb = hsvToRgb(h, s, v);
-        return rgbToHex(rgb[0], rgb[1], rgb[2]);
-      }
-
-      function rgbToHsv(r, g, b) {
-        var red = clampByte(r) / 255;
-        var green = clampByte(g) / 255;
-        var blue = clampByte(b) / 255;
-        var max = Math.max(red, green, blue);
-        var min = Math.min(red, green, blue);
-        var delta = max - min;
-        var hue = 0;
-        var sat = max === 0 ? 0 : delta / max;
-        var val = max;
-
-        if (delta !== 0) {
-          if (max === red) {
-            hue = 60 * (((green - blue) / delta) % 6);
-          } else if (max === green) {
-            hue = 60 * (((blue - red) / delta) + 2);
-          } else {
-            hue = 60 * (((red - green) / delta) + 4);
-          }
-        }
-
-        if (hue < 0) hue += 360;
-        return [hue, sat, val];
-      }
-
-      function clamp01(value) {
-        var next = Number(value);
-        if (!isFinite(next)) next = 0;
-        if (next < 0) next = 0;
-        if (next > 1) next = 1;
-        return next;
-      }
-
-      function isValidPanel(panel) {
-        return !!(panel && panel.IsValid && panel.IsValid());
-      }
-
-      function parsePoint(candidate) {
-        if (!candidate) return null;
-
-        if (typeof candidate.length === "number" && candidate.length >= 2) {
-          var arrayX = Number(candidate[0]);
-          var arrayY = Number(candidate[1]);
-          if (isFinite(arrayX) && isFinite(arrayY)) {
-            return { x: arrayX, y: arrayY };
-          }
-        }
-
-        if (typeof candidate === "object") {
-          var objectX = Number(candidate.x !== undefined ? candidate.x : candidate[0]);
-          var objectY = Number(candidate.y !== undefined ? candidate.y : candidate[1]);
-          if (isFinite(objectX) && isFinite(objectY)) {
-            return { x: objectX, y: objectY };
-          }
-        }
-
-        return null;
-      }
-
-      function getCursorPosition() {
-        try {
-          if (!hasGameUI || typeof GameUI.GetCursorPosition !== "function") return null;
-          return parsePoint(GameUI.GetCursorPosition());
-        } catch (e) {}
-        return null;
-      }
-
-      function getPanelWindowPosition(panel) {
-        if (!isValidPanel(panel)) return null;
-
-        try {
-          if (typeof panel.GetPositionWithinWindow === "function") {
-            var point = parsePoint(panel.GetPositionWithinWindow());
-            if (point) return point;
-          }
-        } catch (e) {}
-
-        var left = Number(panel.actualxoffset || panel.actualx || 0);
-        var top = Number(panel.actualyoffset || panel.actualy || 0);
-        if (!isFinite(left)) left = 0;
-        if (!isFinite(top)) top = 0;
-        return { x: left, y: top };
-      }
-
-      function getPanelBounds(panel) {
-        if (!isValidPanel(panel)) {
-          return { left: 0, top: 0, width: 1, height: 1 };
-        }
-
-        var panelPos = getPanelWindowPosition(panel);
-        var left = panelPos ? panelPos.x : Number(panel.actualxoffset || panel.actualx || 0);
-        var top = panelPos ? panelPos.y : Number(panel.actualyoffset || panel.actualy || 0);
-        var width = Number(panel.actuallayoutwidth || panel.contentwidth || panel.width || 1);
-        var height = Number(panel.actuallayoutheight || panel.contentheight || panel.height || 1);
-
-        if (!isFinite(left)) left = 0;
-        if (!isFinite(top)) top = 0;
-        if (!isFinite(width) || width <= 0) width = 1;
-        if (!isFinite(height) || height <= 0) height = 1;
-
-        return { left: left, top: top, width: width, height: height };
-      }
-
-      function getPanelLocalCursorPosition(panel) {
-        if (!isValidPanel(panel)) return null;
-
-        var bounds = getPanelBounds(panel);
-        var panelPos = getPanelWindowPosition(panel);
-
-        try {
-          if (typeof panel.GetCursorPosition === "function") {
-            var rawPoint = parsePoint(panel.GetCursorPosition());
-            if (rawPoint) {
-              var looksLocal =
-                rawPoint.x >= 0 && rawPoint.x <= bounds.width &&
-                rawPoint.y >= 0 && rawPoint.y <= bounds.height;
-
-              if (looksLocal) {
-                return rawPoint;
-              }
-
-              if (panelPos) {
-                return {
-                  x: rawPoint.x - panelPos.x,
-                  y: rawPoint.y - panelPos.y
-                };
-              }
-
-              return rawPoint;
+        },
+        writeSessionMirror: function(config, encoded) {
+            var key = this.getStorageKey(config);
+            if (key) try {
+                var rootPanel = this.getRootPanel();
+                rootPanel && rootPanel.SetAttributeString && rootPanel.SetAttributeString(key, encoded);
+                var hudPanel = rootPanel && rootPanel.FindChildTraverse ? rootPanel.FindChildTraverse("Hud") : null;
+                hudPanel && hudPanel.SetAttributeString && hudPanel.SetAttributeString(key, encoded);
+            } catch (eSess) {}
+        },
+        writeHpSharedSnapshot: function(config) {
+            if (this.isHpColorsConfig(config) && Array.isArray(config.elements)) try {
+                for (var out = {}, i = 0; i < config.elements.length; i++) {
+                    var element = config.elements[i];
+                    element && element.id && this.shouldPersistElement(element) && (out[element.id] = this.sanitizeValue(element, void 0 !== element.currentValue ? element.currentValue : element.defaultValue));
+                }
+                "undefined" != typeof GameUI && GameUI && GameUI.CustomUIConfig && (GameUI.CustomUIConfig().__hpColorsCfgRaw = JSON.stringify(out));
+            } catch (eShare) {}
+        },
+        writeConvar: function(key, value) {
+            "undefined" != typeof GameInterfaceAPI && GameInterfaceAPI && "function" == typeof GameInterfaceAPI.SetSettingString ? GameInterfaceAPI.SetSettingString(key, value) : "undefined" != typeof GameInterfaceAPI && GameInterfaceAPI && "function" == typeof GameInterfaceAPI.ConsoleCommand && GameInterfaceAPI.ConsoleCommand(key + ' "' + value + '"');
+        },
+        writeConvarBestEffort: function(key, value) {
+            var command = String(key || "") + ' "' + String(value || "") + '"';
+            if (this.canWriteConvarDirect()) try {
+                return this.writeConvar(key, value), !0;
+            } catch (e0) {}
+            if (function(commandText) {
+                var cmd = String(commandText || "").trim(), didAny = !1;
+                if (!cmd) return !1;
+                try {
+                    $.DispatchEvent("CitadelConCommand", cmd), didAny = !0;
+                } catch (e0) {}
+                try {
+                    "undefined" != typeof GameInterfaceAPI && GameInterfaceAPI && "function" == typeof GameInterfaceAPI.ConsoleCommand && (GameInterfaceAPI.ConsoleCommand(cmd),
+                    didAny = !0);
+                } catch (e1) {}
+                try {
+                    $.DispatchEvent("ConsoleCommand", cmd), didAny = !0;
+                } catch (e2) {}
+                try {
+                    $.DispatchEvent("GameUIRunCommand", cmd), didAny = !0;
+                } catch (e3) {}
+                return didAny;
+            }(command)) {
+                try {
+                    "undefined" != typeof GameInterfaceAPI && GameInterfaceAPI && "function" == typeof GameInterfaceAPI.SetSettingString && GameInterfaceAPI.SetSettingString(key, value);
+                } catch (eSS) {}
+                return !0;
             }
-          }
-        } catch (e) {}
-
-        var screenCursor = getCursorPosition();
-        if (!screenCursor || !panelPos) return null;
-
-        return {
-          x: screenCursor.x - panelPos.x,
-          y: screenCursor.y - panelPos.y
-        };
-      }
-
-      function getBoxStateFromColor(colorCode) {
-        var rgb = hexToRgbLocal(colorCode);
-        var hsv = rgbToHsv(rgb[0], rgb[1], rgb[2]);
-        return {
-          hue: Math.round(hsv[0]) % 360,
-          sat: clamp01(hsv[1])
-        };
-      }
-
-      function normalizeBoxState(boxState, fallbackColorCode) {
-        var fallback = getBoxStateFromColor(fallbackColorCode || currentColor);
-        if (!boxState) {
-          return fallback;
-        }
-
-        var hue = Number(boxState.hue);
-        var sat = Number(boxState.sat);
-        if (!isFinite(hue)) hue = fallback.hue;
-        if (!isFinite(sat)) sat = fallback.sat;
-
-        hue = Math.round(hue) % 360;
-        if (hue < 0) hue += 360;
-        sat = clamp01(sat);
-
-        return { hue: hue, sat: sat };
-      }
-
-      function setPickerBoxState(boxState, fallbackColorCode) {
-        var resolved = normalizeBoxState(boxState, fallbackColorCode);
-        pickerBoxHue = resolved.hue;
-        pickerBoxSat = resolved.sat;
-        return resolved;
-      }
-
-      function colorFromBoxState(hue, sat) {
-        return hsvToHex(hue, sat, 1);
-      }
-
-      function hueFromRelX(relX) {
-        var normalized = clamp01(relX);
-        return Math.max(0, Math.min(359, Math.round(normalized * 359)));
-      }
-
-      function hueToSliderPercent(hue) {
-        var nextHue = Number(hue);
-        if (!isFinite(nextHue)) nextHue = 0;
-        nextHue = Math.max(0, Math.min(359, Math.round(nextHue)));
-        return Math.round((nextHue / 359) * 100);
-      }
-
-      function normalizeHexColor(colorCode) {
-        if (!colorCode) return "#FFFFFF";
-
-        if (typeof colorCode === "number") {
-          var packed = Math.max(0, Math.floor(colorCode));
-          var packedHex = packed.toString(16).toUpperCase();
-          while (packedHex.length < 6) packedHex = "0" + packedHex;
-          return "#" + packedHex.slice(-6);
-        }
-
-        if (typeof colorCode === "string") {
-          var text = String(colorCode).trim();
-          if (!text) return "#FFFFFF";
-          if (text.charAt(0) === "#") {
-            var hex = text.slice(1);
-            if (hex.length === 3) {
-              hex = hex[0] + hex[0] + hex[1] + hex[1] + hex[2] + hex[2];
+            return !1;
+        },
+        getElements: function(config) {
+            return config && Array.isArray(config.elements) ? config.elements : [];
+        },
+        warnPersistence: function(config, reason) {
+            config && config.title && String(config.title);
+            config && config.__anitaLastPersistWarning === reason || config && (config.__anitaLastPersistWarning = reason);
+        },
+        shouldPersistElement: function(element) {
+            return !(!element || !element.id || "button" === element.type);
+        },
+        sanitizeValue: function(element, value) {
+            if (!element) return value;
+            var fallback = element.defaultValue, type = String(element.type || "");
+            if ("toggle" === type) {
+                if (!0 === value || !1 === value) return value;
+                if (1 === value || "1" === value) return !0;
+                if (0 === value || "0" === value) return !1;
+                if ("string" == typeof value) {
+                    var lowered = value.toLowerCase();
+                    if ("true" === lowered) return !0;
+                    if ("false" === lowered) return !1;
+                }
+                return !!fallback;
             }
-            if (hex.length >= 6) {
-              return "#" + hex.slice(0, 6).toUpperCase();
+            if ("cycler" === type) {
+                var count = Array.isArray(element.options) ? element.options.length : 0, nextIndex = Number(value);
+                if (isFinite(nextIndex) || (nextIndex = Number(fallback)), isFinite(nextIndex) || (nextIndex = 0),
+                (nextIndex = Math.round(nextIndex)) < 0 && (nextIndex = 0), count > 0 && nextIndex >= count) {
+                    var fallbackIndex = Number(fallback);
+                    (!isFinite(fallbackIndex) || fallbackIndex < 0 || fallbackIndex >= count) && (fallbackIndex = 0),
+                    nextIndex = fallbackIndex;
+                }
+                return nextIndex;
             }
-          }
-
-          var rgbMatch = text.match(/rgb\((\d+),\s*(\d+),\s*(\d+)\)/i);
-          if (rgbMatch) {
-            return rgbToHex(rgbMatch[1], rgbMatch[2], rgbMatch[3]).toUpperCase();
-          }
-        }
-
-        if (typeof colorCode === "object") {
-          if (colorCode.hex !== undefined) {
-            return normalizeHexColor(colorCode.hex);
-          }
-          if (colorCode.h !== undefined && colorCode.s !== undefined && colorCode.v !== undefined) {
-            return hsvToHex(colorCode.h, colorCode.s, colorCode.v);
-          }
-          if (colorCode.r !== undefined && colorCode.g !== undefined && colorCode.b !== undefined) {
-            return rgbToHex(colorCode.r, colorCode.g, colorCode.b).toUpperCase();
-          }
-          if (colorCode.red !== undefined && colorCode.green !== undefined && colorCode.blue !== undefined) {
-            return rgbToHex(colorCode.red, colorCode.green, colorCode.blue).toUpperCase();
-          }
-        }
-
-        return "#FFFFFF";
-      }
-
-      function getColorBoxMetrics() {
-        var refPanel = isValidPanel(colorBoxFrame) ? colorBoxFrame : colorBoxPanel;
-        if (!isValidPanel(refPanel)) return null;
-
-        var bounds = getPanelBounds(refPanel);
-        var width = Number(refPanel.actuallayoutwidth || bounds.width || 240);
-        var height = Number(refPanel.actuallayoutheight || bounds.height || 240);
-        var cursorWidth = Number(isValidPanel(colorBoxCursor) ? (colorBoxCursor.actuallayoutwidth || colorBoxCursor.contentwidth || colorBoxCursor.width) : 18);
-        var cursorHeight = Number(isValidPanel(colorBoxCursor) ? (colorBoxCursor.actuallayoutheight || colorBoxCursor.contentheight || colorBoxCursor.height) : 18);
-
-        if (!isFinite(width) || width <= 1) width = 240;
-        if (!isFinite(height) || height <= 1) height = 240;
-        if (!isFinite(cursorWidth) || cursorWidth <= 0) cursorWidth = 18;
-        if (!isFinite(cursorHeight) || cursorHeight <= 0) cursorHeight = 18;
-
-        return {
-          panel: refPanel,
-          bounds: { left: bounds.left, top: bounds.top, width: width, height: height },
-          width: width,
-          height: height,
-          cursorWidth: cursorWidth,
-          cursorHeight: cursorHeight,
-          maxCursorX: Math.max(0, width - cursorWidth),
-          maxCursorY: Math.max(0, height - cursorHeight)
-        };
-      }
-
-      function applyColorBoxCursorPosition(cursorX, cursorY) {
-        if (!isValidPanel(colorBoxCursor)) return false;
-
-        var metrics = getColorBoxMetrics();
-        if (!metrics) return false;
-
-        var nextX = Number(cursorX);
-        var nextY = Number(cursorY);
-        if (!isFinite(nextX)) nextX = 0;
-        if (!isFinite(nextY)) nextY = 0;
-        if (nextX < 0) nextX = 0;
-        if (nextY < 0) nextY = 0;
-        if (nextX > metrics.maxCursorX) nextX = metrics.maxCursorX;
-        if (nextY > metrics.maxCursorY) nextY = metrics.maxCursorY;
-        colorDragAnchorX = nextX;
-        colorDragAnchorY = nextY;
-
-        colorBoxCursor.style.x = nextX + "px";
-        colorBoxCursor.style.y = nextY + "px";
-        colorBoxCursor.style.transform = "none";
-        colorBoxCursor.style.zIndex = "20";
-        colorBoxCursor.style.opacity = "1";
-        colorBoxCursor.style.visibility = "visible";
-        colorBoxCursor.style.backgroundColor = "#FFFFFF";
-        colorBoxCursor.style.washColor = "#FFFFFF";
-        return true;
-      }
-
-      function rememberColorDragAnchor(cursorX, cursorY) {
-        var metrics = getColorBoxMetrics();
-        if (!metrics) return false;
-
-        var nextX = Number(cursorX);
-        var nextY = Number(cursorY);
-        if (!isFinite(nextX) || !isFinite(nextY)) return false;
-
-        if (nextX < 0) nextX = 0;
-        if (nextY < 0) nextY = 0;
-        if (nextX > metrics.maxCursorX) nextX = metrics.maxCursorX;
-        if (nextY > metrics.maxCursorY) nextY = metrics.maxCursorY;
-
-        colorDragAnchorX = nextX;
-        colorDragAnchorY = nextY;
-        return true;
-      }
-
-      function syncFromAnchoredCursorPosition(emitUpdateEvent) {
-        var metrics = getColorBoxMetrics();
-        if (!metrics) return false;
-        if (!isFinite(colorDragAnchorX) || !isFinite(colorDragAnchorY) ||
-            colorDragAnchorX < 0 || colorDragAnchorY < 0) {
-          return false;
-        }
-
-        var relX = metrics.maxCursorX > 0 ? clamp01(colorDragAnchorX / metrics.maxCursorX) : 0;
-        var relY = metrics.maxCursorY > 0 ? clamp01(colorDragAnchorY / metrics.maxCursorY) : 0;
-        var hue = hueFromRelX(relX);
-        var sat = clamp01(relY);
-
-        applyColorBoxCursorPosition(colorDragAnchorX, colorDragAnchorY);
-        syncColorVisuals(colorFromBoxState(hue, sat), emitUpdateEvent, false, { hue: hue, sat: sat });
-        return true;
-      }
-
-      function getCursorPositionWithinColorBox() {
-        if (!isValidPanel(colorBoxCursor)) return null;
-
-        var metrics = getColorBoxMetrics();
-        if (!metrics) return null;
-
-        function chooseLocalAxis(layoutValue, boundsValue, maxValue, allowLayoutValue) {
-          var tolerance = 1;
-          var layoutValid = !!allowLayoutValue && isFinite(layoutValue) && layoutValue >= -tolerance && layoutValue <= (maxValue + tolerance);
-          var boundsValid = isFinite(boundsValue) && boundsValue >= -tolerance && boundsValue <= (maxValue + tolerance);
-
-          if (layoutValid && boundsValid) {
-            if (Math.abs(layoutValue - boundsValue) <= 12) return layoutValue;
-            return boundsValue;
-          }
-          if (boundsValid) return boundsValue;
-          if (layoutValid) return layoutValue;
-          if (isFinite(boundsValue)) return boundsValue;
-          if (isFinite(layoutValue)) return layoutValue;
-          return 0;
-        }
-
-        var layoutX = Number(colorBoxCursor.actuallayoutx);
-        var layoutY = Number(colorBoxCursor.actuallayouty);
-        var parentMatches = !!(colorBoxCursor.GetParent && colorBoxCursor.GetParent() === metrics.panel);
-        var cursorBounds = getPanelBounds(colorBoxCursor);
-        var boundsX = cursorBounds.left - metrics.bounds.left;
-        var boundsY = cursorBounds.top - metrics.bounds.top;
-        var localX = chooseLocalAxis(layoutX, boundsX, metrics.maxCursorX, parentMatches);
-        var localY = chooseLocalAxis(layoutY, boundsY, metrics.maxCursorY, parentMatches);
-
-        if (localX < 0) localX = 0;
-        if (localY < 0) localY = 0;
-        if (localX > metrics.maxCursorX) localX = metrics.maxCursorX;
-        if (localY > metrics.maxCursorY) localY = metrics.maxCursorY;
-        rememberColorDragAnchor(localX, localY);
-
-        return {
-          metrics: metrics,
-          x: localX,
-          y: localY
-        };
-      }
-
-      function updateBoxCursorVisual(colorCode) {
-        if (!colorBoxPanel || !colorBoxPanel.IsValid || !colorBoxPanel.IsValid()) return;
-
-        var metrics = getColorBoxMetrics();
-        if (!metrics) return;
-
-        if (!isFinite(pickerBoxHue) || !isFinite(pickerBoxSat)) {
-          setPickerBoxState(null, colorCode);
-        }
-
-        var cursorX = metrics.maxCursorX > 0 ? (pickerBoxHue / 359) * metrics.maxCursorX : 0;
-        var cursorY = metrics.maxCursorY > 0 ? pickerBoxSat * metrics.maxCursorY : 0;
-        applyColorBoxCursorPosition(cursorX, cursorY);
-      }
-
-      function syncFromCursorPanelPosition(emitUpdateEvent) {
-        if (!isValidPanel(colorBoxPanel) || !isValidPanel(colorBoxCursor)) return false;
-
-        var cursorPosition = getCursorPositionWithinColorBox();
-        if (!cursorPosition) return false;
-
-        const metrics = cursorPosition.metrics;
-        const relX = metrics.maxCursorX > 0 ? clamp01(cursorPosition.x / metrics.maxCursorX) : 0;
-        const relY = metrics.maxCursorY > 0 ? clamp01(cursorPosition.y / metrics.maxCursorY) : 0;
-        const hue = hueFromRelX(relX);
-        const sat = clamp01(relY);
-
-        syncColorVisuals(colorFromBoxState(hue, sat), emitUpdateEvent, false, { hue: hue, sat: sat });
-        return true;
-      }
-
-      function syncFromBoxPosition(boxX, boxY, emitUpdateEvent) {
-        if (!isValidPanel(colorBoxPanel)) return false;
-
-        const metrics = getColorBoxMetrics();
-        if (!metrics) return false;
-
-        const relX = clamp01(Number(boxX) / metrics.width);
-        const relY = clamp01(Number(boxY) / metrics.height);
-        const hue = hueFromRelX(relX);
-        const sat = clamp01(relY);
-        syncColorVisuals(colorFromBoxState(hue, sat), emitUpdateEvent, false, { hue: hue, sat: sat });
-        return true;
-      }
-
-      function syncFromScreenCursorPosition(emitUpdateEvent) {
-        var cursor = getCursorPosition();
-        if (!cursor || !isValidPanel(colorBoxPanel)) return false;
-
-        const metrics = getColorBoxMetrics();
-        if (!metrics) return false;
-
-        const bounds = metrics.bounds;
-        const relX = clamp01((cursor.x - bounds.left) / bounds.width);
-        const relY = clamp01((cursor.y - bounds.top) / bounds.height);
-        const hue = hueFromRelX(relX);
-        const sat = clamp01(relY);
-        syncColorVisuals(colorFromBoxState(hue, sat), emitUpdateEvent, false, { hue: hue, sat: sat });
-        return true;
-      }
-
-      function syncFromLocalBoxCursor(emitUpdateEvent) {
-        var localPoint = getPanelLocalCursorPosition(colorBoxPanel);
-        if (!localPoint) return false;
-        return syncFromBoxPosition(localPoint.x, localPoint.y, emitUpdateEvent);
-      }
-
-      function syncFromBestDragSource(emitUpdateEvent) {
-        if (!hasGameUI) {
-          if ((colorDragSource === "cursor" || colorDragSource === "drag_event") &&
-              syncFromCursorPanelPosition(emitUpdateEvent)) return "cursor_panel";
-          if (syncFromLocalBoxCursor(emitUpdateEvent)) return "panel_cursor";
-          if (syncFromCursorPanelPosition(emitUpdateEvent)) return "cursor_panel";
-          return "";
-        }
-
-        if ((colorDragSource === "cursor" || colorDragSource === "drag_event") &&
-            syncFromCursorPanelPosition(emitUpdateEvent)) return "drag_panel";
-        if ((colorDragSource === "box" || colorDragSource === "gameui") &&
-            syncFromScreenCursorPosition(emitUpdateEvent)) return "screen_cursor";
-        if (syncFromLocalBoxCursor(emitUpdateEvent)) return "panel_cursor";
-        if (syncFromScreenCursorPosition(emitUpdateEvent)) return "screen_cursor";
-        if (syncFromCursorPanelPosition(emitUpdateEvent)) return "drag_panel";
-        return "";
-      }
-
-      function setMouseCaptureState(active) {
-        var next = !!active;
-        if (isValidPanel(colorBoxPanel) && typeof colorBoxPanel.SetMouseCapture === "function") {
-          try { colorBoxPanel.SetMouseCapture(next); } catch (e) {}
-        }
-        if (hasGameUI && isValidPanel(colorBoxCursor) && typeof colorBoxCursor.SetMouseCapture === "function") {
-          try { colorBoxCursor.SetMouseCapture(next); } catch (e) {}
-        }
-      }
-
-      function beginColorDrag(sourceName, emitUpdateEvent) {
-        colorDragging = true;
-        colorDragSource = String(sourceName || "");
-        if (!syncFromCursorPanelPosition(false)) {
-          updateBoxCursorVisual(currentColor);
-        }
-        if (emitUpdateEvent) {
-          if (colorDragSource === "cursor" || colorDragSource === "drag_event") {
-            if (!syncFromCursorPanelPosition(true)) {
-              syncFromBestDragSource(true);
+            if ("stepper" === type || "slider" === type) {
+                var nextNumber = Number(value);
+                isFinite(nextNumber) || (nextNumber = Number(fallback)), isFinite(nextNumber) || (nextNumber = 0);
+                var min = Number(element.min), max = Number(element.max);
+                isFinite(min) && nextNumber < min && (nextNumber = min), isFinite(max) && nextNumber > max && (nextNumber = max);
+                var step = Number(element.step);
+                return isFinite(step) && 0 !== step || (step = 1), nextNumber = Math.round(step) === step ? Math.round(nextNumber) : parseFloat(nextNumber.toFixed(2)),
+                isFinite(min) && nextNumber < min && (nextNumber = min), isFinite(max) && nextNumber > max && (nextNumber = max),
+                nextNumber;
             }
-          } else if (!syncFromScreenCursorPosition(true) && !syncFromLocalBoxCursor(true)) {
-            syncFromCursorPanelPosition(true);
-          }
-        }
-        setMouseCaptureState(true);
-        startPickerPolling();
-      }
-
-      function endColorDrag() {
-        colorDragging = false;
-        colorDragSource = "";
-        setMouseCaptureState(false);
-      }
-
-      function stopPickerPolling() {
-        colorPickerPollGeneration++;
-      }
-
-      function startPickerPolling() {
-        if (!colorPopupPanel || !colorPopupPanel.IsValid || !colorPopupPanel.IsValid()) return;
-
-        const generation = ++colorPickerPollGeneration;
-        function tick() {
-          if (generation !== colorPickerPollGeneration) return;
-          if (!colorPopupPanel || !colorPopupPanel.IsValid || !colorPopupPanel.IsValid()) return;
-
-          if (colorDragging) {
-            syncFromBestDragSource(true);
-
-            if (hasGameUI && typeof GameUI.IsMouseDown === "function") {
-              if (!GameUI.IsMouseDown(0)) {
-                endColorDrag();
-                return;
-              }
+            return "colorpicker" === type ? "string" == typeof value && value.length > 0 ? value : "string" == typeof fallback && fallback.length > 0 ? fallback : "#FFFFFF" : void 0 !== value ? value : fallback;
+        },
+        ensureDefaults: function(config) {
+            for (var elements = this.getElements(config), i = 0; i < elements.length; i++) {
+                var element = elements[i];
+                if (this.shouldPersistElement(element)) {
+                    var sourceValue = void 0 !== element.currentValue ? element.currentValue : element.defaultValue;
+                    element.currentValue = this.sanitizeValue(element, sourceValue);
+                } else void 0 === element.currentValue && void 0 !== element.defaultValue && (element.currentValue = element.defaultValue);
             }
-
-            $.Schedule(0.016, tick);
-            return;
-          }
-
-          $.Schedule(0.05, tick);
-        }
-
-        $.Schedule(0.016, tick);
-      }
-
-      function syncFromCursorPosition(emitUpdateEvent) {
-        if (!syncFromScreenCursorPosition(emitUpdateEvent)) {
-          if (!syncFromLocalBoxCursor(emitUpdateEvent)) {
-            syncFromCursorPanelPosition(emitUpdateEvent);
-          }
-        }
-      }
-
-      function closePalette() {
-        stopPickerPolling();
-        endColorDrag();
-
-        if (hasGameUI && typeof GameUI.SetMouseCallback === "function") {
-          try {
-            GameUI.SetMouseCallback(null);
-          } catch (e) {}
-        }
-
-        if (colorPopupPanel && colorPopupPanel.IsValid && colorPopupPanel.IsValid()) {
-          colorPopupPanel.DeleteAsync(0);
-        }
-
-        colorPopupPanel = null;
-        colorBoxFrame = null;
-        colorBoxPanel = null;
-        colorBoxCursor = null;
-        popupPreview = null;
-        popupHexLabel = null;
-        popupMetaLabel = null;
-        colorPickerSyncing = false;
-        pickerHueSlider = null;
-        pickerSatSlider = null;
-        pickerSatTrack = null;
-        pickerHueValue = null;
-        pickerSatValue = null;
-        colorDragSource = "";
-        if (AnitaRenderer.activeColorPickerClose === closePalette) {
-          AnitaRenderer.activeColorPickerClose = null;
-        }
-      }
-
-      function syncColorVisuals(colorCode, emitUpdateEvent, closeAfter, boxState) {
-        currentColor = normalizeHexColor(colorCode);
-        config.currentValue = currentColor;
-        var pickerState = setPickerBoxState(boxState, currentColor);
-
-        if (colorPreview && colorPreview.IsValid && colorPreview.IsValid()) {
-          colorPreview.style.backgroundColor = currentColor;
-        }
-
-        if (rowHexLabel) {
-          rowHexLabel.text = currentColor;
-        }
-
-        if (popupPreview && popupPreview.IsValid && popupPreview.IsValid()) {
-          popupPreview.style.backgroundColor = currentColor;
-        }
-
-        if (popupHexLabel) {
-          popupHexLabel.text = currentColor;
-        }
-
-        if (popupMetaLabel) {
-          popupMetaLabel.text = "Hue " + pickerState.hue + "\u00B0 | Sat " + Math.round(pickerState.sat * 100) + "%";
-        }
-
-        const previewBtn = row.FindChildTraverse("ColorPreviewBtn");
-        if (previewBtn) previewBtn.style.backgroundColor = currentColor;
-
-        updateBoxCursorVisual(currentColor);
-
-        colorPickerSyncing = true;
-        try {
-          if (pickerHueSlider && pickerHueSlider.IsValid && pickerHueSlider.IsValid()) {
-            var hueSliderPct = hueToSliderPercent(pickerState.hue);
-            if (Math.round(Number(pickerHueSlider.value)) !== hueSliderPct) {
-              try {
-                pickerHueSlider.value = hueSliderPct;
-              } catch (e) {}
+        },
+        expandStoredValues: function(config, parsed) {
+            if (!parsed || "object" != typeof parsed || !parsed.values || "object" != typeof parsed.values) return null;
+            var rawValues = parsed.values, expanded = {};
+            if (!(this.isHpColorsConfig(config) && (1 === parsed.c || !0 === parsed.compact))) {
+                for (var key in rawValues) Object.prototype.hasOwnProperty.call(rawValues, key) && (expanded[key] = rawValues[key]);
+                return expanded;
             }
-            if (pickerHueValue) pickerHueValue.text = pickerState.hue + "\u00B0";
-          }
-          if (pickerSatSlider && pickerSatSlider.IsValid && pickerSatSlider.IsValid()) {
-            var satPct = Math.round(pickerState.sat * 100);
-            if (Math.round(Number(pickerSatSlider.value)) !== satPct) {
-              try {
-                pickerSatSlider.value = satPct;
-              } catch (e) {}
+            for (var alias in rawValues) if (Object.prototype.hasOwnProperty.call(rawValues, alias)) {
+                var fullId = HP_PERSIST_ALIAS_TO_ID[alias];
+                fullId && (expanded[fullId] = rawValues[alias]);
             }
-            if (pickerSatValue) pickerSatValue.text = satPct + "%";
-            if (pickerSatTrack && pickerSatTrack.IsValid && pickerSatTrack.IsValid()) {
-              pickerSatTrack.style.backgroundColor = "gradient( linear, 0% 0%, 100% 0%, from( #ffffff ), to( " + hsvToHex(pickerState.hue, 1, 1) + " ) )";
+            return expanded;
+        },
+        parseStoredPayload: function(config, raw, sourceLabel) {
+            var text = String(raw || "");
+            if (!text) return null;
+            var parsed = null;
+            try {
+                parsed = JSON.parse(text);
+            } catch (e) {
+                return null;
             }
-          }
-        } finally {
-          colorPickerSyncing = false;
-        }
-
-        if (emitUpdateEvent) {
-          if (config.id && modTitle) {
-            emitUpdate(modTitle, config.id, currentColor);
-          }
-          if (config.onChange) config.onChange(currentColor);
-        }
-
-        if (closeAfter) closePalette();
-      }
-
-      function openPalette() {
-        if (colorPopupPanel) {
-          closePalette();
-          return;
-        }
-
-        if (AnitaRenderer.activeColorPickerClose &&
-            AnitaRenderer.activeColorPickerClose !== closePalette) {
-          try {
-            AnitaRenderer.activeColorPickerClose();
-          } catch (closeErr) {
-          }
-        }
-
-        var colorPopupParent = $.GetContextPanel();
-        colorPopupPanel = $.CreatePanel("Panel", colorPopupParent, "");
-        AnitaRenderer.activeColorPickerClose = closePalette;
-        colorPopupPanel.AddClass("AnitaColorPopup");
-        var colorPopupX = 24;
-        var colorPopupY = 24;
-        try {
-          var colorAnchorPanel = (preview && preview.IsValid && preview.IsValid()) ? preview : parent;
-          var colorAnchorBounds = getPanelBounds(colorAnchorPanel);
-          colorPopupX = Math.max(8, Math.round(colorAnchorBounds.left + colorAnchorBounds.width + 12));
-          colorPopupY = Math.max(8, Math.round(colorAnchorBounds.top));
-        } catch (e) {}
-        colorPopupPanel.style.align = "left top";
-        colorPopupPanel.style.transform = "translate3d(" + colorPopupX + "px, " + colorPopupY + "px, 0px)";
-        colorPopupPanel.style.uiScale = "100%";
-        colorPopupPanel.style.flowChildren = "down";
-        colorPopupPanel.style.ignoreParentFlow = true;
-        colorPopupPanel.SetPanelEvent("oncancel", closePalette);
-
-        const header = $.CreatePanel("Panel", colorPopupPanel, "");
-        header.AddClass("AnitaColorPopupHeader");
-
-        popupPreview = $.CreatePanel("Panel", header, "ColorPreviewBtn");
-        popupPreview.AddClass("AnitaColorPickerPreview");
-        popupPreview.AddClass("AnitaColorPopupPreview");
-        popupPreview.style.backgroundColor = currentColor;
-
-        popupHexLabel = $.CreatePanel("Label", header, "");
-        popupHexLabel.AddClass("AnitaColorPopupHex");
-        popupHexLabel.text = currentColor;
-
-        popupMetaLabel = $.CreatePanel("Label", colorPopupPanel, "");
-        popupMetaLabel.AddClass("AnitaColorPopupMeta");
-
-        const hint = $.CreatePanel("Label", colorPopupPanel, "");
-        hint.AddClass("AnitaColorPopupHint");
-        hint.text = "Drag the marker or use the sliders below to change hue and saturation.";
-
-        const boxWrap = $.CreatePanel("Panel", colorPopupPanel, "");
-        boxWrap.AddClass("AnitaColorBoxWrap");
-
-        colorBoxFrame = $.CreatePanel("Panel", boxWrap, "");
-        colorBoxFrame.AddClass("AnitaColorBoxFrame");
-
-        const boxHueLayer = $.CreatePanel("Panel", colorBoxFrame, "");
-        boxHueLayer.AddClass("AnitaColorBoxHueLayer");
-
-        const boxSaturationLayer = $.CreatePanel("Panel", colorBoxFrame, "");
-        boxSaturationLayer.AddClass("AnitaColorBoxSaturationLayer");
-
-        colorBoxPanel = $.CreatePanel("Panel", colorBoxFrame, "");
-        colorBoxPanel.AddClass("AnitaColorBox");
-        colorBoxPanel.hittest = true;
-        colorBoxPanel.hittestchildren = true;
-        colorBoxPanel.style.ignoreParentFlow = true;
-        colorBoxPanel.style.width = "100%";
-        colorBoxPanel.style.height = "100%";
-        colorBoxPanel.SetPanelEvent("onmouseactivate", function () {
-          if (!hasGameUI) {
+            if (!parsed || "object" != typeof parsed) return null;
+            var expandedValues = this.expandStoredValues(config, parsed);
+            if (!expandedValues) return null;
+            for (var values = {}, elements = this.getElements(config), i = 0; i < elements.length; i++) {
+                var element = elements[i];
+                this.shouldPersistElement(element) && (Object.prototype.hasOwnProperty.call(expandedValues, element.id) && (values[element.id] = this.sanitizeValue(element, expandedValues[element.id])));
+            }
+            return {
+                raw: text,
+                values: values
+            };
+        },
+        readPersistentStoragePayload: function(config) {
+            var key = this.getStorageKey(config), storageAvailable = this.hasPersistentStorage();
+            if (!key || !storageAvailable) return null;
+            var encoded = "";
             try {
-              var movePt = getPanelLocalCursorPosition(colorBoxPanel);
-              if (movePt) {
-                syncFromBoxPosition(movePt.x, movePt.y, true);
-              }
-            } catch (e) {}
-            return;
-          }
-          beginColorDrag("box", true);
-        });
-        colorBoxPanel.SetPanelEvent("onactivate", function () {
-          if (!hasGameUI) return;
-          beginColorDrag("box", true);
-        });
-        colorBoxPanel.SetPanelEvent("onmousemove", function () {
-          if (!colorDragging) return;
-          if (!hasGameUI) {
-            var movePt = getPanelLocalCursorPosition(colorBoxPanel);
-            if (movePt) syncFromBoxPosition(movePt.x, movePt.y, true);
-            return;
-          }
-          syncFromBestDragSource(true);
-        });
-        colorBoxPanel.SetPanelEvent("onmouseup", function () {
-          endColorDrag();
-        });
-        colorBoxPanel.SetPanelEvent("onmouseout", function () {
-          if (!colorDragging || hasGameUI) return;
-          endColorDrag();
-        });
-
-        colorBoxCursor = $.CreatePanel("Button", colorBoxFrame, "");
-        colorBoxCursor.AddClass("AnitaColorBoxCursor");
-        colorBoxCursor.hittest = true;
-        colorBoxCursor.hittestchildren = false;
-        colorBoxCursor.style.ignoreParentFlow = true;
-        colorBoxCursor.style.align = "left top";
-        colorBoxCursor.style.visibility = "visible";
-        colorBoxCursor.style.opacity = "1";
-        if (typeof colorBoxCursor.SetDraggable === "function") {
-          try {
-            colorBoxCursor.SetDraggable(true);
-          } catch (e) {}
-        }
-        if (typeof colorBoxCursor.SetDisableFocusOnMouseDown === "function") {
-          try {
-            colorBoxCursor.SetDisableFocusOnMouseDown(true);
-          } catch (e) {}
-        }
-        colorBoxCursor.SetPanelEvent("onmouseactivate", function () {
-          beginColorDrag("cursor", true);
-        });
-        colorBoxCursor.SetPanelEvent("onactivate", function () {
-          beginColorDrag("cursor", true);
-        });
-        colorBoxCursor.SetPanelEvent("onmousemove", function () {
-          if (!colorDragging) return;
-          syncFromBestDragSource(true);
-        });
-        colorBoxCursor.SetPanelEvent("onmouseup", function () {
-          endColorDrag();
-        });
-        colorBoxCursor.SetPanelEvent("onmouseout", function () {
-          if (!colorDragging || hasGameUI) return;
-          endColorDrag();
-        });
-        if (typeof $.RegisterEventHandler === "function") {
-          try {
-            $.RegisterEventHandler("DragStart", colorBoxCursor, function (panel, dragEvent) {
-              if (!panel || panel !== colorBoxCursor) return;
-              if (dragEvent) {
-                dragEvent.displayPanel = colorBoxCursor;
-                dragEvent.removePositionBeforeDrop = false;
-              }
-              colorBoxCursor.style.align = "left top";
-              beginColorDrag("drag_event", false);
-            });
-          } catch (dragStartError) {
-          }
-
-          try {
-            $.RegisterEventHandler("DragEnd", colorBoxCursor, function (_panel, droppedPanel) {
-              if (droppedPanel && droppedPanel.IsValid && droppedPanel.IsValid() && droppedPanel === colorBoxCursor) {
-                if (colorBoxFrame && colorBoxFrame.IsValid && colorBoxFrame.IsValid() &&
-                    droppedPanel.GetParent && droppedPanel.GetParent() !== colorBoxFrame) {
-                  droppedPanel.SetParent(colorBoxFrame);
-                }
-                droppedPanel.style.align = "left top";
-                if (!syncFromAnchoredCursorPosition(true)) {
-                  syncFromCursorPanelPosition(true);
-                  updateBoxCursorVisual(currentColor);
+                encoded = String($.persistentStorage.getItem(key) || "");
+            } catch (eRead) {
+                return null;
+            }
+            if (!encoded) return null;
+            var raw = "";
+            try {
+                raw = AnitaBase64.decode(encoded);
+            } catch (eDecode) {
+                return null;
+            }
+            var parsed = this.parseStoredPayload(config, raw, "persistentStorage");
+            return parsed ? {
+                raw: parsed.raw,
+                encoded: encoded,
+                values: parsed.values,
+                source: "persistentStorage"
+            } : null;
+        },
+        readConvarPayload: function(config) {
+            var ns = this.normalizeNamespace(config && config.storageNamespace);
+            if (!ns) return null;
+            if ("undefined" == typeof GameInterfaceAPI || !GameInterfaceAPI || "function" != typeof GameInterfaceAPI.GetSettingString) return null;
+            var convarRaw = "";
+            try {
+                convarRaw = String(GameInterfaceAPI.GetSettingString(this.CONVAR_KEY) || "");
+            } catch (eRead) {
+                return null;
+            }
+            var tokenMatch = convarRaw.match(new RegExp("\\[" + this.TOKEN_PREFIX + ns + "\\]:([A-Za-z0-9_-]+)"));
+            if (!tokenMatch) return null;
+            var encoded = String(tokenMatch[1] || "");
+            if (!encoded) return null;
+            var raw = "";
+            try {
+                raw = AnitaBase64.decode(encoded);
+            } catch (eDecode) {
+                return null;
+            }
+            var parsed = this.parseStoredPayload(config, raw, "convar");
+            return parsed ? {
+                raw: parsed.raw,
+                encoded: encoded,
+                values: parsed.values,
+                source: "convar"
+            } : null;
+        },
+        readStoredPayload: function(config) {
+            var persisted = null;
+            try {
+                var sessionEncoded = this.getSessionEncoded(config);
+                if (sessionEncoded) {
+                    var sessionRaw = AnitaBase64.decode(sessionEncoded);
+                    if (persisted = this.parseStoredPayload(config, sessionRaw, "session")) return {
+                        raw: persisted.raw,
+                        encoded: sessionEncoded,
+                        values: persisted.values,
+                        source: "session"
+                    };
                 }
-              }
-              endColorDrag();
-            });
-          } catch (dragEndError) {
-          }
-        }
-
-        if (hasGameUI && typeof GameUI.SetMouseCallback === "function") {
-          try {
-            GameUI.SetMouseCallback(function (eventName, arg) {
-              if (!colorPopupPanel || !colorPopupPanel.IsValid || !colorPopupPanel.IsValid()) {
-                return false;
-              }
-
-              if (eventName === "pressed" && arg === 0) {
-                const cursor = getCursorPosition();
-                if (!cursor) return false;
-                const metrics = getColorBoxMetrics();
-                if (!metrics) return false;
-                const bounds = metrics.bounds;
-                const inside = cursor.x >= bounds.left && cursor.x <= (bounds.left + bounds.width) &&
-                  cursor.y >= bounds.top && cursor.y <= (bounds.top + bounds.height);
-                if (inside) {
-                  syncFromCursorPosition(true);
-                  beginColorDrag("gameui", false);
-                  return true;
+            } catch (eSess) {}
+            return (persisted = this.readPersistentStoragePayload(config)) || (persisted = this.readConvarPayload(config)) ? (config && config.title,
+            persisted) : null;
+        },
+        applyResolvedValues: function(config, values) {
+            for (var elements = this.getElements(config), i = 0; i < elements.length; i++) {
+                var element = elements[i];
+                if (this.shouldPersistElement(element)) {
+                    var nextValue = Object.prototype.hasOwnProperty.call(values || {}, element.id) ? values[element.id] : element.defaultValue;
+                    element.currentValue = this.sanitizeValue(element, nextValue);
+                } else void 0 === element.currentValue && void 0 !== element.defaultValue && (element.currentValue = element.defaultValue);
+            }
+        },
+        hydrateConfig: function(config) {
+            this.ensureDefaults(config);
+            var hydrateSource = "defaults";
+            this.normalizeNamespace(config && config.storageNamespace);
+            if (!this.hasPersistentConfig(config)) return config.__anitaLastPersistedRaw = "",
+            void this.applyResolvedValues(config, {});
+            var persisted = this.readStoredPayload(config);
+            persisted && (hydrateSource = persisted.source), persisted ? (this.applyResolvedValues(config, persisted.values),
+            persisted.encoded && "session" !== hydrateSource && this.writeSessionMirror(config, persisted.encoded)) : this.applyResolvedValues(config, {}),
+            config.__anitaLastPersistedRaw = persisted ? persisted.raw : "", this.writeHpSharedSnapshot(config),
+            config && config.title;
+        },
+        buildStoredPayload: function(config) {
+            for (var elements = this.getElements(config), values = {}, isHpColors = this.isHpColorsConfig(config), i = 0; i < elements.length; i++) {
+                var element = elements[i];
+                if (this.shouldPersistElement(element)) {
+                    var value = this.sanitizeValue(element, void 0 !== element.currentValue ? element.currentValue : element.defaultValue);
+                    element.currentValue = value, isHpColors && value === this.sanitizeValue(element, element.defaultValue) || (values[isHpColors && HP_PERSIST_ALIASES[element.id] || element.id] = value);
                 }
-              }
-
-              if (eventName === "released" && arg === 0 && colorDragging) {
-                endColorDrag();
-                return true;
-              }
-
-              return false;
+            }
+            return isHpColors ? JSON.stringify({
+                v: this.getVersion(config),
+                c: 1,
+                values: values
+            }) : JSON.stringify({
+                version: this.getVersion(config),
+                values: values
             });
-          } catch (e) {
-          }
-        } else {
-        }
-
-        // Hue slider row
-        var hueGroup = $.CreatePanel("Panel", colorPopupPanel, "");
-        hueGroup.AddClass("AnitaHueSliderGroup");
-
-        var hueLbl = $.CreatePanel("Label", hueGroup, "");
-        hueLbl.text = "H";
-        hueLbl.AddClass("AnitaHueValue");
-        hueLbl.AddClass("AnitaPickerAxisLabel");
-
-        var hueContainer = $.CreatePanel("Panel", hueGroup, "");
-        hueContainer.AddClass("AnitaHueSliderContainer");
-        hueContainer.AddClass("AnitaPickerSliderTrack");
-
-        pickerHueSlider = $.CreatePanel("Slider", hueContainer, "", { direction: "horizontal" });
-        pickerHueSlider.AddClass("AnitaHueSlider");
-        pickerHueSlider.AddClass("HorizontalSlider");
-        pickerHueSlider.min = 0;
-        pickerHueSlider.max = 100;
-        pickerHueSlider.increment = 1;
-        if (typeof pickerHueSlider.SetShowDefaultValue === "function") { try { pickerHueSlider.SetShowDefaultValue(false); } catch (e) {} }
-        if (typeof pickerHueSlider.SetRequiresSelection === "function") { try { pickerHueSlider.SetRequiresSelection(false); } catch (e) {} }
-
-        pickerHueValue = $.CreatePanel("Label", hueGroup, "");
-        pickerHueValue.AddClass("AnitaHueValue");
-        pickerHueValue.AddClass("AnitaPickerReadout");
-
-        pickerHueSlider.SetPanelEvent("onvaluechanged", function () {
-          if (colorPickerSyncing) return;
-          var h = hueFromRelX(clamp01(Number(pickerHueSlider.value) / 100));
-          var newColor = colorFromBoxState(h, pickerBoxSat);
-          if (pickerHueValue) pickerHueValue.text = h + "\u00B0";
-          if (pickerSatTrack && pickerSatTrack.IsValid && pickerSatTrack.IsValid()) {
-            pickerSatTrack.style.backgroundColor = "gradient( linear, 0% 0%, 100% 0%, from( #ffffff ), to( " + hsvToHex(h, 1, 1) + " ) )";
-          }
-          syncColorVisuals(newColor, true, false, { hue: h, sat: pickerBoxSat });
-        });
-
-        // Saturation slider row
-        var satGroup = $.CreatePanel("Panel", colorPopupPanel, "");
-        satGroup.AddClass("AnitaHueSliderGroup");
-
-        var satLbl = $.CreatePanel("Label", satGroup, "");
-        satLbl.text = "S";
-        satLbl.AddClass("AnitaHueValue");
-        satLbl.AddClass("AnitaPickerAxisLabel");
-
-        pickerSatTrack = $.CreatePanel("Panel", satGroup, "");
-        pickerSatTrack.AddClass("AnitaSatSliderContainer");
-        pickerSatTrack.AddClass("AnitaPickerSliderTrack");
-
-        pickerSatSlider = $.CreatePanel("Slider", pickerSatTrack, "", { direction: "horizontal" });
-        pickerSatSlider.AddClass("AnitaHueSlider");
-        pickerSatSlider.AddClass("HorizontalSlider");
-        pickerSatSlider.min = 0;
-        pickerSatSlider.max = 100;
-        pickerSatSlider.increment = 1;
-        if (typeof pickerSatSlider.SetShowDefaultValue === "function") { try { pickerSatSlider.SetShowDefaultValue(false); } catch (e) {} }
-        if (typeof pickerSatSlider.SetRequiresSelection === "function") { try { pickerSatSlider.SetRequiresSelection(false); } catch (e) {} }
-
-        pickerSatValue = $.CreatePanel("Label", satGroup, "");
-        pickerSatValue.AddClass("AnitaHueValue");
-        pickerSatValue.AddClass("AnitaPickerReadout");
-
-        pickerSatSlider.SetPanelEvent("onvaluechanged", function () {
-          if (colorPickerSyncing) return;
-          var s = clamp01(pickerSatSlider.value / 100);
-          var newColor2 = colorFromBoxState(pickerBoxHue, s);
-          if (pickerSatValue) pickerSatValue.text = Math.round(s * 100) + "%";
-          syncColorVisuals(newColor2, true, false, { hue: pickerBoxHue, sat: s });
-        });
-
-        const footer = $.CreatePanel("Panel", colorPopupPanel, "");
-        footer.AddClass("AnitaColorPopupFooter");
-
-        const closeBtn = $.CreatePanel("Button", footer, "");
-        closeBtn.AddClass("AnitaColorPopupBtn");
-        const closeLbl = $.CreatePanel("Label", closeBtn, "");
-        closeLbl.text = "Close";
-        closeBtn.SetPanelEvent("onactivate", closePalette);
-
-        syncColorVisuals(currentColor, false, false);
-        $.Schedule(0.0, function () {
-          if (colorPopupPanel && colorPopupPanel.IsValid && colorPopupPanel.IsValid()) {
-            syncColorVisuals(currentColor, false, false);
-          }
-        });
-
-        const initState = getBoxStateFromColor(currentColor);
-      }
-
-      const preview = $.CreatePanel("Panel", row, "ColorPreviewBtn");
-      preview.AddClass("AnitaColorPickerPreview");
-      preview.style.backgroundColor = currentColor;
-      preview.style.marginRight = "6px";
-      preview.SetPanelEvent("onactivate", () => openPalette());
-      colorPreview = preview;
-
-      rowHexLabel = $.CreatePanel("Label", row, "");
-      rowHexLabel.AddClass("AnitaColorHexValue");
-      rowHexLabel.text = currentColor;
-
-      return row;
-    },
-
-    createPositionPicker: function (parent, config, modTitle) {
-      const row = $.CreatePanel("Panel", parent, "");
-      row.AddClass("AnitaRow");
-      row.AddClass("AnitaSliderRow");
-      row.style.overflow = "noclip";
-      row.style.width = "100%";
-
-      const lbl = $.CreatePanel("Label", row, "");
-      lbl.text = config.label || "Position";
-      lbl.AddClass("AnitaLabel");
-
-      function posPickerClamp(value) {
-        var next = Number(value);
-        if (!isFinite(next)) next = 0;
-        if (next < 0) next = 0;
-        if (next > 400) next = 400;
-        return Math.round(next);
-      }
-
-      function posPickerParse(candidate) {
-        var x = 0;
-        var y = 200;
-        var raw = candidate;
-
-        if (raw && typeof raw === "object") {
-          if (Array.isArray(raw)) {
-            if (raw.length > 0) x = posPickerClamp(raw[0]);
-            if (raw.length > 1) y = posPickerClamp(raw[1]);
-          } else {
-            if (Object.prototype.hasOwnProperty.call(raw, "x")) x = posPickerClamp(raw.x);
-            if (Object.prototype.hasOwnProperty.call(raw, "y")) y = posPickerClamp(raw.y);
-          }
-          return { x: x, y: y };
-        }
-
-        if (typeof raw === "string") {
-          var parts = raw.match(/-?\d+(?:\.\d+)?/g);
-          if (parts && parts.length > 0) {
-            x = posPickerClamp(parts[0]);
-            if (parts.length > 1) y = posPickerClamp(parts[1]);
-            return { x: x, y: y };
-          }
-        }
-
-        if (typeof raw === "number") {
-          y = posPickerClamp(raw);
-          return { x: x, y: y };
-        }
-
-        return { x: x, y: y };
-      }
-
-      function posPickerNormalize(candidate) {
-        var parsed = posPickerParse(candidate);
-        return {
-          x: posPickerClamp(parsed.x),
-          y: posPickerClamp(parsed.y)
-        };
-      }
-
-      function posPickerFormat(pos) {
-        var parsed = posPickerNormalize(pos);
-        return Math.round(parsed.x) + "," + Math.round(parsed.y);
-      }
-
-      function posPickerPercent(pos) {
-        var parsed = posPickerNormalize(pos);
-        return Math.round(parsed.x / 4);
-      }
-
-      let posPickerCurrent = posPickerNormalize((config.currentValue !== undefined) ? config.currentValue : (config.defaultValue || "0,200"));
-      let posPickerSyncing = false;
-      const posPickerValueGroup = $.CreatePanel("Panel", row, "");
-      posPickerValueGroup.AddClass("AnitaSliderValueGroup");
-      posPickerValueGroup.AddClass("SliderValueGroup");
-      posPickerValueGroup.AddClass("AnitaPositionPickerGroup");
-      posPickerValueGroup.style.flowChildren = "down";
-      posPickerValueGroup.style.verticalAlign = "center";
-      posPickerValueGroup.style.horizontalAlign = "left";
-      posPickerValueGroup.style.width = "332px";
-      posPickerValueGroup.style.overflow = "noclip";
-
-      const posPickerXGroup = $.CreatePanel("Panel", posPickerValueGroup, "");
-      posPickerXGroup.AddClass("AnitaHueSliderGroup");
-      posPickerXGroup.AddClass("AnitaPositionSliderRow");
-      posPickerXGroup.style.overflow = "noclip";
-
-      const posPickerXLbl = $.CreatePanel("Label", posPickerXGroup, "");
-      posPickerXLbl.text = "L/R";
-      posPickerXLbl.AddClass("AnitaHueValue");
-      posPickerXLbl.AddClass("AnitaPositionAxisLabel");
-
-      const posPickerXContainer = $.CreatePanel("Panel", posPickerXGroup, "");
-      posPickerXContainer.AddClass("AnitaSliderContainer");
-      posPickerXContainer.AddClass("SliderContainer");
-      posPickerXContainer.AddClass("AnitaPositionSliderContainer");
-      posPickerXContainer.style.width = "230px";
-      posPickerXContainer.style.height = "26px";
-      posPickerXContainer.style.padding = "0px";
-      posPickerXContainer.style.verticalAlign = "center";
-      posPickerXContainer.style.overflow = "noclip";
-
-      const posPickerXSlider = $.CreatePanel("Slider", posPickerXContainer, "", { direction: "horizontal" });
-      posPickerXSlider.AddClass("AnitaSlider");
-      posPickerXSlider.AddClass("HorizontalSlider");
-      posPickerXSlider.style.width = "100%";
-      posPickerXSlider.style.height = "100%";
-      posPickerXSlider.style.verticalAlign = "center";
-      posPickerXSlider.style.overflow = "noclip";
-      posPickerXSlider.min = 0;
-      posPickerXSlider.max = 400;
-      posPickerXSlider.increment = 1;
-      if (typeof posPickerXSlider.SetShowDefaultValue === "function") {
-        try { posPickerXSlider.SetShowDefaultValue(false); } catch (e) {}
-      }
-      if (typeof posPickerXSlider.SetRequiresSelection === "function") {
-        try { posPickerXSlider.SetRequiresSelection(false); } catch (e) {}
-      }
-
-      const posPickerXValueLbl = $.CreatePanel("Label", posPickerXGroup, "");
-      posPickerXValueLbl.AddClass("AnitaSliderValue");
-      posPickerXValueLbl.AddClass("AnitaPositionReadout");
-      posPickerXValueLbl.style.textOverflow = "clip";
-      posPickerXValueLbl.style.overflow = "noclip";
-
-      const posPickerYGroup = $.CreatePanel("Panel", posPickerValueGroup, "");
-      posPickerYGroup.AddClass("AnitaHueSliderGroup");
-      posPickerYGroup.AddClass("AnitaPositionSliderRow");
-      posPickerYGroup.style.overflow = "noclip";
-
-      const posPickerYLbl = $.CreatePanel("Label", posPickerYGroup, "");
-      posPickerYLbl.text = "T/B";
-      posPickerYLbl.AddClass("AnitaHueValue");
-      posPickerYLbl.AddClass("AnitaPositionAxisLabel");
-
-      const posPickerYContainer = $.CreatePanel("Panel", posPickerYGroup, "");
-      posPickerYContainer.AddClass("AnitaSliderContainer");
-      posPickerYContainer.AddClass("SliderContainer");
-      posPickerYContainer.AddClass("AnitaPositionSliderContainer");
-      posPickerYContainer.style.width = "230px";
-      posPickerYContainer.style.height = "26px";
-      posPickerYContainer.style.padding = "0px";
-      posPickerYContainer.style.verticalAlign = "center";
-      posPickerYContainer.style.overflow = "noclip";
-
-      const posPickerYSlider = $.CreatePanel("Slider", posPickerYContainer, "", { direction: "horizontal" });
-      posPickerYSlider.AddClass("AnitaSlider");
-      posPickerYSlider.AddClass("HorizontalSlider");
-      posPickerYSlider.style.width = "100%";
-      posPickerYSlider.style.height = "100%";
-      posPickerYSlider.style.verticalAlign = "center";
-      posPickerYSlider.style.overflow = "noclip";
-      posPickerYSlider.min = 0;
-      posPickerYSlider.max = 400;
-      posPickerYSlider.increment = 1;
-      if (typeof posPickerYSlider.SetShowDefaultValue === "function") {
-        try { posPickerYSlider.SetShowDefaultValue(false); } catch (e) {}
-      }
-      if (typeof posPickerYSlider.SetRequiresSelection === "function") {
-        try { posPickerYSlider.SetRequiresSelection(false); } catch (e) {}
-      }
-
-      const posPickerYValueLbl = $.CreatePanel("Label", posPickerYGroup, "");
-      posPickerYValueLbl.AddClass("AnitaSliderValue");
-      posPickerYValueLbl.AddClass("AnitaPositionReadout");
-      posPickerYValueLbl.style.textOverflow = "clip";
-      posPickerYValueLbl.style.overflow = "noclip";
-
-      function syncPosition(nextPos, emitUpdateEvent) {
-        var normalized = posPickerNormalize(nextPos);
-        var nextValue = posPickerFormat(normalized);
-        var changed = String(config.currentValue || "") !== nextValue;
-        posPickerCurrent = normalized;
-        config.currentValue = nextValue;
-
-        if (posPickerXSlider && posPickerXSlider.IsValid && posPickerXSlider.IsValid()) {
-          if (Number(posPickerXSlider.value) !== normalized.x) {
-            posPickerSyncing = true;
+        },
+        persistConfig: function(config, forceWrite) {
+            if (!this.hasPersistentConfig(config)) return !1;
+            var raw = this.buildStoredPayload(config);
+            if (!raw) return !1;
+            if (!forceWrite && raw === String(config.__anitaLastPersistedRaw || "")) return !1;
+            var ns = this.normalizeNamespace(config.storageNamespace), encoded = "";
+            try {
+                encoded = AnitaBase64.encode(raw);
+            } catch (eEnc) {
+                return !1;
+            }
+            var token = "[" + this.TOKEN_PREFIX + ns + "]:" + encoded;
+            try {
+                var current = "";
+                "undefined" != typeof GameInterfaceAPI && GameInterfaceAPI && "function" == typeof GameInterfaceAPI.GetSettingString && (current = String(GameInterfaceAPI.GetSettingString(this.CONVAR_KEY) || ""));
+                var cleaned = current.replace(this.getCleanupRegex(ns), "").replace(/,,+/g, ",").replace(/^,|,$/, ""), finalValue = (cleaned ? cleaned + "," : "") + token;
+                finalValue.length > 1900 ? this.warnPersistence(config, "convar skipped (payload_too_large)") : this.writeConvarBestEffort(this.CONVAR_KEY, finalValue) || this.warnPersistence(config, "convar unavailable");
+            } catch (e) {}
             try {
-              if (typeof posPickerXSlider.SetValueNoEvents === "function") {
-                posPickerXSlider.SetValueNoEvents(normalized.x);
-              } else {
-                posPickerXSlider.value = normalized.x;
-              }
-            } finally {
-              posPickerSyncing = false;
+                if (this.hasPersistentStorage()) {
+                    var storageKey = this.getStorageKey(config);
+                    storageKey && $.persistentStorage.setItem(storageKey, encoded);
+                }
+            } catch (ePersist) {}
+            return this.writeSessionMirror(config, encoded), this.writeHpSharedSnapshot(config), config.__anitaLastPersistedRaw = raw,
+            !0;
+        },
+        requestBootstrap: function(config, reason) {
+            if (this.hasPersistentConfig(config)) {
+                var payload = {
+                    magic_word: "ANITA_REQUEST_BOOTSTRAP",
+                    mod_title: config.title,
+                    storageNamespace: this.normalizeNamespace(config.storageNamespace),
+                    reason: String(reason || "request")
+                };
+                $.DispatchEvent("ClientUI_FireOutput", JSON.stringify(payload));
             }
-          }
-        }
-
-        if (posPickerYSlider && posPickerYSlider.IsValid && posPickerYSlider.IsValid()) {
-          if (Number(posPickerYSlider.value) !== normalized.y) {
-            posPickerSyncing = true;
-            try {
-              if (typeof posPickerYSlider.SetValueNoEvents === "function") {
-                posPickerYSlider.SetValueNoEvents(normalized.y);
-              } else {
-                posPickerYSlider.value = normalized.y;
-              }
-            } finally {
-              posPickerSyncing = false;
+        },
+        applyUpdate: function(config, settingId, value) {
+            for (var elements = this.getElements(config), i = 0; i < elements.length; i++) {
+                var element = elements[i];
+                if (element && element.id === settingId) return element.currentValue = this.sanitizeValue(element, value),
+                this.writeHpSharedSnapshot(config),
+                !0;
+            }
+            return !1;
+        }
+    }, AnitaComponents_createToggle = function(parent, config, modTitle) {
+        const row = $.CreatePanel("Panel", parent, "");
+        row.AddClass("AnitaToggleRow");
+        const btn = $.CreatePanel("Button", row, "");
+        btn.AddClass("AnitaToggleBtn");
+        const lbl = $.CreatePanel("Label", row, "");
+        lbl.text = config.label || "Option", lbl.AddClass("AnitaLabel");
+        const box = $.CreatePanel("Panel", row, "");
+        box.AddClass("AnitaCheckBox");
+        $.CreatePanel("Panel", box, "").AddClass("AnitaCheckMark");
+        let isOn = void 0 !== config.currentValue ? config.currentValue : config.defaultValue || !1;
+        const updateState = active => row.SetHasClass("Checked", active);
+        return updateState(isOn), btn.SetPanelEvent("onactivate", () => {
+            isOn = !isOn, updateState(isOn), config.currentValue = isOn, config.id && emitUpdate(modTitle, config.id, isOn),
+            config.onChange && config.onChange(isOn);
+            var ownerConfig = AnitaCore.findRegisteredMod(modTitle);
+            ownerConfig && AnitaRenderer.hasVisibilityDependents(ownerConfig, config.id) && AnitaCore.queueRenderRefresh(ownerConfig);
+        }), row;
+    }, AnitaComponents_createStepper = function(parent, config, modTitle) {
+        const row = $.CreatePanel("Panel", parent, "");
+        row.AddClass("AnitaRow");
+        const lbl = $.CreatePanel("Label", row, "");
+        lbl.text = config.label || "Value", lbl.AddClass("AnitaLabel");
+        const controls = $.CreatePanel("Panel", row, "");
+        controls.AddClass("AnitaStepperControls");
+        const btnM = $.CreatePanel("Button", controls, "");
+        btnM.AddClass("AnitaStepBtn"), $.CreatePanel("Label", btnM, "less").text = "-";
+        const input = $.CreatePanel("TextEntry", controls, "");
+        input.AddClass("AnitaStepInput");
+        const btnP = $.CreatePanel("Button", controls, "");
+        btnP.AddClass("AnitaStepBtn"), $.CreatePanel("Label", btnP, "").text = "+";
+        let val = void 0 !== config.currentValue ? config.currentValue : config.defaultValue || 0;
+        const step = config.step || 1, isFloat = !Number.isInteger(step);
+        function update(newVal) {
+            newVal = isFloat ? parseFloat(newVal.toFixed(2)) : Math.round(newVal), val = newVal,
+            config.currentValue = val, input.text = val.toString(), config.onChange && config.onChange(val),
+            config.id && modTitle && emitUpdate(modTitle, config.id, val);
+        }
+        return input.text = isFloat ? val.toFixed(2) : val, input.SetPanelEvent("ontextentrychange", () => {
+            let v = parseFloat(input.text);
+            isNaN(v) || (val = v, config.currentValue = v);
+        }), input.SetPanelEvent("oncancel", () => {
+            AnitaRenderer.toggle(!1);
+        }), btnM.SetPanelEvent("onactivate", () => update(val - step)), btnP.SetPanelEvent("onactivate", () => update(val + step)),
+        input.SetPanelEvent("oninputsubmit", () => {
+            update(val), $.DispatchEvent("DropInputFocus", input), AnitaRenderer.mainWindow.SetFocus();
+        }), input.SetPanelEvent("onfocusout", () => {
+            update(val);
+        }), row;
+    }, AnitaComponents_createSlider = function(parent, config, modTitle) {
+        const row = $.CreatePanel("Panel", parent, "");
+        row.AddClass("AnitaRow"), row.AddClass("AnitaSliderRow"), row.style.width = "100%";
+        const lbl = $.CreatePanel("Label", row, "");
+        lbl.text = config.label || "Value", lbl.AddClass("AnitaLabel");
+        const valueGroup = $.CreatePanel("Panel", row, "");
+        valueGroup.AddClass("AnitaSliderValueGroup"), valueGroup.AddClass("SliderValueGroup"),
+        valueGroup.style.flowChildren = "right", valueGroup.style.verticalAlign = "center",
+        valueGroup.style.horizontalAlign = "left", valueGroup.style.width = "296px";
+        const sliderContainer = $.CreatePanel("Panel", valueGroup, "");
+        sliderContainer.AddClass("AnitaSliderContainer"), sliderContainer.AddClass("SliderContainer"),
+        sliderContainer.style.width = "230px", sliderContainer.style.height = "26px", sliderContainer.style.padding = "0px",
+        sliderContainer.style.verticalAlign = "center", sliderContainer.style.overflow = "noclip";
+        const slider = $.CreatePanel("Slider", sliderContainer, "", {
+            direction: "horizontal"
+        });
+        slider.AddClass("AnitaSlider"), slider.AddClass("HorizontalSlider"), slider.style.width = "100%",
+        slider.style.height = "100%", slider.style.verticalAlign = "center", slider.style.overflow = "noclip";
+        const valueLbl = $.CreatePanel("Label", valueGroup, "");
+        valueLbl.AddClass("AnitaSliderValue");
+        const rawMin = Number(config.min), rawMax = Number(config.max), rawStep = Number(config.step), min = isFinite(rawMin) ? rawMin : 0, max = isFinite(rawMax) ? rawMax : 100, step = isFinite(rawStep) && rawStep > 0 ? rawStep : 1;
+        let val = void 0 !== config.currentValue ? config.currentValue : void 0 !== config.defaultValue ? config.defaultValue : min, isSyncing = !1;
+        function normalize(nextVal) {
+            let next = Number(nextVal);
+            return isFinite(next) || (next = Number(config.defaultValue)), isFinite(next) || (next = min),
+            next < min && (next = min), next > max && (next = max), next = Math.round(step) === step ? Math.round(next) : parseFloat(next.toFixed(2)),
+            next;
+        }
+        function syncVisuals(nextVal, emitUpdateEvent) {
+            const normalized = normalize(nextVal);
+            if (val = normalized, config.currentValue = normalized, valueLbl.text = normalized.toString() + "%",
+            slider && slider.IsValid && slider.IsValid() && Number(slider.value) !== normalized) {
+                isSyncing = !0;
+                try {
+                    "function" == typeof slider.SetValueNoEvents ? slider.SetValueNoEvents(normalized) : slider.value = normalized;
+                } finally {
+                    isSyncing = !1;
+                }
+            }
+            emitUpdateEvent && (config.onChange && config.onChange(normalized), config.id && modTitle && emitUpdate(modTitle, config.id, normalized));
+        }
+        return slider.min = min, slider.max = max, slider.increment = step, slider.value = normalize(val),
+        "function" == typeof slider.SetShowDefaultValue && slider.SetShowDefaultValue(!1),
+        "function" == typeof slider.SetRequiresSelection && slider.SetRequiresSelection(!1),
+        syncVisuals(val, !1), slider.SetPanelEvent("onvaluechanged", function() {
+            isSyncing || syncVisuals(slider.value, !0);
+        }), slider.SetPanelEvent("oncancel", () => {
+            AnitaRenderer.toggle(!1);
+        }), row;
+    }, AnitaComponents_createButton = function(parent, config, modTitle) {
+        const btn = $.CreatePanel("Button", parent, "");
+        btn.AddClass("AnitaActionBtn");
+        return $.CreatePanel("Label", btn, "").text = config.label || "Action", btn.SetPanelEvent("onactivate", () => {
+            config.onClick && config.onClick(), config.id && modTitle && emitUpdate(modTitle, config.id, !0),
+            btn.AddClass("Activated"), $.Schedule(.1, () => btn.RemoveClass("Activated"));
+        }), btn;
+    }, AnitaComponents_createCycler = function(parent, config, modTitle) {
+        const row = $.CreatePanel("Panel", parent, "");
+        row.AddClass("AnitaRow");
+        const lbl = $.CreatePanel("Label", row, "");
+        lbl.text = config.label || "Cycle", lbl.AddClass("AnitaLabel");
+        const btn = $.CreatePanel("Button", row, "");
+        btn.AddClass("AnitaCyclerBtn");
+        const valLbl = $.CreatePanel("Label", btn, ""), options = config.options || [ "OFF", "ON" ];
+        let idx = void 0 !== config.currentValue ? config.currentValue : config.defaultValue || 0;
+        (idx < 0 || idx >= options.length) && (idx = 0);
+        const updateVisuals = () => {
+            valLbl.text = options[idx];
+        };
+        return updateVisuals(), btn.SetPanelEvent("onactivate", () => {
+            idx = (idx + 1) % options.length, updateVisuals(), config.currentValue = idx;
+            var ownerConfig = AnitaCore.findRegisteredMod(modTitle);
+            ownerConfig && AnitaRenderer.hasVisibilityDependents(ownerConfig, config.id) && AnitaCore.queueRenderRefresh(ownerConfig),
+            config.id && modTitle && emitUpdate(modTitle, config.id, idx), config.onChange && config.onChange(idx, options[idx]);
+        }), row;
+    }, AnitaComponents_createColorPicker = function(parent, config, modTitle) {
+        const row = $.CreatePanel("Panel", parent, "");
+        row.AddClass("AnitaRow"), row.AddClass("AnitaSliderRow"), row.style.overflow = "noclip",
+        row.style.width = "100%";
+        const lbl = $.CreatePanel("Label", row, "");
+        lbl.text = config.label || "Color", lbl.AddClass("AnitaLabel");
+        let currentColor = normalizeHexColor(void 0 !== config.currentValue ? config.currentValue : config.defaultValue || "#FF0000"), pickerBoxHue = 0, pickerBoxSat = 1, colorPopupPanel = null, colorBoxFrame = null, colorBoxPanel = null, colorBoxCursor = null, colorPickerSyncing = !1, colorPickerPollGeneration = 0, colorDragging = !1;
+        const hasGameUI = "undefined" != typeof GameUI && null !== GameUI;
+        let colorDragAnchorX = -1, colorDragAnchorY = -1, colorDragSource = "", popupPreview = null, popupHexLabel = null, popupMetaLabel = null, colorPreview = null, rowHexLabel = null, pickerHueSlider = null, pickerSatSlider = null, pickerSatTrack = null, pickerHueValue = null, pickerSatValue = null;
+        function clampByte(value) {
+            let next = Number(value);
+            return isFinite(next) || (next = 0), next < 0 && (next = 0), next > 255 && (next = 255),
+            Math.round(next);
+        }
+        function byteToHex(value) {
+            const hex = clampByte(value).toString(16).toUpperCase();
+            return hex.length < 2 ? "0" + hex : hex;
+        }
+        function rgbToHex(r, g, b) {
+            return "#" + byteToHex(r) + byteToHex(g) + byteToHex(b);
+        }
+        function hsvToHex(h, s, v) {
+            var rgb = function(h, s, v) {
+                var hue = Number(h), sat = Number(s), val = Number(v);
+                isFinite(hue) || (hue = 0), isFinite(sat) || (sat = 0), isFinite(val) || (val = 0),
+                hue = (hue % 360 + 360) % 360, sat = Math.max(0, Math.min(1, sat));
+                var c = (val = Math.max(0, Math.min(1, val))) * sat, x = c * (1 - Math.abs(hue / 60 % 2 - 1)), m = val - c, r = 0, g = 0, b = 0;
+                return hue < 60 ? (r = c, g = x, b = 0) : hue < 120 ? (r = x, g = c, b = 0) : hue < 180 ? (r = 0,
+                g = c, b = x) : hue < 240 ? (r = 0, g = x, b = c) : hue < 300 ? (r = x, g = 0, b = c) : (r = c,
+                g = 0, b = x), [ clampByte(255 * (r + m)), clampByte(255 * (g + m)), clampByte(255 * (b + m)) ];
+            }(h, s, v);
+            return rgbToHex(rgb[0], rgb[1], rgb[2]);
+        }
+        function clamp01(value) {
+            var next = Number(value);
+            return isFinite(next) || (next = 0), next < 0 && (next = 0), next > 1 && (next = 1),
+            next;
+        }
+        function isValidPanel(panel) {
+            return !!(panel && panel.IsValid && panel.IsValid());
+        }
+        function parsePoint(candidate) {
+            if (!candidate) return null;
+            if ("number" == typeof candidate.length && candidate.length >= 2) {
+                var arrayX = Number(candidate[0]), arrayY = Number(candidate[1]);
+                if (isFinite(arrayX) && isFinite(arrayY)) return {
+                    x: arrayX,
+                    y: arrayY
+                };
+            }
+            if ("object" == typeof candidate) {
+                var objectX = Number(void 0 !== candidate.x ? candidate.x : candidate[0]), objectY = Number(void 0 !== candidate.y ? candidate.y : candidate[1]);
+                if (isFinite(objectX) && isFinite(objectY)) return {
+                    x: objectX,
+                    y: objectY
+                };
             }
-          }
+            return null;
         }
-
-        posPickerXValueLbl.text = posPickerPercent(normalized) + "%";
-        posPickerYValueLbl.text = Math.round(normalized.y / 4) + "%";
-
-        if (emitUpdateEvent && changed) {
-          if (config.onChange) config.onChange(nextValue);
-          if (config.id && modTitle) {
-            emitUpdate(modTitle, config.id, nextValue);
-          }
+        function getCursorPosition() {
+            try {
+                return hasGameUI && "function" == typeof GameUI.GetCursorPosition ? parsePoint(GameUI.GetCursorPosition()) : null;
+            } catch (e) {}
+            return null;
         }
-      }
-
-      syncPosition(posPickerCurrent, false);
-
-      posPickerXSlider.SetPanelEvent("onvaluechanged", function () {
-        if (posPickerSyncing) return;
-        syncPosition({ x: posPickerXSlider.value, y: posPickerCurrent.y }, true);
-      });
-
-      posPickerYSlider.SetPanelEvent("onvaluechanged", function () {
-        if (posPickerSyncing) return;
-        syncPosition({ x: posPickerCurrent.x, y: posPickerYSlider.value }, true);
-      });
-
-      posPickerXSlider.SetPanelEvent("oncancel", () => {
-        AnitaRenderer.toggle(false);
-      });
-      posPickerYSlider.SetPanelEvent("oncancel", () => {
-        AnitaRenderer.toggle(false);
-      });
-
-      return row;
-    }
-  };
-
-  const AnitaRenderer = {
-    mainWindow: null,
-    backdrop: null,
-    navBar: null,
-    menuArea: null,
-    contentArea: null,
-    activeModTitle: "",
-    isOpen: false,
-    activeColorPickerClose: null,
-
-    findElementById: function (config, elementId) {
-      if (!config || !Array.isArray(config.elements) || !elementId) return null;
-      for (var i = 0; i < config.elements.length; i++) {
-        var element = config.elements[i];
-        if (element && element.id === elementId) return element;
-      }
-      return null;
-    },
-
-    isElementVisible: function (config, element) {
-      if (!element || !element.visibleWhen) return true;
-      var rule = element.visibleWhen;
-      var source = this.findElementById(config, rule.id);
-      if (!source) return true;
-      var current = source.currentValue;
-      if (Array.isArray(rule.equals)) {
-        for (var i = 0; i < rule.equals.length; i++) {
-          if (current === rule.equals[i]) return true;
+        function getPanelWindowPosition(panel) {
+            if (!isValidPanel(panel)) return null;
+            try {
+                if ("function" == typeof panel.GetPositionWithinWindow) {
+                    var point = parsePoint(panel.GetPositionWithinWindow());
+                    if (point) return point;
+                }
+            } catch (e) {}
+            var left = Number(panel.actualxoffset || panel.actualx || 0), top = Number(panel.actualyoffset || panel.actualy || 0);
+            return isFinite(left) || (left = 0), isFinite(top) || (top = 0), {
+                x: left,
+                y: top
+            };
         }
-        return false;
-      }
-      if (Object.prototype.hasOwnProperty.call(rule, "equals")) {
-        return current === rule.equals;
-      }
-      return !!current;
-    },
-
-    applyElementVisibility: function (config, element) {
-      if (!element || !element.__anitaRowPanel || !element.__anitaRowPanel.IsValid || !element.__anitaRowPanel.IsValid()) return;
-      var visible = this.isElementVisible(config, element);
-      element.__anitaRowPanel.style.visibility = visible ? "visible" : "collapse";
-      element.__anitaRowPanel.hittest = visible;
-    },
-
-    refreshConditionalVisibility: function (config) {
-      if (!config || !Array.isArray(config.elements)) return;
-      for (var i = 0; i < config.elements.length; i++) {
-        this.applyElementVisibility(config, config.elements[i]);
-      }
-    },
-
-    hasVisibilityDependents: function (config, sourceId) {
-      if (!config || !Array.isArray(config.elements) || !sourceId) return false;
-      for (var i = 0; i < config.elements.length; i++) {
-        var element = config.elements[i];
-        if (!element || !element.visibleWhen) continue;
-        if (element.visibleWhen.id === sourceId) return true;
-      }
-      return false;
-    },
-
-    getElementCategory: function (element) {
-      var label = String((element && element.category) || "General").trim();
-      return label || "General";
-    },
-
-    getCategoryList: function (config) {
-      var categories = [];
-      var seen = {};
-      if (!config || !Array.isArray(config.elements)) return categories;
-      for (var i = 0; i < config.elements.length; i++) {
-        var category = this.getElementCategory(config.elements[i]);
-        if (seen[category]) continue;
-        seen[category] = true;
-        categories.push(category);
-      }
-      return categories;
-    },
-
-    getCategoryElements: function (config, category) {
-      var elements = [];
-      if (!config || !Array.isArray(config.elements)) return elements;
-      for (var i = 0; i < config.elements.length; i++) {
-        var element = config.elements[i];
-        if (this.getElementCategory(element) !== category) continue;
-        elements.push(element);
-      }
-      return elements;
-    },
-
-    ensureActiveCategory: function (config) {
-      var categories = this.getCategoryList(config);
-      if (categories.length === 0) {
-        config.__anitaActiveCategory = "";
-        return "";
-      }
-      var active = String(config.__anitaActiveCategory || "");
-      for (var i = 0; i < categories.length; i++) {
-        if (categories[i] === active) return active;
-      }
-      config.__anitaActiveCategory = categories[0];
-      return categories[0];
-    },
-
-    getSaveCodeToken: function (config) {
-      if (!config || !config.storageNamespace) return "";
-
-      var raw = AnitaPersistence.buildStoredPayload(config);
-      if (!raw) return "";
-
-      var ns = AnitaPersistence.normalizeNamespace(config.storageNamespace);
-      if (!ns) return "";
-
-      return "[" + AnitaPersistence.TOKEN_PREFIX + ns + "]:" + AnitaBase64.encode(raw);
-    },
-
-    extractSaveCodeToken: function (config, text) {
-      if (!config || !config.storageNamespace) return "";
-
-      var body = String(text || "").trim();
-      if (!body) return "";
-
-      var ns = AnitaPersistence.normalizeNamespace(config.storageNamespace);
-      if (!ns) return "";
-
-      var scopedMatch = body.match(AnitaPersistence.getTokenRegex(ns));
-      if (scopedMatch && scopedMatch[0]) return scopedMatch[0];
-
-      var genericMatch = body.match(/\[ANITA-v1-[a-z0-9_]+\]:[A-Za-z0-9_-]+/i);
-      if (genericMatch && genericMatch[0]) return genericMatch[0];
-
-      if (/^[A-Za-z0-9_-]+$/.test(body)) {
-        return "[" + AnitaPersistence.TOKEN_PREFIX + ns + "]:" + body;
-      }
-
-      return "";
-    },
-
-    syncSaveCodeInput: function (config) {
-      if (!config || !config.__anitaSaveCodeInput) return;
-      if (!config.__anitaSaveCodeInput.IsValid || !config.__anitaSaveCodeInput.IsValid()) return;
-
-      var token = this.getSaveCodeToken(config);
-      if (String(config.__anitaSaveCodeInput.text || "") === token) return;
-
-      config.__anitaSaveCodeInput.text = token;
-    },
-
-    initWindow: function (root) {
-      if (root.FindChildTraverse(CONFIG.IDS.WINDOW)) root.FindChildTraverse(CONFIG.IDS.WINDOW).DeleteAsync(0);
-      if (root.FindChildTraverse(CONFIG.IDS.BACKDROP)) root.FindChildTraverse(CONFIG.IDS.BACKDROP).DeleteAsync(0);
-
-
-      this.backdrop = $.CreatePanel("Panel", root, CONFIG.IDS.BACKDROP);
-      this.backdrop.AddClass("AnitaBackdrop");
-      this.backdrop.SetPanelEvent("onactivate", () => this.toggle(false));
-
-      this.mainWindow = $.CreatePanel("Panel", root, CONFIG.IDS.WINDOW);
-      this.mainWindow.AddClass("AnitaWindow");
-
-      this.mainWindow.canfocus = true;
-      this.mainWindow.SetPanelEvent("oncancel", () => this.toggle(false));
-
-      this.mainWindow.SetPanelEvent("onactivate", () => {
-        this.mainWindow.SetFocus();
-      });
-
-      this.navBar = $.CreatePanel("Panel", this.mainWindow, CONFIG.IDS.NAVBAR);
-      this.navBar.AddClass("AnitaNavBar");
-
-      const closeBtn = $.CreatePanel("Button", this.navBar, "");
-      closeBtn.AddClass("AnitaCloseBtn");
-      closeBtn.SetPanelEvent("onactivate", () => this.toggle(false));
-
-      const sep = $.CreatePanel("Label", this.navBar, "");
-      sep.text = "/";
-      sep.AddClass("AnitaTabSeparator");
-
-      this.menuArea = $.CreatePanel("Panel", this.navBar, "AnitaTabContainer");
-      this.menuArea.AddClass("AnitaTabContainer");
-      this.contentArea = $.CreatePanel("Panel", this.mainWindow, CONFIG.IDS.CONTENT);
-      this.contentArea.AddClass("AnitaContentArea");
-    },
-
-    toggle: function (forceState) {
-      if (!this.mainWindow || !this.backdrop) return;
-      this.isOpen = (forceState !== undefined) ? forceState : !this.isOpen;
-
-      this.mainWindow.SetHasClass(CONFIG.CLASSES.OPEN, this.isOpen);
-      this.mainWindow.hittest = this.isOpen;
-      this.backdrop.SetHasClass(CONFIG.CLASSES.OPEN, this.isOpen);
-      this.backdrop.hittest = this.isOpen;
-
-      if (this.isOpen) {
-        this.mainWindow.SetFocus();
-      } else {
-        if (this.activeColorPickerClose) {
-          try {
-            this.activeColorPickerClose();
-          } catch (closeErr) {}
-          this.activeColorPickerClose = null;
+        function getPanelBounds(panel) {
+            if (!isValidPanel(panel)) return {
+                left: 0,
+                top: 0,
+                width: 1,
+                height: 1
+            };
+            var panelPos = getPanelWindowPosition(panel), left = panelPos ? panelPos.x : Number(panel.actualxoffset || panel.actualx || 0), top = panelPos ? panelPos.y : Number(panel.actualyoffset || panel.actualy || 0), width = Number(panel.actuallayoutwidth || panel.contentwidth || panel.width || 1), height = Number(panel.actuallayoutheight || panel.contentheight || panel.height || 1);
+            return isFinite(left) || (left = 0), isFinite(top) || (top = 0), (!isFinite(width) || width <= 0) && (width = 1),
+            (!isFinite(height) || height <= 0) && (height = 1), {
+                left: left,
+                top: top,
+                width: width,
+                height: height
+            };
         }
-        $.DispatchEvent("DropInputFocus", this.mainWindow);
-
-        let root = $.GetContextPanel();
-        while (root.GetParent()) root = root.GetParent();
-        root.SetFocus();
-      }
-    },
-
-    addTab: function (modTitle, onClick) {
-      let displayTitle = modTitle;
-      const MAX_CHARS = CONFIG.UI.TAB_MAX_CHARS;
-      if (displayTitle.length > MAX_CHARS) displayTitle = displayTitle.substring(0, MAX_CHARS) + "...";
-
-      const btn = $.CreatePanel("Button", this.menuArea, "");
-      btn.AddClass("AnitaTabBtn");
-      const lbl = $.CreatePanel("Label", btn, "");
-      lbl.text = displayTitle;
-
-      const sep = $.CreatePanel("Label", this.menuArea, "");
-      sep.text = "/"; sep.AddClass("AnitaTabSeparator");
-
-      btn.SetPanelEvent("onactivate", () => {
-        this.menuArea.Children().forEach(c => {
-          if (c.paneltype === "Button" && !c.BHasClass("AnitaCloseBtn")) c.RemoveClass("Active");
-        });
-        btn.AddClass("Active");
-        this.activeModTitle = modTitle;
-        onClick();
-      });
-
-      if (this.menuArea.GetChildCount() <= 4) {
-        btn.AddClass("Active");
-        this.activeModTitle = modTitle;
-        onClick();
-      }
-    },
-
-    renderModSettings: function (config) {
-      this.contentArea.RemoveAndDeleteChildren();
-
-      this.contentArea.canfocus = true;
-      this.contentArea.SetPanelEvent("onactivate", () => this.contentArea.SetFocus());
-
-      const container = $.CreatePanel("Panel", this.contentArea, "");
-      container.AddClass("ModContainer");
-      container.canfocus = true;
-
-      const bgShield = $.CreatePanel("Panel", container, "BackgroundShield");
-      bgShield.style.width = "100%";
-      bgShield.style.height = "100%";
-      bgShield.style.ignoreParentFlow = "true";
-      bgShield.style.zIndex = "-1";
-      bgShield.hittest = true;
-
-      const syncAll = () => {
-        if (config.elements) {
-          config.elements.forEach(el => {
-            if (el.id && el.currentValue !== undefined) {
-              emitUpdate(config.title, el.id, el.currentValue, {
-                update_source: "ui_resync",
-                skip_bridge_persist: true
-              });
-            }
-          });
+        function getPanelLocalCursorPosition(panel) {
+            if (!isValidPanel(panel)) return null;
+            var bounds = getPanelBounds(panel), panelPos = getPanelWindowPosition(panel);
+            try {
+                if ("function" == typeof panel.GetCursorPosition) {
+                    var rawPoint = parsePoint(panel.GetCursorPosition());
+                    if (rawPoint) return rawPoint.x >= 0 && rawPoint.x <= bounds.width && rawPoint.y >= 0 && rawPoint.y <= bounds.height ? rawPoint : panelPos ? {
+                        x: rawPoint.x - panelPos.x,
+                        y: rawPoint.y - panelPos.y
+                    } : rawPoint;
+                }
+            } catch (e) {}
+            var screenCursor = getCursorPosition();
+            return screenCursor && panelPos ? {
+                x: screenCursor.x - panelPos.x,
+                y: screenCursor.y - panelPos.y
+            } : null;
+        }
+        function getBoxStateFromColor(colorCode) {
+            var r, g, b, red, green, blue, max, delta, hue, rgb = function(colorCode) {
+                if (!colorCode) return [ 255, 255, 255 ];
+                const text = String(colorCode).trim();
+                if ("#" === text.charAt(0)) {
+                    let hex = text.slice(1);
+                    if (3 === hex.length && (hex = hex[0] + hex[0] + hex[1] + hex[1] + hex[2] + hex[2]),
+                    hex.length >= 6) return [ parseInt(hex.slice(0, 2), 16) || 0, parseInt(hex.slice(2, 4), 16) || 0, parseInt(hex.slice(4, 6), 16) || 0 ];
+                }
+                const match = text.match(/rgb\((\d+),\s*(\d+),\s*(\d+)\)/i);
+                return match ? [ clampByte(match[1]), clampByte(match[2]), clampByte(match[3]) ] : [ 255, 255, 255 ];
+            }(colorCode), hsv = (r = rgb[0], g = rgb[1], b = rgb[2], red = clampByte(r) / 255,
+            green = clampByte(g) / 255, blue = clampByte(b) / 255, max = Math.max(red, green, blue),
+            delta = max - Math.min(red, green, blue), hue = 0, 0 !== delta && (hue = max === red ? (green - blue) / delta % 6 * 60 : max === green ? 60 * ((blue - red) / delta + 2) : 60 * ((red - green) / delta + 4)),
+            hue < 0 && (hue += 360), [ hue, 0 === max ? 0 : delta / max, max ]);
+            return {
+                hue: Math.round(hsv[0]) % 360,
+                sat: clamp01(hsv[1])
+            };
         }
-      };
-
-      bgShield.SetPanelEvent("onmouseover", () => {
-        syncAll();
-      });
-
-      bgShield.SetPanelEvent("onactivate", () => {
-        container.SetFocus();
-        syncAll();
-      });
-
-      const title = $.CreatePanel("Label", container, "");
-      title.text = config.title; title.AddClass("SectionHeader");
-      const line = $.CreatePanel("Panel", container, ""); line.AddClass("SectionHeaderLine");
-
-      if (config.description) {
-        const desc = $.CreatePanel("Label", container, "");
-        desc.text = config.description; desc.AddClass("ModDescription");
-      }
-
-      const shell = $.CreatePanel("Panel", container, "");
-      shell.AddClass("AnitaSettingsShell");
-
-      const treePanel = $.CreatePanel("Panel", shell, "");
-      treePanel.AddClass("AnitaTreePanel");
-
-      const treeHeader = $.CreatePanel("Label", treePanel, "");
-      treeHeader.text = "Settings";
-      treeHeader.AddClass("AnitaTreeHeader");
-
-      const treeList = $.CreatePanel("Panel", treePanel, "");
-      treeList.AddClass("AnitaTreeList");
-
-      const detailPanel = $.CreatePanel("Panel", shell, "");
-      detailPanel.AddClass("AnitaDetailPanel");
-
-      const activeCategory = this.ensureActiveCategory(config);
-      const categories = this.getCategoryList(config);
-
-      for (var c = 0; c < categories.length; c++) {
-        var category = categories[c];
-        var categoryBtn = $.CreatePanel("Button", treeList, "");
-        categoryBtn.AddClass("AnitaTreeNode");
-        categoryBtn.SetHasClass("Active", category === activeCategory);
-
-        var categoryGlyph = $.CreatePanel("Label", categoryBtn, "");
-        categoryGlyph.AddClass("AnitaTreeNodeGlyph");
-        categoryGlyph.text = category === activeCategory ? "v" : ">";
-
-        var categoryLabel = $.CreatePanel("Label", categoryBtn, "");
-        categoryLabel.AddClass("AnitaTreeNodeLabel");
-        categoryLabel.text = category;
-
-        var categoryCount = $.CreatePanel("Label", categoryBtn, "");
-        categoryCount.AddClass("AnitaTreeNodeCount");
-        categoryCount.text = String(this.getCategoryElements(config, category).length);
-
-        categoryBtn.SetPanelEvent("onactivate", function (nextCategory) {
-          return function () {
-            config.__anitaActiveCategory = nextCategory;
-            AnitaRenderer.renderModSettings(config);
-          };
-        }(category));
-      }
-
-      const detailHeaderRow = $.CreatePanel("Panel", detailPanel, "");
-      detailHeaderRow.AddClass("AnitaDetailHeaderRow");
-
-      const detailHeader = $.CreatePanel("Label", detailHeaderRow, "");
-      detailHeader.text = activeCategory || config.title;
-      detailHeader.AddClass("AnitaDetailHeader");
-
-      const detailHint = $.CreatePanel("Label", detailPanel, "");
-      detailHint.text = "Select a setting group from the tree on the left.";
-      detailHint.AddClass("AnitaDetailHint");
-
-      const settingsList = $.CreatePanel("Panel", detailPanel, "");
-      settingsList.AddClass("AnitaSettingsList");
-
-      if (config.elements) {
-        config.elements.forEach(el => {
-          el.__anitaRowPanel = null;
-        });
-        this.getCategoryElements(config, activeCategory).forEach(el => {
-          var row = null;
-          switch (el.type) {
-            case "toggle": row = AnitaComponents.createToggle(settingsList, el, config.title); break;
-            case "stepper": row = AnitaComponents.createStepper(settingsList, el, config.title); break;
-            case "slider": row = AnitaComponents.createSlider(settingsList, el, config.title); break;
-            case "button": row = AnitaComponents.createButton(settingsList, el, config.title); break;
-            case "cycler": row = AnitaComponents.createCycler(settingsList, el, config.title); break;
-            case "positionpicker": row = AnitaComponents.createPositionPicker(settingsList, el, config.title); break;
-            case "colorpicker": row = AnitaComponents.createColorPicker(settingsList, el, config.title); break;
-          }
-          el.__anitaRowPanel = row || null;
-        });
-        this.refreshConditionalVisibility(config);
-      }
-
-      // Footer: Copy / Reset / Import (only for mods with storageNamespace)
-      if (config.storageNamespace) {
-        var footerWrap = $.CreatePanel("Panel", detailPanel, "");
-        footerWrap.AddClass("AnitaFooterWrap");
-
-        var footer = $.CreatePanel("Panel", footerWrap, "");
-        footer.AddClass("AnitaFooterRow");
-
-        function makeFooterBtn(parent, label, id) {
-          var btn = $.CreatePanel("Button", parent, id || "");
-          btn.AddClass("AnitaFooterBtn");
-          var lbl = $.CreatePanel("Label", btn, "");
-          lbl.text = label;
-          return { btn: btn, lbl: lbl };
+        function setPickerBoxState(boxState, fallbackColorCode) {
+            var resolved = function(boxState, fallbackColorCode) {
+                var fallback = getBoxStateFromColor(fallbackColorCode || currentColor);
+                if (!boxState) return fallback;
+                var hue = Number(boxState.hue), sat = Number(boxState.sat);
+                return isFinite(hue) || (hue = fallback.hue), isFinite(sat) || (sat = fallback.sat),
+                (hue = Math.round(hue) % 360) < 0 && (hue += 360), {
+                    hue: hue,
+                    sat: sat = clamp01(sat)
+                };
+            }(boxState, fallbackColorCode);
+            return pickerBoxHue = resolved.hue, pickerBoxSat = resolved.sat, resolved;
         }
-
-        function flashLabel(btn, lbl, msg, durationSec) {
-          if (!btn || !btn.IsValid || !btn.IsValid()) return;
-          if (!lbl || !lbl.IsValid || !lbl.IsValid()) return;
-          var orig = lbl.text;
-          lbl.text = msg;
-          btn.AddClass("AnitaFooterBtnSuccess");
-          $.Schedule(durationSec, function () {
-            if (lbl && lbl.IsValid()) lbl.text = orig;
-            if (btn && btn.IsValid()) btn.RemoveClass("AnitaFooterBtnSuccess");
-          });
+        function colorFromBoxState(hue, sat) {
+            return hsvToHex(hue, sat, 1);
         }
-
-        // Copy button
-        var copyB = makeFooterBtn(footer, "Copy", "");
-        copyB.btn.SetPanelEvent("onactivate", function () {
-          AnitaRenderer.syncSaveCodeInput(config);
-          var token = AnitaRenderer.getSaveCodeToken(config);
-          if (!token) {
-            flashLabel(copyB.btn, copyB.lbl, "Empty", 1.5);
-            return;
-          }
-          try {
-            $.DispatchEvent("CopyStringToClipboard", token, $.GetContextPanel());
-            flashLabel(copyB.btn, copyB.lbl, "Copied!", 1.5);
-          } catch (e) {
-            flashLabel(copyB.btn, copyB.lbl, "Failed", 1.5);
-          }
-        });
-
-        var resetB = makeFooterBtn(footer, "Reset", "");
-        resetB.btn.SetPanelEvent("onactivate", function () {
-          AnitaPersistence.applyResolvedValues(config, {});
-          AnitaPersistence.persistConfig(config, true);
-          AnitaRenderer.syncSaveCodeInput(config);
-          flashLabel(resetB.btn, resetB.lbl, "Reset", 1.5);
-          AnitaRenderer.renderModSettings(config);
-          AnitaCore.emitCurrentValues(config, {
-            update_source: "ui_reset",
-            force_persist: true
-          });
-        });
-
-        // Paste row — visible TextEntry the user can paste a token into, then Apply
-        var importRowOpen = false;
-        var importToggleBtn = makeFooterBtn(footer, "Import", "");
-
-        var importCodeRow = $.CreatePanel("Panel", footerWrap, "");
-        importCodeRow.AddClass("AnitaPasteRow");
-        importCodeRow.AddClass("AnitaPasteRowCompact");
-        importCodeRow.hittest = true;
-        importCodeRow.style.visibility = "collapse";
-        importCodeRow.style.height = "0px";
-        importCodeRow.style.marginTop = "0px";
-
-        var importCodeLabel = $.CreatePanel("Label", importCodeRow, "");
-        importCodeLabel.AddClass("AnitaLabel");
-        importCodeLabel.text = "Import Code";
-        importCodeLabel.style.width = "84px";
-        importCodeLabel.style.fontSize = "12px";
-        importCodeLabel.style.marginRight = "6px";
-
-        var importCodeInput = $.CreatePanel("TextEntry", importCodeRow, "");
-        importCodeInput.AddClass("AnitaPasteInput");
-        importCodeInput.placeholder = "Paste a code to apply...";
-        importCodeInput.style.width = "320px";
-        importCodeInput.style.height = "24px";
-        importCodeInput.style.fontSize = "9px";
-        config.__anitaImportCodeInput = importCodeInput;
-
-        var applyCodeB = makeFooterBtn(importCodeRow, "Apply", "");
-
-        function setImportRowVisible(visible) {
-          importRowOpen = !!visible;
-          if (importCodeRow && importCodeRow.IsValid && importCodeRow.IsValid()) {
-            importCodeRow.style.visibility = importRowOpen ? "visible" : "collapse";
-            importCodeRow.style.height = importRowOpen ? "30px" : "0px";
-            importCodeRow.style.marginTop = importRowOpen ? "6px" : "0px";
-          }
-          if (importToggleBtn && importToggleBtn.lbl) {
-            importToggleBtn.lbl.text = importRowOpen ? "Hide" : "Import";
-          }
+        function hueFromRelX(relX) {
+            var normalized = clamp01(relX);
+            return Math.max(0, Math.min(359, Math.round(359 * normalized)));
         }
-
-        importToggleBtn.btn.SetPanelEvent("onactivate", function () {
-          setImportRowVisible(!importRowOpen);
-        });
-
-        function applySaveCodeInput() {
-          var text = String(importCodeInput.text || "").trim();
-          if (!text) { flashLabel(applyCodeB.btn, applyCodeB.lbl, "Empty", 1.5); return; }
-
-          var token = AnitaRenderer.extractSaveCodeToken(config, text);
-          if (!token) { flashLabel(applyCodeB.btn, applyCodeB.lbl, "Invalid", 1.5); return; }
-
-          var encoded = token.split("]:")[1] || "";
-          try {
-            var raw = AnitaBase64.decode(encoded);
-            var parsed = AnitaPersistence.parseStoredPayload(config, raw, "code");
-            if (!parsed) { flashLabel(applyCodeB.btn, applyCodeB.lbl, "Invalid", 1.5); return; }
-            if (!parsed.values || !Object.keys(parsed.values).length) {
-              flashLabel(applyCodeB.btn, applyCodeB.lbl, "No IDs", 1.5);
-              return;
+        function normalizeHexColor(colorCode) {
+            if (!colorCode) return "#FFFFFF";
+            if ("number" == typeof colorCode) {
+                for (var packedHex = Math.max(0, Math.floor(colorCode)).toString(16).toUpperCase(); packedHex.length < 6; ) packedHex = "0" + packedHex;
+                return "#" + packedHex.slice(-6);
             }
-
-            AnitaPersistence.applyResolvedValues(config, parsed.values);
-            AnitaPersistence.persistConfig(config, true);
-            AnitaRenderer.syncSaveCodeInput(config);
-            flashLabel(applyCodeB.btn, applyCodeB.lbl, "Applied", 1.5);
-            AnitaRenderer.renderModSettings(config);
-            if (config.__anitaImportCodeInput && config.__anitaImportCodeInput.IsValid && config.__anitaImportCodeInput.IsValid()) {
-              config.__anitaImportCodeInput.text = "";
+            if ("string" == typeof colorCode) {
+                var text = String(colorCode).trim();
+                if (!text) return "#FFFFFF";
+                if ("#" === text.charAt(0)) {
+                    var hex = text.slice(1);
+                    if (3 === hex.length && (hex = hex[0] + hex[0] + hex[1] + hex[1] + hex[2] + hex[2]),
+                    hex.length >= 6) return "#" + hex.slice(0, 6).toUpperCase();
+                }
+                var rgbMatch = text.match(/rgb\((\d+),\s*(\d+),\s*(\d+)\)/i);
+                if (rgbMatch) return rgbToHex(rgbMatch[1], rgbMatch[2], rgbMatch[3]).toUpperCase();
             }
-            AnitaCore.emitCurrentValues(config, {
-              update_source: "ui_code_apply",
-              force_persist: true
-            });
-          } catch (eDec) {
-            flashLabel(applyCodeB.btn, applyCodeB.lbl, "Invalid", 1.5);
-          }
+            if ("object" == typeof colorCode) {
+                if (void 0 !== colorCode.hex) return normalizeHexColor(colorCode.hex);
+                if (void 0 !== colorCode.h && void 0 !== colorCode.s && void 0 !== colorCode.v) return hsvToHex(colorCode.h, colorCode.s, colorCode.v);
+                if (void 0 !== colorCode.r && void 0 !== colorCode.g && void 0 !== colorCode.b) return rgbToHex(colorCode.r, colorCode.g, colorCode.b).toUpperCase();
+                if (void 0 !== colorCode.red && void 0 !== colorCode.green && void 0 !== colorCode.blue) return rgbToHex(colorCode.red, colorCode.green, colorCode.blue).toUpperCase();
+            }
+            return "#FFFFFF";
+        }
+        function getColorBoxMetrics() {
+            var refPanel = isValidPanel(colorBoxFrame) ? colorBoxFrame : colorBoxPanel;
+            if (!isValidPanel(refPanel)) return null;
+            var bounds = getPanelBounds(refPanel), width = Number(refPanel.actuallayoutwidth || bounds.width || 240), height = Number(refPanel.actuallayoutheight || bounds.height || 240), cursorWidth = Number(isValidPanel(colorBoxCursor) ? colorBoxCursor.actuallayoutwidth || colorBoxCursor.contentwidth || colorBoxCursor.width : 18), cursorHeight = Number(isValidPanel(colorBoxCursor) ? colorBoxCursor.actuallayoutheight || colorBoxCursor.contentheight || colorBoxCursor.height : 18);
+            return (!isFinite(width) || width <= 1) && (width = 240), (!isFinite(height) || height <= 1) && (height = 240),
+            (!isFinite(cursorWidth) || cursorWidth <= 0) && (cursorWidth = 18), (!isFinite(cursorHeight) || cursorHeight <= 0) && (cursorHeight = 18),
+            {
+                panel: refPanel,
+                bounds: {
+                    left: bounds.left,
+                    top: bounds.top,
+                    width: width,
+                    height: height
+                },
+                width: width,
+                height: height,
+                cursorWidth: cursorWidth,
+                cursorHeight: cursorHeight,
+                maxCursorX: Math.max(0, width - cursorWidth),
+                maxCursorY: Math.max(0, height - cursorHeight)
+            };
         }
-
-        applyCodeB.btn.SetPanelEvent("onactivate", applySaveCodeInput);
-        importCodeInput.SetPanelEvent("ontextentrysubmit", applySaveCodeInput);
-        setImportRowVisible(false);
-      }
-    },
-
-  }
-
-  const AnitaCore = {
-    registeredMods: [],
-
-    init: function () {
-      const root = this.getRoot($.GetContextPanel());
-      AnitaRenderer.initWindow(root);
-
-      root.AnitaUI = {
-        GetVersion: () => CONFIG.VERSION,
-        Register: (config) => this.registerMod(config),
-        Toggle: () => AnitaRenderer.toggle(),
-        IsReady: () => true
-      };
-
-      this.setupEventListener();
-      this.createOverlayButton(root);
-      this.monitorEscapeMenu(root);
-      if (this.registeredMods.length === 0) {
-        this.registerMod({
-          title: "Anita-UI",
-          description: "No detected mods. Check your installed mods.",
-          isDummy: true,
-          elements: []
-        });
-      }
-
-      $.DispatchEvent("ClientUI_FireOutput", JSON.stringify({
-        magic_word: "ANITA_ALIVE"
-      }));
-    },
-
-    registerMod: function (config) {
-      if (config && config.title === "HP Colors") {
-      }
-      if (this.registeredMods.length === 1 && this.registeredMods[0].isDummy) {
-        this.registeredMods = [];
-        AnitaRenderer.menuArea.RemoveAndDeleteChildren();
-        AnitaRenderer.contentArea.RemoveAndDeleteChildren();
-      }
-
-      AnitaPersistence.hydrateConfig(config);
-
-      for (let i = 0; i < this.registeredMods.length; i++) {
-        if (this.registeredMods[i].title === config.title) {
-          return;
+        function applyColorBoxCursorPosition(cursorX, cursorY) {
+            if (!isValidPanel(colorBoxCursor)) return !1;
+            var metrics = getColorBoxMetrics();
+            if (!metrics) return !1;
+            var nextX = Number(cursorX), nextY = Number(cursorY);
+            return isFinite(nextX) || (nextX = 0), isFinite(nextY) || (nextY = 0), nextX < 0 && (nextX = 0),
+            nextY < 0 && (nextY = 0), nextX > metrics.maxCursorX && (nextX = metrics.maxCursorX),
+            nextY > metrics.maxCursorY && (nextY = metrics.maxCursorY), colorDragAnchorX = nextX,
+            colorDragAnchorY = nextY, colorBoxCursor.style.x = nextX + "px", colorBoxCursor.style.y = nextY + "px",
+            colorBoxCursor.style.transform = "none", colorBoxCursor.style.zIndex = "20", colorBoxCursor.style.opacity = "1",
+            colorBoxCursor.style.visibility = "visible", colorBoxCursor.style.backgroundColor = "#FFFFFF",
+            colorBoxCursor.style.washColor = "#FFFFFF", !0;
+        }
+        function getCursorPositionWithinColorBox() {
+            if (!isValidPanel(colorBoxCursor)) return null;
+            var metrics = getColorBoxMetrics();
+            if (!metrics) return null;
+            function chooseLocalAxis(layoutValue, boundsValue, maxValue, allowLayoutValue) {
+                var layoutValid = !!allowLayoutValue && isFinite(layoutValue) && layoutValue >= -1 && layoutValue <= maxValue + 1, boundsValid = isFinite(boundsValue) && boundsValue >= -1 && boundsValue <= maxValue + 1;
+                return layoutValid && boundsValid ? Math.abs(layoutValue - boundsValue) <= 12 ? layoutValue : boundsValue : boundsValid ? boundsValue : layoutValid ? layoutValue : isFinite(boundsValue) ? boundsValue : isFinite(layoutValue) ? layoutValue : 0;
+            }
+            var layoutX = Number(colorBoxCursor.actuallayoutx), layoutY = Number(colorBoxCursor.actuallayouty), parentMatches = !(!colorBoxCursor.GetParent || colorBoxCursor.GetParent() !== metrics.panel), cursorBounds = getPanelBounds(colorBoxCursor), boundsX = cursorBounds.left - metrics.bounds.left, boundsY = cursorBounds.top - metrics.bounds.top, localX = chooseLocalAxis(layoutX, boundsX, metrics.maxCursorX, parentMatches), localY = chooseLocalAxis(layoutY, boundsY, metrics.maxCursorY, parentMatches);
+            return localX < 0 && (localX = 0), localY < 0 && (localY = 0), localX > metrics.maxCursorX && (localX = metrics.maxCursorX),
+            localY > metrics.maxCursorY && (localY = metrics.maxCursorY), function(cursorX, cursorY) {
+                var metrics = getColorBoxMetrics();
+                if (!metrics) return !1;
+                var nextX = Number(cursorX), nextY = Number(cursorY);
+                !isFinite(nextX) || !isFinite(nextY) || (nextX < 0 && (nextX = 0), nextY < 0 && (nextY = 0),
+                nextX > metrics.maxCursorX && (nextX = metrics.maxCursorX), nextY > metrics.maxCursorY && (nextY = metrics.maxCursorY),
+                colorDragAnchorX = nextX, colorDragAnchorY = nextY);
+            }(localX, localY), {
+                metrics: metrics,
+                x: localX,
+                y: localY
+            };
         }
-      }
-      this.registeredMods.push(config);
-      if (config.title === "HP Colors") {
-      }
-      AnitaRenderer.addTab(config.title, () => {
-        AnitaRenderer.renderModSettings(config);
-      });
-      this.updateWindowWidth();
-      $.DispatchEvent("ClientUI_FireOutput", JSON.stringify({
-        magic_word: "ANITA_HANDSHAKE",
-        mod_title: config.title
-      }));
-      AnitaPersistence.requestBootstrap(config, "core_handshake");
-    },
-
-    emitCurrentValues: function (config, meta) {
-      if (!config || !Array.isArray(config.elements)) return;
-      if (config.title === "HP Colors") {
-      }
-      for (var i = 0; i < config.elements.length; i++) {
-        var element = config.elements[i];
-        if (!element || !element.id || element.currentValue === undefined) continue;
-        emitUpdate(config.title, element.id, element.currentValue, meta);
-      }
-    },
-
-    findRegisteredMod: function (modTitle) {
-      for (var i = 0; i < this.registeredMods.length; i++) {
-        if (this.registeredMods[i] && this.registeredMods[i].title === modTitle) {
-          return this.registeredMods[i];
+        function updateBoxCursorVisual(colorCode) {
+            if (colorBoxPanel && colorBoxPanel.IsValid && colorBoxPanel.IsValid()) {
+                var metrics = getColorBoxMetrics();
+                if (metrics) isFinite(pickerBoxHue) && isFinite(pickerBoxSat) || setPickerBoxState(null, colorCode),
+                applyColorBoxCursorPosition(metrics.maxCursorX > 0 ? pickerBoxHue / 359 * metrics.maxCursorX : 0, metrics.maxCursorY > 0 ? pickerBoxSat * metrics.maxCursorY : 0);
+            }
         }
-      }
-      return null;
-    },
-
-    queueRenderRefresh: function (config) {
-      if (!config) return;
-      var refreshToken = (config.__anitaUiRefreshToken || 0) + 1;
-      config.__anitaUiRefreshToken = refreshToken;
-      $.Schedule(RENDER_REFRESH_DEBOUNCE_SEC, function () {
-        if (!config || config.__anitaUiRefreshToken !== refreshToken) return;
-        if (AnitaRenderer.activeModTitle !== config.title) return;
-        AnitaRenderer.renderModSettings(config);
-      });
-    },
-
-    emitPortableSync: function (config, reason) {
-      if (!config || config.title !== "HP Colors") return;
-      this.emitCurrentValues(config, {
-        update_source: "core_auto_resync",
-        skip_bridge_persist: true,
-        sync_reason: String(reason || "tick")
-      });
-    },
-
-    queuePortableSyncBurst: function (config, reason) {
-      if (!config || config.title !== "HP Colors") return;
-      var token = (config.__anitaPortableSyncBurstToken || 0) + 1;
-      config.__anitaPortableSyncBurstToken = token;
-      var delays = [0.35, 1.0, 2.0];
-      for (var i = 0; i < delays.length; i++) {
-        (function (delaySec, burstToken, burstIndex) {
-          $.Schedule(delaySec, function () {
-            if (!config || config.__anitaPortableSyncBurstToken !== burstToken) return;
-            AnitaCore.emitPortableSync(config, String(reason || "burst") + "_" + String(burstIndex + 1));
-          });
-        })(delays[i], token, i);
-      }
-    },
-
-    startPortableSyncLoop: function (config) {
-      if (!config || config.title !== "HP Colors") return;
-      config.__anitaPortableSyncReason = String(config.__anitaPortableSyncReason || "update");
-      if (config.__anitaPortableSyncLoopStarted) return;
-      config.__anitaPortableSyncLoopStarted = true;
-
-      var tick = () => {
-        if (!config) return;
-        if (this.findRegisteredMod(config.title) !== config) {
-          config.__anitaPortableSyncLoopStarted = false;
-          return;
+        function syncFromCursorPanelPosition(emitUpdateEvent) {
+            if (!isValidPanel(colorBoxPanel) || !isValidPanel(colorBoxCursor)) return !1;
+            var cursorPosition = getCursorPositionWithinColorBox();
+            if (!cursorPosition) return !1;
+            const metrics = cursorPosition.metrics, relX = metrics.maxCursorX > 0 ? clamp01(cursorPosition.x / metrics.maxCursorX) : 0, relY = metrics.maxCursorY > 0 ? clamp01(cursorPosition.y / metrics.maxCursorY) : 0, hue = hueFromRelX(relX), sat = clamp01(relY);
+            return syncColorVisuals(colorFromBoxState(hue, sat), emitUpdateEvent, !1, {
+                hue: hue,
+                sat: sat
+            }), !0;
+        }
+        function syncFromBoxPosition(boxX, boxY, emitUpdateEvent) {
+            if (!isValidPanel(colorBoxPanel)) return !1;
+            const metrics = getColorBoxMetrics();
+            if (!metrics) return !1;
+            const relX = clamp01(Number(boxX) / metrics.width), relY = clamp01(Number(boxY) / metrics.height), hue = hueFromRelX(relX), sat = clamp01(relY);
+            return syncColorVisuals(colorFromBoxState(hue, sat), emitUpdateEvent, !1, {
+                hue: hue,
+                sat: sat
+            }), !0;
+        }
+        function syncFromScreenCursorPosition(emitUpdateEvent) {
+            var cursor = getCursorPosition();
+            if (!cursor || !isValidPanel(colorBoxPanel)) return !1;
+            const metrics = getColorBoxMetrics();
+            if (!metrics) return !1;
+            const bounds = metrics.bounds, relX = clamp01((cursor.x - bounds.left) / bounds.width), relY = clamp01((cursor.y - bounds.top) / bounds.height), hue = hueFromRelX(relX), sat = clamp01(relY);
+            return syncColorVisuals(colorFromBoxState(hue, sat), emitUpdateEvent, !1, {
+                hue: hue,
+                sat: sat
+            }), !0;
+        }
+        function syncFromLocalBoxCursor(emitUpdateEvent) {
+            var localPoint = getPanelLocalCursorPosition(colorBoxPanel);
+            return !!localPoint && syncFromBoxPosition(localPoint.x, localPoint.y, emitUpdateEvent);
+        }
+        function syncFromBestDragSource(emitUpdateEvent) {
+            return hasGameUI ? "cursor" !== colorDragSource && "drag_event" !== colorDragSource || !syncFromCursorPanelPosition(emitUpdateEvent) ? "box" !== colorDragSource && "gameui" !== colorDragSource || !syncFromScreenCursorPosition(emitUpdateEvent) ? syncFromLocalBoxCursor(emitUpdateEvent) ? "panel_cursor" : syncFromScreenCursorPosition(emitUpdateEvent) ? "screen_cursor" : syncFromCursorPanelPosition(emitUpdateEvent) ? "drag_panel" : "" : "screen_cursor" : "drag_panel" : "cursor" !== colorDragSource && "drag_event" !== colorDragSource || !syncFromCursorPanelPosition(emitUpdateEvent) ? syncFromLocalBoxCursor(emitUpdateEvent) ? "panel_cursor" : syncFromCursorPanelPosition(emitUpdateEvent) ? "cursor_panel" : "" : "cursor_panel";
+        }
+        function setMouseCaptureState(active) {
+            var next = !!active;
+            if (isValidPanel(colorBoxPanel) && "function" == typeof colorBoxPanel.SetMouseCapture) try {
+                colorBoxPanel.SetMouseCapture(next);
+            } catch (e) {}
+            if (hasGameUI && isValidPanel(colorBoxCursor) && "function" == typeof colorBoxCursor.SetMouseCapture) try {
+                colorBoxCursor.SetMouseCapture(next);
+            } catch (e) {}
         }
-        this.emitPortableSync(config, "heartbeat_" + String(config.__anitaPortableSyncReason || "update"));
-        $.Schedule(3.0, tick);
-      };
-
-      $.Schedule(3.0, tick);
-    },
-
-    handleUpdateEvent: function (data) {
-      if (!data || !data.mod_title || !data.setting_id) return;
-      var config = this.findRegisteredMod(data.mod_title);
-      if (!config) return;
-      var updateSource = String(data.update_source || "");
-      var isBootstrap = updateSource === "bridge_bootstrap";
-      var isReplaySource = isBootstrap ||
-        updateSource === "ui_resync" ||
-        updateSource === "ui_reset" ||
-        updateSource === "ui_code_apply" ||
-        updateSource === "core_auto_resync";
-      if (data.mod_title === "HP Colors") {
-      }
-      if (!AnitaPersistence.applyUpdate(config, data.setting_id, data.value)) {
-        return;
-      }
-      AnitaRenderer.syncSaveCodeInput(config);
-      if (isBootstrap) {
-        config.__anitaBootstrapReceived = true;
-      }
-
-      if (!isReplaySource && config.title === "HP Colors") {
-        config.__anitaPortableSyncReason = "update_" + String(data.setting_id);
-        this.emitPortableSync(config, "update_" + String(data.setting_id) + "_immediate");
-        this.queuePortableSyncBurst(config, "update_" + String(data.setting_id));
-        this.startPortableSyncLoop(config);
-      }
-
-      var writeToken = (config.__anitaPendingWriteToken || 0) + 1;
-      config.__anitaPendingWriteToken = writeToken;
-      $.Schedule(2.0, function () {
-        if (!config || config.__anitaPendingWriteToken !== writeToken) return;
-        AnitaPersistence.persistConfig(config, false);
-      });
-
-      if (this.hasVisibilityDependents(config, data.setting_id)) {
-        this.queueRenderRefresh(config);
-        return;
-      }
-      if (isBootstrap) {
-        this.queueRenderRefresh(config);
-        return;
-      }
-    },
-
-    handleBootstrapRequest: function (data) {
-      if (!data || !data.mod_title) return;
-      var config = this.findRegisteredMod(data.mod_title);
-      if (!config) {
-        if (data.mod_title === "HP Colors") {
+        function beginColorDrag(sourceName, emitUpdateEvent) {
+            colorDragging = !0, colorDragSource = String(sourceName || ""), syncFromCursorPanelPosition(!1) || updateBoxCursorVisual(currentColor),
+            emitUpdateEvent && ("cursor" === colorDragSource || "drag_event" === colorDragSource ? syncFromCursorPanelPosition(!0) || syncFromBestDragSource(!0) : syncFromScreenCursorPosition(!0) || syncFromLocalBoxCursor(!0) || syncFromCursorPanelPosition(!0)),
+            setMouseCaptureState(!0), function() {
+                if (!colorPopupPanel || !colorPopupPanel.IsValid || !colorPopupPanel.IsValid()) return;
+                const generation = ++colorPickerPollGeneration;
+                function tick() {
+                    if (generation === colorPickerPollGeneration && colorPopupPanel && colorPopupPanel.IsValid && colorPopupPanel.IsValid()) return colorDragging ? (syncFromBestDragSource(!0),
+                    hasGameUI && "function" == typeof GameUI.IsMouseDown && !GameUI.IsMouseDown(0) ? void endColorDrag() : void $.Schedule(.016, tick)) : void $.Schedule(.05, tick);
+                }
+                $.Schedule(.016, tick);
+            }();
         }
-        return;
-      }
-      if (data.mod_title === "HP Colors") {
-        if (!config.__anitaPortableSyncLoopStarted) {
-          config.__anitaPortableSyncReason = String(config.__anitaPortableSyncReason || "bootstrap_request");
-          this.startPortableSyncLoop(config);
+        function endColorDrag() {
+            colorDragging = !1, colorDragSource = "", setMouseCaptureState(!1);
         }
-      }
-      this.emitCurrentValues(config, {
-        update_source: "bridge_bootstrap",
-        skip_bridge_persist: true,
-        bootstrap_reason: String(data.reason || "request")
-      });
-    },
-
-    updateWindowWidth: function () {
-      if (!AnitaRenderer.mainWindow) return;
-
-      const count = this.registeredMods.length;
-      let width = null;
-
-      if (count === 1 && this.registeredMods[0].isDummy) {
-        width = 500;
-      } else if (count <= 4) {
-        width = count * 300;
-      }
-
-      if (width) {
-        AnitaRenderer.mainWindow.style.minWidth = width + "px";
-      } else {
-        AnitaRenderer.mainWindow.style.minWidth = "90%";
-      }
-    },
-
-    setupEventListener: function () {
-      try {
-        $.RegisterForUnhandledEvent("ClientUI_FireOutput", (payload) => {
-          try {
-            let data = (typeof payload === 'string') ? JSON.parse(payload) : payload;
-            if (data && data.magic_word === "ANITA_REGISTER") {
-              if (data.config && data.config.title === "HP Colors") {
-              }
-              this.registerMod(data.config);
-            } else if (data && data.magic_word === "ANITA_REQUEST_BOOTSTRAP") {
-              if (data.mod_title === "HP Colors") {
-              }
-              this.handleBootstrapRequest(data);
-            } else if (data && data.magic_word === "ANITA_UPDATE") {
-              this.handleUpdateEvent(data);
+        function closePalette() {
+            if (colorPickerPollGeneration++, endColorDrag(), hasGameUI && "function" == typeof GameUI.SetMouseCallback) try {
+                GameUI.SetMouseCallback(null);
+            } catch (e) {}
+            colorPopupPanel && colorPopupPanel.IsValid && colorPopupPanel.IsValid() && colorPopupPanel.DeleteAsync(0),
+            colorPopupPanel = null, colorBoxFrame = null, colorBoxPanel = null, colorBoxCursor = null,
+            popupPreview = null, popupHexLabel = null, popupMetaLabel = null, colorPickerSyncing = !1,
+            pickerHueSlider = null, pickerSatSlider = null, pickerSatTrack = null, pickerHueValue = null,
+            pickerSatValue = null, colorDragSource = "", AnitaRenderer.activeColorPickerClose === closePalette && (AnitaRenderer.activeColorPickerClose = null);
+        }
+        function syncColorVisuals(colorCode, emitUpdateEvent, closeAfter, boxState) {
+            currentColor = normalizeHexColor(colorCode), config.currentValue = currentColor;
+            var pickerState = setPickerBoxState(boxState, currentColor);
+            colorPreview && colorPreview.IsValid && colorPreview.IsValid() && (colorPreview.style.backgroundColor = currentColor),
+            rowHexLabel && (rowHexLabel.text = currentColor), popupPreview && popupPreview.IsValid && popupPreview.IsValid() && (popupPreview.style.backgroundColor = currentColor),
+            popupHexLabel && (popupHexLabel.text = currentColor), popupMetaLabel && (popupMetaLabel.text = "Hue " + pickerState.hue + "° | Sat " + Math.round(100 * pickerState.sat) + "%");
+            const previewBtn = row.FindChildTraverse("ColorPreviewBtn");
+            previewBtn && (previewBtn.style.backgroundColor = currentColor), updateBoxCursorVisual(currentColor),
+            colorPickerSyncing = !0;
+            try {
+                if (pickerHueSlider && pickerHueSlider.IsValid && pickerHueSlider.IsValid()) {
+                    var hueSliderPct = (hue = pickerState.hue, nextHue = Number(hue), isFinite(nextHue) || (nextHue = 0),
+                    nextHue = Math.max(0, Math.min(359, Math.round(nextHue))), Math.round(nextHue / 359 * 100));
+                    if (Math.round(Number(pickerHueSlider.value)) !== hueSliderPct) try {
+                        pickerHueSlider.value = hueSliderPct;
+                    } catch (e) {}
+                    pickerHueValue && (pickerHueValue.text = pickerState.hue + "°");
+                }
+                if (pickerSatSlider && pickerSatSlider.IsValid && pickerSatSlider.IsValid()) {
+                    var satPct = Math.round(100 * pickerState.sat);
+                    if (Math.round(Number(pickerSatSlider.value)) !== satPct) try {
+                        pickerSatSlider.value = satPct;
+                    } catch (e) {}
+                    pickerSatValue && (pickerSatValue.text = satPct + "%"), pickerSatTrack && pickerSatTrack.IsValid && pickerSatTrack.IsValid() && (pickerSatTrack.style.backgroundColor = "gradient( linear, 0% 0%, 100% 0%, from( #ffffff ), to( " + hsvToHex(pickerState.hue, 1, 1) + " ) )");
+                }
+            } finally {
+                colorPickerSyncing = !1;
             }
-          } catch (e) {
-          }
-        });
-      } catch (e) {
-      }
-    },
-
-    createOverlayButton: function (parent) {
-      const existing = parent.FindChildTraverse(CONFIG.IDS.OVERLAY_BTN);
-      if (existing) existing.DeleteAsync(0);
-
-      const btn = $.CreatePanel("Button", parent, CONFIG.IDS.OVERLAY_BTN);
-      btn.AddClass("AnitaOverlayBtn");
-      this._overlayBtn = btn;
-
-      btn.SetPanelEvent("onmouseover", () => $.DispatchEvent("UIShowTextTooltip", btn, "Anita-UI Settings"));
-      btn.SetPanelEvent("onmouseout", () => $.DispatchEvent("UIHideTextTooltip", btn));
-
-      btn.SetPanelEvent("onactivate", () => AnitaRenderer.toggle());
-    },
-
-    monitorEscapeMenu: function (root) {
-      let hudPanel = this._hudPanel;
-      let btn = this._overlayBtn;
-      let nextDelay = CONFIG.UI.MONITOR_INTERVAL;
-
-      if (!hudPanel || !hudPanel.IsValid || !hudPanel.IsValid()) {
-        hudPanel = root.FindChildTraverse(CONFIG.IDS.HUD_ROOT);
-        if (!hudPanel) {
-          let p = $.GetContextPanel();
-          while (p) {
-            if (p.id === CONFIG.IDS.HUD_ROOT) { hudPanel = p; break; }
-            p = p.GetParent();
-          }
-        }
-        this._hudPanel = hudPanel || null;
-      }
-
-      if (!btn || !btn.IsValid || !btn.IsValid()) {
-        btn = root.FindChildTraverse(CONFIG.IDS.OVERLAY_BTN);
-        this._overlayBtn = btn || null;
-      }
-
-      if (typeof this._lastEscapeState !== "boolean") this._lastEscapeState = false;
-
-      if (hudPanel && btn) {
-        const isMenuOpen = hudPanel.BHasClass(CONFIG.CLASSES.ESCAPE_MENU);
-        const stateChanged = this._lastEscapeState !== isMenuOpen;
-
-        if (stateChanged || !btn.BHasClass(CONFIG.CLASSES.VISIBLE)) {
-          btn.SetHasClass(CONFIG.CLASSES.VISIBLE, isMenuOpen);
-        }
-        if (!!btn.hittest !== isMenuOpen) {
-          btn.hittest = isMenuOpen;
+            var hue, nextHue;
+            emitUpdateEvent && (config.id && modTitle && emitUpdate(modTitle, config.id, currentColor),
+            config.onChange && config.onChange(currentColor)), closeAfter && closePalette();
+        }
+        function openPalette() {
+            if (colorPopupPanel) return void closePalette();
+            if (AnitaRenderer.activeColorPickerClose && AnitaRenderer.activeColorPickerClose !== closePalette) try {
+                AnitaRenderer.activeColorPickerClose();
+            } catch (closeErr) {}
+            var colorPopupParent = $.GetContextPanel();
+            colorPopupPanel = $.CreatePanel("Panel", colorPopupParent, ""), AnitaRenderer.activeColorPickerClose = closePalette,
+            colorPopupPanel.AddClass("AnitaColorPopup");
+            var colorPopupX = 24, colorPopupY = 24;
+            try {
+                var colorAnchorBounds = getPanelBounds(preview && preview.IsValid && preview.IsValid() ? preview : parent);
+                colorPopupX = Math.max(8, Math.round(colorAnchorBounds.left + colorAnchorBounds.width + 12)),
+                colorPopupY = Math.max(8, Math.round(colorAnchorBounds.top));
+            } catch (e) {}
+            colorPopupPanel.style.align = "left top", colorPopupPanel.style.transform = "translate3d(" + colorPopupX + "px, " + colorPopupY + "px, 0px)",
+            colorPopupPanel.style.uiScale = "100%", colorPopupPanel.style.flowChildren = "down",
+            colorPopupPanel.style.ignoreParentFlow = !0, colorPopupPanel.SetPanelEvent("oncancel", closePalette);
+            const header = $.CreatePanel("Panel", colorPopupPanel, "");
+            header.AddClass("AnitaColorPopupHeader"), popupPreview = $.CreatePanel("Panel", header, "ColorPreviewBtn"),
+            popupPreview.AddClass("AnitaColorPickerPreview"), popupPreview.AddClass("AnitaColorPopupPreview"),
+            popupPreview.style.backgroundColor = currentColor, popupHexLabel = $.CreatePanel("Label", header, ""),
+            popupHexLabel.AddClass("AnitaColorPopupHex"), popupHexLabel.text = currentColor,
+            popupMetaLabel = $.CreatePanel("Label", colorPopupPanel, ""), popupMetaLabel.AddClass("AnitaColorPopupMeta");
+            const hint = $.CreatePanel("Label", colorPopupPanel, "");
+            hint.AddClass("AnitaColorPopupHint"), hint.text = "Drag the marker or use the sliders below to change hue and saturation.";
+            const boxWrap = $.CreatePanel("Panel", colorPopupPanel, "");
+            boxWrap.AddClass("AnitaColorBoxWrap"), colorBoxFrame = $.CreatePanel("Panel", boxWrap, ""),
+            colorBoxFrame.AddClass("AnitaColorBoxFrame");
+            $.CreatePanel("Panel", colorBoxFrame, "").AddClass("AnitaColorBoxHueLayer");
+            if ($.CreatePanel("Panel", colorBoxFrame, "").AddClass("AnitaColorBoxSaturationLayer"),
+            colorBoxPanel = $.CreatePanel("Panel", colorBoxFrame, ""), colorBoxPanel.AddClass("AnitaColorBox"),
+            colorBoxPanel.hittest = !0, colorBoxPanel.hittestchildren = !0, colorBoxPanel.style.ignoreParentFlow = !0,
+            colorBoxPanel.style.width = "100%", colorBoxPanel.style.height = "100%", colorBoxPanel.SetPanelEvent("onmouseactivate", function() {
+                if (hasGameUI) beginColorDrag("box", !0); else try {
+                    var movePt = getPanelLocalCursorPosition(colorBoxPanel);
+                    movePt && syncFromBoxPosition(movePt.x, movePt.y, !0);
+                } catch (e) {}
+            }), colorBoxPanel.SetPanelEvent("onactivate", function() {
+                hasGameUI && beginColorDrag("box", !0);
+            }), colorBoxPanel.SetPanelEvent("onmousemove", function() {
+                if (colorDragging) if (hasGameUI) syncFromBestDragSource(!0); else {
+                    var movePt = getPanelLocalCursorPosition(colorBoxPanel);
+                    movePt && syncFromBoxPosition(movePt.x, movePt.y, !0);
+                }
+            }), colorBoxPanel.SetPanelEvent("onmouseup", function() {
+                endColorDrag();
+            }), colorBoxPanel.SetPanelEvent("onmouseout", function() {
+                colorDragging && !hasGameUI && endColorDrag();
+            }), colorBoxCursor = $.CreatePanel("Button", colorBoxFrame, ""), colorBoxCursor.AddClass("AnitaColorBoxCursor"),
+            colorBoxCursor.hittest = !0, colorBoxCursor.hittestchildren = !1, colorBoxCursor.style.ignoreParentFlow = !0,
+            colorBoxCursor.style.align = "left top", colorBoxCursor.style.visibility = "visible",
+            colorBoxCursor.style.opacity = "1", "function" == typeof colorBoxCursor.SetDraggable) try {
+                colorBoxCursor.SetDraggable(!0);
+            } catch (e) {}
+            if ("function" == typeof colorBoxCursor.SetDisableFocusOnMouseDown) try {
+                colorBoxCursor.SetDisableFocusOnMouseDown(!0);
+            } catch (e) {}
+            if (colorBoxCursor.SetPanelEvent("onmouseactivate", function() {
+                beginColorDrag("cursor", !0);
+            }), colorBoxCursor.SetPanelEvent("onactivate", function() {
+                beginColorDrag("cursor", !0);
+            }), colorBoxCursor.SetPanelEvent("onmousemove", function() {
+                colorDragging && syncFromBestDragSource(!0);
+            }), colorBoxCursor.SetPanelEvent("onmouseup", function() {
+                endColorDrag();
+            }), colorBoxCursor.SetPanelEvent("onmouseout", function() {
+                colorDragging && !hasGameUI && endColorDrag();
+            }), "function" == typeof $.RegisterEventHandler) {
+                try {
+                    $.RegisterEventHandler("DragStart", colorBoxCursor, function(panel, dragEvent) {
+                        panel && panel === colorBoxCursor && (dragEvent && (dragEvent.displayPanel = colorBoxCursor,
+                        dragEvent.removePositionBeforeDrop = !1), colorBoxCursor.style.align = "left top",
+                        beginColorDrag("drag_event", !1));
+                    });
+                } catch (dragStartError) {}
+                try {
+                    $.RegisterEventHandler("DragEnd", colorBoxCursor, function(_panel, droppedPanel) {
+                        droppedPanel && droppedPanel.IsValid && droppedPanel.IsValid() && droppedPanel === colorBoxCursor && (colorBoxFrame && colorBoxFrame.IsValid && colorBoxFrame.IsValid() && droppedPanel.GetParent && droppedPanel.GetParent() !== colorBoxFrame && droppedPanel.SetParent(colorBoxFrame),
+                        droppedPanel.style.align = "left top", function(emitUpdateEvent) {
+                            var metrics = getColorBoxMetrics();
+                            if (!metrics) return !1;
+                            if (!isFinite(colorDragAnchorX) || !isFinite(colorDragAnchorY) || colorDragAnchorX < 0 || colorDragAnchorY < 0) return !1;
+                            var relX = metrics.maxCursorX > 0 ? clamp01(colorDragAnchorX / metrics.maxCursorX) : 0, relY = metrics.maxCursorY > 0 ? clamp01(colorDragAnchorY / metrics.maxCursorY) : 0, hue = hueFromRelX(relX), sat = clamp01(relY);
+                            return applyColorBoxCursorPosition(colorDragAnchorX, colorDragAnchorY), syncColorVisuals(colorFromBoxState(hue, sat), emitUpdateEvent, !1, {
+                                hue: hue,
+                                sat: sat
+                            }), !0;
+                        }(!0) || (syncFromCursorPanelPosition(!0), updateBoxCursorVisual(currentColor))),
+                        endColorDrag();
+                    });
+                } catch (dragEndError) {}
+            }
+            if (hasGameUI && "function" == typeof GameUI.SetMouseCallback) try {
+                GameUI.SetMouseCallback(function(eventName, arg) {
+                    if (!colorPopupPanel || !colorPopupPanel.IsValid || !colorPopupPanel.IsValid()) return !1;
+                    if ("pressed" === eventName && 0 === arg) {
+                        const cursor = getCursorPosition();
+                        if (!cursor) return !1;
+                        const metrics = getColorBoxMetrics();
+                        if (!metrics) return !1;
+                        const bounds = metrics.bounds;
+                        if (cursor.x >= bounds.left && cursor.x <= bounds.left + bounds.width && cursor.y >= bounds.top && cursor.y <= bounds.top + bounds.height) return syncFromScreenCursorPosition(emitUpdateEvent = !0) || syncFromLocalBoxCursor(emitUpdateEvent) || syncFromCursorPanelPosition(emitUpdateEvent),
+                        beginColorDrag("gameui", !1), !0;
+                    }
+                    var emitUpdateEvent;
+                    return !("released" !== eventName || 0 !== arg || !colorDragging) && (endColorDrag(),
+                    !0);
+                });
+            } catch (e) {}
+            var hueGroup = $.CreatePanel("Panel", colorPopupPanel, "");
+            hueGroup.AddClass("AnitaHueSliderGroup");
+            var hueLbl = $.CreatePanel("Label", hueGroup, "");
+            hueLbl.text = "H", hueLbl.AddClass("AnitaHueValue"), hueLbl.AddClass("AnitaPickerAxisLabel");
+            var hueContainer = $.CreatePanel("Panel", hueGroup, "");
+            if (hueContainer.AddClass("AnitaHueSliderContainer"), hueContainer.AddClass("AnitaPickerSliderTrack"),
+            pickerHueSlider = $.CreatePanel("Slider", hueContainer, "", {
+                direction: "horizontal"
+            }), pickerHueSlider.AddClass("AnitaHueSlider"), pickerHueSlider.AddClass("HorizontalSlider"),
+            pickerHueSlider.min = 0, pickerHueSlider.max = 100, pickerHueSlider.increment = 1,
+            "function" == typeof pickerHueSlider.SetShowDefaultValue) try {
+                pickerHueSlider.SetShowDefaultValue(!1);
+            } catch (e) {}
+            if ("function" == typeof pickerHueSlider.SetRequiresSelection) try {
+                pickerHueSlider.SetRequiresSelection(!1);
+            } catch (e) {}
+            pickerHueValue = $.CreatePanel("Label", hueGroup, ""), pickerHueValue.AddClass("AnitaHueValue"),
+            pickerHueValue.AddClass("AnitaPickerReadout"), pickerHueSlider.SetPanelEvent("onvaluechanged", function() {
+                if (!colorPickerSyncing) {
+                    var h = hueFromRelX(clamp01(Number(pickerHueSlider.value) / 100)), newColor = colorFromBoxState(h, pickerBoxSat);
+                    pickerHueValue && (pickerHueValue.text = h + "°"), pickerSatTrack && pickerSatTrack.IsValid && pickerSatTrack.IsValid() && (pickerSatTrack.style.backgroundColor = "gradient( linear, 0% 0%, 100% 0%, from( #ffffff ), to( " + hsvToHex(h, 1, 1) + " ) )"),
+                    syncColorVisuals(newColor, !0, !1, {
+                        hue: h,
+                        sat: pickerBoxSat
+                    });
+                }
+            });
+            var satGroup = $.CreatePanel("Panel", colorPopupPanel, "");
+            satGroup.AddClass("AnitaHueSliderGroup");
+            var satLbl = $.CreatePanel("Label", satGroup, "");
+            if (satLbl.text = "S", satLbl.AddClass("AnitaHueValue"), satLbl.AddClass("AnitaPickerAxisLabel"),
+            pickerSatTrack = $.CreatePanel("Panel", satGroup, ""), pickerSatTrack.AddClass("AnitaSatSliderContainer"),
+            pickerSatTrack.AddClass("AnitaPickerSliderTrack"), pickerSatSlider = $.CreatePanel("Slider", pickerSatTrack, "", {
+                direction: "horizontal"
+            }), pickerSatSlider.AddClass("AnitaHueSlider"), pickerSatSlider.AddClass("HorizontalSlider"),
+            pickerSatSlider.min = 0, pickerSatSlider.max = 100, pickerSatSlider.increment = 1,
+            "function" == typeof pickerSatSlider.SetShowDefaultValue) try {
+                pickerSatSlider.SetShowDefaultValue(!1);
+            } catch (e) {}
+            if ("function" == typeof pickerSatSlider.SetRequiresSelection) try {
+                pickerSatSlider.SetRequiresSelection(!1);
+            } catch (e) {}
+            pickerSatValue = $.CreatePanel("Label", satGroup, ""), pickerSatValue.AddClass("AnitaHueValue"),
+            pickerSatValue.AddClass("AnitaPickerReadout"), pickerSatSlider.SetPanelEvent("onvaluechanged", function() {
+                if (!colorPickerSyncing) {
+                    var s = clamp01(pickerSatSlider.value / 100), newColor2 = colorFromBoxState(pickerBoxHue, s);
+                    pickerSatValue && (pickerSatValue.text = Math.round(100 * s) + "%"), syncColorVisuals(newColor2, !0, !1, {
+                        hue: pickerBoxHue,
+                        sat: s
+                    });
+                }
+            });
+            const footer = $.CreatePanel("Panel", colorPopupPanel, "");
+            footer.AddClass("AnitaColorPopupFooter");
+            const closeBtn = $.CreatePanel("Button", footer, "");
+            closeBtn.AddClass("AnitaColorPopupBtn");
+            $.CreatePanel("Label", closeBtn, "").text = "Close", closeBtn.SetPanelEvent("onactivate", closePalette),
+            syncColorVisuals(currentColor, !1, !1), $.Schedule(0, function() {
+                colorPopupPanel && colorPopupPanel.IsValid && colorPopupPanel.IsValid() && syncColorVisuals(currentColor, !1, !1);
+            });
+            getBoxStateFromColor(currentColor);
+        }
+        const preview = $.CreatePanel("Panel", row, "ColorPreviewBtn");
+        return preview.AddClass("AnitaColorPickerPreview"), preview.style.backgroundColor = currentColor,
+        preview.style.marginRight = "6px", preview.SetPanelEvent("onactivate", () => openPalette()),
+        colorPreview = preview, rowHexLabel = $.CreatePanel("Label", row, ""), rowHexLabel.AddClass("AnitaColorHexValue"),
+        rowHexLabel.text = currentColor, row;
+    }, AnitaComponents_createPositionPicker = function(parent, config, modTitle) {
+        const row = $.CreatePanel("Panel", parent, "");
+        row.AddClass("AnitaRow"), row.AddClass("AnitaSliderRow"), row.style.overflow = "noclip",
+        row.style.width = "100%";
+        const lbl = $.CreatePanel("Label", row, "");
+        function posPickerClamp(value) {
+            var next = Number(value);
+            return isFinite(next) || (next = 0), next < 0 && (next = 0), next > 400 && (next = 400),
+            Math.round(next);
+        }
+        function posPickerNormalize(candidate) {
+            var parsed = function(candidate) {
+                var x = 0, y = 200, raw = candidate;
+                if (raw && "object" == typeof raw) return Array.isArray(raw) ? (raw.length > 0 && (x = posPickerClamp(raw[0])),
+                raw.length > 1 && (y = posPickerClamp(raw[1]))) : (Object.prototype.hasOwnProperty.call(raw, "x") && (x = posPickerClamp(raw.x)),
+                Object.prototype.hasOwnProperty.call(raw, "y") && (y = posPickerClamp(raw.y))),
+                {
+                    x: x,
+                    y: y
+                };
+                if ("string" == typeof raw) {
+                    var parts = raw.match(/-?\d+(?:\.\d+)?/g);
+                    if (parts && parts.length > 0) return x = posPickerClamp(parts[0]), parts.length > 1 && (y = posPickerClamp(parts[1])),
+                    {
+                        x: x,
+                        y: y
+                    };
+                }
+                return "number" == typeof raw ? {
+                    x: x,
+                    y: y = posPickerClamp(raw)
+                } : {
+                    x: x,
+                    y: y
+                };
+            }(candidate);
+            return {
+                x: posPickerClamp(parsed.x),
+                y: posPickerClamp(parsed.y)
+            };
         }
-
-        if (stateChanged) {
-          if (isMenuOpen) {
-            const attentionToken = (this._attentionToken || 0) + 1;
-            this._attentionToken = attentionToken;
-            btn.AddClass(CONFIG.CLASSES.ATTENTION);
-            $.Schedule(4.0, () => {
-              if (this._attentionToken !== attentionToken) return;
-              if (btn && btn.IsValid && btn.IsValid()) {
-                btn.RemoveClass(CONFIG.CLASSES.ATTENTION);
-              }
+        lbl.text = config.label || "Position", lbl.AddClass("AnitaLabel");
+        let posPickerCurrent = posPickerNormalize(void 0 !== config.currentValue ? config.currentValue : config.defaultValue || "0,200"), posPickerSyncing = !1;
+        const posPickerValueGroup = $.CreatePanel("Panel", row, "");
+        posPickerValueGroup.AddClass("AnitaSliderValueGroup"), posPickerValueGroup.AddClass("SliderValueGroup"),
+        posPickerValueGroup.AddClass("AnitaPositionPickerGroup"), posPickerValueGroup.style.flowChildren = "down",
+        posPickerValueGroup.style.verticalAlign = "center", posPickerValueGroup.style.horizontalAlign = "left",
+        posPickerValueGroup.style.width = "332px", posPickerValueGroup.style.overflow = "noclip";
+        const posPickerXGroup = $.CreatePanel("Panel", posPickerValueGroup, "");
+        posPickerXGroup.AddClass("AnitaHueSliderGroup"), posPickerXGroup.AddClass("AnitaPositionSliderRow"),
+        posPickerXGroup.style.overflow = "noclip";
+        const posPickerXLbl = $.CreatePanel("Label", posPickerXGroup, "");
+        posPickerXLbl.text = "L/R", posPickerXLbl.AddClass("AnitaHueValue"), posPickerXLbl.AddClass("AnitaPositionAxisLabel");
+        const posPickerXContainer = $.CreatePanel("Panel", posPickerXGroup, "");
+        posPickerXContainer.AddClass("AnitaSliderContainer"), posPickerXContainer.AddClass("SliderContainer"),
+        posPickerXContainer.AddClass("AnitaPositionSliderContainer"), posPickerXContainer.style.width = "230px",
+        posPickerXContainer.style.height = "26px", posPickerXContainer.style.padding = "0px",
+        posPickerXContainer.style.verticalAlign = "center", posPickerXContainer.style.overflow = "noclip";
+        const posPickerXSlider = $.CreatePanel("Slider", posPickerXContainer, "", {
+            direction: "horizontal"
+        });
+        if (posPickerXSlider.AddClass("AnitaSlider"), posPickerXSlider.AddClass("HorizontalSlider"),
+        posPickerXSlider.style.width = "100%", posPickerXSlider.style.height = "100%", posPickerXSlider.style.verticalAlign = "center",
+        posPickerXSlider.style.overflow = "noclip", posPickerXSlider.min = 0, posPickerXSlider.max = 400,
+        posPickerXSlider.increment = 1, "function" == typeof posPickerXSlider.SetShowDefaultValue) try {
+            posPickerXSlider.SetShowDefaultValue(!1);
+        } catch (e) {}
+        if ("function" == typeof posPickerXSlider.SetRequiresSelection) try {
+            posPickerXSlider.SetRequiresSelection(!1);
+        } catch (e) {}
+        const posPickerXValueLbl = $.CreatePanel("Label", posPickerXGroup, "");
+        posPickerXValueLbl.AddClass("AnitaSliderValue"), posPickerXValueLbl.AddClass("AnitaPositionReadout"),
+        posPickerXValueLbl.style.textOverflow = "clip", posPickerXValueLbl.style.overflow = "noclip";
+        const posPickerYGroup = $.CreatePanel("Panel", posPickerValueGroup, "");
+        posPickerYGroup.AddClass("AnitaHueSliderGroup"), posPickerYGroup.AddClass("AnitaPositionSliderRow"),
+        posPickerYGroup.style.overflow = "noclip";
+        const posPickerYLbl = $.CreatePanel("Label", posPickerYGroup, "");
+        posPickerYLbl.text = "T/B", posPickerYLbl.AddClass("AnitaHueValue"), posPickerYLbl.AddClass("AnitaPositionAxisLabel");
+        const posPickerYContainer = $.CreatePanel("Panel", posPickerYGroup, "");
+        posPickerYContainer.AddClass("AnitaSliderContainer"), posPickerYContainer.AddClass("SliderContainer"),
+        posPickerYContainer.AddClass("AnitaPositionSliderContainer"), posPickerYContainer.style.width = "230px",
+        posPickerYContainer.style.height = "26px", posPickerYContainer.style.padding = "0px",
+        posPickerYContainer.style.verticalAlign = "center", posPickerYContainer.style.overflow = "noclip";
+        const posPickerYSlider = $.CreatePanel("Slider", posPickerYContainer, "", {
+            direction: "horizontal"
+        });
+        if (posPickerYSlider.AddClass("AnitaSlider"), posPickerYSlider.AddClass("HorizontalSlider"),
+        posPickerYSlider.style.width = "100%", posPickerYSlider.style.height = "100%", posPickerYSlider.style.verticalAlign = "center",
+        posPickerYSlider.style.overflow = "noclip", posPickerYSlider.min = 0, posPickerYSlider.max = 400,
+        posPickerYSlider.increment = 1, "function" == typeof posPickerYSlider.SetShowDefaultValue) try {
+            posPickerYSlider.SetShowDefaultValue(!1);
+        } catch (e) {}
+        if ("function" == typeof posPickerYSlider.SetRequiresSelection) try {
+            posPickerYSlider.SetRequiresSelection(!1);
+        } catch (e) {}
+        const posPickerYValueLbl = $.CreatePanel("Label", posPickerYGroup, "");
+        function syncPosition(nextPos, emitUpdateEvent) {
+            var parsed, normalized = posPickerNormalize(nextPos), nextValue = (parsed = posPickerNormalize(normalized),
+            Math.round(parsed.x) + "," + Math.round(parsed.y)), changed = String(config.currentValue || "") !== nextValue;
+            if (posPickerCurrent = normalized, config.currentValue = nextValue, posPickerXSlider && posPickerXSlider.IsValid && posPickerXSlider.IsValid() && Number(posPickerXSlider.value) !== normalized.x) {
+                posPickerSyncing = !0;
+                try {
+                    "function" == typeof posPickerXSlider.SetValueNoEvents ? posPickerXSlider.SetValueNoEvents(normalized.x) : posPickerXSlider.value = normalized.x;
+                } finally {
+                    posPickerSyncing = !1;
+                }
+            }
+            if (posPickerYSlider && posPickerYSlider.IsValid && posPickerYSlider.IsValid() && Number(posPickerYSlider.value) !== normalized.y) {
+                posPickerSyncing = !0;
+                try {
+                    "function" == typeof posPickerYSlider.SetValueNoEvents ? posPickerYSlider.SetValueNoEvents(normalized.y) : posPickerYSlider.value = normalized.y;
+                } finally {
+                    posPickerSyncing = !1;
+                }
+            }
+            posPickerXValueLbl.text = function(pos) {
+                var parsed = posPickerNormalize(pos);
+                return Math.round(parsed.x / 4);
+            }(normalized) + "%", posPickerYValueLbl.text = Math.round(normalized.y / 4) + "%",
+            emitUpdateEvent && changed && (config.onChange && config.onChange(nextValue), config.id && modTitle && emitUpdate(modTitle, config.id, nextValue));
+        }
+        return posPickerYValueLbl.AddClass("AnitaSliderValue"), posPickerYValueLbl.AddClass("AnitaPositionReadout"),
+        posPickerYValueLbl.style.textOverflow = "clip", posPickerYValueLbl.style.overflow = "noclip",
+        syncPosition(posPickerCurrent, !1), posPickerXSlider.SetPanelEvent("onvaluechanged", function() {
+            posPickerSyncing || syncPosition({
+                x: posPickerXSlider.value,
+                y: posPickerCurrent.y
+            }, !0);
+        }), posPickerYSlider.SetPanelEvent("onvaluechanged", function() {
+            posPickerSyncing || syncPosition({
+                x: posPickerCurrent.x,
+                y: posPickerYSlider.value
+            }, !0);
+        }), posPickerXSlider.SetPanelEvent("oncancel", () => {
+            AnitaRenderer.toggle(!1);
+        }), posPickerYSlider.SetPanelEvent("oncancel", () => {
+            AnitaRenderer.toggle(!1);
+        }), row;
+    }, AnitaRenderer = {
+        mainWindow: null,
+        backdrop: null,
+        navBar: null,
+        menuArea: null,
+        contentArea: null,
+        activeModTitle: "",
+        isOpen: !1,
+        activeColorPickerClose: null,
+        findElementById: function(config, elementId) {
+            if (!config || !Array.isArray(config.elements) || !elementId) return null;
+            for (var i = 0; i < config.elements.length; i++) {
+                var element = config.elements[i];
+                if (element && element.id === elementId) return element;
+            }
+            return null;
+        },
+        isElementVisible: function(config, element) {
+            if (!element || !element.visibleWhen) return !0;
+            var rule = element.visibleWhen, source = this.findElementById(config, rule.id);
+            if (!source) return !0;
+            var current = source.currentValue;
+            if (Array.isArray(rule.equals)) {
+                for (var i = 0; i < rule.equals.length; i++) if (current === rule.equals[i]) return !0;
+                return !1;
+            }
+            return Object.prototype.hasOwnProperty.call(rule, "equals") ? current === rule.equals : !!current;
+        },
+        applyElementVisibility: function(config, element) {
+            if (element && element.__anitaRowPanel && element.__anitaRowPanel.IsValid && element.__anitaRowPanel.IsValid()) {
+                var visible = this.isElementVisible(config, element);
+                element.__anitaRowPanel.style.visibility = visible ? "visible" : "collapse", element.__anitaRowPanel.hittest = visible;
+            }
+        },
+        refreshConditionalVisibility: function(config) {
+            if (config && Array.isArray(config.elements)) for (var i = 0; i < config.elements.length; i++) this.applyElementVisibility(config, config.elements[i]);
+        },
+        hasVisibilityDependents: function(config, sourceId) {
+            if (!config || !Array.isArray(config.elements) || !sourceId) return !1;
+            for (var i = 0; i < config.elements.length; i++) {
+                var element = config.elements[i];
+                if (element && element.visibleWhen && element.visibleWhen.id === sourceId) return !0;
+            }
+            return !1;
+        },
+        getElementCategory: function(element) {
+            return String(element && element.category || "General").trim() || "General";
+        },
+        getCategoryList: function(config) {
+            var categories = [], seen = {};
+            if (!config || !Array.isArray(config.elements)) return categories;
+            for (var i = 0; i < config.elements.length; i++) {
+                var category = this.getElementCategory(config.elements[i]);
+                seen[category] || (seen[category] = !0, categories.push(category));
+            }
+            return categories;
+        },
+        getCategoryElements: function(config, category) {
+            var elements = [];
+            if (!config || !Array.isArray(config.elements)) return elements;
+            for (var i = 0; i < config.elements.length; i++) {
+                var element = config.elements[i];
+                this.getElementCategory(element) === category && elements.push(element);
+            }
+            return elements;
+        },
+        ensureActiveCategory: function(config) {
+            var categories = this.getCategoryList(config);
+            if (0 === categories.length) return config.__anitaActiveCategory = "", "";
+            for (var active = String(config.__anitaActiveCategory || ""), i = 0; i < categories.length; i++) if (categories[i] === active) return active;
+            return config.__anitaActiveCategory = categories[0], categories[0];
+        },
+        getSaveCodeToken: function(config) {
+            if (!config || !config.storageNamespace) return "";
+            var raw = AnitaPersistence.buildStoredPayload(config);
+            if (!raw) return "";
+            var ns = AnitaPersistence.normalizeNamespace(config.storageNamespace);
+            return ns ? "[" + AnitaPersistence.TOKEN_PREFIX + ns + "]:" + AnitaBase64.encode(raw) : "";
+        },
+        extractSaveCodeToken: function(config, text) {
+            if (!config || !config.storageNamespace) return "";
+            var body = String(text || "").trim();
+            if (!body) return "";
+            var ns = AnitaPersistence.normalizeNamespace(config.storageNamespace);
+            if (!ns) return "";
+            var scopedMatch = body.match(AnitaPersistence.getTokenRegex(ns));
+            if (scopedMatch && scopedMatch[0]) return scopedMatch[0];
+            var genericMatch = body.match(/\[ANITA-v1-[a-z0-9_]+\]:[A-Za-z0-9_-]+/i);
+            return genericMatch && genericMatch[0] ? genericMatch[0] : /^[A-Za-z0-9_-]+$/.test(body) ? "[" + AnitaPersistence.TOKEN_PREFIX + ns + "]:" + body : "";
+        },
+        syncSaveCodeInput: function(config) {
+            if (config && config.__anitaSaveCodeInput && config.__anitaSaveCodeInput.IsValid && config.__anitaSaveCodeInput.IsValid()) {
+                var token = this.getSaveCodeToken(config);
+                String(config.__anitaSaveCodeInput.text || "") !== token && (config.__anitaSaveCodeInput.text = token);
+            }
+        },
+        initWindow: function(root) {
+            root.FindChildTraverse(CONFIG_IDS.WINDOW) && root.FindChildTraverse(CONFIG_IDS.WINDOW).DeleteAsync(0),
+            root.FindChildTraverse(CONFIG_IDS.BACKDROP) && root.FindChildTraverse(CONFIG_IDS.BACKDROP).DeleteAsync(0),
+            this.backdrop = $.CreatePanel("Panel", root, CONFIG_IDS.BACKDROP), this.backdrop.AddClass("AnitaBackdrop"),
+            this.backdrop.SetPanelEvent("onactivate", () => this.toggle(!1)), this.mainWindow = $.CreatePanel("Panel", root, CONFIG_IDS.WINDOW),
+            this.mainWindow.AddClass("AnitaWindow"), this.mainWindow.canfocus = !0, this.mainWindow.SetPanelEvent("oncancel", () => this.toggle(!1)),
+            this.mainWindow.SetPanelEvent("onactivate", () => {
+                this.mainWindow.SetFocus();
+            }), this.navBar = $.CreatePanel("Panel", this.mainWindow, CONFIG_IDS.NAVBAR), this.navBar.AddClass("AnitaNavBar");
+            const closeBtn = $.CreatePanel("Button", this.navBar, "");
+            closeBtn.AddClass("AnitaCloseBtn"), closeBtn.SetPanelEvent("onactivate", () => this.toggle(!1));
+            const sep = $.CreatePanel("Label", this.navBar, "");
+            sep.text = "/", sep.AddClass("AnitaTabSeparator"), this.menuArea = $.CreatePanel("Panel", this.navBar, "AnitaTabContainer"),
+            this.menuArea.AddClass("AnitaTabContainer"), this.contentArea = $.CreatePanel("Panel", this.mainWindow, CONFIG_IDS.CONTENT),
+            this.contentArea.AddClass("AnitaContentArea");
+        },
+        toggle: function(forceState) {
+            if (this.mainWindow && this.backdrop) if (this.isOpen = void 0 !== forceState ? forceState : !this.isOpen,
+            this.mainWindow.SetHasClass(CONFIG_CLASSES.OPEN, this.isOpen), this.mainWindow.hittest = this.isOpen,
+            this.backdrop.SetHasClass(CONFIG_CLASSES.OPEN, this.isOpen), this.backdrop.hittest = this.isOpen,
+            this.isOpen) this.mainWindow.SetFocus(); else {
+                if (this.activeColorPickerClose) {
+                    try {
+                        this.activeColorPickerClose();
+                    } catch (closeErr) {}
+                    this.activeColorPickerClose = null;
+                }
+                $.DispatchEvent("DropInputFocus", this.mainWindow);
+                let root = $.GetContextPanel();
+                for (;root.GetParent(); ) root = root.GetParent();
+                root.SetFocus();
+            }
+        },
+        addTab: function(modTitle, onClick) {
+            let displayTitle = modTitle;
+            const MAX_CHARS = CONFIG_UI.TAB_MAX_CHARS;
+            displayTitle.length > MAX_CHARS && (displayTitle = displayTitle.substring(0, MAX_CHARS) + "...");
+            const btn = $.CreatePanel("Button", this.menuArea, "");
+            btn.AddClass("AnitaTabBtn");
+            $.CreatePanel("Label", btn, "").text = displayTitle;
+            const sep = $.CreatePanel("Label", this.menuArea, "");
+            sep.text = "/", sep.AddClass("AnitaTabSeparator"), btn.SetPanelEvent("onactivate", () => {
+                this.menuArea.Children().forEach(c => {
+                    "Button" !== c.paneltype || c.BHasClass("AnitaCloseBtn") || c.RemoveClass("Active");
+                }), btn.AddClass("Active"), this.activeModTitle = modTitle, onClick();
+            }), this.menuArea.GetChildCount() <= 4 && (btn.AddClass("Active"), this.activeModTitle = modTitle,
+            onClick());
+        },
+        renderModSettings: function(config) {
+            this.contentArea.RemoveAndDeleteChildren(), this.contentArea.canfocus = !0, this.contentArea.SetPanelEvent("onactivate", () => this.contentArea.SetFocus());
+            const container = $.CreatePanel("Panel", this.contentArea, "");
+            container.AddClass("ModContainer"), container.canfocus = !0;
+            const bgShield = $.CreatePanel("Panel", container, "BackgroundShield");
+            bgShield.style.width = "100%", bgShield.style.height = "100%", bgShield.style.ignoreParentFlow = "true",
+            bgShield.style.zIndex = "-1", bgShield.hittest = !0, bgShield.SetPanelEvent("onactivate", () => {
+                container.SetFocus();
             });
-          } else {
-            btn.RemoveClass(CONFIG.CLASSES.ATTENTION);
-          }
+            const title = $.CreatePanel("Label", container, "");
+            title.text = config.title, title.AddClass("SectionHeader");
+            if ($.CreatePanel("Panel", container, "").AddClass("SectionHeaderLine"), config.description) {
+                const desc = $.CreatePanel("Label", container, "");
+                desc.text = config.description, desc.AddClass("ModDescription");
+            }
+            const shell = $.CreatePanel("Panel", container, "");
+            shell.AddClass("AnitaSettingsShell");
+            const treePanel = $.CreatePanel("Panel", shell, "");
+            treePanel.AddClass("AnitaTreePanel");
+            const treeHeader = $.CreatePanel("Label", treePanel, "");
+            treeHeader.text = "Settings", treeHeader.AddClass("AnitaTreeHeader");
+            const treeList = $.CreatePanel("Panel", treePanel, "");
+            treeList.AddClass("AnitaTreeList");
+            const detailPanel = $.CreatePanel("Panel", shell, "");
+            detailPanel.AddClass("AnitaDetailPanel");
+            const activeCategory = this.ensureActiveCategory(config), categories = this.getCategoryList(config);
+            for (var c = 0; c < categories.length; c++) {
+                var category = categories[c], categoryBtn = $.CreatePanel("Button", treeList, "");
+                categoryBtn.AddClass("AnitaTreeNode"), categoryBtn.SetHasClass("Active", category === activeCategory);
+                var categoryGlyph = $.CreatePanel("Label", categoryBtn, "");
+                categoryGlyph.AddClass("AnitaTreeNodeGlyph"), categoryGlyph.text = category === activeCategory ? "v" : ">";
+                var categoryLabel = $.CreatePanel("Label", categoryBtn, "");
+                categoryLabel.AddClass("AnitaTreeNodeLabel"), categoryLabel.text = category;
+                var categoryCount = $.CreatePanel("Label", categoryBtn, "");
+                categoryCount.AddClass("AnitaTreeNodeCount"), categoryCount.text = String(this.getCategoryElements(config, category).length),
+                categoryBtn.SetPanelEvent("onactivate", function(nextCategory) {
+                    return function() {
+                        config.__anitaActiveCategory = nextCategory, AnitaRenderer.renderModSettings(config);
+                    };
+                }(category));
+            }
+            const detailHeaderRow = $.CreatePanel("Panel", detailPanel, "");
+            detailHeaderRow.AddClass("AnitaDetailHeaderRow");
+            const detailHeader = $.CreatePanel("Label", detailHeaderRow, "");
+            detailHeader.text = activeCategory || config.title, detailHeader.AddClass("AnitaDetailHeader");
+            const detailHint = $.CreatePanel("Label", detailPanel, "");
+            detailHint.text = "Select a setting group from the tree on the left.", detailHint.AddClass("AnitaDetailHint");
+            const settingsList = $.CreatePanel("Panel", detailPanel, "");
+            if (settingsList.AddClass("AnitaSettingsList"), config.elements && (config.elements.forEach(el => {
+                el.__anitaRowPanel = null;
+            }), this.getCategoryElements(config, activeCategory).forEach(el => {
+                var row = null;
+                switch (el.type) {
+                  case "toggle":
+                    row = AnitaComponents_createToggle(settingsList, el, config.title);
+                    break;
+
+                  case "stepper":
+                    row = AnitaComponents_createStepper(settingsList, el, config.title);
+                    break;
+
+                  case "slider":
+                    row = AnitaComponents_createSlider(settingsList, el, config.title);
+                    break;
+
+                  case "button":
+                    row = AnitaComponents_createButton(settingsList, el, config.title);
+                    break;
+
+                  case "cycler":
+                    row = AnitaComponents_createCycler(settingsList, el, config.title);
+                    break;
+
+                  case "positionpicker":
+                    row = AnitaComponents_createPositionPicker(settingsList, el, config.title);
+                    break;
+
+                  case "colorpicker":
+                    row = AnitaComponents_createColorPicker(settingsList, el, config.title);
+                }
+                el.__anitaRowPanel = row || null;
+            }), this.refreshConditionalVisibility(config)), config.storageNamespace) {
+                var footerWrap = $.CreatePanel("Panel", detailPanel, "");
+                footerWrap.AddClass("AnitaFooterWrap");
+                var footer = $.CreatePanel("Panel", footerWrap, "");
+                function makeFooterBtn(parent, label, id) {
+                    var btn = $.CreatePanel("Button", parent, id || "");
+                    btn.AddClass("AnitaFooterBtn");
+                    var lbl = $.CreatePanel("Label", btn, "");
+                    return lbl.text = label, {
+                        btn: btn,
+                        lbl: lbl
+                    };
+                }
+                function flashLabel(btn, lbl, msg, durationSec) {
+                    if (btn && btn.IsValid && btn.IsValid() && lbl && lbl.IsValid && lbl.IsValid()) {
+                        var orig = lbl.text;
+                        lbl.text = msg, btn.AddClass("AnitaFooterBtnSuccess"), $.Schedule(durationSec, function() {
+                            lbl && lbl.IsValid() && (lbl.text = orig), btn && btn.IsValid() && btn.RemoveClass("AnitaFooterBtnSuccess");
+                        });
+                    }
+                }
+                footer.AddClass("AnitaFooterRow");
+                var copyB = makeFooterBtn(footer, "Copy", "");
+                copyB.btn.SetPanelEvent("onactivate", function() {
+                    AnitaRenderer.syncSaveCodeInput(config);
+                    var token = AnitaRenderer.getSaveCodeToken(config);
+                    if (token) try {
+                        $.DispatchEvent("CopyStringToClipboard", token, $.GetContextPanel()), flashLabel(copyB.btn, copyB.lbl, "Copied!", 1.5);
+                    } catch (e) {
+                        flashLabel(copyB.btn, copyB.lbl, "Failed", 1.5);
+                    } else flashLabel(copyB.btn, copyB.lbl, "Empty", 1.5);
+                });
+                var resetB = makeFooterBtn(footer, "Reset", "");
+                resetB.btn.SetPanelEvent("onactivate", function() {
+                    AnitaPersistence.applyResolvedValues(config, {}), AnitaPersistence.persistConfig(config, !0),
+                    AnitaRenderer.syncSaveCodeInput(config), flashLabel(resetB.btn, resetB.lbl, "Reset", 1.5),
+                    AnitaRenderer.renderModSettings(config), AnitaCore.emitCurrentValues(config, {
+                        update_source: "ui_reset",
+                        force_persist: !0
+                    });
+                });
+                var importRowOpen = !1, importToggleBtn = makeFooterBtn(footer, "Import", ""), importCodeRow = $.CreatePanel("Panel", footerWrap, "");
+                importCodeRow.AddClass("AnitaPasteRow"), importCodeRow.AddClass("AnitaPasteRowCompact"),
+                importCodeRow.hittest = !0, importCodeRow.style.visibility = "collapse", importCodeRow.style.height = "0px",
+                importCodeRow.style.marginTop = "0px";
+                var importCodeLabel = $.CreatePanel("Label", importCodeRow, "");
+                importCodeLabel.AddClass("AnitaLabel"), importCodeLabel.text = "Import Code", importCodeLabel.style.width = "84px",
+                importCodeLabel.style.fontSize = "12px", importCodeLabel.style.marginRight = "6px";
+                var importCodeInput = $.CreatePanel("TextEntry", importCodeRow, "");
+                importCodeInput.AddClass("AnitaPasteInput"), importCodeInput.placeholder = "Paste a code to apply...",
+                importCodeInput.style.width = "320px", importCodeInput.style.height = "24px", importCodeInput.style.fontSize = "9px",
+                config.__anitaImportCodeInput = importCodeInput;
+                var applyCodeB = makeFooterBtn(importCodeRow, "Apply", "");
+                function setImportRowVisible(visible) {
+                    importRowOpen = !!visible, importCodeRow && importCodeRow.IsValid && importCodeRow.IsValid() && (importCodeRow.style.visibility = importRowOpen ? "visible" : "collapse",
+                    importCodeRow.style.height = importRowOpen ? "30px" : "0px", importCodeRow.style.marginTop = importRowOpen ? "6px" : "0px"),
+                    importToggleBtn && importToggleBtn.lbl && (importToggleBtn.lbl.text = importRowOpen ? "Hide" : "Import");
+                }
+                function applySaveCodeInput() {
+                    var text = String(importCodeInput.text || "").trim();
+                    if (text) {
+                        var token = AnitaRenderer.extractSaveCodeToken(config, text);
+                        if (token) {
+                            var encoded = token.split("]:")[1] || "";
+                            try {
+                                var raw = AnitaBase64.decode(encoded), parsed = AnitaPersistence.parseStoredPayload(config, raw, "code");
+                                if (!parsed) return void flashLabel(applyCodeB.btn, applyCodeB.lbl, "Invalid", 1.5);
+                                if (!parsed.values || !Object.keys(parsed.values).length) return void flashLabel(applyCodeB.btn, applyCodeB.lbl, "No IDs", 1.5);
+                                AnitaPersistence.applyResolvedValues(config, parsed.values), AnitaPersistence.persistConfig(config, !0),
+                                AnitaRenderer.syncSaveCodeInput(config), flashLabel(applyCodeB.btn, applyCodeB.lbl, "Applied", 1.5),
+                                AnitaRenderer.renderModSettings(config), config.__anitaImportCodeInput && config.__anitaImportCodeInput.IsValid && config.__anitaImportCodeInput.IsValid() && (config.__anitaImportCodeInput.text = ""),
+                                AnitaCore.emitCurrentValues(config, {
+                                    update_source: "ui_code_apply",
+                                    force_persist: !0
+                                });
+                            } catch (eDec) {
+                                flashLabel(applyCodeB.btn, applyCodeB.lbl, "Invalid", 1.5);
+                            }
+                        } else flashLabel(applyCodeB.btn, applyCodeB.lbl, "Invalid", 1.5);
+                    } else flashLabel(applyCodeB.btn, applyCodeB.lbl, "Empty", 1.5);
+                }
+                importToggleBtn.btn.SetPanelEvent("onactivate", function() {
+                    setImportRowVisible(!importRowOpen);
+                }), applyCodeB.btn.SetPanelEvent("onactivate", applySaveCodeInput), importCodeInput.SetPanelEvent("ontextentrysubmit", applySaveCodeInput),
+                setImportRowVisible(!1);
+            }
         }
-
-        this._lastEscapeState = isMenuOpen;
-
-        if (!isMenuOpen && AnitaRenderer.isOpen) {
-          AnitaRenderer.toggle(false);
+    }, AnitaCore = {
+        registeredMods: [],
+        init: function() {
+            const root = this.getRoot($.GetContextPanel());
+            AnitaRenderer.initWindow(root), root.AnitaUI = {
+                GetVersion: () => CONFIG_VERSION,
+                Register: config => this.registerMod(config),
+                Toggle: () => AnitaRenderer.toggle(),
+                IsReady: () => !0
+            }, this.setupEventListener(), this.createOverlayButton(root), this.monitorEscapeMenu(root),
+            0 === this.registeredMods.length && this.registerMod({
+                title: "Anita-UI",
+                description: "No detected mods. Check your installed mods.",
+                isDummy: !0,
+                elements: []
+            }), $.DispatchEvent("ClientUI_FireOutput", JSON.stringify({
+                magic_word: "ANITA_ALIVE"
+            }));
+        },
+        registerMod: function(config) {
+            config && config.title, 1 === this.registeredMods.length && this.registeredMods[0].isDummy && (this.registeredMods = [],
+            AnitaRenderer.menuArea.RemoveAndDeleteChildren(), AnitaRenderer.contentArea.RemoveAndDeleteChildren()),
+            AnitaPersistence.hydrateConfig(config);
+            for (let i = 0; i < this.registeredMods.length; i++) if (this.registeredMods[i].title === config.title) return;
+            this.registeredMods.push(config), AnitaRenderer.addTab(config.title, () => {
+                AnitaRenderer.renderModSettings(config);
+            }), this.updateWindowWidth(), $.DispatchEvent("ClientUI_FireOutput", JSON.stringify({
+                magic_word: "ANITA_HANDSHAKE",
+                mod_title: config.title
+            })), AnitaPersistence.requestBootstrap(config, "core_handshake");
+        },
+        emitCurrentValues: function(config, meta) {
+            if (config && Array.isArray(config.elements)) for (var i = 0; i < config.elements.length; i++) {
+                var element = config.elements[i];
+                element && element.id && void 0 !== element.currentValue && emitUpdate(config.title, element.id, element.currentValue, meta);
+            }
+        },
+        findRegisteredMod: function(modTitle) {
+            for (var i = 0; i < this.registeredMods.length; i++) if (this.registeredMods[i] && this.registeredMods[i].title === modTitle) return this.registeredMods[i];
+            return null;
+        },
+        queueRenderRefresh: function(config) {
+            if (config) {
+                var refreshToken = (config.__anitaUiRefreshToken || 0) + 1;
+                config.__anitaUiRefreshToken = refreshToken, $.Schedule(.05, function() {
+                    config && config.__anitaUiRefreshToken === refreshToken && AnitaRenderer.activeModTitle === config.title && AnitaRenderer.renderModSettings(config);
+                });
+            }
+        },
+        handleUpdateEvent: function(data) {
+            if (data && data.mod_title && data.setting_id) {
+                var config = this.findRegisteredMod(data.mod_title);
+                if (config) {
+                    var updateSource = String(data.update_source || ""), isBootstrap = "bridge_bootstrap" === updateSource, isReplaySource = isBootstrap || "ui_resync" === updateSource || "ui_reset" === updateSource || "ui_code_apply" === updateSource || "core_auto_resync" === updateSource || "ui_refresh_after_apply" === updateSource;
+                    if (AnitaPersistence.applyUpdate(config, data.setting_id, data.value)) {
+                        if (AnitaRenderer.syncSaveCodeInput(config), isBootstrap && (config.__anitaBootstrapReceived = !0),
+                        !isReplaySource && "HP Colors" === config.title) {
+                            var replayToken = (config.__anitaHpReplayToken || 0) + 1;
+                            config.__anitaHpReplayToken = replayToken, $.Schedule(.35, function() {
+                                config && config.__anitaHpReplayToken === replayToken && AnitaCore.emitCurrentValues(config, {
+                                    update_source: "ui_refresh_after_apply",
+                                    skip_bridge_persist: !0
+                                });
+                            });
+                        }
+                        var writeToken = (config.__anitaPendingWriteToken || 0) + 1;
+                        config.__anitaPendingWriteToken = writeToken, $.Schedule(2, function() {
+                            config && config.__anitaPendingWriteToken === writeToken && AnitaPersistence.persistConfig(config, !1);
+                        }), (this.hasVisibilityDependents(config, data.setting_id) || isBootstrap) && this.queueRenderRefresh(config);
+                    }
+                }
+            }
+        },
+        handleBootstrapRequest: function(data) {
+            if (data && data.mod_title) {
+                var config = this.findRegisteredMod(data.mod_title);
+                config && this.emitCurrentValues(config, {
+                    update_source: "bridge_bootstrap",
+                    skip_bridge_persist: !0,
+                    bootstrap_reason: String(data.reason || "request")
+                });
+            }
+        },
+        updateWindowWidth: function() {
+            if (!AnitaRenderer.mainWindow) return;
+            const count = this.registeredMods.length;
+            let width = null;
+            1 === count && this.registeredMods[0].isDummy ? width = 500 : count <= 4 && (width = 300 * count),
+            AnitaRenderer.mainWindow.style.minWidth = width ? width + "px" : "90%";
+        },
+        setupEventListener: function() {
+            try {
+                $.RegisterForUnhandledEvent("ClientUI_FireOutput", payload => {
+                    try {
+                        let data = "string" == typeof payload ? JSON.parse(payload) : payload;
+                        data && "ANITA_REGISTER" === data.magic_word ? (data.config && data.config.title,
+                        this.registerMod(data.config)) : data && "ANITA_REQUEST_BOOTSTRAP" === data.magic_word ? (data.mod_title,
+                        this.handleBootstrapRequest(data)) : data && "ANITA_UPDATE" === data.magic_word && this.handleUpdateEvent(data);
+                    } catch (e) {}
+                });
+            } catch (e) {}
+        },
+        createOverlayButton: function(parent) {
+            const existing = parent.FindChildTraverse(CONFIG_IDS.OVERLAY_BTN);
+            existing && existing.DeleteAsync(0);
+            const btn = $.CreatePanel("Button", parent, CONFIG_IDS.OVERLAY_BTN);
+            btn.AddClass("AnitaOverlayBtn"), this._overlayBtn = btn, btn.SetPanelEvent("onmouseover", () => $.DispatchEvent("UIShowTextTooltip", btn, "Anita-UI Settings")),
+            btn.SetPanelEvent("onmouseout", () => $.DispatchEvent("UIHideTextTooltip", btn)),
+            btn.SetPanelEvent("onactivate", () => AnitaRenderer.toggle());
+        },
+        monitorEscapeMenu: function(root) {
+            let hudPanel = this._hudPanel, btn = this._overlayBtn, nextDelay = CONFIG_UI.MONITOR_INTERVAL;
+            if (!hudPanel || !hudPanel.IsValid || !hudPanel.IsValid()) {
+                if (hudPanel = root.FindChildTraverse(CONFIG_IDS.HUD_ROOT), !hudPanel) {
+                    let p = $.GetContextPanel();
+                    for (;p; ) {
+                        if (p.id === CONFIG_IDS.HUD_ROOT) {
+                            hudPanel = p;
+                            break;
+                        }
+                        p = p.GetParent();
+                    }
+                }
+                this._hudPanel = hudPanel || null;
+            }
+            if (btn && btn.IsValid && btn.IsValid() || (btn = root.FindChildTraverse(CONFIG_IDS.OVERLAY_BTN),
+            this._overlayBtn = btn || null), "boolean" != typeof this._lastEscapeState && (this._lastEscapeState = !1),
+            hudPanel && btn) {
+                const isMenuOpen = hudPanel.BHasClass(CONFIG_CLASSES.ESCAPE_MENU), stateChanged = this._lastEscapeState !== isMenuOpen;
+                if (!stateChanged && btn.BHasClass(CONFIG_CLASSES.VISIBLE) || btn.SetHasClass(CONFIG_CLASSES.VISIBLE, isMenuOpen),
+                !!btn.hittest !== isMenuOpen && (btn.hittest = isMenuOpen), stateChanged) if (isMenuOpen) {
+                    const attentionToken = (this._attentionToken || 0) + 1;
+                    this._attentionToken = attentionToken, btn.AddClass(CONFIG_CLASSES.ATTENTION), $.Schedule(4, () => {
+                        this._attentionToken === attentionToken && btn && btn.IsValid && btn.IsValid() && btn.RemoveClass(CONFIG_CLASSES.ATTENTION);
+                    });
+                } else btn.RemoveClass(CONFIG_CLASSES.ATTENTION);
+                this._lastEscapeState = isMenuOpen, !isMenuOpen && AnitaRenderer.isOpen && AnitaRenderer.toggle(!1),
+                nextDelay = isMenuOpen ? CONFIG_UI.MONITOR_INTERVAL : Math.max(8 * CONFIG_UI.MONITOR_INTERVAL, .25);
+            } else nextDelay = Math.max(4 * CONFIG_UI.MONITOR_INTERVAL, .2);
+            $.Schedule(nextDelay, () => this.monitorEscapeMenu(root));
+        },
+        getRoot: function(p) {
+            for (;p.GetParent && p.GetParent(); ) p = p.GetParent();
+            return p;
         }
-
-        nextDelay = isMenuOpen ? CONFIG.UI.MONITOR_INTERVAL : Math.max(CONFIG.UI.MONITOR_INTERVAL * 8, 0.25);
-      } else {
-        nextDelay = Math.max(CONFIG.UI.MONITOR_INTERVAL * 4, 0.2);
-      }
-
-      $.Schedule(nextDelay, () => this.monitorEscapeMenu(root));
-    },
-
-    getRoot: function (p) {
-      while (p.GetParent && p.GetParent()) p = p.GetParent();
-      return p;
-    }
-  };
-
-  AnitaCore.init();
-
-})();
+    };
+    AnitaCore.init();
+}();
diff --git a/hp_colors/panorama/scripts/healthbar_logic.js b/hp_colors/panorama/scripts/healthbar_logic.js
index 12cc981..c278718 100644
--- a/hp_colors/panorama/scripts/healthbar_logic.js
+++ b/hp_colors/panorama/scripts/healthbar_logic.js
@@ -1,1093 +1,785 @@
-'use strict';
-(function () {
-  // â”€â”€ Settings â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
-  var TITLE = "HP Colors";
-  var DEFAULTS = {
-    hp_enabled: true,
-    hp_mode: 1,
-    hp_low_threshold: 25,
-    hp_high_threshold: 65,
-    hp_bg_visible: true,
-    hp_team_colors: false,
-    hp_color_low: "#E16161",
-    hp_color_mid: "#FF7B00",
-    hp_color_high: "#00FF00",
-    hp_counter_size: 120,
-    hp_counter_position: "20,196",
-    hp_text_color_mode: 0,
-    hp_text_color_low: "#E16161",
-    hp_text_color_mid: "#FF7B00",
-    hp_text_color_high: "#FFFFFF",
-    hp_pulse_bpm: 75,
-    hp_pulse_intensity: 1,
-    hp_pulse_enabled: true,
-    hp_pulse_text_enabled: true,
-    hp_pulse_text_scale: 120,
-    hp_pulse_text_position: "20,196",
-    hp_pulse_hide_bar: false,
-    hp_skip_buildings: false,
-    hp_pulse_threshold: 25,
-    hp_counter_format: 0,
-    hp_friend_enabled: false,
-    hp_friend_pulse_enabled: false,
-    hp_friend_pulse_bpm: 75,
-    hp_friend_pulse_intensity: 1,
-    hp_friend_pulse_threshold: 25,
-    hp_friend_color_low: "#E16161",
-    hp_friend_color_mid: "#FF7B00",
-    hp_friend_color_high: "#00FF00",
-    hp_friend_pulse_color_enabled: false,
-    hp_friend_pulse_color: "#FF2222",
-    hp_level_number_visible: true,
-    hp_kill_zone_enabled: false,
-    hp_kill_zone_threshold: 25,
-    hp_kill_zone_color: "#FF2222",
-    hp_kill_zone_width: 3
-  };
-  var cfg = {};
-  var TEAM1_HIGH = "#FFC961";
-  var TEAM2_HIGH = "#6485FC";
-  var CSS_TEAM1_COLOR = "#E7B659";
-  var CSS_TEAM2_COLOR = "#5B79E6";
-  var CSS_TEAM_FRIEND_COLOR = "#FFEFD7";
-  var CSS_TEAM_ENEMY_COLOR = "#e16161";
-  var WHITE_WASH = "#ffffff";
-  var LP = 'low_hp_pulsing';
-  var PULSE_INTENSITY = ['pulse_subtle', '', 'pulse_intense'];
-
-  // ── Loop control ────────────────────────────────────────────────────────────
-  var gRunning = false;
-  var aRunning = false;
-
-  // ── Pulse state ─────────────────────────────────────────────────────────────
-  var pulse = 0;
-  var lPD = null;
-  var lPI = -1;
-  var lTB = null;
-
-  // ── Ally state ───────────────────────────────────────────────────────────────
-  var rbA = null, cpA = null;
-  var lColA = null, lWA = -1, lPWA = -1, sfcA = 0;
-  var pulseA = 0, lPDA = null, lPIA = -1;
-  var aScanF2 = -1, aScanT2 = 0, aScanAt = 0;
-
-  function removePulseIntensityClasses(panel) {
-    if (!panel) return;
-    try {
-      for (var i = 0; i < PULSE_INTENSITY.length; i++) {
-        if (PULSE_INTENSITY[i]) panel.RemoveClass(PULSE_INTENSITY[i]);
-      }
-    } catch (e) {}
-  }
-
-  function getPulseTextSize(fallback) {
-    return clampNum(cfg.hp_pulse_text_scale, 72, 320, fallback);
-  }
-
-  function applyPulseTextState() {
-    if (!hc) return;
-    try {
-      if (!(pulse && cfg.hp_pulse_text_enabled)) {
-        if (hc.style) hc.style.animationDuration = '';
-        if (hc.style) hc.style.brightness = '';
-        lTB = null;
-      }
-    } catch (e2) {}
-  }
-
-  function updatePulseTextBrightness(now) {
-    if (!hc || !hc.style || !pulse || !cfg.hp_pulse_text_enabled) return;
-    var bpm = clampNum(cfg.hp_pulse_bpm, 30, 300, 75);
-    var period = Math.max(1, 60000 / bpm);
-    var phase = (now % period) / period;
-    var wave = phase < 0.5 ? phase * 2 : (1 - phase) * 2;
-    var idx = clampNum(cfg.hp_pulse_intensity, 0, 2, 1) | 0;
-    var hi = idx === 2 ? 2.0 : (idx === 0 ? 1.15 : 1.5);
-    var lo = idx === 2 ? 0.55 : (idx === 0 ? 0.85 : 0.65);
-    var next = (lo + (hi - lo) * wave).toFixed(2);
-    if (lTB === next) return;
-    try { hc.style.brightness = next; lTB = next; } catch (e) { lTB = null; }
-  }
-
-  function applyPulseDuration() {
-    var bpm = Number(cfg.hp_pulse_bpm) || 75;
-    if (bpm < 30) bpm = 30;
-    if (bpm > 300) bpm = 300;
-    var dur = (60 / bpm).toFixed(3) + 's';
-    if (lPD === dur) return;
-    lPD = dur;
-    if (rb) rb.style.animationDuration = dur;
-    if (ui) ui.style.animationDuration = dur;
-  }
-
-  function applyPulseIntensity() {
-    var idx = Number(cfg.hp_pulse_intensity) | 0;
-    if (idx < 0 || idx > 2) idx = 1;
-    if (lPI === idx) return;
-    var oldCls = lPI >= 0 ? PULSE_INTENSITY[lPI] : '';
-    var newCls = PULSE_INTENSITY[idx];
-    lPI = idx;
-    if (oldCls) {
-      try { if (rb) rb.RemoveClass(oldCls); } catch (e) {}
-      try { if (ui) ui.RemoveClass(oldCls); } catch (e2) {}
+"use strict";
+
+!function() {
+    var DEFAULTS = {
+        hp_enabled: !0,
+        hp_mode: 1,
+        hp_low_threshold: 25,
+        hp_high_threshold: 65,
+        hp_bg_visible: !0,
+        hp_team_colors: !1,
+        hp_color_low: "#E16161",
+        hp_color_mid: "#FF7B00",
+        hp_color_high: "#00FF00",
+        hp_counter_size: 120,
+        hp_counter_position: "20,196",
+        hp_text_color_mode: 0,
+        hp_text_color_low: "#E16161",
+        hp_text_color_mid: "#FF7B00",
+        hp_text_color_high: "#FFFFFF",
+        hp_pulse_bpm: 75,
+        hp_pulse_intensity: 1,
+        hp_pulse_enabled: !0,
+        hp_pulse_text_enabled: !0,
+        hp_pulse_text_scale: 120,
+        hp_pulse_text_position: "20,196",
+        hp_pulse_hide_bar: !1,
+        hp_skip_buildings: !1,
+        hp_pulse_threshold: 25,
+        hp_counter_format: 0,
+        hp_friend_enabled: !1,
+        hp_friend_pulse_enabled: !1,
+        hp_friend_pulse_bpm: 75,
+        hp_friend_pulse_intensity: 1,
+        hp_friend_pulse_threshold: 25,
+        hp_friend_color_low: "#E16161",
+        hp_friend_color_mid: "#FF7B00",
+        hp_friend_color_high: "#00FF00",
+        hp_friend_pulse_color_enabled: !1,
+        hp_friend_pulse_color: "#FF2222",
+        hp_level_number_visible: !0,
+        hp_kill_zone_enabled: !1,
+        hp_kill_zone_threshold: 25,
+        hp_kill_zone_color: "#FF2222",
+        hp_kill_zone_width: 3
+    }, cfg = {}, LP = "low_hp_pulsing", PULSE_INTENSITY = [ "pulse_subtle", "", "pulse_intense" ], gRunning = !1, aRunning = !1, pulse = 0, lPD = null, lPI = -1, lTB = null, rbA = null, cpA = null, lColA = null, lWA = -1, lPWA = -1, sfcA = 0, pulseA = 0, lPIA = -1, aScanF2 = -1, aScanT2 = 0, aScanAt = 0;
+    function applyPulseTextState() {
+        if (hc) try {
+            pulse && cfg.hp_pulse_text_enabled || (hc.style && (hc.style.animationDuration = ""),
+            hc.style && (hc.style.brightness = ""), lTB = null);
+        } catch (e2) {}
     }
-    if (newCls) {
-      try { if (rb) rb.AddClass(newCls); } catch (e3) {}
-      try { if (ui) ui.AddClass(newCls); } catch (e4) {}
+    function applyPulseDuration() {
+        var bpm = Number(cfg.hp_pulse_bpm) || 75;
+        bpm < 30 && (bpm = 30), bpm > 300 && (bpm = 300);
+        var dur = (60 / bpm).toFixed(3) + "s";
+        lPD !== dur && (lPD = dur, rb && (rb.style.animationDuration = dur), ui && (ui.style.animationDuration = dur));
     }
-    if (pulse) applyPulseTextState();
-  }
-
-  function startPulse() {
-    if (pulse) return;
-    pulse = 1;
-    lCol = lUlt = lTxt = null;
-    try { if (rb) rb.AddClass(LP); } catch (e) {}
-    try { if (ui) ui.AddClass(LP); } catch (e3) {}
-    applyPulseIntensity();
-    applyPulseDuration();
-  }
-
-  function clearPulsePanel(panel, oldCls) {
-    if (!panel) return;
-    try {
-      panel.RemoveClass(LP);
-      if (oldCls) panel.RemoveClass(oldCls);
-      if (panel.style) {
-        panel.style.animationDuration = '';
-        panel.style.brightness = '';
-      }
-    } catch (e) {}
-  }
-
-  function clearPulse() {
-    var oldCls = lPI >= 0 ? PULSE_INTENSITY[lPI] : '';
-    pulse = 0; lPD = null; lPI = -1; lTB = null; lCol = lUlt = lTxt = null;
-    clearPulsePanel(rb, oldCls);
-    clearPulsePanel(hc, oldCls);
-    clearPulsePanel(ui, oldCls);
-  }
-
-  function clearAllyPulse() {
-    var oldCls = lPIA >= 0 ? PULSE_INTENSITY[lPIA] : '';
-    pulseA = 0; lPDA = null; lPIA = -1; lColA = null;
-    clearPulsePanel(rbA, oldCls);
-  }
-
-  function coerceCfgValue(id, value) {
-    if (!Object.prototype.hasOwnProperty.call(DEFAULTS, id)) return value;
-
-    var fallback = DEFAULTS[id];
-    if (id === "hp_counter_position" || id === "hp_pulse_text_position") {
-      var posX = 0;
-      var posY = 200;
-      var rawPos = value;
-
-      if (rawPos && typeof rawPos === "object") {
-        if (Array.isArray(rawPos)) {
-          if (rawPos.length > 0) posX = clampNum(rawPos[0], 0, 400, 0);
-          if (rawPos.length > 1) posY = clampNum(rawPos[1], 0, 400, 200);
-        } else {
-          if (Object.prototype.hasOwnProperty.call(rawPos, "x")) posX = clampNum(rawPos.x, 0, 400, 0);
-          if (Object.prototype.hasOwnProperty.call(rawPos, "y")) posY = clampNum(rawPos.y, 0, 400, 200);
-        }
-        return Math.round(posX) + "," + Math.round(posY);
-      }
-
-      if (typeof rawPos === "string") {
-        var posParts = rawPos.match(/-?\d+(?:\.\d+)?/g);
-        if (posParts && posParts.length > 0) {
-          posX = clampNum(posParts[0], 0, 400, 0);
-          if (posParts.length > 1) posY = clampNum(posParts[1], 0, 400, 200);
-          return Math.round(posX) + "," + Math.round(posY);
+    function applyPulseIntensity() {
+        var idx = 0 | Number(cfg.hp_pulse_intensity);
+        if ((idx < 0 || idx > 2) && (idx = 1), lPI !== idx) {
+            var oldCls = lPI >= 0 ? PULSE_INTENSITY[lPI] : "", newCls = PULSE_INTENSITY[idx];
+            if (lPI = idx, oldCls) {
+                try {
+                    rb && rb.RemoveClass(oldCls);
+                } catch (e) {}
+                try {
+                    ui && ui.RemoveClass(oldCls);
+                } catch (e2) {}
+            }
+            if (newCls) {
+                try {
+                    rb && rb.AddClass(newCls);
+                } catch (e3) {}
+                try {
+                    ui && ui.AddClass(newCls);
+                } catch (e4) {}
+            }
+            pulse && applyPulseTextState();
         }
-      }
-
-      if (typeof rawPos === "number") {
-        posY = clampNum(rawPos, 0, 400, 200);
-      }
-
-      return Math.round(posX) + "," + Math.round(posY);
-    }
-
-    if (typeof fallback === "boolean") {
-      if (value === true || value === false) return value;
-      if (value === 1 || value === "1") return true;
-      if (value === 0 || value === "0") return false;
-      if (typeof value === "string") {
-        var lowered = value.toLowerCase();
-        if (lowered === "true") return true;
-        if (lowered === "false") return false;
-      }
-      return !!fallback;
-    }
-
-    if (typeof fallback === "number") {
-      var next = Number(value);
-      if (!isFinite(next)) return fallback;
-      return Math.round(next);
-    }
-
-    if (typeof fallback === "string") {
-      return (typeof value === "string" && value.length > 0) ? value : fallback;
     }
-
-    return value;
-  }
-
-  function loadCfgDefaults() {
-    for (var k in DEFAULTS) {
-      if (Object.prototype.hasOwnProperty.call(DEFAULTS, k)) {
-        cfg[k] = DEFAULTS[k];
-      }
+    function clearPulsePanel(panel, oldCls) {
+        if (panel) try {
+            panel.RemoveClass(LP), oldCls && panel.RemoveClass(oldCls), panel.style && (panel.style.animationDuration = "",
+            panel.style.brightness = "");
+        } catch (e) {}
     }
-  }
-
-  loadCfgDefaults();
-  var BOOTSTRAP_NAMESPACE = "hp_colors";
-  var BOOTSTRAP_MAX_ATTEMPTS = 8;
-  var BOOTSTRAP_RETRY_SEC = 0.5;
-  var bootstrapApplied = false;
-  var bootstrapAttempts = 0;
-  var bootstrapFinished = false;
-  var settingsDirty = true;
-
-  function getRootPanel() {
-    var panel = $.GetContextPanel();
-    while (panel && panel.GetParent && panel.GetParent()) {
-      panel = panel.GetParent();
+    function clearPulse() {
+        var oldCls = lPI >= 0 ? PULSE_INTENSITY[lPI] : "";
+        pulse = 0, lPD = null, lPI = -1, lTB = null, lCol = lUlt = lTxt = null, clearPulsePanel(rb, oldCls),
+        clearPulsePanel(hc, oldCls), clearPulsePanel(ui, oldCls);
     }
-    return panel || null;
-  }
-
-  function requestBootstrap(reason) {
-    var root = getRootPanel();
-    var now = Date.now ? Date.now() : (new Date()).getTime();
-    try {
-      if (root) {
-        var nextAllowedAt = Number(root.__hpColorsBootstrapNextAllowedAt || 0);
-        if (isFinite(nextAllowedAt) && nextAllowedAt > now) return;
-        root.__hpColorsBootstrapNextAllowedAt = now + Math.floor(BOOTSTRAP_RETRY_SEC * 1000 * 0.8);
-      }
-    } catch (eRate) {}
-
-    try {
-      $.DispatchEvent("ClientUI_FireOutput", JSON.stringify({
-        magic_word: "ANITA_REQUEST_BOOTSTRAP",
-        mod_title: TITLE,
-        storageNamespace: BOOTSTRAP_NAMESPACE,
-        reason: String(reason || "overlay_request")
-      }));
-    } catch (e) {}
-  }
-
-  function scheduleBootstrapRetry() {
-    if (bootstrapApplied || bootstrapFinished) return;
-    if (bootstrapAttempts >= BOOTSTRAP_MAX_ATTEMPTS) {
-      bootstrapFinished = true;
-      return;
+    function clearAllyPulse() {
+        var oldCls = lPIA >= 0 ? PULSE_INTENSITY[lPIA] : "";
+        pulseA = 0, lPIA = -1, lColA = null, clearPulsePanel(rbA, oldCls);
     }
-
-    bootstrapAttempts += 1;
-    requestBootstrap(bootstrapAttempts === 1 ? "overlay_startup" : "overlay_retry");
-    $.Schedule(BOOTSTRAP_RETRY_SEC, scheduleBootstrapRetry);
-  }
-
-  // Live updates from Anita UI, including boot-time bootstrap values.
-  $.RegisterForUnhandledEvent("ClientUI_FireOutput", function (payload) {
-    try {
-      var d = typeof payload === 'string' ? JSON.parse(payload) : payload;
-      if (!d || d.mod_title !== TITLE) return;
-
-      if (d.magic_word === "ANITA_UPDATE") {
-        if (Object.prototype.hasOwnProperty.call(DEFAULTS, d.setting_id)) {
-          if (d.setting_id === "hp_counter_position" && d.update_source === "hp_counter_autoposition") {
-            return;
-          }
-          cfg[d.setting_id] = coerceCfgValue(d.setting_id, d.value); settingsDirty = true;
-          if (d.setting_id === "hp_level_number_visible") lLvVis = null;
-          if (d.setting_id === "hp_enabled" && cfg.hp_enabled && !gRunning) { gRunning = true; $.Schedule(0.05, gL); }
-          if (d.setting_id === "hp_friend_enabled" && cfg.hp_friend_enabled && !aRunning) { aRunning = true; $.Schedule(0.05, aL); }
-          if (d.setting_id === "hp_friend_enabled" && !cfg.hp_friend_enabled) {
-            if (isAllyPlayerFlags(aScanF2)) resetAllyBarColor(rbA, aScanT2, aScanF2);
-            else clearAllyPulse();
-            aRunning = false;
-          }
-          if (pulse && (d.setting_id === "hp_pulse_bpm" || d.setting_id === "hp_pulse_intensity" || d.setting_id === "hp_pulse_text_enabled" || d.setting_id === "hp_pulse_text_scale")) {
-            if (d.setting_id === "hp_pulse_bpm") { lPD = null; applyPulseDuration(); }
-            if (d.setting_id === "hp_pulse_intensity") applyPulseIntensity();
-            if (d.setting_id === "hp_pulse_text_enabled" || d.setting_id === "hp_pulse_text_scale") applyPulseTextState();
-          }
-          if (d.setting_id === "hp_pulse_enabled" && !cfg.hp_pulse_enabled) clearPulse();
+    function coerceCfgValue(id, value) {
+        if (!Object.prototype.hasOwnProperty.call(DEFAULTS, id)) return value;
+        var fallback = DEFAULTS[id];
+        if ("hp_counter_position" === id || "hp_pulse_text_position" === id) {
+            var posX = 0, posY = 200, rawPos = value;
+            if (rawPos && "object" == typeof rawPos) return Array.isArray(rawPos) ? (rawPos.length > 0 && (posX = clampNum(rawPos[0], 0, 400, 0)),
+            rawPos.length > 1 && (posY = clampNum(rawPos[1], 0, 400, 200))) : (Object.prototype.hasOwnProperty.call(rawPos, "x") && (posX = clampNum(rawPos.x, 0, 400, 0)),
+            Object.prototype.hasOwnProperty.call(rawPos, "y") && (posY = clampNum(rawPos.y, 0, 400, 200))),
+            Math.round(posX) + "," + Math.round(posY);
+            if ("string" == typeof rawPos) {
+                var posParts = rawPos.match(/-?\d+(?:\.\d+)?/g);
+                if (posParts && posParts.length > 0) return posX = clampNum(posParts[0], 0, 400, 0),
+                posParts.length > 1 && (posY = clampNum(posParts[1], 0, 400, 200)), Math.round(posX) + "," + Math.round(posY);
+            }
+            return "number" == typeof rawPos && (posY = clampNum(rawPos, 0, 400, 200)), Math.round(posX) + "," + Math.round(posY);
         }
-        if (d.update_source === "bridge_bootstrap") { bootstrapApplied = true;
-          bootstrapFinished = true;
-          try {
-            var root = getRootPanel();
-            if (root) root.__hpColorsBootstrapAppliedAt = Date.now ? Date.now() : (new Date()).getTime();
-          } catch (eBoot) {}
+        if ("boolean" == typeof fallback) {
+            if (!0 === value || !1 === value) return value;
+            if (1 === value || "1" === value) return !0;
+            if (0 === value || "0" === value) return !1;
+            if ("string" == typeof value) {
+                var lowered = value.toLowerCase();
+                if ("true" === lowered) return !0;
+                if ("false" === lowered) return !1;
+            }
+            return !!fallback;
         }
-        return;
-      }
-    } catch (e) {}
-  });
-
-  // â”€â”€ Color helpers â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
-  function hexToRgb(s) {
-    if (!s) return [255, 255, 255];
-    if (s.charAt(0) === '#') {
-      var h = s.slice(1);
-      if (h.length === 3) h = h[0] + h[0] + h[1] + h[1] + h[2] + h[2];
-      return [parseInt(h.slice(0, 2), 16), parseInt(h.slice(2, 4), 16), parseInt(h.slice(4, 6), 16)];
+        if ("number" == typeof fallback) {
+            var next = Number(value);
+            return isFinite(next) ? Math.round(next) : fallback;
+        }
+        return "string" == typeof fallback ? "string" == typeof value && value.length > 0 ? value : fallback : value;
     }
-    var m = s.match(/rgb\((\d+),\s*(\d+),\s*(\d+)\)/);
-    if (m) return [+m[1], +m[2], +m[3]];
-    return [255, 255, 255];
-  }
-
-  function clampByte(v) {
-    v = Number(v);
-    if (!isFinite(v)) return 0;
-    v = v | 0;
-    if (v < 0) return 0;
-    if (v > 255) return 255;
-    return v;
-  }
-
-  function byteHex(v) {
-    var h = clampByte(v).toString(16);
-    return h.length === 1 ? '0' + h : h;
-  }
-
-  function rgbToHex(c) {
-    return '#' + byteHex(c[0]) + byteHex(c[1]) + byteHex(c[2]);
-  }
-
-  function normalizeWashColor(color) {
-    if (typeof color !== 'string') return '';
-    var trimmed = color.trim();
-    if (!trimmed) return '';
-
-    if (trimmed.charAt(0) === '#') {
-      if (trimmed.length === 4) {
-        return ('#' +
-          trimmed.charAt(1) + trimmed.charAt(1) +
-          trimmed.charAt(2) + trimmed.charAt(2) +
-          trimmed.charAt(3) + trimmed.charAt(3)).toLowerCase();
-      }
-      if (trimmed.length >= 7) return trimmed.slice(0, 7).toLowerCase();
-      return trimmed;
+    !function() {
+        for (var k in DEFAULTS) Object.prototype.hasOwnProperty.call(DEFAULTS, k) && (cfg[k] = DEFAULTS[k]);
+    }();
+    var HP_PERSIST_ALIAS_TO_ID = {
+        e: "hp_enabled",
+        m: "hp_mode",
+        l: "hp_low_threshold",
+        h: "hp_high_threshold",
+        b: "hp_bg_visible",
+        t: "hp_team_colors",
+        cl: "hp_color_low",
+        cm: "hp_color_mid",
+        ch: "hp_color_high",
+        s: "hp_counter_size",
+        p: "hp_counter_position",
+        tm: "hp_text_color_mode",
+        lnv: "hp_level_number_visible",
+        tl: "hp_text_color_low",
+        ti: "hp_text_color_mid",
+        th: "hp_text_color_high",
+        bp: "hp_pulse_bpm",
+        pi: "hp_pulse_intensity",
+        pe: "hp_pulse_enabled",
+        pte: "hp_pulse_text_enabled",
+        pts: "hp_pulse_text_scale",
+        ptp: "hp_pulse_text_position",
+        phb: "hp_pulse_hide_bar",
+        sb: "hp_skip_buildings",
+        pt: "hp_pulse_threshold",
+        fe: "hp_friend_enabled",
+        fpe: "hp_friend_pulse_enabled",
+        fpb: "hp_friend_pulse_bpm",
+        fpi: "hp_friend_pulse_intensity",
+        fpt: "hp_friend_pulse_threshold",
+        fcl: "hp_friend_color_low",
+        fcm: "hp_friend_color_mid",
+        fch: "hp_friend_color_high",
+        fpce: "hp_friend_pulse_color_enabled",
+        fpc: "hp_friend_pulse_color",
+        kze: "hp_kill_zone_enabled",
+        kzt: "hp_kill_zone_threshold",
+        kzc: "hp_kill_zone_color",
+        kzw: "hp_kill_zone_width"
+    }, bootstrapApplied = !1, directBootstrapApplied = !1, bootstrapAttempts = 0, bootstrapFinished = !1, settingsDirty = !0, lastBootstrapRequestAt = 0, lastDirectBootstrapAt = 0, lastStyleReapplyAt = 0, sharedCfgRaw = "";
+    function getSharedStore() {
+        try {
+            if ("undefined" != typeof GameUI && GameUI && GameUI.CustomUIConfig) return GameUI.CustomUIConfig();
+        } catch (e) {}
+        return null;
     }
-
-    var m = trimmed.match(/^rgba?\(\s*(\d+)\s*,\s*(\d+)\s*,\s*(\d+)/i);
-    if (m) return rgbToHex([m[1], m[2], m[3]]);
-
-    return trimmed;
-  }
-
-  function lerp(a, b, t) { return (a + (b - a) * t) | 0; }
-  function ip(c1, c2, t) { return [lerp(c1[0], c2[0], t), lerp(c1[1], c2[1], t), lerp(c1[2], c2[2], t)]; }
-
-  function getHighColor() {
-    if (!cfg.hp_team_colors) return cfg.hp_color_high;
-    return tid === 2 ? TEAM2_HIGH : TEAM1_HIGH;
-  }
-
-  // Get text color based on HP and mode
-  // mode 0 (By HP %): use bar colors (low/mid/high)
-  // mode 1 (Custom): use custom text colors
-  function getTextColor(hp, low, high) {
-    if (cfg.hp_text_color_mode) {
-      // Custom mode - use custom text colors
-      if (hp <= low) return cfg.hp_text_color_low;
-      if (hp <= high) return cfg.hp_text_color_mid;
-      return cfg.hp_text_color_high;
+    function readSessionMirrorEncoded() {
+        var root = function() {
+            for (var panel = $.GetContextPanel(); panel && panel.GetParent && panel.GetParent(); ) panel = panel.GetParent();
+            return panel || null;
+        }();
+        if (!root) return "";
+        try {
+            if (root.GetAttributeString) {
+                var rootValue = String(root.GetAttributeString("anita_v1_hp_colors", "") || "");
+                if (rootValue) return rootValue;
+            }
+        } catch (eRoot) {}
+        try {
+            var hud = root.FindChildTraverse ? root.FindChildTraverse("Hud") : null;
+            if (hud && hud.GetAttributeString) return String(hud.GetAttributeString("anita_v1_hp_colors", "") || "");
+        } catch (eHud) {}
+        return "";
     }
-    // By HP % mode - use bar colors
-    if (hp <= low) return cfg.hp_color_low;
-    if (hp <= high) return cfg.hp_color_mid;
-    return getHighColor();
-  }
-
-  // Get gradient text color (interpolated)
-  // mode 0 (By HP %): use bar colors
-  // mode 1 (Custom): use custom text colors
-  function getGradientTextColor(hp, low, high) {
-    var denomMid = Math.max(1, high - low);
-    var denomHigh = Math.max(1, 100 - high);
-    if (cfg.hp_text_color_mode) {
-      // Custom mode - use custom text colors
-      if (hp <= low) return cfg.hp_text_color_low;
-      if (hp <= high) {
-        var t = (hp - low) / denomMid;
-        return rgbToHex(ip(hexToRgb(cfg.hp_text_color_low), hexToRgb(cfg.hp_text_color_mid), t));
-      }
-      var t2 = (hp - high) / denomHigh;
-      return rgbToHex(ip(hexToRgb(cfg.hp_text_color_mid), hexToRgb(cfg.hp_text_color_high), t2));
+    function writeSharedSnapshot() {
+        var store = getSharedStore();
+        if (store) try {
+            sharedCfgRaw = JSON.stringify(function() {
+                var out = {};
+                for (var k in DEFAULTS) Object.prototype.hasOwnProperty.call(DEFAULTS, k) && (out[k] = cfg[k]);
+                return out;
+            }()), store.__hpColorsCfgRaw = sharedCfgRaw;
+        } catch (e) {}
     }
-    // By HP % mode - use bar colors (same interpolation as bar)
-    if (hp <= low) return cfg.hp_color_low;
-    if (hp <= high) {
-      var t3 = (hp - low) / denomMid;
-      return rgbToHex(ip(hexToRgb(cfg.hp_color_low), hexToRgb(cfg.hp_color_mid), t3));
+    function invalidateEnemyVisualState() {
+        lCol = lUlt = lTxt = lBgVis = lBgOp = lHpSize = lHpPos = lHpMarginLeft = lHpHeight = null,
+        lKzVis = lKzX = lKzW = lKzColor = lLvVis = null, lSH = lSM = -1, lVis = null, lW = lPW = lHp = pPct = -1,
+        sFC = 0, settingsDirty = !0;
     }
-    var t4 = (hp - high) / denomHigh;
-    var highCol = getHighColor();
-    return rgbToHex(ip(hexToRgb(cfg.hp_color_mid), hexToRgb(highCol), t4));
-  }
-
-  // â”€â”€ Panel cache â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
-  var ctx = $.GetContextPanel();
-  var us = null, hc = null, bg = null, pl = null, lb = null, lbp = null, rb = null, cp = null, ui = null, kz = null;
-  var cached = 0, att = 0;
-  var lBgVis = null, lBgOp = null, lHpSize = null, lHpPos = null, lHpMarginLeft = null, lHpHeight = null;
-
-  function fRB() {
-    return ctx.FindChildTraverse('unit_healthbar_lagging') ||
-      ctx.FindChildTraverse('health_bar') ||
-      ctx.FindChildTraverse('unit_health');
-  }
-
-  function tryCache() {
-    if (cached) return 1;
-    if (att >= 10) return 0;
-    att++;
-    if (!us || !us.IsValid()) us = ctx.FindChildTraverse('UnitStatus');
-    if (!us) return 0;
-    if (!hc || !hc.IsValid()) hc = us.FindChildTraverse('hp_counter');
-    if (!bg || !bg.IsValid()) bg = us.FindChildTraverse('unit_healthbar_bg');
-    if (!pl || !pl.IsValid()) pl = us.FindChildTraverse('unit_healthbar_pip_label');
-    if (!lb || !lb.IsValid()) lb = us.FindChildTraverse('unit_healthbar_lagging');
-    if (!kz || !kz.IsValid()) kz = us.FindChildTraverse('hp_kill_zone_marker');
-    if (lb && (!lbp || !lbp.IsValid())) lbp = lb.GetParent();
-    if (pl && lb && lbp) { cached = 1; return 1; }
-    return 0;
-  }
-
-  // â”€â”€ Team/flag scan (walk up to find team classes) â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
-  var tid = 0, fl = 0, lAT = 0;
-
-  function scan(p) {
-    var t = 0, f = 0, d = 0, c = p;
-    while (c && d < 10) {
-      if (c.BHasClass) {
-        if (!t) { if (c.BHasClass('team2')) t = 2; else if (c.BHasClass('team1')) t = 1; }
-        if (!(f & 1) && c.BHasClass('enemy')) f |= 1;
-        if (!(f & 2) && (c.BHasClass('team_neutral') || c.BHasClass('neutral'))) f |= 2;
-        if (!(f & 4) && (c.BHasClass('building') || c.BHasClass('boss_tier1') || c.BHasClass('boss_tier2') || c.BHasClass('boss_barracks'))) f |= 4;
-        if (!(f & 8) && c.BHasClass('friend')) f |= 8;
-        if (t && (f & (1|2|4|8))) break;
-      }
-      if (!c.GetParent) break;
-      c = c.GetParent(); d++;
+    function applyDirectSnapshot(values, source) {
+        if (!values) return !1;
+        var count = 0;
+        for (var k in values) Object.prototype.hasOwnProperty.call(values, k) && Object.prototype.hasOwnProperty.call(DEFAULTS, k) && (cfg[k] = coerceCfgValue(k, values[k]),
+        count += 1);
+        return !!count && (bootstrapApplied = !0, directBootstrapApplied = !0, bootstrapFinished = !0, "shared" !== source && writeSharedSnapshot(),
+        invalidateEnemyVisualState(), !0);
     }
-    tid = t; fl = f;
-  }
-
-  // â”€â”€ Setter helpers (skip redundant writes) â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
-  var lCol = null, lUlt = null, lTxt = null;
-  var lSH = -1, lSM = -1, lVis = null;
-  var lTx = null, cMax = 0;
-  var lCounterText = "";
-  var lCounterLowMode = false;
-  var lCounterAutoPos = null;
-  var lastRbPanel = null, lastHcPanel = null, lastBgPanel = null;
-  var panelBornAt = 0;
-
-  function sBC(c) {
-    var next = normalizeWashColor(c);
-    if (lCol !== next && rb) {
-      try { rb.style.washColor = next; lCol = next; } catch (e) { lCol = null; }
+    function tryDecodeDirectPayload(encoded, source) {
+        if (!encoded) return !1;
+        try {
+            return applyDirectSnapshot(function(raw) {
+                var parsed = null;
+                try {
+                    parsed = JSON.parse(String(raw || ""));
+                } catch (e) {
+                    return null;
+                }
+                var data = parsed && "object" == typeof parsed ? parsed.values || parsed : null;
+                if (!data || "object" != typeof data) return null;
+                var out = {};
+                for (var key in data) if (Object.prototype.hasOwnProperty.call(data, key)) {
+                    var id = Object.prototype.hasOwnProperty.call(DEFAULTS, key) ? key : HP_PERSIST_ALIAS_TO_ID[key];
+                    id && Object.prototype.hasOwnProperty.call(DEFAULTS, id) && (out[id] = coerceCfgValue(id, data[key]));
+                }
+                return out;
+            }(function(str) {
+                for (var chars = "ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789-_", lookup = {}, i = 0; i < 64; i++) lookup[chars[i]] = i;
+                function val(ch) {
+                    if (void 0 === ch) return 0;
+                    if (!Object.prototype.hasOwnProperty.call(lookup, ch)) throw new Error("bad base64url");
+                    return lookup[ch];
+                }
+                for (var bytes = [], j = 0; j < str.length; j += 4) {
+                    var c0 = val(str[j]), c1 = val(str[j + 1]), c2 = void 0 !== str[j + 2] ? val(str[j + 2]) : 0, c3 = void 0 !== str[j + 3] ? val(str[j + 3]) : 0;
+                    bytes.push(c0 << 2 | c1 >> 4), void 0 !== str[j + 2] && bytes.push((15 & c1) << 4 | c2 >> 2),
+                    void 0 !== str[j + 3] && bytes.push((3 & c2) << 6 | c3);
+                }
+                for (var out = "", k = 0; k < bytes.length; k++) {
+                    var b0 = bytes[k];
+                    if (b0 < 128) out += String.fromCharCode(b0); else if (b0 < 224) out += String.fromCharCode((31 & b0) << 6 | 63 & bytes[++k]); else {
+                        var b1 = bytes[++k], b2 = bytes[++k];
+                        out += String.fromCharCode((15 & b0) << 12 | (63 & b1) << 6 | 63 & b2);
+                    }
+                }
+                return out;
+            }(String(encoded))), source);
+        } catch (e) {}
+        return !1;
     }
-  }
-  function sUC(c) {
-    var next = normalizeWashColor(c);
-    if (!ui || !ui.IsValid()) ui = ctx.FindChildTraverse('unit_ult_ready_icon') || ctx.FindChildTraverse('ult_icon');
-    if (!ui || !ui.style) return;
-    if (lUlt !== next) {
-      try { ui.style.washColor = next; lUlt = next; } catch (e) { lUlt = null; }
+    function tryApplyDirectBootstrap() {
+        return function() {
+            var store = getSharedStore();
+            if (!store) return !1;
+            var raw = "";
+            try {
+                raw = String(store.__hpColorsCfgRaw || "");
+            } catch (e) {}
+            if (!raw || raw === sharedCfgRaw) return !1;
+            sharedCfgRaw = raw;
+            try {
+                return applyDirectSnapshot(JSON.parse(raw), "shared");
+            } catch (eParse) {}
+            return !1;
+        }() || tryDecodeDirectPayload(readSessionMirrorEncoded(), "session") || tryDecodeDirectPayload(function() {
+            try {
+                if ("undefined" == typeof GameInterfaceAPI || !GameInterfaceAPI || !GameInterfaceAPI.GetSettingString) return "";
+                var match = String(GameInterfaceAPI.GetSettingString("deadlock_hero_debuts_seen") || "").match(/\[ANITA-v1-hp_colors\]:([A-Za-z0-9_-]+)/);
+                return match ? match[1] : "";
+            } catch (e) {}
+            return "";
+        }(), "convar");
     }
-  }
-  function sTC(c) {
-    var next = normalizeWashColor(c);
-    if (!hc || !hc.style) return;
-    if (lTxt !== next) {
-      try { hc.style.washColor = next; lTxt = next; } catch (e) { lTxt = null; }
+    function requestBootstrap(reason) {
+        var now = Date.now ? Date.now() : (new Date).getTime();
+        if (!(lastBootstrapRequestAt && now - lastBootstrapRequestAt < 400)) {
+            lastBootstrapRequestAt = now;
+            try {
+                $.DispatchEvent("ClientUI_FireOutput", JSON.stringify({
+                    magic_word: "ANITA_REQUEST_BOOTSTRAP",
+                    mod_title: "HP Colors",
+                    storageNamespace: "hp_colors",
+                    reason: String(reason || "overlay_request")
+                }));
+            } catch (e) {}
+        }
     }
-  }
-
-  function getDefaultBarColor(teamId, flags) {
-    if (flags & 1) return CSS_TEAM_FRIEND_COLOR;
-    if (teamId === 2) return CSS_TEAM2_COLOR;
-    if (teamId === 1) return CSS_TEAM1_COLOR;
-    return CSS_TEAM_FRIEND_COLOR;
-  }
-
-  function isAllyPlayerFlags(flags) {
-    return !!((flags & 1) && (flags & 2));
-  }
-
-  function resetAllyBarColor(panel, teamId, flags) {
-    clearAllyPulse();
-    if (!panel || !panel.style) return;
-    var color = getDefaultBarColor(teamId | 0, flags | 0);
-    try {
-      panel.style.washColor = color;
-      lColA = normalizeWashColor(color);
-    } catch (e) {
-      lColA = null;
+    function hexToRgb(s) {
+        if (!s) return [ 255, 255, 255 ];
+        if ("#" === s.charAt(0)) {
+            var h = s.slice(1);
+            return 3 === h.length && (h = h[0] + h[0] + h[1] + h[1] + h[2] + h[2]), [ parseInt(h.slice(0, 2), 16), parseInt(h.slice(2, 4), 16), parseInt(h.slice(4, 6), 16) ];
+        }
+        var m = s.match(/rgb\((\d+),\s*(\d+),\s*(\d+)\)/);
+        return m ? [ +m[1], +m[2], +m[3] ] : [ 255, 255, 255 ];
     }
-  }
-
-  function clampNum(v, min, max, fallback) {
-    var next = Number(v);
-    if (!isFinite(next)) next = Number(fallback);
-    if (!isFinite(next)) next = 0;
-    if (isFinite(min) && next < min) next = min;
-    if (isFinite(max) && next > max) next = max;
-    return next;
-  }
-
-  function estimateCounterUnits(text) {
-    var s = String(text || "");
-    var units = 0;
-    for (var i = 0; i < s.length; i++) {
-      var ch = s.charCodeAt(i);
-      if (ch >= 48 && ch <= 57) units += 1.0;
-      else if (ch === 32) units += 0.35;
-      else if (ch === 47) units += 0.55;
-      else if (ch === 58 || ch === 46) units += 0.40;
-      else units += 0.80;
+    function byteHex(v) {
+        var h = function(v) {
+            return v = Number(v), isFinite(v) ? (v |= 0) < 0 ? 0 : v > 255 ? 255 : v : 0;
+        }(v).toString(16);
+        return 1 === h.length ? "0" + h : h;
     }
-    return units > 0 ? units : 1;
-  }
-
-  function getCounterAvailableWidth() {
-    if (hc && hc.actuallayoutwidth > 0) return hc.actuallayoutwidth;
-    if (us && us.actuallayoutwidth > 0) return us.actuallayoutwidth;
-    if (cp && cp.actuallayoutwidth > 0) return cp.actuallayoutwidth;
-    if (rb && rb.actuallayoutwidth > 0) return rb.actuallayoutwidth;
-    return 0;
-  }
-
-  // BG visibility with opacity fix - keeps panel visible for HP updates
-  function sHBV(visible) {
-    if (!bg || !bg.style) return;
-    // Always keep visibility 'visible', only change opacity
-    // This ensures HP bar width updates work even when "hidden"
-    if (lBgVis !== 'visible') { bg.style.visibility = 'visible'; lBgVis = 'visible'; }
-    var nextOp = visible ? '1.0' : '0.01';
-    if (lBgOp !== nextOp) { bg.style.opacity = nextOp; lBgOp = nextOp; }
-  }
-
-  var lKzVis = null, lKzX = null, lKzW = null, lKzColor = null;
-
-  function sKZ(show, parentWidth) {
-    if (!kz || !kz.style) return;
-    if (!show || !cfg.hp_kill_zone_enabled || parentWidth <= 0) {
-      if (lKzVis !== 'collapse') { kz.style.visibility = 'collapse'; lKzVis = 'collapse'; }
-      try { kz.style.opacity = '0'; } catch (eHide) {}
-      return;
+    function rgbToHex(c) {
+        return "#" + byteHex(c[0]) + byteHex(c[1]) + byteHex(c[2]);
     }
-
-    var threshold = cfg.hp_kill_zone_threshold | 0;
-    if (threshold < 0) threshold = 0;
-    if (threshold > 100) threshold = 100;
-    var width = cfg.hp_kill_zone_width | 0;
-    if (width < 1) width = 1;
-    if (width > 20) width = 20;
-    var pos = Math.round(parentWidth * threshold / 100 - width / 2);
-    if (pos < 0) pos = 0;
-    if (pos > parentWidth - width) pos = Math.max(0, parentWidth - width);
-    var posStr = pos + 'px';
-    var widthStr = width + 'px';
-    var color = normalizeWashColor(cfg.hp_kill_zone_color || "#FF2222");
-
-    if (lKzVis !== 'visible') { kz.style.visibility = 'visible'; lKzVis = 'visible'; }
-    try { kz.style.opacity = '0.95'; } catch (eOp) {}
-    try { kz.style.zIndex = '1000'; } catch (eZi) {}
-    if (lKzX !== posStr) { kz.style.marginLeft = posStr; lKzX = posStr; }
-    if (lKzW !== widthStr) { kz.style.width = widthStr; lKzW = widthStr; }
-    if (lKzColor !== color) {
-      try { kz.style.backgroundColor = color; lKzColor = color; } catch (eCol) { lKzColor = null; }
+    function normalizeWashColor(color) {
+        if ("string" != typeof color) return "";
+        var trimmed = color.trim();
+        if (!trimmed) return "";
+        if ("#" === trimmed.charAt(0)) return 4 === trimmed.length ? ("#" + trimmed.charAt(1) + trimmed.charAt(1) + trimmed.charAt(2) + trimmed.charAt(2) + trimmed.charAt(3) + trimmed.charAt(3)).toLowerCase() : trimmed.length >= 7 ? trimmed.slice(0, 7).toLowerCase() : trimmed;
+        var m = trimmed.match(/^rgba?\(\s*(\d+)\s*,\s*(\d+)\s*,\s*(\d+)/i);
+        return m ? rgbToHex([ m[1], m[2], m[3] ]) : trimmed;
     }
-  }
-
-  function parseCounterPositionValue(value) {
-    var x = 0;
-    var y = 200;
-    var raw = value;
-
-    if (raw && typeof raw === "object") {
-      if (Array.isArray(raw)) {
-        if (raw.length > 0) x = clampNum(raw[0], 0, 400, 0);
-        if (raw.length > 1) y = clampNum(raw[1], 0, 400, 200);
-      } else {
-        if (Object.prototype.hasOwnProperty.call(raw, "x")) x = clampNum(raw.x, 0, 400, 0);
-        if (Object.prototype.hasOwnProperty.call(raw, "y")) y = clampNum(raw.y, 0, 400, 200);
-      }
-      return { x: x, y: y };
+    function lerp(a, b, t) {
+        return a + (b - a) * t | 0;
     }
-
-    if (typeof raw === "string") {
-      var parts = raw.match(/-?\d+(?:\.\d+)?/g);
-      if (parts && parts.length > 0) {
-        x = clampNum(parts[0], 0, 400, 0);
-        if (parts.length > 1) y = clampNum(parts[1], 0, 400, 200);
-        return { x: x, y: y };
-      }
+    function ip(c1, c2, t) {
+        return [ lerp(c1[0], c2[0], t), lerp(c1[1], c2[1], t), lerp(c1[2], c2[2], t) ];
     }
-
-    if (typeof raw === "number") {
-      y = clampNum(raw, 0, 400, 200);
-      return { x: x, y: y };
+    function getHighColor() {
+        return cfg.hp_team_colors ? 2 === tid ? "#6485FC" : "#FFC961" : cfg.hp_color_high;
     }
-
-    return { x: x, y: y };
-  }
-
-  function formatCounterPositionValue(pos) {
-    var parsed = parseCounterPositionValue(pos);
-    return Math.round(parsed.x) + "," + Math.round(parsed.y);
-  }
-
-  function syncCounterPositionSetting(nextPos) {
-    var normalized = formatCounterPositionValue(nextPos);
-    if (lCounterAutoPos === normalized) return;
-    lCounterAutoPos = normalized;
-    settingsDirty = true;
-    try {
-      $.DispatchEvent("ClientUI_FireOutput", JSON.stringify({
-        magic_word: "ANITA_UPDATE",
-        mod_title: TITLE,
-        setting_id: "hp_counter_position",
-        value: normalized,
-        update_source: "hp_counter_autoposition",
-        skip_bridge_persist: true
-      }));
-    } catch (e) {}
-  }
-
-  function sHCS(lowMode, textHint) {
-    if (!hc || !hc.style) return;
-    var pulseTextMode = !!(lowMode && cfg.hp_pulse_enabled && cfg.hp_pulse_text_enabled);
-    var defaultSize = clampNum(cfg.hp_counter_size, 72, 400, 120);
-    var baseSize = pulseTextMode ? getPulseTextSize(defaultSize) : defaultSize;
-    var basePos = parseCounterPositionValue(pulseTextMode ? cfg.hp_pulse_text_position : cfg.hp_counter_position);
-    var text = String(textHint !== undefined ? textHint : lCounterText || "");
-    var available = getCounterAvailableWidth();
-    var units = estimateCounterUnits(text);
-    var fitWidthSize = available > 0 ? Math.floor((available - 12) / (units * 0.45)) : baseSize;
-    var fitSize = fitWidthSize;
-    fitSize = clampNum(fitSize, 72, 400, baseSize);
-    var size = Math.min(baseSize, fitSize);
-    var posX = clampNum(basePos.x, 0, 400, 0);
-    var posY = clampNum(basePos.y, 0, 400, 200);
-    if (!cfg.hp_bg_visible && !pulseTextMode) {
-      posX = Math.min(Math.round(baseSize * 0.025), 8);
-      posY = Math.min(Math.round(baseSize * 0.5), 150);
-      syncCounterPositionSetting({ x: posX, y: posY });
-    } else {
-      lCounterAutoPos = null;
+    function getTextColor(hp, low, high) {
+        return cfg.hp_text_color_mode ? hp <= low ? cfg.hp_text_color_low : hp <= high ? cfg.hp_text_color_mid : cfg.hp_text_color_high : hp <= low ? cfg.hp_color_low : hp <= high ? cfg.hp_color_mid : getHighColor();
     }
-    var marginLeft = (!cfg.hp_bg_visible && baseSize >= 320) ? '8%' : Math.round(posX) + '%';
-    var fontSize = size + 'px';
-    var baseHeight = 130;
-    try {
-      var hpParent = hc.GetParent ? hc.GetParent() : null;
-      if (hpParent && hpParent.actuallayoutheight > 0) baseHeight = hpParent.actuallayoutheight;
-    } catch (e) {}
-    var panelHeightPx = pulseTextMode ? Math.max(baseHeight, Math.round(size * 1.85)) : baseHeight;
-    var marginTopY = pulseTextMode ? (posY * baseHeight / panelHeightPx) : posY;
-    var marginTop = '-' + Math.round(marginTopY) + '%';
-    var panelHeight = pulseTextMode ? panelHeightPx + 'px' : '100%';
-    if (lHpSize !== fontSize) { hc.style.fontSize = fontSize; lHpSize = fontSize; }
-    if (lHpPos !== marginTop) { hc.style.marginTop = marginTop; lHpPos = marginTop; }
-    if (lHpMarginLeft !== marginLeft) { hc.style.marginLeft = marginLeft; lHpMarginLeft = marginLeft; }
-    if (lHpHeight !== panelHeight) { hc.style.height = panelHeight; lHpHeight = panelHeight; }
-  }
-
-  function resetStyleStateForNewPanels() {
-    if (rb === lastRbPanel && hc === lastHcPanel && bg === lastBgPanel) return;
-    lastRbPanel = rb;
-    lastHcPanel = hc;
-    lastBgPanel = bg;
-    panelBornAt = Date.now();
-    clearPulse();
-    lBgVis = lBgOp = lHpSize = lHpPos = lHpMarginLeft = lHpHeight = null;
-    lKzVis = lKzX = lKzW = lKzColor = null;
-    lLvVis = null;
-    lSH = -1;
-    lSM = -1;
-    lVis = null;
-    lW = -1;
-    lPW = -1;
-    lHp = -1;
-    pPct = -1;
-    sFC = 0;
-    lCounterLowMode = false;
-    settingsDirty = true;
-  }
-
-  function applyCurrentSettings(isEnemy) {
-    sHBV(!isEnemy || !!cfg.hp_bg_visible);
-    if (!isEnemy || !cfg.hp_bg_visible) sKZ(false, 0);
-    sHCS(lCounterLowMode, lCounterText);
-    settingsDirty = false;
-  }
-
-  // Decode max HP from pip label string (e.g. "|||| ..." â†’ 2000)
-  function pMax(t) {
-    if (t === lTx) return cMax;
-    lTx = t; var p = 0, q = 0, li = t.lastIndexOf('|');
-    for (var i = 0; i < t.length; i++) {
-      var c = t.charCodeAt(i);
-      if (c === 124) p++;
-      else if ((c === 34 || c === 39) && (li === -1 || i > li)) q++;
+    function getGradientTextColor(hp, low, high) {
+        var denomMid = Math.max(1, high - low), denomHigh = Math.max(1, 100 - high);
+        if (cfg.hp_text_color_mode) {
+            if (hp <= low) return cfg.hp_text_color_low;
+            if (hp <= high) {
+                var t = (hp - low) / denomMid;
+                return rgbToHex(ip(hexToRgb(cfg.hp_text_color_low), hexToRgb(cfg.hp_text_color_mid), t));
+            }
+            var t2 = (hp - high) / denomHigh;
+            return rgbToHex(ip(hexToRgb(cfg.hp_text_color_mid), hexToRgb(cfg.hp_text_color_high), t2));
+        }
+        if (hp <= low) return cfg.hp_color_low;
+        if (hp <= high) {
+            var t3 = (hp - low) / denomMid;
+            return rgbToHex(ip(hexToRgb(cfg.hp_color_low), hexToRgb(cfg.hp_color_mid), t3));
+        }
+        var t4 = (hp - high) / denomHigh, highCol = getHighColor();
+        return rgbToHex(ip(hexToRgb(cfg.hp_color_mid), hexToRgb(highCol), t4));
     }
-    cMax = p * 500 + q * 100; return cMax;
-  }
-
-  function uHT(cu, mx, lowMode) {
-    if (!hc || (cu === lSH && mx === lSM)) return;
-    if (lVis !== 'visible') { hc.style.visibility = 'visible'; lVis = 'visible'; }
-    var fmt = cfg.hp_counter_format | 0;
-    var s;
-    if (fmt === 1) {
-      var pct = mx > 0 ? Math.round(cu / mx * 100) : 0;
-      s = pct + '%';
-    } else {
-      s = cu + ' / ' + mx;
+    $.RegisterForUnhandledEvent("ClientUI_FireOutput", function(payload) {
+        try {
+            var d = "string" == typeof payload ? JSON.parse(payload) : payload;
+            if (!d || "HP Colors" !== d.mod_title) return;
+            if ("ANITA_UPDATE" === d.magic_word) {
+                if (Object.prototype.hasOwnProperty.call(DEFAULTS, d.setting_id)) {
+                    if ("hp_counter_position" === d.setting_id && "hp_counter_autoposition" === d.update_source) return;
+                    cfg[d.setting_id] = coerceCfgValue(d.setting_id, d.value), settingsDirty = !0, "hp_level_number_visible" === d.setting_id && (lLvVis = null),
+                    "hp_enabled" === d.setting_id && cfg.hp_enabled && !gRunning && (gRunning = !0,
+                    $.Schedule(.05, gL)), "hp_friend_enabled" === d.setting_id && cfg.hp_friend_enabled && !aRunning && (aRunning = !0,
+                    $.Schedule(.05, aL)), "hp_friend_enabled" !== d.setting_id || cfg.hp_friend_enabled || (isAllyPlayerFlags(aScanF2) ? resetAllyBarColor(rbA, aScanT2, aScanF2) : clearAllyPulse(),
+                    aRunning = !1), !pulse || "hp_pulse_bpm" !== d.setting_id && "hp_pulse_intensity" !== d.setting_id && "hp_pulse_text_enabled" !== d.setting_id && "hp_pulse_text_scale" !== d.setting_id || ("hp_pulse_bpm" === d.setting_id && (lPD = null,
+                    applyPulseDuration()), "hp_pulse_intensity" === d.setting_id && applyPulseIntensity(),
+                    "hp_pulse_text_enabled" !== d.setting_id && "hp_pulse_text_scale" !== d.setting_id || applyPulseTextState()),
+                    "hp_pulse_enabled" !== d.setting_id || cfg.hp_pulse_enabled || clearPulse(), writeSharedSnapshot();
+                }
+                return void (("bridge_bootstrap" === (source = String(d.update_source || "")) || "ui_resync" === source || "ui_reset" === source || "ui_code_apply" === source || "core_auto_resync" === source || "ui_refresh_after_apply" === source) && (bootstrapApplied = !0,
+                bootstrapFinished = !0, invalidateEnemyVisualState()));
+            }
+        } catch (e) {}
+        var source;
+    });
+    var ctx = $.GetContextPanel(), us = null, hc = null, bg = null, pl = null, lb = null, lbp = null, rb = null, cp = null, ui = null, kz = null, cached = 0, att = 0, lBgVis = null, lBgOp = null, lHpSize = null, lHpPos = null, lHpMarginLeft = null, lHpHeight = null;
+    function fRB() {
+        return ctx.FindChildTraverse("unit_healthbar_lagging") || ctx.FindChildTraverse("health_bar") || ctx.FindChildTraverse("unit_health");
     }
-    try { if (hc.text !== s) hc.text = s; } catch (e) { try { hc.SetAttributeString('text', s); } catch (e2) {} }
-    lCounterText = s;
-    lCounterLowMode = !!lowMode;
-    sHCS(lCounterLowMode, lCounterText);
-    lSH = cu; lSM = mx;
-  }
-
-  // â”€â”€ Poll state â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
-  var lUT = 0, lW = -1, lPW = -1, lHp = -1, pPct = -1, sFC = 0;
-
-  // â”€â”€ Main poll loop â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
-  function gL() {
-    gRunning = true;
-    try {
-      if (!cfg.hp_enabled) { clearPulse();
-        sKZ(false, 0);
-        sBC("");
-        sUC("");
-        if (bg && bg.style) { bg.style.visibility = 'collapse'; bg.style.opacity = '0'; lBgVis = 'collapse'; lBgOp = '0'; }
-        if (hc && hc.style) {
-          hc.style.fontSize = "";
-          hc.style.marginTop = "";
-          hc.style.marginLeft = "";
-          hc.style.height = "";
-          sTC("");
-          lHpSize = null;
-          lHpPos = null;
-          lHpMarginLeft = null;
-          lHpHeight = null;
+    var tid = 0, fl = 0;
+    var lCol = null, lUlt = null, lTxt = null, lSH = -1, lSM = -1, lVis = null, lTx = null, cMax = 0, lCounterText = "", lCounterLowMode = !1, lCounterAutoPos = null, lastRbPanel = null, lastHcPanel = null, lastBgPanel = null, panelBornAt = 0;
+    function sBC(c) {
+        var next = normalizeWashColor(c);
+        if (lCol !== next && rb) try {
+            rb.style.washColor = next, lCol = next;
+        } catch (e) {
+            lCol = null;
         }
-        gRunning = false; return;
-      }
-
-      var now = Date.now();
-      if (!rb) { rb = fRB(); if (!rb) { $.Schedule(0.15, gL); return; } }
-      if (!cached && !tryCache()) { $.Schedule(0.15, gL); return; }
-      resetStyleStateForNewPanels();
-      if (rb.GetParent) { var p = rb.GetParent(); if (cp !== p) cp = p; }
-
-      scan(rb); lAT = now;
-      var isEnemy = !!(fl & 1) && !(fl & 2);
-      if (cfg.hp_skip_buildings && (fl & 4)) {
-        clearPulse();
-        if (!isEnemy) {
-          sBC("");
-          sUC("");
-          sTC("");
-          sHBV(true);
-          sKZ(false, 0);
-          $.Schedule(0.5, gL);
-          return;
+    }
+    function sUC(c) {
+        var next = normalizeWashColor(c);
+        if (ui && ui.IsValid() || (ui = ctx.FindChildTraverse("unit_ult_ready_icon") || ctx.FindChildTraverse("ult_icon")),
+        ui && ui.style && lUlt !== next) try {
+            ui.style.washColor = next, lUlt = next;
+        } catch (e) {
+            lUlt = null;
         }
-
-        if (bg && bg.style) {
-          bg.style.visibility = 'collapse';
-          bg.style.opacity = '0';
-          lBgVis = 'collapse';
-          lBgOp = '0';
+    }
+    function sTC(c) {
+        var next = normalizeWashColor(c);
+        if (hc && hc.style && lTxt !== next) try {
+            hc.style.washColor = next, lTxt = next;
+        } catch (e) {
+            lTxt = null;
         }
-
-        var skipColor = CSS_TEAM_ENEMY_COLOR;
-        sBC(skipColor);
-        sUC(skipColor);
-        sTC(skipColor);
-        sHBV(!!cfg.hp_bg_visible);
-        sKZ(false, 0);
-        $.Schedule(0.5, gL);
-        return;
-      }
-      if (settingsDirty) applyCurrentSettings(isEnemy);
-      else sHBV(isEnemy && !!cfg.hp_bg_visible);
-
-      // Neutral unit
-      if (fl & 2) { clearPulse();
-        sHBV(true);
-        sKZ(false, 0);
-        sBC('#5BEFB5');
-        sTC(WHITE_WASH);
-        lUT = now;
-        $.Schedule(1.5, gL); return;
-      }
-      // Not an enemy â€” skip coloring
-      if (!(fl & 1)) { sHBV(true); sKZ(false, 0); sBC(""); sUC(""); lUT = now; $.Schedule(0.4, gL); return; }
-
-      var w = rb.actuallayoutwidth | 0;
-      var pw = cp && cp.actuallayoutwidth !== undefined ? cp.actuallayoutwidth | 0 : 0;
-
-      // No change in width â€” back off
-      if (w === lW && pw === lPW && !pulse) {
-        if (now - lUT > 2000) { $.Schedule(1, gL); return; }
-        $.Schedule(0.15, gL); return;
-      }
-      lW = w; lPW = pw; lUT = now;
-      if (pw <= 0) { $.Schedule(0.18, gL); return; }
-      sKZ(true, pw);
-
-      var hp = (w / pw * 100) | 0;
-      var low = cfg.hp_low_threshold | 0;
-      var high = cfg.hp_high_threshold | 0;
-      if (low < 0) low = 0;
-      if (low > 100) low = 100;
-      if (high < 0) high = 0;
-      if (high > 100) high = 100;
-      if (high < low) high = low;
-
-      // Small change above low threshold â€” back off
-      if (Math.abs(hp - lHp) < 3 && hp > low && lHp > low && !pulse) { $.Schedule(0.15, gL); return; }
-      var prevHp = lHp;
-      if (hp === pPct) sFC++; else { sFC = 0; pPct = hp; }
-      lHp = hp;
-
-      // Update HP counter label
-      var txt = '';
-      if (pl) { try { txt = pl.text || pl.GetAttributeString('text', '') || ''; } catch (e) { txt = ''; } }
-      if (lb && lbp) {
-        var bw = lb.actuallayoutwidth || 0, bpw = lbp.actuallayoutwidth || 0;
-        var ratio = bpw > 0 ? bw / bpw : 0;
-        var mx = pMax(txt);
-        uHT(ratio >= 0.97 ? mx : Math.round(mx * ratio), mx, hp <= low);
-      }
-
-      var sc = 0.15, cl, textCol;
-
-      var pulseThresh = cfg.hp_pulse_threshold | 0;
-      if (hp <= low) {
-        if (panelBornAt && (now - panelBornAt) < 900 && (prevHp < 0 || (prevHp <= low && hp > prevHp))) {
-          var warmupCol = getHighColor();
-          clearPulse();
-          sBC(warmupCol); sUC(warmupCol); sTC(getTextColor(100, low, high));
-          $.Schedule(0.05, gL); return;
+    }
+    function isAllyPlayerFlags(flags) {
+        return !!(1 & flags && 2 & flags);
+    }
+    function resetAllyBarColor(panel, teamId, flags) {
+        if (clearAllyPulse(), panel && panel.style) {
+            var color = function(teamId, flags) {
+                return 1 & flags ? "#FFEFD7" : 2 === teamId ? "#5B79E6" : 1 === teamId ? "#E7B659" : "#FFEFD7";
+            }(0 | teamId, 0 | flags);
+            try {
+                panel.style.washColor = color, lColA = normalizeWashColor(color);
+            } catch (e) {
+                lColA = null;
+            }
         }
-        sHBV(cfg.hp_pulse_hide_bar ? false : !!cfg.hp_bg_visible);
-        if (cfg.hp_mode === 1) {
-          cl = cfg.hp_color_low;
-          textCol = cfg.hp_text_color_mode ? cfg.hp_text_color_low : cfg.hp_color_low;
-        } else {
-          sBC(cfg.hp_color_low);
-          textCol = getTextColor(hp, low, high);
+    }
+    function clampNum(v, min, max, fallback) {
+        var next = Number(v);
+        return isFinite(next) || (next = Number(fallback)), isFinite(next) || (next = 0),
+        isFinite(min) && next < min && (next = min), isFinite(max) && next > max && (next = max),
+        next;
+    }
+    function sHBV(visible) {
+        if (bg && bg.style) {
+            "visible" !== lBgVis && (bg.style.visibility = "visible", lBgVis = "visible");
+            var nextOp = visible ? "1.0" : "0.01";
+            lBgOp !== nextOp && (bg.style.opacity = nextOp, lBgOp = nextOp);
         }
-        // Start or maintain pulse brightness animation
-        if (cfg.hp_pulse_enabled && hp <= pulseThresh) {
-          if (!pulse) startPulse();
-          updatePulseTextBrightness(now);
-          if (cfg.hp_pulse_text_enabled) sc = 0.05;
+    }
+    var lKzVis = null, lKzX = null, lKzW = null, lKzColor = null;
+    function sKZ(show, parentWidth) {
+        if (kz && kz.style) if (!show || !cfg.hp_kill_zone_enabled || parentWidth <= 0) {
+            "collapse" !== lKzVis && (kz.style.visibility = "collapse", lKzVis = "collapse");
+            try {
+                kz.style.opacity = "0";
+            } catch (eHide) {}
         } else {
-          if (pulse) clearPulse();
+            var threshold = 0 | cfg.hp_kill_zone_threshold;
+            threshold < 0 && (threshold = 0), threshold > 100 && (threshold = 100);
+            var width = 0 | cfg.hp_kill_zone_width;
+            width < 1 && (width = 1), width > 20 && (width = 20);
+            var pos = Math.round(parentWidth * threshold / 100 - width / 2);
+            pos < 0 && (pos = 0), pos > parentWidth - width && (pos = Math.max(0, parentWidth - width));
+            var posStr = pos + "px", widthStr = width + "px", color = normalizeWashColor(cfg.hp_kill_zone_color || "#FF2222");
+            "visible" !== lKzVis && (kz.style.visibility = "visible", lKzVis = "visible");
+            try {
+                kz.style.opacity = "0.95";
+            } catch (eOp) {}
+            try {
+                kz.style.zIndex = "1000";
+            } catch (eZi) {}
+            if (lKzX !== posStr && (kz.style.marginLeft = posStr, lKzX = posStr), lKzW !== widthStr && (kz.style.width = widthStr,
+            lKzW = widthStr), lKzColor !== color) try {
+                kz.style.backgroundColor = color, lKzColor = color;
+            } catch (eCol) {
+                lKzColor = null;
+            }
         }
-        sTC(textCol); sUC(cfg.hp_color_low);
-        if (cfg.hp_mode === 1) { sBC(cl); sUC(cl); }
-      } else {
-        clearPulse();
-        sHBV(!!cfg.hp_bg_visible);
-        var denomMid = Math.max(1, high - low);
-        var denomHigh = Math.max(1, 100 - high);
-        var highCol = getHighColor();
-
-        if (hp <= high) {
-          if (cfg.hp_mode === 1) {
-            cl = rgbToHex(ip(hexToRgb(cfg.hp_color_low), hexToRgb(cfg.hp_color_mid), (hp - low) / denomMid));
-            textCol = getGradientTextColor(hp, low, high);
-          } else {
-            cl = cfg.hp_color_mid;
-            textCol = getTextColor(hp, low, high);
-          }
-        } else {
-          if (cfg.hp_mode === 1) {
-            cl = rgbToHex(ip(hexToRgb(cfg.hp_color_mid), hexToRgb(highCol), (hp - high) / denomHigh));
-            textCol = getGradientTextColor(hp, low, high);
-          } else {
-            cl = highCol;
-            textCol = getTextColor(hp, low, high);
-          }
-          if (sFC >= 5) sc = Math.min(0.15 * Math.pow(2, Math.floor(sFC / 5)), 1);
+    }
+    function parseCounterPositionValue(value) {
+        var x = 0, y = 200, raw = value;
+        if (raw && "object" == typeof raw) return Array.isArray(raw) ? (raw.length > 0 && (x = clampNum(raw[0], 0, 400, 0)),
+        raw.length > 1 && (y = clampNum(raw[1], 0, 400, 200))) : (Object.prototype.hasOwnProperty.call(raw, "x") && (x = clampNum(raw.x, 0, 400, 0)),
+        Object.prototype.hasOwnProperty.call(raw, "y") && (y = clampNum(raw.y, 0, 400, 200))),
+        {
+            x: x,
+            y: y
+        };
+        if ("string" == typeof raw) {
+            var parts = raw.match(/-?\d+(?:\.\d+)?/g);
+            if (parts && parts.length > 0) return x = clampNum(parts[0], 0, 400, 0), parts.length > 1 && (y = clampNum(parts[1], 0, 400, 200)),
+            {
+                x: x,
+                y: y
+            };
         }
-        sBC(cl); sUC(cl); sTC(textCol);
-      }
-
-      $.Schedule(sc, gL);
-    } catch (e) {
-      $.Schedule(0.5, gL);
+        return "number" == typeof raw ? {
+            x: x,
+            y: y = clampNum(raw, 0, 400, 200)
+        } : {
+            x: x,
+            y: y
+        };
     }
-  }
-
-  // â”€â”€ Level tier coloring â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
-  var LT_ = [11, 19, 27, 35], LC_ = ['level_tier2', 'level_tier3', 'level_tier4', 'level_tier5'];
-  var ll = null, lc = null, wr = null, lLv = -1, lLvVis = null;
-
-  function pLv(t) { var v = 0; for (var i = 0; i < t.length; i++) { var c = t.charCodeAt(i) - 48; if (c >= 0 && c <= 9) v = v * 10 + c; } return v; }
-  function fER(p) { var c = p; while (c) { if (c.BHasClass && c.BHasClass('enemy')) return c; if (!c.GetParent) break; c = c.GetParent(); } return null; }
-
-  function sLNV() {
-    if (!ll || !ll.style) return;
-    var v = cfg.hp_level_number_visible ? 'visible' : 'collapse';
-    if (lLvVis !== v) { ll.style.visibility = v; lLvVis = v; }
-  }
-
-  function cLU() {
-    if (!ll || !ll.IsValid()) ll = ctx.FindChildTraverse('unit_level_label');
-    if (!lc || !lc.IsValid()) lc = ctx.FindChildTraverse('LevelContainer');
-    if (lc && (!wr || !wr.IsValid())) wr = fER(lc);
-    return ll && lc && wr;
-  }
-
-  function uLT() {
-    if (!cLU()) return;
-    sLNV();
-    var t = ''; try { t = ll.text || ll.GetAttributeString('text', '') || ''; } catch (e) { t = ''; }
-    if (!t || t.charCodeAt(0) === 123) return;
-    var lv = pLv(t);
-    if (lv === lLv || !lv) return;
-    lLv = lv;
-    for (var i = 0; i < 4; i++) wr.RemoveClass(LC_[i]);
-    for (var j = 3; j >= 0; j--) { if (lv >= LT_[j]) { wr.AddClass(LC_[j]); break; } }
-  }
-
-  function lL() { uLT(); $.Schedule(0.5, lL); }
-
-  // ── Ally bar loop ────────────────────────────────────────────────────────────
-  function aL() {
-    aRunning = true;
-    try {
-      if (!cfg.hp_friend_enabled) {
-        if (isAllyPlayerFlags(aScanF2)) resetAllyBarColor(rbA, aScanT2, aScanF2);
-        else clearAllyPulse();
-        aRunning = false; return;
-      }
-
-      var now = Date.now();
-      if (!rbA || !rbA.IsValid()) {
-        rbA = null; cpA = null; aScanF2 = -1; aScanAt = 0;
-        rbA = fRB();
-        if (!rbA) { $.Schedule(0.2, aL); return; }
-      }
-      if (rbA.GetParent) { var pa = rbA.GetParent(); if (cpA !== pa) cpA = pa; }
-
-      // Scan with 2000ms cache — friend/player class hierarchy is stable
-      var f2, t2;
-      if (aScanF2 < 0 || (now - aScanAt) >= 2000) {
-        t2 = 0; f2 = 0; var d2 = 0, c2 = rbA;
-        while (c2 && d2 < 10) {
-          if (c2.BHasClass) {
-            if (!t2) { if (c2.BHasClass('team2')) t2 = 2; else if (c2.BHasClass('team1')) t2 = 1; }
-            if (!(f2 & 1) && c2.BHasClass('friend')) f2 |= 1;
-            if (!(f2 & 2) && c2.BHasClass('player')) f2 |= 2;
-            if (t2 && (f2 & 3)) break;
-          }
-          if (!c2.GetParent) break;
-          c2 = c2.GetParent(); d2++;
+    function syncCounterPositionSetting(nextPos) {
+        var parsed, normalized = (parsed = parseCounterPositionValue(nextPos), Math.round(parsed.x) + "," + Math.round(parsed.y));
+        if (lCounterAutoPos !== normalized) {
+            lCounterAutoPos = normalized, settingsDirty = !0;
+            try {
+                $.DispatchEvent("ClientUI_FireOutput", JSON.stringify({
+                    magic_word: "ANITA_UPDATE",
+                    mod_title: "HP Colors",
+                    setting_id: "hp_counter_position",
+                    value: normalized,
+                    update_source: "hp_counter_autoposition",
+                    skip_bridge_persist: !0
+                }));
+            } catch (e) {}
         }
-        aScanF2 = f2; aScanT2 = t2; aScanAt = now;
-      } else {
-        f2 = aScanF2; t2 = aScanT2;
-      }
-
-      if (!isAllyPlayerFlags(f2)) {
-        clearAllyPulse();
-        lColA = null;
-        sfcA = 0;
-        $.Schedule(1.5, aL); return;
-      }
-
-      var aw = rbA.actuallayoutwidth | 0;
-      var apw = cpA && cpA.actuallayoutwidth !== undefined ? cpA.actuallayoutwidth | 0 : 0;
-      if (apw <= 0) { $.Schedule(0.2, aL); return; }
-
-      if (aw === lWA && apw === lPWA && !pulseA) {
-        sfcA++;
-        var scIdle = Math.min(0.35 * Math.pow(2, Math.floor(sfcA / 3)), 2.0);
-        $.Schedule(scIdle, aL); return;
-      }
-      sfcA = 0; lWA = aw; lPWA = apw;
-
-      var ahp = (aw / apw * 100) | 0;
-      var alow = cfg.hp_low_threshold | 0;
-      var ahigh = cfg.hp_high_threshold | 0;
-      if (alow < 0) alow = 0; if (alow > 100) alow = 100;
-      if (ahigh < 0) ahigh = 0; if (ahigh > 100) ahigh = 100;
-      if (ahigh < alow) ahigh = alow;
-
-      var apulseThresh = cfg.hp_friend_pulse_threshold | 0;
-      var inPulse = cfg.hp_friend_pulse_enabled && ahp <= apulseThresh;
-
-      // Use pulse color override when active, otherwise gradient/fixed
-      var acl;
-      if (inPulse && cfg.hp_friend_pulse_color_enabled) {
-        acl = cfg.hp_friend_pulse_color;
-      } else if (cfg.hp_mode === 1) {
-        if (ahp <= alow) acl = cfg.hp_friend_color_low;
-        else if (ahp <= ahigh) acl = rgbToHex(ip(hexToRgb(cfg.hp_friend_color_low), hexToRgb(cfg.hp_friend_color_mid), (ahp - alow) / Math.max(1, ahigh - alow)));
-        else acl = rgbToHex(ip(hexToRgb(cfg.hp_friend_color_mid), hexToRgb(cfg.hp_friend_color_high), (ahp - ahigh) / Math.max(1, 100 - ahigh)));
-      } else {
-        if (ahp <= alow) acl = cfg.hp_friend_color_low;
-        else if (ahp <= ahigh) acl = cfg.hp_friend_color_mid;
-        else acl = cfg.hp_friend_color_high;
-      }
-      var nextColA = normalizeWashColor(acl);
-      if (lColA !== nextColA && rbA) {
-        try { rbA.style.washColor = nextColA; lColA = nextColA; } catch (e) { lColA = null; }
-      }
-
-      var sc = 0.35;
-      if (inPulse) {
-        if (!pulseA) {
-          pulseA = 1; lPDA = null; lPIA = -1; lColA = null;
-          try { if (rbA) rbA.AddClass(LP); } catch (e) {}
-          var aidx = Number(cfg.hp_friend_pulse_intensity) | 0;
-          if (aidx < 0 || aidx > 2) aidx = 1;
-          lPIA = aidx;
-          var acls = PULSE_INTENSITY[aidx];
-          if (acls) { try { rbA.AddClass(acls); } catch (e) {} }
-          var abpm = Number(cfg.hp_friend_pulse_bpm) || 75;
-          if (abpm < 30) abpm = 30; if (abpm > 300) abpm = 300;
-          var adur = (60 / abpm).toFixed(3) + 's';
-          lPDA = adur;
-          try { if (rbA) rbA.style.animationDuration = adur; } catch (e) {}
+    }
+    function sHCS(lowMode, textHint) {
+        if (hc && hc.style) {
+            var fallback, pulseTextMode = !!(lowMode && cfg.hp_pulse_enabled && cfg.hp_pulse_text_enabled), defaultSize = clampNum(cfg.hp_counter_size, 72, 400, 120), baseSize = pulseTextMode ? (fallback = defaultSize,
+            clampNum(cfg.hp_pulse_text_scale, 72, 320, fallback)) : defaultSize, basePos = parseCounterPositionValue(pulseTextMode ? cfg.hp_pulse_text_position : cfg.hp_counter_position), text = String(void 0 !== textHint ? textHint : lCounterText || ""), available = hc && hc.actuallayoutwidth > 0 ? hc.actuallayoutwidth : us && us.actuallayoutwidth > 0 ? us.actuallayoutwidth : cp && cp.actuallayoutwidth > 0 ? cp.actuallayoutwidth : rb && rb.actuallayoutwidth > 0 ? rb.actuallayoutwidth : 0, units = function(text) {
+                for (var s = String(text || ""), units = 0, i = 0; i < s.length; i++) {
+                    var ch = s.charCodeAt(i);
+                    units += ch >= 48 && ch <= 57 ? 1 : 32 === ch ? .35 : 47 === ch ? .55 : 58 === ch || 46 === ch ? .4 : .8;
+                }
+                return units > 0 ? units : 1;
+            }(text), fitSize = available > 0 ? Math.floor((available - 12) / (.45 * units)) : baseSize;
+            fitSize = clampNum(fitSize, 72, 400, baseSize);
+            var size = Math.min(baseSize, fitSize), posX = clampNum(basePos.x, 0, 400, 0), posY = clampNum(basePos.y, 0, 400, 200);
+            cfg.hp_bg_visible || pulseTextMode ? lCounterAutoPos = null : syncCounterPositionSetting({
+                x: posX = Math.min(Math.round(.025 * baseSize), 8),
+                y: posY = Math.min(Math.round(.5 * baseSize), 150)
+            });
+            var marginLeft = !cfg.hp_bg_visible && baseSize >= 320 ? "8%" : Math.round(posX) + "%", fontSize = size + "px", baseHeight = 130;
+            try {
+                var hpParent = hc.GetParent ? hc.GetParent() : null;
+                hpParent && hpParent.actuallayoutheight > 0 && (baseHeight = hpParent.actuallayoutheight);
+            } catch (e) {}
+            var panelHeightPx = pulseTextMode ? Math.max(baseHeight, Math.round(1.85 * size)) : baseHeight, marginTopY = pulseTextMode ? posY * baseHeight / panelHeightPx : posY, marginTop = "-" + Math.round(marginTopY) + "%", panelHeight = pulseTextMode ? panelHeightPx + "px" : "100%";
+            lHpSize !== fontSize && (hc.style.fontSize = fontSize, lHpSize = fontSize), lHpPos !== marginTop && (hc.style.marginTop = marginTop,
+            lHpPos = marginTop), lHpMarginLeft !== marginLeft && (hc.style.marginLeft = marginLeft,
+            lHpMarginLeft = marginLeft), lHpHeight !== panelHeight && (hc.style.height = panelHeight,
+            lHpHeight = panelHeight);
         }
-        sc = 0.15;
-      } else {
-        if (pulseA) { clearAllyPulse(); lColA = null; }
-      }
-
-      $.Schedule(sc, aL);
-    } catch (e) {
-      $.Schedule(0.5, aL);
     }
-  }
-
-  gRunning = true; gL();
-  aRunning = true; aL();
-  lL();
-  $.Schedule(0.05, scheduleBootstrapRetry);
-})();
+    var lUT = 0, lW = -1, lPW = -1, lHp = -1, pPct = -1, sFC = 0;
+    function gL() {
+        gRunning = !0;
+        try {
+            if (!cfg.hp_enabled) return clearPulse(), sKZ(!1, 0), sBC(""), sUC(""), bg && bg.style && (bg.style.visibility = "collapse",
+            bg.style.opacity = "0", lBgVis = "collapse", lBgOp = "0"), hc && hc.style && (hc.style.fontSize = "",
+            hc.style.marginTop = "", hc.style.marginLeft = "", hc.style.height = "", sTC(""),
+            lHpSize = null, lHpPos = null, lHpMarginLeft = null, lHpHeight = null), void (gRunning = !1);
+            var now = Date.now();
+            if (!rb && !(rb = fRB())) return void $.Schedule(.15, gL);
+            if (!cached && !(cached || !(att >= 10) && (att++, us && us.IsValid() || (us = ctx.FindChildTraverse("UnitStatus")),
+            us && (hc && hc.IsValid() || (hc = us.FindChildTraverse("hp_counter")), bg && bg.IsValid() || (bg = us.FindChildTraverse("unit_healthbar_bg")),
+            pl && pl.IsValid() || (pl = us.FindChildTraverse("unit_healthbar_pip_label")), lb && lb.IsValid() || (lb = us.FindChildTraverse("unit_healthbar_lagging")),
+            kz && kz.IsValid() || (kz = us.FindChildTraverse("hp_kill_zone_marker")), !lb || lbp && lbp.IsValid() || (lbp = lb.GetParent()),
+            pl && lb && lbp && (cached = 1, 1))))) return void $.Schedule(.15, gL);
+            if (rb === lastRbPanel && hc === lastHcPanel && bg === lastBgPanel || (lastRbPanel = rb,
+            lastHcPanel = hc, lastBgPanel = bg, panelBornAt = Date.now(), clearPulse(), lBgVis = lBgOp = lHpSize = lHpPos = lHpMarginLeft = lHpHeight = null,
+            lKzVis = lKzX = lKzW = lKzColor = null, lLvVis = null, lSH = -1, lSM = -1, lVis = null,
+            lW = -1, lPW = -1, lHp = -1, pPct = -1, sFC = 0, lCounterLowMode = !1, settingsDirty = !0),
+            rb.GetParent) {
+                var p = rb.GetParent();
+                cp !== p && (cp = p);
+            }
+            !function(p) {
+                for (var t = 0, f = 0, d = 0, c = p; c && d < 10 && !(c.BHasClass && (t || (c.BHasClass("team2") ? t = 2 : c.BHasClass("team1") && (t = 1)),
+                1 & f || !c.BHasClass("enemy") || (f |= 1), 2 & f || !c.BHasClass("team_neutral") && !c.BHasClass("neutral") || (f |= 2),
+                4 & f || !(c.BHasClass("building") || c.BHasClass("boss_tier1") || c.BHasClass("boss_tier2") || c.BHasClass("boss_barracks")) || (f |= 4),
+                8 & f || !c.BHasClass("friend") || (f |= 8), t && 15 & f)) && c.GetParent; ) c = c.GetParent(),
+                d++;
+                tid = t, fl = f;
+            }(rb);
+            var isEnemy = !(!(1 & fl) || 2 & fl);
+            if (isEnemy && !directBootstrapApplied && now - lastDirectBootstrapAt >= 1e3 && (lastDirectBootstrapAt = now,
+            tryApplyDirectBootstrap()), cfg.hp_skip_buildings && 4 & fl) {
+                if (clearPulse(), !isEnemy) return sBC(""), sUC(""), sTC(""), sHBV(!0), sKZ(!1, 0),
+                void $.Schedule(.5, gL);
+                bg && bg.style && (bg.style.visibility = "collapse", bg.style.opacity = "0", lBgVis = "collapse",
+                lBgOp = "0");
+                return sBC("#e16161"), sUC("#e16161"), sTC("#e16161"), sHBV(!!cfg.hp_bg_visible),
+                sKZ(!1, 0), void $.Schedule(.5, gL);
+            }
+            if (isEnemy && now - lastStyleReapplyAt >= 1e3 && (lastStyleReapplyAt = now, invalidateEnemyVisualState()),
+            settingsDirty ? function(isEnemy) {
+                sHBV(!isEnemy || !!cfg.hp_bg_visible), isEnemy && cfg.hp_bg_visible || sKZ(!1, 0),
+                sHCS(lCounterLowMode, lCounterText), settingsDirty = !1;
+            }(isEnemy) : sHBV(isEnemy && !!cfg.hp_bg_visible), 2 & fl) return clearPulse(),
+            sHBV(!0), sKZ(!1, 0), sBC("#5BEFB5"), sTC("#ffffff"), lUT = now, void $.Schedule(1.5, gL);
+            if (!(1 & fl)) return sHBV(!0), sKZ(!1, 0), sBC(""), sUC(""), lUT = now, void $.Schedule(.4, gL);
+            var w = 0 | rb.actuallayoutwidth, pw = cp && void 0 !== cp.actuallayoutwidth ? 0 | cp.actuallayoutwidth : 0;
+            if (w === lW && pw === lPW && !pulse) return now - lUT > 2e3 ? void $.Schedule(1, gL) : void $.Schedule(.15, gL);
+            if (lW = w, lPW = pw, lUT = now, pw <= 0) return void $.Schedule(.18, gL);
+            sKZ(!0, pw);
+            var hp = w / pw * 100 | 0, low = 0 | cfg.hp_low_threshold, high = 0 | cfg.hp_high_threshold;
+            if (low < 0 && (low = 0), low > 100 && (low = 100), high < 0 && (high = 0), high > 100 && (high = 100),
+            high < low && (high = low), Math.abs(hp - lHp) < 3 && hp > low && lHp > low && !pulse) return void $.Schedule(.15, gL);
+            var prevHp = lHp;
+            hp === pPct ? sFC++ : (sFC = 0, pPct = hp), lHp = hp;
+            var txt = "";
+            if (pl) try {
+                txt = pl.text || pl.GetAttributeString("text", "") || "";
+            } catch (e) {
+                txt = "";
+            }
+            if (lb && lbp) {
+                var bw = lb.actuallayoutwidth || 0, bpw = lbp.actuallayoutwidth || 0, ratio = bpw > 0 ? bw / bpw : 0, mx = function(t) {
+                    if (t === lTx) return cMax;
+                    lTx = t;
+                    for (var p = 0, q = 0, li = t.lastIndexOf("|"), i = 0; i < t.length; i++) {
+                        var c = t.charCodeAt(i);
+                        124 === c ? p++ : 34 !== c && 39 !== c || !(-1 === li || i > li) || q++;
+                    }
+                    return cMax = 500 * p + 100 * q;
+                }(txt);
+                !function(cu, mx, lowMode) {
+                    if (hc && (cu !== lSH || mx !== lSM)) {
+                        var s;
+                        "visible" !== lVis && (hc.style.visibility = "visible", lVis = "visible"), s = 1 == (0 | cfg.hp_counter_format) ? (mx > 0 ? Math.round(cu / mx * 100) : 0) + "%" : cu + " / " + mx;
+                        try {
+                            hc.text !== s && (hc.text = s);
+                        } catch (e) {
+                            try {
+                                hc.SetAttributeString("text", s);
+                            } catch (e2) {}
+                        }
+                        sHCS(lCounterLowMode = !!lowMode, lCounterText = s), lSH = cu, lSM = mx;
+                    }
+                }(ratio >= .97 ? mx : Math.round(mx * ratio), mx, hp <= low);
+            }
+            var cl, textCol, sc = .15, pulseThresh = 0 | cfg.hp_pulse_threshold;
+            if (hp <= low) {
+                if (panelBornAt && now - panelBornAt < 900 && (prevHp < 0 || prevHp <= low && hp > prevHp)) {
+                    var warmupCol = getHighColor();
+                    return clearPulse(), sBC(warmupCol), sUC(warmupCol), sTC(getTextColor(100, low, high)),
+                    void $.Schedule(.05, gL);
+                }
+                sHBV(!cfg.hp_pulse_hide_bar && !!cfg.hp_bg_visible), 1 === cfg.hp_mode ? (cl = cfg.hp_color_low,
+                textCol = cfg.hp_text_color_mode ? cfg.hp_text_color_low : cfg.hp_color_low) : (sBC(cfg.hp_color_low),
+                textCol = getTextColor(hp, low, high)), cfg.hp_pulse_enabled && hp <= pulseThresh ? (pulse || function() {
+                    if (!pulse) {
+                        pulse = 1, lCol = lUlt = lTxt = null;
+                        try {
+                            rb && rb.AddClass(LP);
+                        } catch (e) {}
+                        try {
+                            ui && ui.AddClass(LP);
+                        } catch (e3) {}
+                        applyPulseIntensity(), applyPulseDuration();
+                    }
+                }(), function(now) {
+                    if (hc && hc.style && pulse && cfg.hp_pulse_text_enabled) {
+                        var bpm = clampNum(cfg.hp_pulse_bpm, 30, 300, 75), period = Math.max(1, 6e4 / bpm), phase = now % period / period, wave = phase < .5 ? 2 * phase : 2 * (1 - phase), idx = 0 | clampNum(cfg.hp_pulse_intensity, 0, 2, 1), lo = 2 === idx ? .55 : 0 === idx ? .85 : .65, next = (lo + ((2 === idx ? 2 : 0 === idx ? 1.15 : 1.5) - lo) * wave).toFixed(2);
+                        if (lTB !== next) try {
+                            hc.style.brightness = next, lTB = next;
+                        } catch (e) {
+                            lTB = null;
+                        }
+                    }
+                }(now), cfg.hp_pulse_text_enabled && (sc = .05)) : pulse && clearPulse(), sTC(textCol),
+                sUC(cfg.hp_color_low), 1 === cfg.hp_mode && (sBC(cl), sUC(cl));
+            } else {
+                clearPulse(), sHBV(!!cfg.hp_bg_visible);
+                var denomMid = Math.max(1, high - low), denomHigh = Math.max(1, 100 - high), highCol = getHighColor();
+                hp <= high ? 1 === cfg.hp_mode ? (cl = rgbToHex(ip(hexToRgb(cfg.hp_color_low), hexToRgb(cfg.hp_color_mid), (hp - low) / denomMid)),
+                textCol = getGradientTextColor(hp, low, high)) : (cl = cfg.hp_color_mid, textCol = getTextColor(hp, low, high)) : (1 === cfg.hp_mode ? (cl = rgbToHex(ip(hexToRgb(cfg.hp_color_mid), hexToRgb(highCol), (hp - high) / denomHigh)),
+                textCol = getGradientTextColor(hp, low, high)) : (cl = highCol, textCol = getTextColor(hp, low, high)),
+                sFC >= 5 && (sc = Math.min(.15 * Math.pow(2, Math.floor(sFC / 5)), 1))), sBC(cl),
+                sUC(cl), sTC(textCol);
+            }
+            $.Schedule(sc, gL);
+        } catch (e) {
+            $.Schedule(.5, gL);
+        }
+    }
+    var LT_ = [ 11, 19, 27, 35 ], LC_ = [ "level_tier2", "level_tier3", "level_tier4", "level_tier5" ], ll = null, lc = null, wr = null, lLv = -1, lLvVis = null;
+    function uLT() {
+        if (ll && ll.IsValid() || (ll = ctx.FindChildTraverse("unit_level_label")), lc && lc.IsValid() || (lc = ctx.FindChildTraverse("LevelContainer")),
+        !lc || wr && wr.IsValid() || (wr = function(p) {
+            for (var c = p; c; ) {
+                if (c.BHasClass && c.BHasClass("enemy")) return c;
+                if (!c.GetParent) break;
+                c = c.GetParent();
+            }
+            return null;
+        }(lc)), ll && lc && wr) {
+            !function() {
+                if (ll && ll.style) {
+                    var v = cfg.hp_level_number_visible ? "visible" : "collapse";
+                    lLvVis !== v && (ll.style.visibility = v, lLvVis = v);
+                }
+            }();
+            var t = "";
+            try {
+                t = ll.text || ll.GetAttributeString("text", "") || "";
+            } catch (e) {
+                t = "";
+            }
+            if (t && 123 !== t.charCodeAt(0)) {
+                var lv = function(t) {
+                    for (var v = 0, i = 0; i < t.length; i++) {
+                        var c = t.charCodeAt(i) - 48;
+                        c >= 0 && c <= 9 && (v = 10 * v + c);
+                    }
+                    return v;
+                }(t);
+                if (lv !== lLv && lv) {
+                    lLv = lv;
+                    for (var i = 0; i < 4; i++) wr.RemoveClass(LC_[i]);
+                    for (var j = 3; j >= 0; j--) if (lv >= LT_[j]) {
+                        wr.AddClass(LC_[j]);
+                        break;
+                    }
+                }
+            }
+        }
+    }
+    function aL() {
+        aRunning = !0;
+        try {
+            if (!cfg.hp_friend_enabled) return isAllyPlayerFlags(aScanF2) ? resetAllyBarColor(rbA, aScanT2, aScanF2) : clearAllyPulse(),
+            void (aRunning = !1);
+            var f2, t2, now = Date.now();
+            if (!(rbA && rbA.IsValid() || (rbA = null, cpA = null, aScanF2 = -1, aScanAt = 0,
+            rbA = fRB()))) return void $.Schedule(.2, aL);
+            if (rbA.GetParent) {
+                var pa = rbA.GetParent();
+                cpA !== pa && (cpA = pa);
+            }
+            if (aScanF2 < 0 || now - aScanAt >= 2e3) {
+                t2 = 0, f2 = 0;
+                for (var d2 = 0, c2 = rbA; c2 && d2 < 10 && !(c2.BHasClass && (t2 || (c2.BHasClass("team2") ? t2 = 2 : c2.BHasClass("team1") && (t2 = 1)),
+                1 & f2 || !c2.BHasClass("friend") || (f2 |= 1), 2 & f2 || !c2.BHasClass("player") || (f2 |= 2),
+                t2 && 3 & f2)) && c2.GetParent; ) c2 = c2.GetParent(), d2++;
+                aScanF2 = f2, aScanT2 = t2, aScanAt = now;
+            } else f2 = aScanF2, t2 = aScanT2;
+            if (!isAllyPlayerFlags(f2)) return clearAllyPulse(), lColA = null, sfcA = 0, void $.Schedule(1.5, aL);
+            var aw = 0 | rbA.actuallayoutwidth, apw = cpA && void 0 !== cpA.actuallayoutwidth ? 0 | cpA.actuallayoutwidth : 0;
+            if (apw <= 0) return void $.Schedule(.2, aL);
+            if (aw === lWA && apw === lPWA && !pulseA) {
+                sfcA++;
+                var scIdle = Math.min(.35 * Math.pow(2, Math.floor(sfcA / 3)), 2);
+                return void $.Schedule(scIdle, aL);
+            }
+            sfcA = 0, lWA = aw, lPWA = apw;
+            var ahp = aw / apw * 100 | 0, alow = 0 | cfg.hp_low_threshold, ahigh = 0 | cfg.hp_high_threshold;
+            alow < 0 && (alow = 0), alow > 100 && (alow = 100), ahigh < 0 && (ahigh = 0), ahigh > 100 && (ahigh = 100),
+            ahigh < alow && (ahigh = alow);
+            var apulseThresh = 0 | cfg.hp_friend_pulse_threshold, inPulse = cfg.hp_friend_pulse_enabled && ahp <= apulseThresh, nextColA = normalizeWashColor(inPulse && cfg.hp_friend_pulse_color_enabled ? cfg.hp_friend_pulse_color : 1 === cfg.hp_mode ? ahp <= alow ? cfg.hp_friend_color_low : rgbToHex(ahp <= ahigh ? ip(hexToRgb(cfg.hp_friend_color_low), hexToRgb(cfg.hp_friend_color_mid), (ahp - alow) / Math.max(1, ahigh - alow)) : ip(hexToRgb(cfg.hp_friend_color_mid), hexToRgb(cfg.hp_friend_color_high), (ahp - ahigh) / Math.max(1, 100 - ahigh))) : ahp <= alow ? cfg.hp_friend_color_low : ahp <= ahigh ? cfg.hp_friend_color_mid : cfg.hp_friend_color_high);
+            if (lColA !== nextColA && rbA) try {
+                rbA.style.washColor = nextColA, lColA = nextColA;
+            } catch (e) {
+                lColA = null;
+            }
+            var sc = .35;
+            if (inPulse) {
+                if (!pulseA) {
+                    pulseA = 1, lPIA = -1, lColA = null;
+                    try {
+                        rbA && rbA.AddClass(LP);
+                    } catch (e) {}
+                    var aidx = 0 | Number(cfg.hp_friend_pulse_intensity);
+                    (aidx < 0 || aidx > 2) && (aidx = 1), lPIA = aidx;
+                    var acls = PULSE_INTENSITY[aidx];
+                    if (acls) try {
+                        rbA.AddClass(acls);
+                    } catch (e) {}
+                    var abpm = Number(cfg.hp_friend_pulse_bpm) || 75;
+                    abpm < 30 && (abpm = 30), abpm > 300 && (abpm = 300);
+                    var adur = (60 / abpm).toFixed(3) + "s";
+                    0;
+                    try {
+                        rbA && (rbA.style.animationDuration = adur);
+                    } catch (e) {}
+                }
+                sc = .15;
+            } else pulseA && (clearAllyPulse(), lColA = null);
+            $.Schedule(sc, aL);
+        } catch (e) {
+            $.Schedule(.5, aL);
+        }
+    }
+    tryApplyDirectBootstrap(), gRunning = !0, gL(), aRunning = !0, aL(), function lL() {
+        uLT(), $.Schedule(.5, lL);
+    }(), $.Schedule(.05, function scheduleBootstrapRetry() {
+        if (!bootstrapApplied && !bootstrapFinished && !tryApplyDirectBootstrap()) {
+            if (bootstrapAttempts >= 8) return requestBootstrap("overlay_slow_retry"), void $.Schedule(3, scheduleBootstrapRetry);
+            requestBootstrap(1 === (bootstrapAttempts += 1) ? "overlay_startup" : "overlay_retry"),
+            $.Schedule(.5, scheduleBootstrapRetry);
+        }
+    });
+}();
\ No newline at end of file
diff --git a/hp_colors/panorama/scripts/hp_registrar.js b/hp_colors/panorama/scripts/hp_registrar.js
index 46b93e6..3f2ebac 100644
--- a/hp_colors/panorama/scripts/hp_registrar.js
+++ b/hp_colors/panorama/scripts/hp_registrar.js
@@ -1,27 +1,22 @@
 'use strict';
 (function () {
-
   var TITLE = "HP Colors";
   var REGISTER_RETRY_DELAY_SEC = 0.25;
   var REGISTER_MAX_ATTEMPTS = 24;
   var didRegister = false;
   var didRequestBootstrap = false;
   var registerAttempts = 0;
-
   var SCHEMA = [
-    // General — shared behaviour for both enemy and ally coloring
     { type: "toggle", id: "hp_enabled", label: "Enable enemy colors", defaultValue: true, category: "General" },
     { type: "cycler", id: "hp_mode", label: "Color mode", options: ["Fixed", "Gradient"], defaultValue: 1, category: "General" },
     { type: "slider", id: "hp_low_threshold", label: "Low HP threshold %", defaultValue: 25, min: 0, max: 100, step: 1, category: "General" },
     { type: "slider", id: "hp_high_threshold", label: "High HP threshold %", defaultValue: 65, min: 0, max: 100, step: 1, category: "General" },
     { type: "toggle", id: "hp_team_colors", label: "Team tint at high HP", defaultValue: false, category: "General" },
-    // Enemy Colors — bar color palette applied to enemy units
     { type: "toggle", id: "hp_bg_visible", label: "Show healthbar background", defaultValue: true, category: "Enemy Colors" },
     { type: "toggle", id: "hp_skip_buildings", label: "Skip buildings & bosses", defaultValue: false, category: "Enemy Colors" },
     { type: "colorpicker", id: "hp_color_low", label: "Low HP color", defaultValue: "#E16161", category: "Enemy Colors" },
     { type: "colorpicker", id: "hp_color_mid", label: "Mid HP color", defaultValue: "#FF7B00", category: "Enemy Colors" },
     { type: "colorpicker", id: "hp_color_high", label: "High HP color", defaultValue: "#00FF00", category: "Enemy Colors" },
-    // Enemy Pulse — low-HP pulse animation on enemy bars
     { type: "toggle", id: "hp_pulse_enabled", label: "Enable pulse", defaultValue: true, category: "Enemy Pulse" },
     { type: "slider", id: "hp_pulse_threshold", label: "Pulse below %", defaultValue: 25, min: 0, max: 100, step: 1, category: "Enemy Pulse", visibleWhen: { id: "hp_pulse_enabled", equals: true } },
     { type: "slider", id: "hp_pulse_bpm", label: "Pulse speed (BPM)", defaultValue: 75, min: 30, max: 300, step: 1, category: "Enemy Pulse", visibleWhen: { id: "hp_pulse_enabled", equals: true } },
@@ -30,7 +25,6 @@
     { type: "toggle", id: "hp_pulse_text_enabled", label: "Pulse HP counter text", defaultValue: true, category: "Enemy Pulse", visibleWhen: { id: "hp_pulse_enabled", equals: true } },
     { type: "slider", id: "hp_pulse_text_scale", label: "Pulse text size (px)", defaultValue: 120, min: 72, max: 320, step: 1, category: "Enemy Pulse", visibleWhen: { id: "hp_pulse_text_enabled", equals: true } },
     { type: "positionpicker", id: "hp_pulse_text_position", label: "Pulse text position", defaultValue: "20,196", category: "Enemy Pulse", visibleWhen: { id: "hp_pulse_text_enabled", equals: true } },
-    // Enemy Counter — HP number overlay on enemy bars
     { type: "slider", id: "hp_counter_size", label: "Counter text size (px)", defaultValue: 120, min: 72, max: 320, step: 1, category: "Enemy Counter" },
     { type: "positionpicker", id: "hp_counter_position", label: "Counter position", defaultValue: "20,196", category: "Enemy Counter" },
     { type: "cycler", id: "hp_counter_format", label: "Counter format", options: ["HP", "%"], defaultValue: 0, category: "Enemy Counter" },
@@ -39,7 +33,6 @@
     { type: "colorpicker", id: "hp_text_color_low", label: "Counter low color", defaultValue: "#E16161", visibleWhen: { id: "hp_text_color_mode", equals: 1 }, category: "Enemy Counter" },
     { type: "colorpicker", id: "hp_text_color_mid", label: "Counter mid color", defaultValue: "#FF7B00", visibleWhen: { id: "hp_text_color_mode", equals: 1 }, category: "Enemy Counter" },
     { type: "colorpicker", id: "hp_text_color_high", label: "Counter high color", defaultValue: "#FFFFFF", visibleWhen: { id: "hp_text_color_mode", equals: 1 }, category: "Enemy Counter" },
-    // Ally Bars — independent coloring for friendly player heroes
     { type: "toggle", id: "hp_friend_enabled", label: "Enable ally colors", defaultValue: false, category: "Ally Bars" },
     { type: "colorpicker", id: "hp_friend_color_low", label: "Ally low HP color", defaultValue: "#E16161", category: "Ally Bars", visibleWhen: { id: "hp_friend_enabled", equals: true } },
     { type: "colorpicker", id: "hp_friend_color_mid", label: "Ally mid HP color", defaultValue: "#FF7B00", category: "Ally Bars", visibleWhen: { id: "hp_friend_enabled", equals: true } },
@@ -55,7 +48,6 @@
     { type: "colorpicker", id: "hp_kill_zone_color", label: "Marker color", defaultValue: "#FF2222", category: "Kill Marker", visibleWhen: { id: "hp_kill_zone_enabled", equals: true } },
     { type: "slider", id: "hp_kill_zone_width", label: "Marker width", defaultValue: 3, min: 1, max: 20, step: 1, category: "Kill Marker", visibleWhen: { id: "hp_kill_zone_enabled", equals: true } }
   ];
-
   function buildConfig() {
     var elements = [];
     for (var i = 0; i < SCHEMA.length; i++) {
@@ -68,7 +60,6 @@
       }
       elements.push(element);
     }
-
     return {
       title: TITLE,
       description: "Enemy healthbar coloring",
@@ -77,7 +68,6 @@
       elements: elements
     };
   }
-
   function getRootPanel() {
     var panel = $.GetContextPanel();
     while (panel && panel.GetParent && panel.GetParent()) {
@@ -85,24 +75,20 @@
     }
     return panel;
   }
-
   function tryDirectRegister(config) {
     var root = getRootPanel();
     if (!root || !root.AnitaUI) return false;
     if (typeof root.AnitaUI.IsReady === "function" && !root.AnitaUI.IsReady()) return false;
     if (typeof root.AnitaUI.Register !== "function") return false;
-
     root.AnitaUI.Register(config);
     return true;
   }
-
   function dispatchRegister(config) {
     $.DispatchEvent("ClientUI_FireOutput", JSON.stringify({
       magic_word: "ANITA_REGISTER",
       config: config
     }));
   }
-
   function requestBootstrap(reason) {
     if (didRequestBootstrap) return;
     didRequestBootstrap = true;
@@ -113,17 +99,14 @@
       reason: String(reason || "registrar_handshake")
     }));
   }
-
   function register() {
     if (didRegister) return;
-
     var config = buildConfig();
     var usedDirect = false;
     try {
       usedDirect = tryDirectRegister(config);
     } catch (e0) {
     }
-
     if (!usedDirect) {
       try {
         dispatchRegister(config);
@@ -137,7 +120,6 @@
       }
     }
   }
-
   function queueRegisterRetry() {
     if (didRegister || registerAttempts >= REGISTER_MAX_ATTEMPTS) return;
     registerAttempts += 1;
@@ -147,7 +129,6 @@
       queueRegisterRetry();
     });
   }
-
   $.RegisterForUnhandledEvent("ClientUI_FireOutput", function (payload) {
     try {
       var data = (typeof payload === "string") ? JSON.parse(payload) : payload;
@@ -161,10 +142,8 @@
     } catch (e) {
     }
   });
-
   $.Schedule(0.05, function () {
     register();
     queueRegisterRetry();
   });
-
 })();
diff --git a/hp_colors_terser/AGENTS.md b/hp_colors_terser/AGENTS.md
index a313056..0ac765e 100644
--- a/hp_colors_terser/AGENTS.md
+++ b/hp_colors_terser/AGENTS.md
@@ -54,30 +54,13 @@ Deadlock addons folder configured in that script.
 
 ## Settings Keys
 Persisted schema keys:
-- `hp_enabled`
-- `hp_mode` (0=Fixed, 1=Gradient)
-- `hp_low_threshold`
-- `hp_high_threshold`
-- `hp_bg_visible`
-- `hp_team_colors`
-- `hp_color_low`
-- `hp_color_mid`
-- `hp_color_high`
-- `hp_counter_size`
-- `hp_counter_position`
-- `hp_text_color_mode` (0=By HP % using bar colors, 1=Custom text colors)
-- `hp_text_color_low`
-- `hp_text_color_mid`
-- `hp_text_color_high`
-- `hp_pulse_bpm` (30–300, default 75 — pulse animation speed in BPM)
-- `hp_pulse_intensity` (0=Subtle, 1=Medium, 2=Intense, default 1 — CSS keyframe preset)
-- `hp_pulse_enabled` (bool, default true — toggle pulse animation on/off)
-- `hp_pulse_bg_mode` (0=Follow behavior, 1=Show while low, 2=Hide while low, default 0)
-- `hp_pulse_text_enabled` (bool, default true — toggle HP counter text pulse)
-- `hp_pulse_text_scale` (100–200, default 108 — pulse text scale %)
+- General: `hp_enabled`, `hp_mode`, `hp_low_threshold`, `hp_high_threshold`, `hp_bg_visible`, `hp_team_colors`
+- Enemy Colors: `hp_color_low`, `hp_color_mid`, `hp_color_high`, `hp_skip_buildings`
+- Enemy Pulse: `hp_pulse_enabled`, `hp_pulse_threshold`, `hp_pulse_bpm`, `hp_pulse_intensity`, `hp_pulse_hide_bar`, `hp_pulse_text_enabled`, `hp_pulse_text_scale`, `hp_pulse_text_position`
+- Enemy Counter: `hp_counter_size`, `hp_counter_position`, `hp_text_color_mode`, `hp_text_color_low`, `hp_text_color_mid`, `hp_text_color_high`
+- Ally Bars: `hp_friend_enabled`, `hp_friend_color_low`, `hp_friend_color_mid`, `hp_friend_color_high`, `hp_friend_pulse_enabled`, `hp_friend_pulse_threshold`, `hp_friend_pulse_bpm`, `hp_friend_pulse_intensity`, `hp_friend_pulse_color_enabled`, `hp_friend_pulse_color`
 
-`hp_npc_poll_slow` was removed — polling is now automatic and adaptive.
-`darkOf` was removed — low-HP pulsing is now CSS keyframe-driven with `hp_pulse_bpm` and `hp_pulse_intensity` control.
+`hp_pulse_bg_mode` and `hp_npc_poll_slow` are removed. Low-HP pulsing follows the main behavior setting, and polling is automatic/adaptive.
 
 If you add/remove a persisted setting, update all schema/default/alias maps
 together in:
@@ -86,7 +69,7 @@ together in:
 - `healthbar_logic.js`
 - `hp_registrar.js`
 
-Also bump the registrar `storageVersion` when compatibility requires it (currently 10).
+Also bump the registrar `storageVersion` when compatibility requires it (currently 18).
 
 ## Persistence Model
 - Storage namespace: `hp_colors`
@@ -114,9 +97,8 @@ Below is the full mapping so you know what each name actually does.
 | `TEAM1_HIGH` | — | Team 1 high-HP color `#FFC961` |
 | `TEAM2_HIGH` | — | Team 2 high-HP color `#6485FC` |
 | `WHITE_WASH` | — | Default white wash color `#ffffff` |
-| `LP` | `LOW_PULSE_CLASS` | CSS class `low_hp_bar_pulse` |
-| `LTX` | `LOW_TEXT_CLASS` | CSS class `low_hp_text_large` |
-| `LS` | `LOW_ULT_CLASS` | CSS class `low_hp_ult_static` |
+| `LP` | `LOW_PULSE_CLASS` | CSS class `low_hp_pulsing` |
+| `PULSE_INTENSITY` | `PULSE_ANIMATIONS` | Keyframe name array `[subtle, none, intense]` |
 | `BOOTSTRAP_NAMESPACE` | — | Bootstrap namespace `"hp_colors"` |
 
 ### Panel Cache Variables
@@ -161,7 +143,7 @@ Below is the full mapping so you know what each name actually does.
 | Short | Full | Purpose |
 |-------|------|---------|
 | `tid` | `teamId` | Detected team (1 or 2) |
-| `fl` | `flags` | Bit flags: 1=enemy, 2=neutral |
+| `fl` | `flags` | Bit flags: 1=enemy, 2=neutral, 4=building/boss, 8=friend |
 
 ### Pulse & Gradient
 | Short | Full | Purpose |
@@ -224,6 +206,8 @@ Below is the full mapping so you know what each name actually does.
 - `healthbar_logic.js` scans up the panel ancestry to classify unit panels.
 - Enemy = `enemy` class and not neutral.
 - Neutral units are intentionally colored green (`#5BEFB5`).
+- `hp_skip_buildings` only affects enemy buildings/bosses; allied buildings short-circuit and keep their current visual state.
+- Ally toggle changes now refresh dependent rows immediately.
 - Polling is automatic and adaptive: fast for heroes, backs off for stable HP.
 - Low HP uses CSS keyframe pulse animation; polling follows normal 0.15s cadence. No JS timer needed for pulse — GPU handles it.
 - Pulse text uses inline `preTransformScale2d` (not CSS font-size/margin changes) to avoid layout shift during pulse animation.
@@ -255,4 +239,4 @@ After behavior edits:
 2. Launch Deadlock with `-dev -tools`.
 3. Open Panorama debugger (`F7`) or VConsole (`F8`).
 4. Verify Anita UI registers HP Colors, settings persist, Copy/Import works when
-   needed, and enemy healthbars update without script errors.
\ No newline at end of file
+   needed, and enemy healthbars update without script errors.
diff --git a/hp_colors_terser/CLAUDE.md b/hp_colors_terser/CLAUDE.md
index 59f1c7b..e54a193 100644
--- a/hp_colors_terser/CLAUDE.md
+++ b/hp_colors_terser/CLAUDE.md
@@ -27,7 +27,7 @@ Script to XML context mapping:
 |---|---|---|
 | `anita_ui_core.js` | `panorama/layout/base_hud.xml` | Creates the Anita-UI window, overlay button, event listener, registration handling, persistence helpers, and value replay. |
 | `hp_registrar.js` | `panorama/layout/base_hud.xml` | Builds the HP Colors config schema, registers it, listens for handshake, and requests bootstrap. |
-| `anita_persist_loader.js` | `panorama/layout/hud_escape_menu.xml` | Captures the config, reads persisted payloads, mirrors session state, and replays stored values. |
+| `anita_persist_loader.js` | `panorama/layout/hud.xml` | Captures the config, reads persisted payloads, mirrors session state, and replays stored values. |
 | `healthbar_logic.js` | `panorama/layout/unit_status_overlay.xml` | Consumes `ANITA_UPDATE`, bootstraps overlay state, scans panel ancestry, and applies live healthbar/counter styling. |
 
 Event flow:
@@ -73,24 +73,37 @@ These are the keys in `DEFAULTS` inside `healthbar_logic.js`:
 | `hp_enabled` | bool | `true` |
 | `hp_mode` | int | `1` |
 | `hp_low_threshold` | int | `25` |
+| `hp_high_threshold` | int | `65` |
 | `hp_bg_visible` | bool | `true` |
+| `hp_team_colors` | bool | `false` |
+| `hp_color_low` | string | `"#E16161"` |
+| `hp_color_mid` | string | `"#FF7B00"` |
+| `hp_color_high` | string | `"#00FF00"` |
 | `hp_counter_size` | int | `120` |
 | `hp_counter_position` | string | `"20,196"` |
 | `hp_text_color_mode` | int | `0` |
 | `hp_text_color_low` | string | `"#E16161"` |
 | `hp_text_color_mid` | string | `"#FF7B00"` |
 | `hp_text_color_high` | string | `"#FFFFFF"` |
-| `hp_high_threshold` | int | `65` |
-| `hp_color_low` | string | `"#E16161"` |
-| `hp_color_mid` | string | `"#FF7B00"` |
-| `hp_color_high` | string | `"#00FF00"` |
-| `hp_team_colors` | bool | `false` |
+| `hp_pulse_enabled` | bool | `true` |
+| `hp_pulse_threshold` | int | `25` |
 | `hp_pulse_bpm` | int | `75` |
 | `hp_pulse_intensity` | int | `1` |
-| `hp_pulse_enabled` | bool | `true` |
-| `hp_pulse_bg_mode` | int | `0` |
+| `hp_pulse_hide_bar` | bool | `false` |
 | `hp_pulse_text_enabled` | bool | `true` |
-| `hp_pulse_text_scale` | int | `108` |
+| `hp_pulse_text_scale` | int | `120` |
+| `hp_pulse_text_position` | string | `"20,196"` |
+| `hp_skip_buildings` | bool | `false` |
+| `hp_friend_enabled` | bool | `false` |
+| `hp_friend_color_low` | string | `"#E16161"` |
+| `hp_friend_color_mid` | string | `"#FF7B00"` |
+| `hp_friend_color_high` | string | `"#00FF00"` |
+| `hp_friend_pulse_enabled` | bool | `false` |
+| `hp_friend_pulse_threshold` | int | `25` |
+| `hp_friend_pulse_bpm` | int | `75` |
+| `hp_friend_pulse_intensity` | int | `1` |
+| `hp_friend_pulse_color_enabled` | bool | `false` |
+| `hp_friend_pulse_color` | string | `"#FF2222"` |
 
 ## 5. PERSISTENCE STACK
 
@@ -149,16 +162,29 @@ Payloads are stored as `{ v: <storageVersion>, c: 1, values: { <alias>: <value>
 | `tl` | `hp_text_color_low` |
 | `ti` | `hp_text_color_mid` |
 | `th` | `hp_text_color_high` |
+| `pe` | `hp_pulse_enabled` |
+| `pt` | `hp_pulse_threshold` |
 | `bp` | `hp_pulse_bpm` |
 | `pi` | `hp_pulse_intensity` |
-| `pe` | `hp_pulse_enabled` |
-| `pbm` | `hp_pulse_bg_mode` |
+| `phb` | `hp_pulse_hide_bar` |
 | `pte` | `hp_pulse_text_enabled` |
 | `pts` | `hp_pulse_text_scale` |
+| `ptp` | `hp_pulse_text_position` |
+| `sb` | `hp_skip_buildings` |
+| `fe` | `hp_friend_enabled` |
+| `fcl` | `hp_friend_color_low` |
+| `fcm` | `hp_friend_color_mid` |
+| `fch` | `hp_friend_color_high` |
+| `fpe` | `hp_friend_pulse_enabled` |
+| `fpt` | `hp_friend_pulse_threshold` |
+| `fpb` | `hp_friend_pulse_bpm` |
+| `fpi` | `hp_friend_pulse_intensity` |
+| `fpce` | `hp_friend_pulse_color_enabled` |
+| `fpc` | `hp_friend_pulse_color` |
 
 ## 7. storageVersion
 
-`hp_registrar.js` sets `storageVersion: 10` in `buildConfig()`. Bump this whenever the schema gains or removes a persisted key, and keep it in sync across all three alias maps.
+`hp_registrar.js` sets `storageVersion: 18` in `buildConfig()`. Bump this whenever the schema gains or removes a persisted key, and keep it in sync across all three alias maps.
 
 ## 8. POLLING CADENCE TIERS
 
@@ -166,22 +192,18 @@ Payloads are stored as `{ v: <storageVersion>, c: 1, values: { <alias>: <value>
 
 | Condition | Next schedule |
 |---|---|
-| `hp_enabled === false` | `1.0` |
-| Root bar not found yet | `0.2` |
-| Panel cache not ready yet | `0.2` |
-| Neutral target (`fl & 2`) | `3.0` |
-| Non-enemy target (`!(fl & 1)`) | `1.5` |
-| **Building** (`fl & 16`) — width stable | `4.0` (flat) |
-| **Building** (`fl & 16`) — high-HP stable | `4.0` (flat) |
-| **Minion/Boss** (`!isHeroEnemy`) — width stable >800 ms | `2.5` |
-| **Hero** or minion/boss — width stable >1200 ms | `1.25` |
-| Width unchanged but within hot window | `0.2` |
-| Parent width `<= 0` | `0.25` |
-| HP change under `3` while above low threshold | `0.22` |
-| Low HP (any mode) — CSS keyframe pulse active | `0.15` (GPU-driven animation; no JS timer) |
+| `hp_enabled === false` | loop stops (`gRunning=false`, no reschedule) |
+| Root bar not found yet | `0.15` |
+| Panel cache not ready yet | `0.15` |
+| skip_buildings + `fl & 4` (building/boss) | `0.5` |
+| Neutral target (`fl & 2`) | `1.5` |
+| Non-enemy target (`!(fl & 1)`) | `0.4` |
+| Parent width `<= 0` | `0.18` |
+| Width unchanged, no pulse, age > 2000 ms | `1.0` |
+| Width unchanged, no pulse, age ≤ 2000 ms | `0.15` |
+| Low HP — pulse text enabled | `0.05` |
 | Default active cadence | `0.15` |
-| **Hero** stable high-HP (`sFC >= 5`) | `min(0.15 × 2^⌊sFC/5⌋, 3.0)` |
-| **Minion/Boss** stable high-HP (`sFC >= 3`) | `min(0.30 × 2^⌊sFC/3⌋, 3.0)` |
+| Stable above high-HP (`sFC >= 5`) | `min(0.15 × 2^⌊sFC/5⌋, 1.0)` |
 | Error recovery path | `0.5` |
 
 Other fixed loops in the same file:
@@ -198,28 +220,25 @@ Other fixed loops in the same file:
 |---|---|---|---|
 | FLAG_ENEMY | `1` | `enemy` | Unit is on the enemy team |
 | FLAG_NEUTRAL | `2` | `team_neutral` or `neutral` | Neutral/jungle unit |
-| FLAG_HERO | `4` | `player` | Player-controlled hero |
-| FLAG_CREATURE | `8` | `creature` | NPC unit (boss, creep, trooper) |
-| FLAG_BUILDING | `16` | `building` | Tower or other structure |
+| FLAG_BUILDING | `4` | `building`, `boss_tier1`, `boss_tier2`, `boss_barracks` | Building or boss — triggers skip_buildings logic |
+| FLAG_FRIEND | `8` | `friend` | Friendly unit — enables early scan break |
 
 Confirmed Deadlock class names from in-game inspection:
-- Player hero panel: `"alive player team2 enemy WorldUIRoot"`
 - Neutral trooper: `"alive creature WorldUIRoot team_neutral trooper_neutral"`
 - Boss: `"alive creature team2 enemy WorldUIRoot boss_tier2"`
 - Building/Tower: `"alive creature team2 enemy WorldUIRoot building"`
 
 Derived flags:
 - `isEnemy = !!(fl & 1) && !(fl & 2)`
-- `isHeroEnemy = isEnemy && !!(fl & 4)`
-- `isBuilding = isEnemy && !!(fl & 16)`
+- `isBuilding = !!(fl & 4)` — checked as `if (cfg.hp_skip_buildings && (fl & 4))`
 
-Early-break rules: scan stops as soon as `team_neutral` (`fl & 2`) or `building` (`fl & 16`) is confirmed with a team ID. Enemy-hero panels keep scanning to find the `player` class.
+Early-break rules: scan breaks when team ID is set AND any of bits `1|2|4|8` confirmed — `if (t && (f & (1|2|4|8))) break`. The `player` class is NOT scanned by `scan()`; the ally loop `aL()` has its own inline scan checking `friend` (bit 1) and `player` (bit 2) in separate vars `f2`/`aScanF2`.
 
 ## 10. SCAN CACHE
 
 - Ancestor scan max depth: `10` levels
-- Rescan TTL for **creature units** (`fl & 8`): `2000` ms — their class hierarchy never changes mid-game
-- Rescan TTL for **heroes / unknown**: `250` ms
+- `scan()` (used by `gL()`) runs every tick — no TTL cache
+- Ally scan TTL (inline in `aL()`): `2000` ms — friend+player class hierarchy is stable mid-game; uses `aScanF2`/`aScanT2`/`aScanAt`
 - `resetScanCache()` clears `scanNextAt` back to `0` and nulls the cached ancestor array
 
 `ensureScanState(now)` rescans when the cached ancestor chain changes or the TTL expires.
@@ -228,8 +247,8 @@ Early-break rules: scan stops as soon as `team_neutral` (`fl & 2`) or `building`
 
 | File | Debug flags actually present |
 |---|---|
-| `healthbar_logic.js` | `DEV_LOG` (set to `false` in production) |
-| `hp_registrar.js` | `DEBUG_LOG` (set to `false`; gates the `log()` helper) |
+| `healthbar_logic.js` | None |
+| `hp_registrar.js` | None |
 | `anita_ui_core.js` | No debug flags in production build |
 | `anita_persist_loader.js` | No debug flags in production build |
 | `anita_ui.css` | None |
diff --git a/hp_colors_terser/panorama/scripts/anita_persist_loader.js b/hp_colors_terser/panorama/scripts/anita_persist_loader.js
index 00c9a56..f21366e 100644
--- a/hp_colors_terser/panorama/scripts/anita_persist_loader.js
+++ b/hp_colors_terser/panorama/scripts/anita_persist_loader.js
@@ -1 +1 @@
-"use strict";!function(){var e,r="HP Colors",t="hp_colors",n="anita_v1_hp_colors",a={hp_enabled:"e",hp_mode:"m",hp_low_threshold:"l",hp_high_threshold:"h",hp_bg_visible:"b",hp_team_colors:"t",hp_color_low:"cl",hp_color_mid:"cm",hp_color_high:"ch",hp_counter_size:"s",hp_counter_position:"p",hp_text_color_mode:"tm",hp_level_number_visible:"lnv",hp_text_color_low:"tl",hp_text_color_mid:"ti",hp_text_color_high:"th",hp_pulse_bpm:"bp",hp_pulse_intensity:"pi",hp_pulse_enabled:"pe",hp_pulse_text_enabled:"pte",hp_pulse_text_scale:"pts",hp_pulse_text_position:"ptp",hp_pulse_hide_bar:"phb",hp_skip_buildings:"sb",hp_pulse_threshold:"pt",hp_friend_enabled:"fe",hp_friend_pulse_enabled:"fpe",hp_friend_pulse_bpm:"fpb",hp_friend_pulse_intensity:"fpi",hp_friend_pulse_threshold:"fpt",hp_friend_color_low:"fcl",hp_friend_color_mid:"fcm",hp_friend_color_high:"fch",hp_friend_pulse_color_enabled:"fpce",hp_friend_pulse_color:"fpc",hp_kill_zone_enabled:"kze",hp_kill_zone_threshold:"kzt",hp_kill_zone_color:"kzc",hp_kill_zone_width:"kzw"},i=function(){var e={};for(var r in a)Object.prototype.hasOwnProperty.call(a,r)&&(e[a[r]]=r);return e}(),o=null,l=null,u=null,s="",p="",c=null,f=0,h=0,_="",d=(e="ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789-_",{encode:function(r){for(var t=[],n=0;n<r.length;n++){var a=r.charCodeAt(n);a<128?t.push(a):a<2048?(t.push(192|a>>6),t.push(128|63&a)):(t.push(224|a>>12),t.push(128|a>>6&63),t.push(128|63&a))}for(var i="",o=0;o<t.length;o+=3){var l=t[o],u=t[o+1]||0,s=t[o+2]||0;i+=e[l>>2],i+=e[(3&l)<<4|u>>4],i+=o+1<t.length?e[(15&u)<<2|s>>6]:"",i+=o+2<t.length?e[63&s]:""}return i},decode:function(r){for(var t={},n=0;n<64;n++)t[e[n]]=n;function getVal(e){if(void 0===e)return 0;if(!Object.prototype.hasOwnProperty.call(t,e))throw new Error("Invalid base64url char: "+e);return t[e]}for(var a=[],i=0;i<r.length;i+=4){var o=getVal(r[i]),l=getVal(r[i+1]),u=void 0!==r[i+2]?getVal(r[i+2]):0,s=void 0!==r[i+3]?getVal(r[i+3]):0;a.push(o<<2|l>>4),void 0!==r[i+2]&&a.push((15&l)<<4|u>>2),void 0!==r[i+3]&&a.push((3&u)<<6|s)}for(var p="",c=0;c<a.length;c++){var f=a[c];if(f<128)p+=String.fromCharCode(f);else if(f<224)p+=String.fromCharCode((31&f)<<6|63&a[++c]);else{var h=a[++c],_=a[++c];p+=String.fromCharCode((15&f)<<12|(63&h)<<6|63&_)}}return p}});function normalizeNamespace(e){return String(e||"").toLowerCase().replace(/[^a-z0-9_]+/g,"_").replace(/^_+|_+$/g,"")}function hasPersistentStorage(){try{return $.persistentStorage.setItem("_anita_probe","1"),$.persistentStorage.removeItem("_anita_probe"),storageSupportLogged||(storageSupportLogged=!0),!0}catch(e){return storageSupportLogged||(storageSupportLogged=!0),!1}}function cloneValues(e){var r={};for(var t in e)Object.prototype.hasOwnProperty.call(e,t)&&(r[t]=e[t]);return r}function findElement(e){if(!o||!Array.isArray(o.elements))return null;for(var r=0;r<o.elements.length;r++){var t=o.elements[r];if(t&&t.id===e)return t}return null}function sanitizeValue(e,r){if(!e)return r;var t=e.defaultValue,n=String(e.type||"");if("positionpicker"===n){var a=0,i=200,o=r;if(o&&"object"==typeof o)Array.isArray(o)?(o.length>0&&(a=Number(o[0])),o.length>1&&(i=Number(o[1]))):(Object.prototype.hasOwnProperty.call(o,"x")&&(a=Number(o.x)),Object.prototype.hasOwnProperty.call(o,"y")&&(i=Number(o.y)));else if("string"==typeof o){var l=o.match(/-?\d+(?:\.\d+)?/g);l&&l.length>0&&(a=Number(l[0]),l.length>1&&(i=Number(l[1])))}else"number"==typeof o&&(i=Number(o));return isFinite(a)||(a=0),isFinite(i)||(i=200),a<0&&(a=0),i<0&&(i=0),a>400&&(a=400),i>400&&(i=400),Math.round(a)+","+Math.round(i)}if("toggle"===n){if(!0===r||!1===r)return r;if(1===r||"1"===r)return!0;if(0===r||"0"===r)return!1;if("string"==typeof r){var u=r.toLowerCase();if("true"===u)return!0;if("false"===u)return!1}return!!t}if("cycler"===n){var s=Array.isArray(e.options)?e.options.length:0,p=Number(r);if(isFinite(p)||(p=Number(t)),isFinite(p)||(p=0),(p=Math.round(p))<0&&(p=0),s>0&&p>=s){var c=Number(t);(!isFinite(c)||c<0||c>=s)&&(c=0),p=c}return p}if("slider"===n||"stepper"===n){var f=Number(r);isFinite(f)||(f=Number(t)),isFinite(f)||(f=0);var h=Number(e.step);isFinite(h)&&0!==h||(h=1);var _=isFinite(Number(e.min))?Number(e.min):-1/0,d=isFinite(Number(e.max))?Number(e.max):1/0;return f<_&&(f=_),f>d&&(f=d),Math.round(h)===h?Math.round(f):parseFloat(f.toFixed(2))}return"colorpicker"===n?"string"==typeof r&&r.length>0?r:"string"==typeof t&&t.length>0?t:"#FFFFFF":void 0!==r?r:t}function buildDefaultValues(e){for(var r={},t=e&&Array.isArray(e.elements)?e.elements:[],n=0;n<t.length;n++){var a=t[n];a&&a.id&&"button"!==a.type&&(r[a.id]=sanitizeValue(a,a.defaultValue))}return r}function mergeWithDefaults(e){var r=buildDefaultValues(o);for(var t in e)if(Object.prototype.hasOwnProperty.call(e,t)){var n=findElement(t);n&&(r[t]=sanitizeValue(n,e[t]))}return r}function parseStoredPayload(e,r){var t=String(e||"");if(!t)return null;var n=null;try{n=JSON.parse(t)}catch(e){return null}if(!n||"object"!=typeof n||!n.values||"object"!=typeof n.values)return null;o&&n.v&&(Number(n.v),Number(o.storageVersion));var a=n.values,l={};if(1===n.c||!0===n.compact){var u={};for(var s in a)if(Object.prototype.hasOwnProperty.call(a,s)){var p=i[s];p&&(u[p]=a[s])}l=mergeWithDefaults(u)}else l=mergeWithDefaults(a);return{raw:t,values:l}}function writeSessionMirror(e){var r=function(){for(var e=$.GetContextPanel();e&&e.GetParent&&e.GetParent();)e=e.GetParent();return e||null}();if(r){try{r.SetAttributeString&&r.SetAttributeString(n,e)}catch(e){}try{var t=r.FindChildTraverse?r.FindChildTraverse("Hud"):null;t&&t.SetAttributeString&&t.SetAttributeString(n,e)}catch(e){}}}function cachePayload(e,r,t,n){p=String(e||""),s=String(r||""),c=cloneValues(t||{}),l=cloneValues(t||{}),u=cloneValues(n||t||{}),writeSessionMirror(s)}function replayValues(e,t){if(e)for(var n in e)Object.prototype.hasOwnProperty.call(e,n)&&$.DispatchEvent("ClientUI_FireOutput",JSON.stringify({magic_word:"ANITA_UPDATE",mod_title:r,setting_id:n,value:e[n],update_source:"bridge_bootstrap",skip_bridge_persist:!0}))}function scheduleBootstrap(e){_=String(e||"request");var r=++h;$.Schedule(0,function(){r===h&&function(e){if(o){var r=function(){if(p&&s&&c)return{raw:p,encoded:s,values:cloneValues(c),source:"cache"};if(!hasPersistentStorage()){if("undefined"==typeof GameInterfaceAPI||!GameInterfaceAPI||"function"!=typeof GameInterfaceAPI.GetSettingString)return null;var e="";try{e=String(GameInterfaceAPI.GetSettingString("deadlock_hero_debuts_seen")||"")}catch(e){return null}var r=e.match(/\[ANITA-v1-hp_colors\]:([A-Za-z0-9_-]+)/);if(!r)return null;var t=r[1],a="";try{a=d.decode(t)}catch(e){return null}var i=parseStoredPayload(a);return i?(cachePayload(i.raw,t,i.values,i.values),{raw:i.raw,encoded:t,values:cloneValues(i.values),source:"convar"}):null}var o="";try{o=String($.persistentStorage.getItem(n)||"")}catch(e){return null}if(!o)return null;var l="";try{l=d.decode(o)}catch(e){return null}var u=parseStoredPayload(l);return u?(cachePayload(u.raw,o,u.values,u.values),{raw:u.raw,encoded:o,values:cloneValues(u.values),source:"persistentStorage"}):null}();if(r)return l=cloneValues(r.values),u=cloneValues(r.values),writeSessionMirror(r.encoded),void replayValues(r.values,r.source);var t=l||u;t&&replayValues(t)}else _=e}(_)})}$.RegisterForUnhandledEvent("ClientUI_FireOutput",function(e){try{var i="string"==typeof e?JSON.parse(e):e;if(!i)return;if("ANITA_REGISTER"===i.magic_word)return void(function(e){if(!e||String(e.title||"")!==r)return!1;if(normalizeNamespace(e.storageNamespace)!==t)return!1;for(var n={title:r,storageNamespace:t,storageVersion:Math.max(1,Math.floor(Number(e.storageVersion)||1)),elements:[]},a=Array.isArray(e.elements)?e.elements:[],i=0;i<a.length;i++){var s=a[i];s&&s.id&&n.elements.push({id:s.id,type:s.type,defaultValue:s.defaultValue,options:Array.isArray(s.options)?s.options.slice(0):null,step:s.step})}return o=n,l||(l=buildDefaultValues(o)),u||(u=cloneValues(l)),!0}(i.config)&&_&&scheduleBootstrap(_));if("ANITA_REQUEST_BOOTSTRAP"===i.magic_word){if(i.mod_title!==r)return;if(normalizeNamespace(i.storageNamespace)!==t)return;return void scheduleBootstrap(String(i.reason||"request"))}if("ANITA_UPDATE"!==i.magic_word||i.mod_title!==r)return;if(!o||!i.setting_id)return;var p=findElement(i.setting_id);if(!p)return;l||(l=buildDefaultValues(o)),u||(u=cloneValues(l));var c=String(i.update_source||"ui_update"),h="core_auto_resync"===c||"ui_resync"===c;if(l[i.setting_id]=sanitizeValue(p,i.value),h||(u[i.setting_id]=sanitizeValue(p,i.value)),i.skip_bridge_persist||"bridge_bootstrap"===c||h)return;String(i.update_source||"ui_update"),g=!!i.force_persist,v=++f,$.Schedule(g?0:.35,function(){v===f&&function(e,r){if(!o)return!1;l||(l=buildDefaultValues(o));var t=function(){if(!o)return"";var e=u||l||buildDefaultValues(o),r=buildDefaultValues(o),t={};for(var n in e)Object.prototype.hasOwnProperty.call(e,n)&&Object.prototype.hasOwnProperty.call(r,n)&&e[n]!==r[n]&&(t[a[n]||n]=e[n]);return JSON.stringify({v:Math.max(1,Math.floor(Number(o.storageVersion)||1)),c:1,values:t})}();if(!t)return!1;var i="";try{i=d.encode(t)}catch(e){return!1}if(!r&&i===s)return writeSessionMirror(i),!1;if(cachePayload(t,i,l,u),!hasPersistentStorage())return!1;try{return $.persistentStorage.setItem(n,i),!0}catch(e){return!1}}(0,!!g)})}catch(e){}var g,v}),hasPersistentStorage()}();
\ No newline at end of file
+!function(){"use strict";var e,r="HP Colors",t="anita_v1_hp_colors",n={hp_enabled:"e",hp_mode:"m",hp_low_threshold:"l",hp_high_threshold:"h",hp_bg_visible:"b",hp_team_colors:"t",hp_color_low:"cl",hp_color_mid:"cm",hp_color_high:"ch",hp_counter_size:"s",hp_counter_position:"p",hp_text_color_mode:"tm",hp_level_number_visible:"lnv",hp_text_color_low:"tl",hp_text_color_mid:"ti",hp_text_color_high:"th",hp_pulse_bpm:"bp",hp_pulse_intensity:"pi",hp_pulse_enabled:"pe",hp_pulse_text_enabled:"pte",hp_pulse_text_scale:"pts",hp_pulse_text_position:"ptp",hp_pulse_hide_bar:"phb",hp_skip_buildings:"sb",hp_pulse_threshold:"pt",hp_friend_enabled:"fe",hp_friend_pulse_enabled:"fpe",hp_friend_pulse_bpm:"fpb",hp_friend_pulse_intensity:"fpi",hp_friend_pulse_threshold:"fpt",hp_friend_color_low:"fcl",hp_friend_color_mid:"fcm",hp_friend_color_high:"fch",hp_friend_pulse_color_enabled:"fpce",hp_friend_pulse_color:"fpc",hp_kill_zone_enabled:"kze",hp_kill_zone_threshold:"kzt",hp_kill_zone_color:"kzc",hp_kill_zone_width:"kzw"},a=function(){var e={};for(var r in n)Object.prototype.hasOwnProperty.call(n,r)&&(e[n[r]]=r);return e}(),i=null,o=null,l=null,u="",s="",p=null,c=0,f=0,h="",_=(e="ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789-_",{encode:function(r){for(var t=[],n=0;n<r.length;n++){var a=r.charCodeAt(n);a<128?t.push(a):a<2048?(t.push(192|a>>6),t.push(128|63&a)):(t.push(224|a>>12),t.push(128|a>>6&63),t.push(128|63&a))}for(var i="",o=0;o<t.length;o+=3){var l=t[o],u=t[o+1]||0,s=t[o+2]||0;i+=e[l>>2],i+=e[(3&l)<<4|u>>4],i+=o+1<t.length?e[(15&u)<<2|s>>6]:"",i+=o+2<t.length?e[63&s]:""}return i},decode:function(r){for(var t={},n=0;n<64;n++)t[e[n]]=n;function getVal(e){if(void 0===e)return 0;if(!Object.prototype.hasOwnProperty.call(t,e))throw new Error("Invalid base64url char: "+e);return t[e]}for(var a=[],i=0;i<r.length;i+=4){var o=getVal(r[i]),l=getVal(r[i+1]),u=void 0!==r[i+2]?getVal(r[i+2]):0,s=void 0!==r[i+3]?getVal(r[i+3]):0;a.push(o<<2|l>>4),void 0!==r[i+2]&&a.push((15&l)<<4|u>>2),void 0!==r[i+3]&&a.push((3&u)<<6|s)}for(var p="",c=0;c<a.length;c++){var f=a[c];if(f<128)p+=String.fromCharCode(f);else if(f<224)p+=String.fromCharCode((31&f)<<6|63&a[++c]);else{var h=a[++c],_=a[++c];p+=String.fromCharCode((15&f)<<12|(63&h)<<6|63&_)}}return p}});function normalizeNamespace(e){return String(e||"").toLowerCase().replace(/[^a-z0-9_]+/g,"_").replace(/^_+|_+$/g,"")}function hasPersistentStorage(){try{return $.persistentStorage.setItem("_anita_probe","1"),$.persistentStorage.removeItem("_anita_probe"),storageSupportLogged||(storageSupportLogged=!0),!0}catch(e){return storageSupportLogged||(storageSupportLogged=!0),!1}}function cloneValues(e){var r={};for(var t in e)Object.prototype.hasOwnProperty.call(e,t)&&(r[t]=e[t]);return r}function findElement(e){if(!i||!Array.isArray(i.elements))return null;for(var r=0;r<i.elements.length;r++){var t=i.elements[r];if(t&&t.id===e)return t}return null}function sanitizeValue(e,r){if(!e)return r;var t=e.defaultValue,n=String(e.type||"");if("positionpicker"===n){var a=0,i=200,o=r;if(o&&"object"==typeof o)Array.isArray(o)?(o.length>0&&(a=Number(o[0])),o.length>1&&(i=Number(o[1]))):(Object.prototype.hasOwnProperty.call(o,"x")&&(a=Number(o.x)),Object.prototype.hasOwnProperty.call(o,"y")&&(i=Number(o.y)));else if("string"==typeof o){var l=o.match(/-?\d+(?:\.\d+)?/g);l&&l.length>0&&(a=Number(l[0]),l.length>1&&(i=Number(l[1])))}else"number"==typeof o&&(i=Number(o));return isFinite(a)||(a=0),isFinite(i)||(i=200),a<0&&(a=0),i<0&&(i=0),a>400&&(a=400),i>400&&(i=400),Math.round(a)+","+Math.round(i)}if("toggle"===n){if(!0===r||!1===r)return r;if(1===r||"1"===r)return!0;if(0===r||"0"===r)return!1;if("string"==typeof r){var u=r.toLowerCase();if("true"===u)return!0;if("false"===u)return!1}return!!t}if("cycler"===n){var s=Array.isArray(e.options)?e.options.length:0,p=Number(r);if(isFinite(p)||(p=Number(t)),isFinite(p)||(p=0),(p=Math.round(p))<0&&(p=0),s>0&&p>=s){var c=Number(t);(!isFinite(c)||c<0||c>=s)&&(c=0),p=c}return p}if("slider"===n||"stepper"===n){var f=Number(r);isFinite(f)||(f=Number(t)),isFinite(f)||(f=0);var h=Number(e.step);isFinite(h)&&0!==h||(h=1);var _=isFinite(Number(e.min))?Number(e.min):-1/0,d=isFinite(Number(e.max))?Number(e.max):1/0;return f<_&&(f=_),f>d&&(f=d),Math.round(h)===h?Math.round(f):parseFloat(f.toFixed(2))}return"colorpicker"===n?"string"==typeof r&&r.length>0?r:"string"==typeof t&&t.length>0?t:"#FFFFFF":void 0!==r?r:t}function buildDefaultValues(e){for(var r={},t=e&&Array.isArray(e.elements)?e.elements:[],n=0;n<t.length;n++){var a=t[n];a&&a.id&&"button"!==a.type&&(r[a.id]=sanitizeValue(a,a.defaultValue))}return r}function mergeWithDefaults(e){var r=buildDefaultValues(i);for(var t in e)if(Object.prototype.hasOwnProperty.call(e,t)){var n=findElement(t);n&&(r[t]=sanitizeValue(n,e[t]))}return r}function parseStoredPayload(e,r){var t=String(e||"");if(!t)return null;var n=null;try{n=JSON.parse(t)}catch(e){return null}if(!n||"object"!=typeof n||!n.values||"object"!=typeof n.values)return null;i&&n.v&&(Number(n.v),Number(i.storageVersion));var o=n.values,l={};if(1===n.c||!0===n.compact){var u={};for(var s in o)if(Object.prototype.hasOwnProperty.call(o,s)){var p=a[s];p&&(u[p]=o[s])}l=mergeWithDefaults(u)}else l=mergeWithDefaults(o);return{raw:t,values:l}}function writeSessionMirror(e){var r=function(){for(var e=$.GetContextPanel();e&&e.GetParent&&e.GetParent();)e=e.GetParent();return e||null}();if(r){try{r.SetAttributeString&&r.SetAttributeString(t,e)}catch(e){}try{var n=r.FindChildTraverse?r.FindChildTraverse("Hud"):null;n&&n.SetAttributeString&&n.SetAttributeString(t,e)}catch(e){}}}function writeSharedSnapshot(e){try{"undefined"!=typeof GameUI&&GameUI&&GameUI.CustomUIConfig&&(GameUI.CustomUIConfig().__hpColorsCfgRaw=JSON.stringify(e||{}))}catch(e){}}function cachePayload(e,r,t,n){s=String(e||""),u=String(r||""),p=cloneValues(t||{}),o=cloneValues(t||{}),l=cloneValues(n||t||{}),writeSessionMirror(u),writeSharedSnapshot(o)}function replayValues(e,t){if(e)for(var n in e)Object.prototype.hasOwnProperty.call(e,n)&&$.DispatchEvent("ClientUI_FireOutput",JSON.stringify({magic_word:"ANITA_UPDATE",mod_title:r,setting_id:n,value:e[n],update_source:"bridge_bootstrap",skip_bridge_persist:!0}))}function scheduleBootstrap(e){h=String(e||"request");var r=++f;$.Schedule(0,function(){r===f&&function(e){if(i){var r=function(){if(s&&u&&p)return{raw:s,encoded:u,values:cloneValues(p),source:"cache"};if(!hasPersistentStorage()){if("undefined"==typeof GameInterfaceAPI||!GameInterfaceAPI||"function"!=typeof GameInterfaceAPI.GetSettingString)return null;var e="";try{e=String(GameInterfaceAPI.GetSettingString("deadlock_hero_debuts_seen")||"")}catch(e){return null}var r=e.match(/\[ANITA-v1-hp_colors\]:([A-Za-z0-9_-]+)/);if(!r)return null;var n=r[1],a="";try{a=_.decode(n)}catch(e){return null}var i=parseStoredPayload(a);return i?(cachePayload(i.raw,n,i.values,i.values),{raw:i.raw,encoded:n,values:cloneValues(i.values),source:"convar"}):null}var o="";try{o=String($.persistentStorage.getItem(t)||"")}catch(e){return null}if(!o)return null;var l="";try{l=_.decode(o)}catch(e){return null}var c=parseStoredPayload(l);return c?(cachePayload(c.raw,o,c.values,c.values),{raw:c.raw,encoded:o,values:cloneValues(c.values),source:"persistentStorage"}):null}();if(r)return o=cloneValues(r.values),l=cloneValues(r.values),writeSessionMirror(r.encoded),void replayValues(r.values,r.source);var n=o||l;n&&replayValues(n)}else h=e}(h)})}$.RegisterForUnhandledEvent("ClientUI_FireOutput",function(e){try{var a="string"==typeof e?JSON.parse(e):e;if(!a)return;if("ANITA_REGISTER"===a.magic_word)return void(function(e){if(!e||String(e.title||"")!==r)return!1;if("hp_colors"!==normalizeNamespace(e.storageNamespace))return!1;for(var t={title:r,storageNamespace:"hp_colors",storageVersion:Math.max(1,Math.floor(Number(e.storageVersion)||1)),elements:[]},n=Array.isArray(e.elements)?e.elements:[],a=0;a<n.length;a++){var u=n[a];u&&u.id&&t.elements.push({id:u.id,type:u.type,defaultValue:u.defaultValue,options:Array.isArray(u.options)?u.options.slice(0):null,step:u.step})}return i=t,o||(o=buildDefaultValues(i)),l||(l=cloneValues(o)),!0}(a.config)&&h&&scheduleBootstrap(h));if("ANITA_REQUEST_BOOTSTRAP"===a.magic_word){if(a.mod_title!==r)return;if("hp_colors"!==normalizeNamespace(a.storageNamespace))return;return void scheduleBootstrap(String(a.reason||"request"))}if("ANITA_UPDATE"!==a.magic_word||a.mod_title!==r)return;if(!i||!a.setting_id)return;var s=findElement(a.setting_id);if(!s)return;o||(o=buildDefaultValues(i)),l||(l=cloneValues(o));var p=String(a.update_source||"ui_update"),f="core_auto_resync"===p||"ui_resync"===p||"ui_refresh_after_apply"===p;if(o[a.setting_id]=sanitizeValue(s,a.value),writeSharedSnapshot(o),f||(l[a.setting_id]=sanitizeValue(s,a.value)),a.skip_bridge_persist||"bridge_bootstrap"===p||f)return;String(a.update_source||"ui_update"),d=!!a.force_persist,g=++c,$.Schedule(d?0:.35,function(){g===c&&function(e,r){if(!i)return!1;o||(o=buildDefaultValues(i));var a=function(){if(!i)return"";var e=l||o||buildDefaultValues(i),r=buildDefaultValues(i),t={};for(var a in e)Object.prototype.hasOwnProperty.call(e,a)&&Object.prototype.hasOwnProperty.call(r,a)&&e[a]!==r[a]&&(t[n[a]||a]=e[a]);return JSON.stringify({v:Math.max(1,Math.floor(Number(i.storageVersion)||1)),c:1,values:t})}();if(!a)return!1;var s="";try{s=_.encode(a)}catch(e){return!1}if(!r&&s===u)return writeSessionMirror(s),!1;if(cachePayload(a,s,o,l),!hasPersistentStorage())return!1;try{return $.persistentStorage.setItem(t,s),!0}catch(e){return!1}}(0,!!d)})}catch(e){}var d,g}),hasPersistentStorage()}();
\ No newline at end of file
diff --git a/hp_colors_terser/panorama/scripts/anita_ui_core.js b/hp_colors_terser/panorama/scripts/anita_ui_core.js
index 5599c2c..b1d59d7 100644
--- a/hp_colors_terser/panorama/scripts/anita_ui_core.js
+++ b/hp_colors_terser/panorama/scripts/anita_ui_core.js
@@ -1 +1 @@
-!function(){"use strict";const e="AnitaUI_Window",t="AnitaUI_Backdrop",n="AnitaUI_NavBar",a="AnitaUI_ContentArea",r="AnitaOverlayBtn",i="Hud",o="ShowEscapeMenu",l="Open",s="Visible",u="Attention",c=17,d=.05,h={hp_enabled:"e",hp_mode:"m",hp_low_threshold:"l",hp_high_threshold:"h",hp_bg_visible:"b",hp_team_colors:"t",hp_color_low:"cl",hp_color_mid:"cm",hp_color_high:"ch",hp_counter_size:"s",hp_counter_position:"p",hp_text_color_mode:"tm",hp_level_number_visible:"lnv",hp_text_color_low:"tl",hp_text_color_mid:"ti",hp_text_color_high:"th",hp_pulse_bpm:"bp",hp_pulse_intensity:"pi",hp_pulse_enabled:"pe",hp_pulse_text_enabled:"pte",hp_pulse_text_scale:"pts",hp_pulse_text_position:"ptp",hp_pulse_hide_bar:"phb",hp_skip_buildings:"sb",hp_pulse_threshold:"pt",hp_friend_enabled:"fe",hp_friend_pulse_enabled:"fpe",hp_friend_pulse_bpm:"fpb",hp_friend_pulse_intensity:"fpi",hp_friend_pulse_threshold:"fpt",hp_friend_color_low:"fcl",hp_friend_color_mid:"fcm",hp_friend_color_high:"fch",hp_friend_pulse_color_enabled:"fpce",hp_friend_pulse_color:"fpc",hp_kill_zone_enabled:"kze",hp_kill_zone_threshold:"kzt",hp_kill_zone_color:"kzc",hp_kill_zone_width:"kzw"},f=function(){var e={};for(var t in h)Object.prototype.hasOwnProperty.call(h,t)&&(e[h[t]]=t);return e}();var p,C=(p="ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789-_",{encode:function(e){for(var t=[],n=0;n<e.length;n++){var a=e.charCodeAt(n);a<128?t.push(a):a<2048?(t.push(192|a>>6),t.push(128|63&a)):(t.push(224|a>>12),t.push(128|a>>6&63),t.push(128|63&a))}for(var r="",i=0;i<t.length;i+=3){var o=t[i],l=t[i+1]||0,s=t[i+2]||0;r+=p[o>>2],r+=p[(3&o)<<4|l>>4],r+=i+1<t.length?p[(15&l)<<2|s>>6]:"",r+=i+2<t.length?p[63&s]:""}return r},decode:function(e){for(var t={},n=0;n<64;n++)t[p[n]]=n;function getVal(e){if(void 0===e)return 0;if(!Object.prototype.hasOwnProperty.call(t,e))throw new Error("Invalid base64url char: "+e);return t[e]}for(var a=[],r=0;r<e.length;r+=4){var i=getVal(e[r]),o=getVal(e[r+1]),l=void 0!==e[r+2]?getVal(e[r+2]):0,s=void 0!==e[r+3]?getVal(e[r+3]):0;a.push(i<<2|o>>4),void 0!==e[r+2]&&a.push((15&o)<<4|l>>2),void 0!==e[r+3]&&a.push((3&l)<<6|s)}for(var u="",c=0;c<a.length;c++){var d=a[c];if(d<128)u+=String.fromCharCode(d);else if(d<224)u+=String.fromCharCode((31&d)<<6|63&a[++c]);else{var h=a[++c],f=a[++c];u+=String.fromCharCode((15&d)<<12|(63&h)<<6|63&f)}}return u}});function emitUpdate(e,t,n,a){var r={magic_word:"ANITA_UPDATE",mod_title:e,setting_id:t,value:n};if(a&&"object"==typeof a)for(var i in a)Object.prototype.hasOwnProperty.call(a,i)&&(r[i]=a[i]);$.DispatchEvent("ClientUI_FireOutput",JSON.stringify(r))}const v={normalizeNamespace:function(e){return String(e||"").toLowerCase().replace(/[^a-z0-9_]+/g,"_").replace(/^_+|_+$/g,"")},getVersion:function(e){var t=Number(e&&e.storageVersion);return!isFinite(t)||t<1?1:Math.floor(t)},hasPersistentConfig:function(e){return this.normalizeNamespace(e&&e.storageNamespace).length>0},isHpColorsConfig:function(e){return!!e&&"HP Colors"===String(e.title||"")&&"hp_colors"===this.normalizeNamespace(e.storageNamespace)},CONVAR_KEY:"deadlock_hero_debuts_seen",TOKEN_PREFIX:"ANITA-v1-",getTokenRegex:function(e){return new RegExp("\\["+this.TOKEN_PREFIX+e+"\\]:[A-Za-z0-9_-]+")},getCleanupRegex:function(e){return new RegExp("\\["+this.TOKEN_PREFIX+e+"\\]:[A-Za-z0-9_-]*","g")},canWriteConvarDirect:function(){return"undefined"!=typeof GameInterfaceAPI&&!!GameInterfaceAPI&&("function"==typeof GameInterfaceAPI.ConsoleCommand||"function"==typeof GameInterfaceAPI.SetSettingString)},getStorageKey:function(e){var t=this.normalizeNamespace(e&&e.storageNamespace);return t?"anita_v1_"+t:""},getRootPanel:function(){for(var e=$.GetContextPanel();e&&e.GetParent&&e.GetParent();)e=e.GetParent();return e||null},getSessionEncoded:function(e){var t=this.getStorageKey(e);if(!t)return"";var n=this.getRootPanel(),a="",r="";try{n&&n.GetAttributeString&&(a=String(n.GetAttributeString(t,"")||""))}catch(e){}try{var i=n&&n.FindChildTraverse?n.FindChildTraverse("Hud"):null;i&&i.GetAttributeString&&(r=String(i.GetAttributeString(t,"")||""))}catch(e){}return a||r||""},hasPersistentStorage:function(){try{return!(!$||!$.persistentStorage||"function"!=typeof $.persistentStorage.getItem||"function"!=typeof $.persistentStorage.setItem)}catch(e){return!1}},writeSessionMirror:function(e,t){var n=this.getStorageKey(e);if(n)try{var a=this.getRootPanel();a&&a.SetAttributeString&&a.SetAttributeString(n,t);var r=a&&a.FindChildTraverse?a.FindChildTraverse("Hud"):null;r&&r.SetAttributeString&&r.SetAttributeString(n,t)}catch(e){}},writeConvar:function(e,t){"undefined"!=typeof GameInterfaceAPI&&GameInterfaceAPI&&"function"==typeof GameInterfaceAPI.SetSettingString?(GameInterfaceAPI.SetSettingString(e,t),this.log("writeConvar via SetSettingString key="+e)):"undefined"!=typeof GameInterfaceAPI&&GameInterfaceAPI&&"function"==typeof GameInterfaceAPI.ConsoleCommand&&(GameInterfaceAPI.ConsoleCommand(e+' "'+t+'"'),this.log("writeConvar via ConsoleCommand key="+e))},writeConvarBestEffort:function(e,t){var n=String(e||"")+' "'+String(t||"")+'"';if(this.canWriteConvarDirect())try{return this.writeConvar(e,t),!0}catch(t){this.log("direct convar write failed key="+e+" err="+t)}if(function(e){var t=String(e||"").trim(),n=!1;if(!t)return!1;try{$.DispatchEvent("CitadelConCommand",t),n=!0}catch(e){}try{"undefined"!=typeof GameInterfaceAPI&&GameInterfaceAPI&&"function"==typeof GameInterfaceAPI.ConsoleCommand&&(GameInterfaceAPI.ConsoleCommand(t),n=!0)}catch(e){}try{$.DispatchEvent("ConsoleCommand",t),n=!0}catch(e){}try{$.DispatchEvent("GameUIRunCommand",t),n=!0}catch(e){}return n}(n)){this.log("writeConvar via command events key="+e);try{"undefined"!=typeof GameInterfaceAPI&&GameInterfaceAPI&&"function"==typeof GameInterfaceAPI.SetSettingString&&GameInterfaceAPI.SetSettingString(e,t)}catch(e){}return!0}return!1},getElements:function(e){return e&&Array.isArray(e.elements)?e.elements:[]},warnPersistence:function(e,t){e&&e.title&&String(e.title),e&&e.__anitaLastPersistWarning===t||e&&(e.__anitaLastPersistWarning=t)},shouldPersistElement:function(e){return!(!e||!e.id||"button"===e.type)},sanitizeValue:function(e,t){if(!e)return t;var n=e.defaultValue,a=String(e.type||"");if("toggle"===a){if(!0===t||!1===t)return t;if(1===t||"1"===t)return!0;if(0===t||"0"===t)return!1;if("string"==typeof t){var r=t.toLowerCase();if("true"===r)return!0;if("false"===r)return!1}return!!n}if("cycler"===a){var i=Array.isArray(e.options)?e.options.length:0,o=Number(t);if(isFinite(o)||(o=Number(n)),isFinite(o)||(o=0),(o=Math.round(o))<0&&(o=0),i>0&&o>=i){var l=Number(n);(!isFinite(l)||l<0||l>=i)&&(l=0),o=l}return o}if("stepper"===a||"slider"===a){var s=Number(t);isFinite(s)||(s=Number(n)),isFinite(s)||(s=0);var u=Number(e.min),c=Number(e.max);isFinite(u)&&s<u&&(s=u),isFinite(c)&&s>c&&(s=c);var d=Number(e.step);return isFinite(d)&&0!==d||(d=1),s=Math.round(d)===d?Math.round(s):parseFloat(s.toFixed(2)),isFinite(u)&&s<u&&(s=u),isFinite(c)&&s>c&&(s=c),s}return"colorpicker"===a?"string"==typeof t&&t.length>0?t:"string"==typeof n&&n.length>0?n:"#FFFFFF":void 0!==t?t:n},ensureDefaults:function(e){for(var t=this.getElements(e),n=0;n<t.length;n++){var a=t[n];if(this.shouldPersistElement(a)){var r=void 0!==a.currentValue?a.currentValue:a.defaultValue;a.currentValue=this.sanitizeValue(a,r)}else void 0===a.currentValue&&void 0!==a.defaultValue&&(a.currentValue=a.defaultValue)}},expandStoredValues:function(e,t){if(!t||"object"!=typeof t||!t.values||"object"!=typeof t.values)return null;var n=t.values,a={};if(!this.isHpColorsConfig(e)||1!==t.c&&!0!==t.compact){for(var r in n)Object.prototype.hasOwnProperty.call(n,r)&&(a[r]=n[r]);return a}for(var i in n)if(Object.prototype.hasOwnProperty.call(n,i)){var o=f[i];o&&(a[o]=n[i])}return a},parseStoredPayload:function(e,t,n){var a=String(t||"");if(!a)return null;var r=null;try{r=JSON.parse(a)}catch(e){return null}if(!r||"object"!=typeof r)return null;var i=this.expandStoredValues(e,r);if(!i)return null;for(var o={},l=this.getElements(e),s=0;s<l.length;s++){var u=l[s];this.shouldPersistElement(u)&&Object.prototype.hasOwnProperty.call(i,u.id)&&(o[u.id]=this.sanitizeValue(u,i[u.id]))}return{raw:a,values:o}},readPersistentStoragePayload:function(e){var t=this.getStorageKey(e),n=this.hasPersistentStorage();if(!t||!n)return null;var a="";try{a=String($.persistentStorage.getItem(t)||"")}catch(e){return null}if(!a)return null;var r="";try{r=C.decode(a)}catch(e){return null}var i=this.parseStoredPayload(e,r,"persistentStorage");return i?{raw:i.raw,encoded:a,values:i.values,source:"persistentStorage"}:null},readConvarPayload:function(e){var t=this.normalizeNamespace(e&&e.storageNamespace);if(!t)return null;if("undefined"==typeof GameInterfaceAPI||!GameInterfaceAPI||"function"!=typeof GameInterfaceAPI.GetSettingString)return null;var n="";try{n=String(GameInterfaceAPI.GetSettingString(this.CONVAR_KEY)||"")}catch(e){return null}var a=n.match(new RegExp("\\["+this.TOKEN_PREFIX+t+"\\]:([A-Za-z0-9_-]+)"));if(!a)return null;var r=String(a[1]||"");if(!r)return null;var i="";try{i=C.decode(r)}catch(e){return null}var o=this.parseStoredPayload(e,i,"convar");return o?{raw:o.raw,encoded:r,values:o.values,source:"convar"}:null},readStoredPayload:function(e){var t=null;try{var n=this.getSessionEncoded(e);if(n){var a=C.decode(n);if(t=this.parseStoredPayload(e,a,"session"))return{raw:t.raw,encoded:n,values:t.values,source:"session"}}}catch(e){}return(t=this.readPersistentStoragePayload(e))||(t=this.readConvarPayload(e))?(e&&e.title,t):null},applyResolvedValues:function(e,t){for(var n=this.getElements(e),a=0;a<n.length;a++){var r=n[a];if(this.shouldPersistElement(r)){var i=Object.prototype.hasOwnProperty.call(t||{},r.id)?t[r.id]:r.defaultValue;r.currentValue=this.sanitizeValue(r,i)}else void 0===r.currentValue&&void 0!==r.defaultValue&&(r.currentValue=r.defaultValue)}},hydrateConfig:function(e){this.ensureDefaults(e);var t="defaults";if(this.normalizeNamespace(e&&e.storageNamespace),!this.hasPersistentConfig(e))return e.__anitaLastPersistedRaw="",void this.applyResolvedValues(e,{});var n=this.readStoredPayload(e);n&&(t=n.source),n?(this.applyResolvedValues(e,n.values),n.encoded&&"session"!==t&&this.writeSessionMirror(e,n.encoded)):this.applyResolvedValues(e,{}),e.__anitaLastPersistedRaw=n?n.raw:"",e&&e.title},buildStoredPayload:function(e){for(var t=this.getElements(e),n={},a=this.isHpColorsConfig(e),r=0;r<t.length;r++){var i=t[r];if(this.shouldPersistElement(i)){var o=this.sanitizeValue(i,void 0!==i.currentValue?i.currentValue:i.defaultValue);i.currentValue=o,a&&o===this.sanitizeValue(i,i.defaultValue)||(n[a&&h[i.id]||i.id]=o)}}return a?JSON.stringify({v:this.getVersion(e),c:1,values:n}):JSON.stringify({version:this.getVersion(e),values:n})},persistConfig:function(e,t){if(!this.hasPersistentConfig(e))return!1;var n=this.buildStoredPayload(e);if(!n)return!1;if(!t&&n===String(e.__anitaLastPersistedRaw||""))return!1;var a=this.normalizeNamespace(e.storageNamespace),r="";try{r=C.encode(n)}catch(e){return!1}var i="["+this.TOKEN_PREFIX+a+"]:"+r;try{var o="";"undefined"!=typeof GameInterfaceAPI&&GameInterfaceAPI&&"function"==typeof GameInterfaceAPI.GetSettingString&&(o=String(GameInterfaceAPI.GetSettingString(this.CONVAR_KEY)||""));var l=o.replace(this.getCleanupRegex(a),"").replace(/,,+/g,",").replace(/^,|,$/,""),s=(l?l+",":"")+i;s.length>400?this.warnPersistence(e,"convar skipped (payload_too_large)"):this.writeConvarBestEffort(this.CONVAR_KEY,s)||this.warnPersistence(e,"convar unavailable")}catch(e){}try{if(this.hasPersistentStorage()){var u=this.getStorageKey(e);u&&$.persistentStorage.setItem(u,r)}}catch(e){}return this.writeSessionMirror(e,r),e.__anitaLastPersistedRaw=n,!0},requestBootstrap:function(e,t){if(this.hasPersistentConfig(e)){var n={magic_word:"ANITA_REQUEST_BOOTSTRAP",mod_title:e.title,storageNamespace:this.normalizeNamespace(e.storageNamespace),reason:String(t||"request")};$.DispatchEvent("ClientUI_FireOutput",JSON.stringify(n))}},applyUpdate:function(e,t,n){for(var a=this.getElements(e),r=0;r<a.length;r++){var i=a[r];if(i&&i.id===t)return i.currentValue=this.sanitizeValue(i,n),!0}return!1}},AnitaComponents_createColorPicker=function(e,t,n){const a=$.CreatePanel("Panel",e,"");a.AddClass("AnitaRow"),a.AddClass("AnitaSliderRow"),a.style.overflow="noclip",a.style.width="100%";const r=$.CreatePanel("Label",a,"");r.text=t.label||"Color",r.AddClass("AnitaLabel");let i=normalizeHexColor(void 0!==t.currentValue?t.currentValue:t.defaultValue||"#FF0000"),o=0,l=1,s=null,u=null,c=null,d=null,h=!1,f=0,p=!1;const C="undefined"!=typeof GameUI&&null!==GameUI;let v=-1,m=-1,y="",P=null,S=null,A=null,b=null,_=null,x=null,V=null,I=null,F=null,w=null;function clampByte(e){let t=Number(e);return isFinite(t)||(t=0),t<0&&(t=0),t>255&&(t=255),Math.round(t)}function byteToHex(e){const t=clampByte(e).toString(16).toUpperCase();return t.length<2?"0"+t:t}function rgbToHex(e,t,n){return"#"+byteToHex(e)+byteToHex(t)+byteToHex(n)}function hsvToHex(e,t,n){var a=function(e,t,n){var a=Number(e),r=Number(t),i=Number(n);isFinite(a)||(a=0),isFinite(r)||(r=0),isFinite(i)||(i=0),a=(a%360+360)%360,r=Math.max(0,Math.min(1,r));var o=(i=Math.max(0,Math.min(1,i)))*r,l=o*(1-Math.abs(a/60%2-1)),s=i-o,u=0,c=0,d=0;return a<60?(u=o,c=l,d=0):a<120?(u=l,c=o,d=0):a<180?(u=0,c=o,d=l):a<240?(u=0,c=l,d=o):a<300?(u=l,c=0,d=o):(u=o,c=0,d=l),[clampByte(255*(u+s)),clampByte(255*(c+s)),clampByte(255*(d+s))]}(e,t,n);return rgbToHex(a[0],a[1],a[2])}function clamp01(e){var t=Number(e);return isFinite(t)||(t=0),t<0&&(t=0),t>1&&(t=1),t}function isValidPanel(e){return!!(e&&e.IsValid&&e.IsValid())}function parsePoint(e){if(!e)return null;if("number"==typeof e.length&&e.length>=2){var t=Number(e[0]),n=Number(e[1]);if(isFinite(t)&&isFinite(n))return{x:t,y:n}}if("object"==typeof e){var a=Number(void 0!==e.x?e.x:e[0]),r=Number(void 0!==e.y?e.y:e[1]);if(isFinite(a)&&isFinite(r))return{x:a,y:r}}return null}function getCursorPosition(){try{return C&&"function"==typeof GameUI.GetCursorPosition?parsePoint(GameUI.GetCursorPosition()):null}catch(e){}return null}function getPanelWindowPosition(e){if(!isValidPanel(e))return null;try{if("function"==typeof e.GetPositionWithinWindow){var t=parsePoint(e.GetPositionWithinWindow());if(t)return t}}catch(e){}var n=Number(e.actualxoffset||e.actualx||0),a=Number(e.actualyoffset||e.actualy||0);return isFinite(n)||(n=0),isFinite(a)||(a=0),{x:n,y:a}}function getPanelBounds(e){if(!isValidPanel(e))return{left:0,top:0,width:1,height:1};var t=getPanelWindowPosition(e),n=t?t.x:Number(e.actualxoffset||e.actualx||0),a=t?t.y:Number(e.actualyoffset||e.actualy||0),r=Number(e.actuallayoutwidth||e.contentwidth||e.width||1),i=Number(e.actuallayoutheight||e.contentheight||e.height||1);return isFinite(n)||(n=0),isFinite(a)||(a=0),(!isFinite(r)||r<=0)&&(r=1),(!isFinite(i)||i<=0)&&(i=1),{left:n,top:a,width:r,height:i}}function getPanelLocalCursorPosition(e){if(!isValidPanel(e))return null;var t=getPanelBounds(e),n=getPanelWindowPosition(e);try{if("function"==typeof e.GetCursorPosition){var a=parsePoint(e.GetCursorPosition());if(a)return a.x>=0&&a.x<=t.width&&a.y>=0&&a.y<=t.height?a:n?{x:a.x-n.x,y:a.y-n.y}:a}}catch(e){}var r=getCursorPosition();return r&&n?{x:r.x-n.x,y:r.y-n.y}:null}function getBoxStateFromColor(e){var t,n,a,r,i,o,l,s,u,c=function(e){if(!e)return[255,255,255];const t=String(e).trim();if("#"===t.charAt(0)){let e=t.slice(1);if(3===e.length&&(e=e[0]+e[0]+e[1]+e[1]+e[2]+e[2]),e.length>=6)return[parseInt(e.slice(0,2),16)||0,parseInt(e.slice(2,4),16)||0,parseInt(e.slice(4,6),16)||0]}const n=t.match(/rgb\((\d+),\s*(\d+),\s*(\d+)\)/i);return n?[clampByte(n[1]),clampByte(n[2]),clampByte(n[3])]:[255,255,255]}(e),d=(t=c[0],n=c[1],a=c[2],r=clampByte(t)/255,i=clampByte(n)/255,o=clampByte(a)/255,u=0,0!==(s=(l=Math.max(r,i,o))-Math.min(r,i,o))&&(u=l===r?(i-o)/s%6*60:l===i?60*((o-r)/s+2):60*((r-i)/s+4)),u<0&&(u+=360),[u,0===l?0:s/l,l]);return{hue:Math.round(d[0])%360,sat:clamp01(d[1])}}function setPickerBoxState(e,t){var n=function(e,t){var n=getBoxStateFromColor(t||i);if(!e)return n;var a=Number(e.hue),r=Number(e.sat);return isFinite(a)||(a=n.hue),isFinite(r)||(r=n.sat),(a=Math.round(a)%360)<0&&(a+=360),{hue:a,sat:r=clamp01(r)}}(e,t);return o=n.hue,l=n.sat,n}function colorFromBoxState(e,t){return hsvToHex(e,t,1)}function hueFromRelX(e){var t=clamp01(e);return Math.max(0,Math.min(359,Math.round(359*t)))}function normalizeHexColor(e){if(!e)return"#FFFFFF";if("number"==typeof e){for(var t=Math.max(0,Math.floor(e)).toString(16).toUpperCase();t.length<6;)t="0"+t;return"#"+t.slice(-6)}if("string"==typeof e){var n=String(e).trim();if(!n)return"#FFFFFF";if("#"===n.charAt(0)){var a=n.slice(1);if(3===a.length&&(a=a[0]+a[0]+a[1]+a[1]+a[2]+a[2]),a.length>=6)return"#"+a.slice(0,6).toUpperCase()}var r=n.match(/rgb\((\d+),\s*(\d+),\s*(\d+)\)/i);if(r)return rgbToHex(r[1],r[2],r[3]).toUpperCase()}if("object"==typeof e){if(void 0!==e.hex)return normalizeHexColor(e.hex);if(void 0!==e.h&&void 0!==e.s&&void 0!==e.v)return hsvToHex(e.h,e.s,e.v);if(void 0!==e.r&&void 0!==e.g&&void 0!==e.b)return rgbToHex(e.r,e.g,e.b).toUpperCase();if(void 0!==e.red&&void 0!==e.green&&void 0!==e.blue)return rgbToHex(e.red,e.green,e.blue).toUpperCase()}return"#FFFFFF"}function getColorBoxMetrics(){var e=isValidPanel(u)?u:c;if(!isValidPanel(e))return null;var t=getPanelBounds(e),n=Number(e.actuallayoutwidth||t.width||240),a=Number(e.actuallayoutheight||t.height||240),r=Number(isValidPanel(d)?d.actuallayoutwidth||d.contentwidth||d.width:18),i=Number(isValidPanel(d)?d.actuallayoutheight||d.contentheight||d.height:18);return(!isFinite(n)||n<=1)&&(n=240),(!isFinite(a)||a<=1)&&(a=240),(!isFinite(r)||r<=0)&&(r=18),(!isFinite(i)||i<=0)&&(i=18),{panel:e,bounds:{left:t.left,top:t.top,width:n,height:a},width:n,height:a,cursorWidth:r,cursorHeight:i,maxCursorX:Math.max(0,n-r),maxCursorY:Math.max(0,a-i)}}function applyColorBoxCursorPosition(e,t){if(!isValidPanel(d))return!1;var n=getColorBoxMetrics();if(!n)return!1;var a=Number(e),r=Number(t);return isFinite(a)||(a=0),isFinite(r)||(r=0),a<0&&(a=0),r<0&&(r=0),a>n.maxCursorX&&(a=n.maxCursorX),r>n.maxCursorY&&(r=n.maxCursorY),v=a,m=r,d.style.x=a+"px",d.style.y=r+"px",d.style.transform="none",d.style.zIndex="20",d.style.opacity="1",d.style.visibility="visible",d.style.backgroundColor="#FFFFFF",d.style.washColor="#FFFFFF",!0}function updateBoxCursorVisual(e){if(c&&c.IsValid&&c.IsValid()){var t=getColorBoxMetrics();t&&(isFinite(o)&&isFinite(l)||setPickerBoxState(null,e),applyColorBoxCursorPosition(t.maxCursorX>0?o/359*t.maxCursorX:0,t.maxCursorY>0?l*t.maxCursorY:0))}}function syncFromCursorPanelPosition(e){if(!isValidPanel(c)||!isValidPanel(d))return!1;var t=function(){if(!isValidPanel(d))return null;var e=getColorBoxMetrics();if(!e)return null;function chooseLocalAxis(e,t,n,a){var r=!!a&&isFinite(e)&&e>=-1&&e<=n+1,i=isFinite(t)&&t>=-1&&t<=n+1;return r&&i?Math.abs(e-t)<=12?e:t:i?t:r?e:isFinite(t)?t:isFinite(e)?e:0}var t=Number(d.actuallayoutx),n=Number(d.actuallayouty),a=!(!d.GetParent||d.GetParent()!==e.panel),r=getPanelBounds(d),i=r.left-e.bounds.left,o=r.top-e.bounds.top,l=chooseLocalAxis(t,i,e.maxCursorX,a),s=chooseLocalAxis(n,o,e.maxCursorY,a);return l<0&&(l=0),s<0&&(s=0),l>e.maxCursorX&&(l=e.maxCursorX),s>e.maxCursorY&&(s=e.maxCursorY),function(e,t){var n=getColorBoxMetrics();if(!n)return!1;var a=Number(e),r=Number(t);!isFinite(a)||!isFinite(r)||(a<0&&(a=0),r<0&&(r=0),a>n.maxCursorX&&(a=n.maxCursorX),r>n.maxCursorY&&(r=n.maxCursorY),v=a,m=r)}(l,s),{metrics:e,x:l,y:s}}();if(!t)return!1;const n=t.metrics,a=n.maxCursorX>0?clamp01(t.x/n.maxCursorX):0,r=n.maxCursorY>0?clamp01(t.y/n.maxCursorY):0,i=hueFromRelX(a),o=clamp01(r);return syncColorVisuals(colorFromBoxState(i,o),e,!1,{hue:i,sat:o}),!0}function syncFromBoxPosition(e,t,n){if(!isValidPanel(c))return!1;const a=getColorBoxMetrics();if(!a)return!1;const r=clamp01(Number(e)/a.width),i=clamp01(Number(t)/a.height),o=hueFromRelX(r),l=clamp01(i);return syncColorVisuals(colorFromBoxState(o,l),n,!1,{hue:o,sat:l}),!0}function syncFromScreenCursorPosition(e){var t=getCursorPosition();if(!t||!isValidPanel(c))return!1;const n=getColorBoxMetrics();if(!n)return!1;const a=n.bounds,r=clamp01((t.x-a.left)/a.width),i=clamp01((t.y-a.top)/a.height),o=hueFromRelX(r),l=clamp01(i);return syncColorVisuals(colorFromBoxState(o,l),e,!1,{hue:o,sat:l}),!0}function syncFromLocalBoxCursor(e){var t=getPanelLocalCursorPosition(c);return!!t&&syncFromBoxPosition(t.x,t.y,e)}function syncFromBestDragSource(e){return C?"cursor"!==y&&"drag_event"!==y||!syncFromCursorPanelPosition(e)?"box"!==y&&"gameui"!==y||!syncFromScreenCursorPosition(e)?syncFromLocalBoxCursor(e)?"panel_cursor":syncFromScreenCursorPosition(e)?"screen_cursor":syncFromCursorPanelPosition(e)?"drag_panel":"":"screen_cursor":"drag_panel":"cursor"!==y&&"drag_event"!==y||!syncFromCursorPanelPosition(e)?syncFromLocalBoxCursor(e)?"panel_cursor":syncFromCursorPanelPosition(e)?"cursor_panel":"":"cursor_panel"}function setMouseCaptureState(e){var t=!!e;if(isValidPanel(c)&&"function"==typeof c.SetMouseCapture)try{c.SetMouseCapture(t)}catch(e){}if(C&&isValidPanel(d)&&"function"==typeof d.SetMouseCapture)try{d.SetMouseCapture(t)}catch(e){}}function beginColorDrag(e,t){p=!0,y=String(e||""),syncFromCursorPanelPosition(!1)||updateBoxCursorVisual(i),t&&("cursor"===y||"drag_event"===y?syncFromCursorPanelPosition(!0)||syncFromBestDragSource(!0):syncFromScreenCursorPosition(!0)||syncFromLocalBoxCursor(!0)||syncFromCursorPanelPosition(!0)),setMouseCaptureState(!0),function(){if(!s||!s.IsValid||!s.IsValid())return;const e=++f;$.Schedule(.016,function tick(){if(e===f&&s&&s.IsValid&&s.IsValid())return p?(syncFromBestDragSource(!0),C&&"function"==typeof GameUI.IsMouseDown&&!GameUI.IsMouseDown(0)?void endColorDrag():void $.Schedule(.016,tick)):void $.Schedule(.05,tick)})}()}function endColorDrag(){p=!1,y="",setMouseCaptureState(!1)}function closePalette(){if(f++,endColorDrag(),C&&"function"==typeof GameUI.SetMouseCallback)try{GameUI.SetMouseCallback(null)}catch(e){}s&&s.IsValid&&s.IsValid()&&s.DeleteAsync(0),s=null,u=null,c=null,d=null,P=null,S=null,A=null,h=!1,x=null,V=null,I=null,F=null,w=null,y="",g.activeColorPickerClose===closePalette&&(g.activeColorPickerClose=null)}function syncColorVisuals(e,r,o,l){i=normalizeHexColor(e),t.currentValue=i;var s=setPickerBoxState(l,i);b&&b.IsValid&&b.IsValid()&&(b.style.backgroundColor=i),_&&(_.text=i),P&&P.IsValid&&P.IsValid()&&(P.style.backgroundColor=i),S&&(S.text=i),A&&(A.text="Hue "+s.hue+"° | Sat "+Math.round(100*s.sat)+"%");const u=a.FindChildTraverse("ColorPreviewBtn");u&&(u.style.backgroundColor=i),updateBoxCursorVisual(i),h=!0;try{if(x&&x.IsValid&&x.IsValid()){var c=(f=s.hue,p=Number(f),isFinite(p)||(p=0),p=Math.max(0,Math.min(359,Math.round(p))),Math.round(p/359*100));if(Math.round(Number(x.value))!==c)try{x.value=c}catch(e){}F&&(F.text=s.hue+"°")}if(V&&V.IsValid&&V.IsValid()){var d=Math.round(100*s.sat);if(Math.round(Number(V.value))!==d)try{V.value=d}catch(e){}w&&(w.text=d+"%"),I&&I.IsValid&&I.IsValid()&&(I.style.backgroundColor="gradient( linear, 0% 0%, 100% 0%, from( #ffffff ), to( "+hsvToHex(s.hue,1,1)+" ) )")}}finally{h=!1}var f,p;r&&(t.id&&n&&emitUpdate(n,t.id,i),t.onChange&&t.onChange(i)),o&&closePalette()}const E=$.CreatePanel("Panel",a,"ColorPreviewBtn");return E.AddClass("AnitaColorPickerPreview"),E.style.backgroundColor=i,E.style.marginRight="6px",E.SetPanelEvent("onactivate",()=>function(){if(s)return void closePalette();if(g.activeColorPickerClose&&g.activeColorPickerClose!==closePalette)try{g.activeColorPickerClose()}catch(e){}var t=$.GetContextPanel();s=$.CreatePanel("Panel",t,""),g.activeColorPickerClose=closePalette,s.AddClass("AnitaColorPopup");var n=24,a=24;try{var r=getPanelBounds(E&&E.IsValid&&E.IsValid()?E:e);n=Math.max(8,Math.round(r.left+r.width+12)),a=Math.max(8,Math.round(r.top))}catch(e){}s.style.align="left top",s.style.transform="translate3d("+n+"px, "+a+"px, 0px)",s.style.uiScale="100%",s.style.flowChildren="down",s.style.ignoreParentFlow=!0,s.SetPanelEvent("oncancel",closePalette);const f=$.CreatePanel("Panel",s,"");f.AddClass("AnitaColorPopupHeader"),P=$.CreatePanel("Panel",f,"ColorPreviewBtn"),P.AddClass("AnitaColorPickerPreview"),P.AddClass("AnitaColorPopupPreview"),P.style.backgroundColor=i,S=$.CreatePanel("Label",f,""),S.AddClass("AnitaColorPopupHex"),S.text=i,A=$.CreatePanel("Label",s,""),A.AddClass("AnitaColorPopupMeta");const y=$.CreatePanel("Label",s,"");y.AddClass("AnitaColorPopupHint"),y.text="Drag the marker or use the sliders below to change hue and saturation.";const b=$.CreatePanel("Panel",s,"");if(b.AddClass("AnitaColorBoxWrap"),u=$.CreatePanel("Panel",b,""),u.AddClass("AnitaColorBoxFrame"),$.CreatePanel("Panel",u,"").AddClass("AnitaColorBoxHueLayer"),$.CreatePanel("Panel",u,"").AddClass("AnitaColorBoxSaturationLayer"),c=$.CreatePanel("Panel",u,""),c.AddClass("AnitaColorBox"),c.hittest=!0,c.hittestchildren=!0,c.style.ignoreParentFlow=!0,c.style.width="100%",c.style.height="100%",c.SetPanelEvent("onmouseactivate",function(){if(C)beginColorDrag("box",!0);else try{var e=getPanelLocalCursorPosition(c);e&&syncFromBoxPosition(e.x,e.y,!0)}catch(e){}}),c.SetPanelEvent("onactivate",function(){C&&beginColorDrag("box",!0)}),c.SetPanelEvent("onmousemove",function(){if(p)if(C)syncFromBestDragSource(!0);else{var e=getPanelLocalCursorPosition(c);e&&syncFromBoxPosition(e.x,e.y,!0)}}),c.SetPanelEvent("onmouseup",function(){endColorDrag()}),c.SetPanelEvent("onmouseout",function(){p&&!C&&endColorDrag()}),d=$.CreatePanel("Button",u,""),d.AddClass("AnitaColorBoxCursor"),d.hittest=!0,d.hittestchildren=!1,d.style.ignoreParentFlow=!0,d.style.align="left top",d.style.visibility="visible",d.style.opacity="1","function"==typeof d.SetDraggable)try{d.SetDraggable(!0)}catch(e){}if("function"==typeof d.SetDisableFocusOnMouseDown)try{d.SetDisableFocusOnMouseDown(!0)}catch(e){}if(d.SetPanelEvent("onmouseactivate",function(){beginColorDrag("cursor",!0)}),d.SetPanelEvent("onactivate",function(){beginColorDrag("cursor",!0)}),d.SetPanelEvent("onmousemove",function(){p&&syncFromBestDragSource(!0)}),d.SetPanelEvent("onmouseup",function(){endColorDrag()}),d.SetPanelEvent("onmouseout",function(){p&&!C&&endColorDrag()}),"function"==typeof $.RegisterEventHandler){try{$.RegisterEventHandler("DragStart",d,function(e,t){e&&e===d&&(t&&(t.displayPanel=d,t.removePositionBeforeDrop=!1),d.style.align="left top",beginColorDrag("drag_event",!1))})}catch(e){}try{$.RegisterEventHandler("DragEnd",d,function(e,t){t&&t.IsValid&&t.IsValid()&&t===d&&(u&&u.IsValid&&u.IsValid()&&t.GetParent&&t.GetParent()!==u&&t.SetParent(u),t.style.align="left top",function(){var e=getColorBoxMetrics();if(!e)return!1;if(!isFinite(v)||!isFinite(m)||v<0||m<0)return!1;var t=e.maxCursorX>0?clamp01(v/e.maxCursorX):0,n=e.maxCursorY>0?clamp01(m/e.maxCursorY):0,a=hueFromRelX(t),r=clamp01(n);return applyColorBoxCursorPosition(v,m),syncColorVisuals(colorFromBoxState(a,r),!0,!1,{hue:a,sat:r}),!0}()||(syncFromCursorPanelPosition(!0),updateBoxCursorVisual(i))),endColorDrag()})}catch(e){}}if(C&&"function"==typeof GameUI.SetMouseCallback)try{GameUI.SetMouseCallback(function(e,t){if(!s||!s.IsValid||!s.IsValid())return!1;if("pressed"===e&&0===t){const e=getCursorPosition();if(!e)return!1;const t=getColorBoxMetrics();if(!t)return!1;const a=t.bounds;if(e.x>=a.left&&e.x<=a.left+a.width&&e.y>=a.top&&e.y<=a.top+a.height)return syncFromScreenCursorPosition(n=!0)||syncFromLocalBoxCursor(n)||syncFromCursorPanelPosition(n),beginColorDrag("gameui",!1),!0}var n;return!("released"!==e||0!==t||!p||(endColorDrag(),0))})}catch(e){}var _=$.CreatePanel("Panel",s,"");_.AddClass("AnitaHueSliderGroup");var B=$.CreatePanel("Label",_,"");B.text="H",B.AddClass("AnitaHueValue"),B.AddClass("AnitaPickerAxisLabel");var R=$.CreatePanel("Panel",_,"");if(R.AddClass("AnitaHueSliderContainer"),R.AddClass("AnitaPickerSliderTrack"),x=$.CreatePanel("Slider",R,"",{direction:"horizontal"}),x.AddClass("AnitaHueSlider"),x.AddClass("HorizontalSlider"),x.min=0,x.max=100,x.increment=1,"function"==typeof x.SetShowDefaultValue)try{x.SetShowDefaultValue(!1)}catch(e){}if("function"==typeof x.SetRequiresSelection)try{x.SetRequiresSelection(!1)}catch(e){}F=$.CreatePanel("Label",_,""),F.AddClass("AnitaHueValue"),F.AddClass("AnitaPickerReadout"),x.SetPanelEvent("onvaluechanged",function(){if(!h){var e=hueFromRelX(clamp01(Number(x.value)/100)),t=colorFromBoxState(e,l);F&&(F.text=e+"°"),I&&I.IsValid&&I.IsValid()&&(I.style.backgroundColor="gradient( linear, 0% 0%, 100% 0%, from( #ffffff ), to( "+hsvToHex(e,1,1)+" ) )"),syncColorVisuals(t,!0,!1,{hue:e,sat:l})}});var k=$.CreatePanel("Panel",s,"");k.AddClass("AnitaHueSliderGroup");var N=$.CreatePanel("Label",k,"");if(N.text="S",N.AddClass("AnitaHueValue"),N.AddClass("AnitaPickerAxisLabel"),I=$.CreatePanel("Panel",k,""),I.AddClass("AnitaSatSliderContainer"),I.AddClass("AnitaPickerSliderTrack"),V=$.CreatePanel("Slider",I,"",{direction:"horizontal"}),V.AddClass("AnitaHueSlider"),V.AddClass("HorizontalSlider"),V.min=0,V.max=100,V.increment=1,"function"==typeof V.SetShowDefaultValue)try{V.SetShowDefaultValue(!1)}catch(e){}if("function"==typeof V.SetRequiresSelection)try{V.SetRequiresSelection(!1)}catch(e){}w=$.CreatePanel("Label",k,""),w.AddClass("AnitaHueValue"),w.AddClass("AnitaPickerReadout"),V.SetPanelEvent("onvaluechanged",function(){if(!h){var e=clamp01(V.value/100),t=colorFromBoxState(o,e);w&&(w.text=Math.round(100*e)+"%"),syncColorVisuals(t,!0,!1,{hue:o,sat:e})}});const M=$.CreatePanel("Panel",s,"");M.AddClass("AnitaColorPopupFooter");const T=$.CreatePanel("Button",M,"");T.AddClass("AnitaColorPopupBtn"),$.CreatePanel("Label",T,"").text="Close",T.SetPanelEvent("onactivate",closePalette),syncColorVisuals(i,!1,!1),$.Schedule(0,function(){s&&s.IsValid&&s.IsValid()&&syncColorVisuals(i,!1,!1)}),getBoxStateFromColor(i)}()),b=E,_=$.CreatePanel("Label",a,""),_.AddClass("AnitaColorHexValue"),_.text=i,a},g={mainWindow:null,backdrop:null,navBar:null,menuArea:null,contentArea:null,activeModTitle:"",isOpen:!1,activeColorPickerClose:null,findElementById:function(e,t){if(!e||!Array.isArray(e.elements)||!t)return null;for(var n=0;n<e.elements.length;n++){var a=e.elements[n];if(a&&a.id===t)return a}return null},isElementVisible:function(e,t){if(!t||!t.visibleWhen)return!0;var n=t.visibleWhen,a=this.findElementById(e,n.id);if(!a)return!0;var r=a.currentValue;if(Array.isArray(n.equals)){for(var i=0;i<n.equals.length;i++)if(r===n.equals[i])return!0;return!1}return Object.prototype.hasOwnProperty.call(n,"equals")?r===n.equals:!!r},applyElementVisibility:function(e,t){if(t&&t.__anitaRowPanel&&t.__anitaRowPanel.IsValid&&t.__anitaRowPanel.IsValid()){var n=this.isElementVisible(e,t);t.__anitaRowPanel.style.visibility=n?"visible":"collapse",t.__anitaRowPanel.hittest=n}},refreshConditionalVisibility:function(e){if(e&&Array.isArray(e.elements))for(var t=0;t<e.elements.length;t++)this.applyElementVisibility(e,e.elements[t])},hasVisibilityDependents:function(e,t){if(!e||!Array.isArray(e.elements)||!t)return!1;for(var n=0;n<e.elements.length;n++){var a=e.elements[n];if(a&&a.visibleWhen&&a.visibleWhen.id===t)return!0}return!1},getElementCategory:function(e){return String(e&&e.category||"General").trim()||"General"},getCategoryList:function(e){var t=[],n={};if(!e||!Array.isArray(e.elements))return t;for(var a=0;a<e.elements.length;a++){var r=this.getElementCategory(e.elements[a]);n[r]||(n[r]=!0,t.push(r))}return t},getCategoryElements:function(e,t){var n=[];if(!e||!Array.isArray(e.elements))return n;for(var a=0;a<e.elements.length;a++){var r=e.elements[a];this.getElementCategory(r)===t&&n.push(r)}return n},ensureActiveCategory:function(e){var t=this.getCategoryList(e);if(0===t.length)return e.__anitaActiveCategory="","";for(var n=String(e.__anitaActiveCategory||""),a=0;a<t.length;a++)if(t[a]===n)return n;return e.__anitaActiveCategory=t[0],t[0]},getSaveCodeToken:function(e){if(!e||!e.storageNamespace)return"";var t=v.buildStoredPayload(e);if(!t)return"";var n=v.normalizeNamespace(e.storageNamespace);return n?"["+v.TOKEN_PREFIX+n+"]:"+C.encode(t):""},extractSaveCodeToken:function(e,t){if(!e||!e.storageNamespace)return"";var n=String(t||"").trim();if(!n)return"";var a=v.normalizeNamespace(e.storageNamespace);if(!a)return"";var r=n.match(v.getTokenRegex(a));if(r&&r[0])return r[0];var i=n.match(/\[ANITA-v1-[a-z0-9_]+\]:[A-Za-z0-9_-]+/i);return i&&i[0]?i[0]:/^[A-Za-z0-9_-]+$/.test(n)?"["+v.TOKEN_PREFIX+a+"]:"+n:""},syncSaveCodeInput:function(e){if(e&&e.__anitaSaveCodeInput&&e.__anitaSaveCodeInput.IsValid&&e.__anitaSaveCodeInput.IsValid()){var t=this.getSaveCodeToken(e);String(e.__anitaSaveCodeInput.text||"")!==t&&(e.__anitaSaveCodeInput.text=t)}},initWindow:function(r){r.FindChildTraverse(e)&&r.FindChildTraverse(e).DeleteAsync(0),r.FindChildTraverse(t)&&r.FindChildTraverse(t).DeleteAsync(0),this.backdrop=$.CreatePanel("Panel",r,t),this.backdrop.AddClass("AnitaBackdrop"),this.backdrop.SetPanelEvent("onactivate",()=>this.toggle(!1)),this.mainWindow=$.CreatePanel("Panel",r,e),this.mainWindow.AddClass("AnitaWindow"),this.mainWindow.canfocus=!0,this.mainWindow.SetPanelEvent("oncancel",()=>this.toggle(!1)),this.mainWindow.SetPanelEvent("onactivate",()=>{this.mainWindow.SetFocus()}),this.navBar=$.CreatePanel("Panel",this.mainWindow,n),this.navBar.AddClass("AnitaNavBar");const i=$.CreatePanel("Button",this.navBar,"");i.AddClass("AnitaCloseBtn"),i.SetPanelEvent("onactivate",()=>this.toggle(!1));const o=$.CreatePanel("Label",this.navBar,"");o.text="/",o.AddClass("AnitaTabSeparator"),this.menuArea=$.CreatePanel("Panel",this.navBar,"AnitaTabContainer"),this.menuArea.AddClass("AnitaTabContainer"),this.contentArea=$.CreatePanel("Panel",this.mainWindow,a),this.contentArea.AddClass("AnitaContentArea")},toggle:function(e){if(this.mainWindow&&this.backdrop)if(this.isOpen=void 0!==e?e:!this.isOpen,this.mainWindow.SetHasClass(l,this.isOpen),this.mainWindow.hittest=this.isOpen,this.backdrop.SetHasClass(l,this.isOpen),this.backdrop.hittest=this.isOpen,this.isOpen)this.mainWindow.SetFocus();else{if(this.activeColorPickerClose){try{this.activeColorPickerClose()}catch(e){}this.activeColorPickerClose=null}$.DispatchEvent("DropInputFocus",this.mainWindow);let e=$.GetContextPanel();for(;e.GetParent();)e=e.GetParent();e.SetFocus()}},addTab:function(e,t){let n=e;const a=c;n.length>a&&(n=n.substring(0,a)+"...");const r=$.CreatePanel("Button",this.menuArea,"");r.AddClass("AnitaTabBtn"),$.CreatePanel("Label",r,"").text=n;const i=$.CreatePanel("Label",this.menuArea,"");i.text="/",i.AddClass("AnitaTabSeparator"),r.SetPanelEvent("onactivate",()=>{this.menuArea.Children().forEach(e=>{"Button"!==e.paneltype||e.BHasClass("AnitaCloseBtn")||e.RemoveClass("Active")}),r.AddClass("Active"),this.activeModTitle=e,t()}),this.menuArea.GetChildCount()<=4&&(r.AddClass("Active"),this.activeModTitle=e,t())},renderModSettings:function(e){this.contentArea.RemoveAndDeleteChildren(),this.contentArea.canfocus=!0,this.contentArea.SetPanelEvent("onactivate",()=>this.contentArea.SetFocus());const t=$.CreatePanel("Panel",this.contentArea,"");t.AddClass("ModContainer"),t.canfocus=!0;const n=$.CreatePanel("Panel",t,"BackgroundShield");n.style.width="100%",n.style.height="100%",n.style.ignoreParentFlow="true",n.style.zIndex="-1",n.hittest=!0;const syncAll=()=>{e.elements&&e.elements.forEach(t=>{t.id&&void 0!==t.currentValue&&emitUpdate(e.title,t.id,t.currentValue,{update_source:"ui_resync",skip_bridge_persist:!0})})};n.SetPanelEvent("onmouseover",()=>{syncAll()}),n.SetPanelEvent("onactivate",()=>{t.SetFocus(),syncAll()});const a=$.CreatePanel("Label",t,"");if(a.text=e.title,a.AddClass("SectionHeader"),$.CreatePanel("Panel",t,"").AddClass("SectionHeaderLine"),e.description){const M=$.CreatePanel("Label",t,"");M.text=e.description,M.AddClass("ModDescription")}const r=$.CreatePanel("Panel",t,"");r.AddClass("AnitaSettingsShell");const i=$.CreatePanel("Panel",r,"");i.AddClass("AnitaTreePanel");const o=$.CreatePanel("Label",i,"");o.text="Settings",o.AddClass("AnitaTreeHeader");const l=$.CreatePanel("Panel",i,"");l.AddClass("AnitaTreeList");const s=$.CreatePanel("Panel",r,"");s.AddClass("AnitaDetailPanel");const u=this.ensureActiveCategory(e),c=this.getCategoryList(e);for(var d=0;d<c.length;d++){var h=c[d],f=$.CreatePanel("Button",l,"");f.AddClass("AnitaTreeNode"),f.SetHasClass("Active",h===u);var p=$.CreatePanel("Label",f,"");p.AddClass("AnitaTreeNodeGlyph"),p.text=h===u?"v":">";var y=$.CreatePanel("Label",f,"");y.AddClass("AnitaTreeNodeLabel"),y.text=h;var P=$.CreatePanel("Label",f,"");P.AddClass("AnitaTreeNodeCount"),P.text=String(this.getCategoryElements(e,h).length),f.SetPanelEvent("onactivate",function(t){return function(){e.__anitaActiveCategory=t,g.renderModSettings(e)}}(h))}const S=$.CreatePanel("Panel",s,"");S.AddClass("AnitaDetailHeaderRow");const A=$.CreatePanel("Label",S,"");A.text=u||e.title,A.AddClass("AnitaDetailHeader");const b=$.CreatePanel("Label",s,"");b.text="Select a setting group from the tree on the left.",b.AddClass("AnitaDetailHint");const _=$.CreatePanel("Panel",s,"");if(_.AddClass("AnitaSettingsList"),e.elements&&(e.elements.forEach(e=>{e.__anitaRowPanel=null}),this.getCategoryElements(e,u).forEach(t=>{var n=null;switch(t.type){case"toggle":n=function(e,t,n){const a=$.CreatePanel("Panel",e,"");a.AddClass("AnitaToggleRow");const r=$.CreatePanel("Button",a,"");r.AddClass("AnitaToggleBtn");const i=$.CreatePanel("Label",a,"");i.text=t.label||"Option",i.AddClass("AnitaLabel");const o=$.CreatePanel("Panel",a,"");o.AddClass("AnitaCheckBox"),$.CreatePanel("Panel",o,"").AddClass("AnitaCheckMark");let l=void 0!==t.currentValue?t.currentValue:t.defaultValue||!1;const updateState=e=>a.SetHasClass("Checked",e);return updateState(l),r.SetPanelEvent("onactivate",()=>{l=!l,updateState(l),t.currentValue=l,t.id&&emitUpdate(n,t.id,l),t.onChange&&t.onChange(l);var e=m.findRegisteredMod(n);e&&g.hasVisibilityDependents(e,t.id)&&m.queueRenderRefresh(e)}),a}(_,t,e.title);break;case"stepper":n=function(e,t,n){const a=$.CreatePanel("Panel",e,"");a.AddClass("AnitaRow");const r=$.CreatePanel("Label",a,"");r.text=t.label||"Value",r.AddClass("AnitaLabel");const i=$.CreatePanel("Panel",a,"");i.AddClass("AnitaStepperControls");const o=$.CreatePanel("Button",i,"");o.AddClass("AnitaStepBtn"),$.CreatePanel("Label",o,"less").text="-";const l=$.CreatePanel("TextEntry",i,"");l.AddClass("AnitaStepInput");const s=$.CreatePanel("Button",i,"");s.AddClass("AnitaStepBtn"),$.CreatePanel("Label",s,"").text="+";let u=void 0!==t.currentValue?t.currentValue:t.defaultValue||0;const c=t.step||1,d=!Number.isInteger(c);function update(e){e=d?parseFloat(e.toFixed(2)):Math.round(e),u=e,t.currentValue=u,l.text=u.toString(),t.onChange&&t.onChange(u),t.id&&n&&emitUpdate(n,t.id,u)}return l.text=d?u.toFixed(2):u,l.SetPanelEvent("ontextentrychange",()=>{let e=parseFloat(l.text);isNaN(e)||(u=e,t.currentValue=e)}),l.SetPanelEvent("oncancel",()=>{g.toggle(!1)}),o.SetPanelEvent("onactivate",()=>update(u-c)),s.SetPanelEvent("onactivate",()=>update(u+c)),l.SetPanelEvent("oninputsubmit",()=>{update(u),$.DispatchEvent("DropInputFocus",l),g.mainWindow.SetFocus()}),l.SetPanelEvent("onfocusout",()=>{update(u)}),a}(_,t,e.title);break;case"slider":n=function(e,t,n){const a=$.CreatePanel("Panel",e,"");a.AddClass("AnitaRow"),a.AddClass("AnitaSliderRow"),a.style.width="100%";const r=$.CreatePanel("Label",a,"");r.text=t.label||"Value",r.AddClass("AnitaLabel");const i=$.CreatePanel("Panel",a,"");i.AddClass("AnitaSliderValueGroup"),i.AddClass("SliderValueGroup"),i.style.flowChildren="right",i.style.verticalAlign="center",i.style.horizontalAlign="left",i.style.width="296px";const o=$.CreatePanel("Panel",i,"");o.AddClass("AnitaSliderContainer"),o.AddClass("SliderContainer"),o.style.width="230px",o.style.height="26px",o.style.padding="0px",o.style.verticalAlign="center",o.style.overflow="noclip";const l=$.CreatePanel("Slider",o,"",{direction:"horizontal"});l.AddClass("AnitaSlider"),l.AddClass("HorizontalSlider"),l.style.width="100%",l.style.height="100%",l.style.verticalAlign="center",l.style.overflow="noclip";const s=$.CreatePanel("Label",i,"");s.AddClass("AnitaSliderValue");const u=Number(t.min),c=Number(t.max),d=Number(t.step),h=isFinite(u)?u:0,f=isFinite(c)?c:100,p=isFinite(d)&&d>0?d:1;let C=void 0!==t.currentValue?t.currentValue:void 0!==t.defaultValue?t.defaultValue:h,v=!1;function normalize(e){let n=Number(e);return isFinite(n)||(n=Number(t.defaultValue)),isFinite(n)||(n=h),n<h&&(n=h),n>f&&(n=f),n=Math.round(p)===p?Math.round(n):parseFloat(n.toFixed(2)),n}function syncVisuals(e,a){const r=normalize(e);if(C=r,t.currentValue=r,s.text=r.toString()+"%",l&&l.IsValid&&l.IsValid()&&Number(l.value)!==r){v=!0;try{"function"==typeof l.SetValueNoEvents?l.SetValueNoEvents(r):l.value=r}finally{v=!1}}a&&(t.onChange&&t.onChange(r),t.id&&n&&emitUpdate(n,t.id,r))}return l.min=h,l.max=f,l.increment=p,l.value=normalize(C),"function"==typeof l.SetShowDefaultValue&&l.SetShowDefaultValue(!1),"function"==typeof l.SetRequiresSelection&&l.SetRequiresSelection(!1),syncVisuals(C,!1),l.SetPanelEvent("onvaluechanged",function(){v||syncVisuals(l.value,!0)}),l.SetPanelEvent("oncancel",()=>{g.toggle(!1)}),a}(_,t,e.title);break;case"button":n=function(e,t,n){const a=$.CreatePanel("Button",e,"");return a.AddClass("AnitaActionBtn"),$.CreatePanel("Label",a,"").text=t.label||"Action",a.SetPanelEvent("onactivate",()=>{t.onClick&&t.onClick(),t.id&&n&&emitUpdate(n,t.id,!0),a.AddClass("Activated"),$.Schedule(.1,()=>a.RemoveClass("Activated"))}),a}(_,t,e.title);break;case"cycler":n=function(e,t,n){const a=$.CreatePanel("Panel",e,"");a.AddClass("AnitaRow");const r=$.CreatePanel("Label",a,"");r.text=t.label||"Cycle",r.AddClass("AnitaLabel");const i=$.CreatePanel("Button",a,"");i.AddClass("AnitaCyclerBtn");const o=$.CreatePanel("Label",i,""),l=t.options||["OFF","ON"];let s=void 0!==t.currentValue?t.currentValue:t.defaultValue||0;(s<0||s>=l.length)&&(s=0);const updateVisuals=()=>{o.text=l[s]};return updateVisuals(),i.SetPanelEvent("onactivate",()=>{s=(s+1)%l.length,updateVisuals(),t.currentValue=s;var e=m.findRegisteredMod(n);e&&g.hasVisibilityDependents(e,t.id)&&m.queueRenderRefresh(e),t.id&&n&&emitUpdate(n,t.id,s),t.onChange&&t.onChange(s,l[s])}),a}(_,t,e.title);break;case"positionpicker":n=function(e,t,n){const a=$.CreatePanel("Panel",e,"");a.AddClass("AnitaRow"),a.AddClass("AnitaSliderRow"),a.style.overflow="noclip",a.style.width="100%";const r=$.CreatePanel("Label",a,"");function posPickerClamp(e){var t=Number(e);return isFinite(t)||(t=0),t<0&&(t=0),t>400&&(t=400),Math.round(t)}function posPickerNormalize(e){var t=function(e){var t=0,n=200,a=e;if(a&&"object"==typeof a)return Array.isArray(a)?(a.length>0&&(t=posPickerClamp(a[0])),a.length>1&&(n=posPickerClamp(a[1]))):(Object.prototype.hasOwnProperty.call(a,"x")&&(t=posPickerClamp(a.x)),Object.prototype.hasOwnProperty.call(a,"y")&&(n=posPickerClamp(a.y))),{x:t,y:n};if("string"==typeof a){var r=a.match(/-?\d+(?:\.\d+)?/g);if(r&&r.length>0)return t=posPickerClamp(r[0]),r.length>1&&(n=posPickerClamp(r[1])),{x:t,y:n}}return"number"==typeof a?{x:t,y:n=posPickerClamp(a)}:{x:t,y:n}}(e);return{x:posPickerClamp(t.x),y:posPickerClamp(t.y)}}r.text=t.label||"Position",r.AddClass("AnitaLabel");let i=posPickerNormalize(void 0!==t.currentValue?t.currentValue:t.defaultValue||"0,200"),o=!1;const l=$.CreatePanel("Panel",a,"");l.AddClass("AnitaSliderValueGroup"),l.AddClass("SliderValueGroup"),l.AddClass("AnitaPositionPickerGroup"),l.style.flowChildren="down",l.style.verticalAlign="center",l.style.horizontalAlign="left",l.style.width="332px",l.style.overflow="noclip";const s=$.CreatePanel("Panel",l,"");s.AddClass("AnitaHueSliderGroup"),s.AddClass("AnitaPositionSliderRow"),s.style.overflow="noclip";const u=$.CreatePanel("Label",s,"");u.text="L/R",u.AddClass("AnitaHueValue"),u.AddClass("AnitaPositionAxisLabel");const c=$.CreatePanel("Panel",s,"");c.AddClass("AnitaSliderContainer"),c.AddClass("SliderContainer"),c.AddClass("AnitaPositionSliderContainer"),c.style.width="230px",c.style.height="26px",c.style.padding="0px",c.style.verticalAlign="center",c.style.overflow="noclip";const d=$.CreatePanel("Slider",c,"",{direction:"horizontal"});if(d.AddClass("AnitaSlider"),d.AddClass("HorizontalSlider"),d.style.width="100%",d.style.height="100%",d.style.verticalAlign="center",d.style.overflow="noclip",d.min=0,d.max=400,d.increment=1,"function"==typeof d.SetShowDefaultValue)try{d.SetShowDefaultValue(!1)}catch(e){}if("function"==typeof d.SetRequiresSelection)try{d.SetRequiresSelection(!1)}catch(e){}const h=$.CreatePanel("Label",s,"");h.AddClass("AnitaSliderValue"),h.AddClass("AnitaPositionReadout"),h.style.textOverflow="clip",h.style.overflow="noclip";const f=$.CreatePanel("Panel",l,"");f.AddClass("AnitaHueSliderGroup"),f.AddClass("AnitaPositionSliderRow"),f.style.overflow="noclip";const p=$.CreatePanel("Label",f,"");p.text="T/B",p.AddClass("AnitaHueValue"),p.AddClass("AnitaPositionAxisLabel");const C=$.CreatePanel("Panel",f,"");C.AddClass("AnitaSliderContainer"),C.AddClass("SliderContainer"),C.AddClass("AnitaPositionSliderContainer"),C.style.width="230px",C.style.height="26px",C.style.padding="0px",C.style.verticalAlign="center",C.style.overflow="noclip";const v=$.CreatePanel("Slider",C,"",{direction:"horizontal"});if(v.AddClass("AnitaSlider"),v.AddClass("HorizontalSlider"),v.style.width="100%",v.style.height="100%",v.style.verticalAlign="center",v.style.overflow="noclip",v.min=0,v.max=400,v.increment=1,"function"==typeof v.SetShowDefaultValue)try{v.SetShowDefaultValue(!1)}catch(e){}if("function"==typeof v.SetRequiresSelection)try{v.SetRequiresSelection(!1)}catch(e){}const m=$.CreatePanel("Label",f,"");function syncPosition(e,a){var r,l=posPickerNormalize(e),s=(r=posPickerNormalize(l),Math.round(r.x)+","+Math.round(r.y)),u=String(t.currentValue||"")!==s;if(i=l,t.currentValue=s,d&&d.IsValid&&d.IsValid()&&Number(d.value)!==l.x){o=!0;try{"function"==typeof d.SetValueNoEvents?d.SetValueNoEvents(l.x):d.value=l.x}finally{o=!1}}if(v&&v.IsValid&&v.IsValid()&&Number(v.value)!==l.y){o=!0;try{"function"==typeof v.SetValueNoEvents?v.SetValueNoEvents(l.y):v.value=l.y}finally{o=!1}}h.text=function(e){var t=posPickerNormalize(e);return Math.round(t.x/4)}(l)+"%",m.text=Math.round(l.y/4)+"%",a&&u&&(t.onChange&&t.onChange(s),t.id&&n&&emitUpdate(n,t.id,s))}return m.AddClass("AnitaSliderValue"),m.AddClass("AnitaPositionReadout"),m.style.textOverflow="clip",m.style.overflow="noclip",syncPosition(i,!1),d.SetPanelEvent("onvaluechanged",function(){o||syncPosition({x:d.value,y:i.y},!0)}),v.SetPanelEvent("onvaluechanged",function(){o||syncPosition({x:i.x,y:v.value},!0)}),d.SetPanelEvent("oncancel",()=>{g.toggle(!1)}),v.SetPanelEvent("oncancel",()=>{g.toggle(!1)}),a}(_,t,e.title);break;case"colorpicker":n=AnitaComponents_createColorPicker(_,t,e.title)}t.__anitaRowPanel=n||null}),this.refreshConditionalVisibility(e)),e.storageNamespace){var x=$.CreatePanel("Panel",s,"");x.AddClass("AnitaFooterWrap");var V=$.CreatePanel("Panel",x,"");function makeFooterBtn(e,t,n){var a=$.CreatePanel("Button",e,n||"");a.AddClass("AnitaFooterBtn");var r=$.CreatePanel("Label",a,"");return r.text=t,{btn:a,lbl:r}}function flashLabel(e,t,n,a){if(e&&e.IsValid&&e.IsValid()&&t&&t.IsValid&&t.IsValid()){var r=t.text;t.text=n,e.AddClass("AnitaFooterBtnSuccess"),$.Schedule(a,function(){t&&t.IsValid()&&(t.text=r),e&&e.IsValid()&&e.RemoveClass("AnitaFooterBtnSuccess")})}}V.AddClass("AnitaFooterRow");var I=makeFooterBtn(V,"Copy","");I.btn.SetPanelEvent("onactivate",function(){g.syncSaveCodeInput(e);var t=g.getSaveCodeToken(e);if(t)try{$.DispatchEvent("CopyStringToClipboard",t,$.GetContextPanel()),flashLabel(I.btn,I.lbl,"Copied!",1.5)}catch(e){flashLabel(I.btn,I.lbl,"Failed",1.5)}else flashLabel(I.btn,I.lbl,"Empty",1.5)});var F=makeFooterBtn(V,"Reset","");F.btn.SetPanelEvent("onactivate",function(){v.applyResolvedValues(e,{}),v.persistConfig(e,!0),g.syncSaveCodeInput(e),flashLabel(F.btn,F.lbl,"Reset",1.5),g.renderModSettings(e),m.emitCurrentValues(e,{update_source:"ui_reset",force_persist:!0})});var w=!1,E=makeFooterBtn(V,"Import",""),B=$.CreatePanel("Panel",x,"");B.AddClass("AnitaPasteRow"),B.AddClass("AnitaPasteRowCompact"),B.hittest=!0,B.style.visibility="collapse",B.style.height="0px",B.style.marginTop="0px";var R=$.CreatePanel("Label",B,"");R.AddClass("AnitaLabel"),R.text="Import Code",R.style.width="84px",R.style.fontSize="12px",R.style.marginRight="6px";var k=$.CreatePanel("TextEntry",B,"");k.AddClass("AnitaPasteInput"),k.placeholder="Paste a code to apply...",k.style.width="320px",k.style.height="24px",k.style.fontSize="9px",e.__anitaImportCodeInput=k;var N=makeFooterBtn(B,"Apply","");function setImportRowVisible(e){w=!!e,B&&B.IsValid&&B.IsValid()&&(B.style.visibility=w?"visible":"collapse",B.style.height=w?"30px":"0px",B.style.marginTop=w?"6px":"0px"),E&&E.lbl&&(E.lbl.text=w?"Hide":"Import")}function applySaveCodeInput(){var t=String(k.text||"").trim();if(t){var n=g.extractSaveCodeToken(e,t);if(n){var a=n.split("]:")[1]||"";try{var r=C.decode(a),i=v.parseStoredPayload(e,r,"code");if(!i)return void flashLabel(N.btn,N.lbl,"Invalid",1.5);if(!i.values||!Object.keys(i.values).length)return void flashLabel(N.btn,N.lbl,"No IDs",1.5);v.applyResolvedValues(e,i.values),v.persistConfig(e,!0),g.syncSaveCodeInput(e),flashLabel(N.btn,N.lbl,"Applied",1.5),g.renderModSettings(e),e.__anitaImportCodeInput&&e.__anitaImportCodeInput.IsValid&&e.__anitaImportCodeInput.IsValid()&&(e.__anitaImportCodeInput.text=""),m.emitCurrentValues(e,{update_source:"ui_code_apply",force_persist:!0})}catch(e){flashLabel(N.btn,N.lbl,"Invalid",1.5)}}else flashLabel(N.btn,N.lbl,"Invalid",1.5)}else flashLabel(N.btn,N.lbl,"Empty",1.5)}E.btn.SetPanelEvent("onactivate",function(){setImportRowVisible(!w)}),N.btn.SetPanelEvent("onactivate",applySaveCodeInput),k.SetPanelEvent("ontextentrysubmit",applySaveCodeInput),setImportRowVisible(!1)}}},m={registeredMods:[],init:function(){const e=this.getRoot($.GetContextPanel());g.initWindow(e),e.AnitaUI={GetVersion:()=>"2.2.3",Register:e=>this.registerMod(e),Toggle:()=>g.toggle(),IsReady:()=>!0},this.setupEventListener(),this.createOverlayButton(e),this.monitorEscapeMenu(e),0===this.registeredMods.length&&this.registerMod({title:"Anita-UI",description:"No detected mods. Check your installed mods.",isDummy:!0,elements:[]}),$.DispatchEvent("ClientUI_FireOutput",JSON.stringify({magic_word:"ANITA_ALIVE"}))},registerMod:function(e){e&&e.title,1===this.registeredMods.length&&this.registeredMods[0].isDummy&&(this.registeredMods=[],g.menuArea.RemoveAndDeleteChildren(),g.contentArea.RemoveAndDeleteChildren()),v.hydrateConfig(e);for(let t=0;t<this.registeredMods.length;t++)if(this.registeredMods[t].title===e.title)return;this.registeredMods.push(e),e.title,g.addTab(e.title,()=>{g.renderModSettings(e)}),this.updateWindowWidth(),$.DispatchEvent("ClientUI_FireOutput",JSON.stringify({magic_word:"ANITA_HANDSHAKE",mod_title:e.title})),v.requestBootstrap(e,"core_handshake")},emitCurrentValues:function(e,t){if(e&&Array.isArray(e.elements)){e.title;for(var n=0;n<e.elements.length;n++){var a=e.elements[n];a&&a.id&&void 0!==a.currentValue&&emitUpdate(e.title,a.id,a.currentValue,t)}}},findRegisteredMod:function(e){for(var t=0;t<this.registeredMods.length;t++)if(this.registeredMods[t]&&this.registeredMods[t].title===e)return this.registeredMods[t];return null},queueRenderRefresh:function(e){if(e){var t=(e.__anitaUiRefreshToken||0)+1;e.__anitaUiRefreshToken=t,$.Schedule(.05,function(){e&&e.__anitaUiRefreshToken===t&&g.activeModTitle===e.title&&g.renderModSettings(e)})}},emitPortableSync:function(e,t){e&&"HP Colors"===e.title&&this.emitCurrentValues(e,{update_source:"core_auto_resync",skip_bridge_persist:!0,sync_reason:String(t||"tick")})},queuePortableSyncBurst:function(e,t){if(e&&"HP Colors"===e.title){var n=(e.__anitaPortableSyncBurstToken||0)+1;e.__anitaPortableSyncBurstToken=n;for(var a=[.35,1,2],r=0;r<a.length;r++)(function(n,a,r){$.Schedule(n,function(){e&&e.__anitaPortableSyncBurstToken===a&&m.emitPortableSync(e,String(t||"burst")+"_"+String(r+1))})})(a[r],n,r)}},startPortableSyncLoop:function(e){if(e&&"HP Colors"===e.title&&(e.__anitaPortableSyncReason=String(e.__anitaPortableSyncReason||"update"),!e.__anitaPortableSyncLoopStarted)){e.__anitaPortableSyncLoopStarted=!0;var tick=()=>{e&&(this.findRegisteredMod(e.title)===e?(this.emitPortableSync(e,"heartbeat_"+String(e.__anitaPortableSyncReason||"update")),$.Schedule(3,tick)):e.__anitaPortableSyncLoopStarted=!1)};$.Schedule(3,tick)}},handleUpdateEvent:function(e){if(e&&e.mod_title&&e.setting_id){var t=this.findRegisteredMod(e.mod_title);if(t){var n=String(e.update_source||""),a="bridge_bootstrap"===n,r=a||"ui_resync"===n||"ui_reset"===n||"ui_code_apply"===n||"core_auto_resync"===n;if(e.mod_title,v.applyUpdate(t,e.setting_id,e.value)){g.syncSaveCodeInput(t),a&&(t.__anitaBootstrapReceived=!0),r||"HP Colors"!==t.title||(t.__anitaPortableSyncReason="update_"+String(e.setting_id),this.emitPortableSync(t,"update_"+String(e.setting_id)+"_immediate"),this.queuePortableSyncBurst(t,"update_"+String(e.setting_id)),this.startPortableSyncLoop(t));var i=(t.__anitaPendingWriteToken||0)+1;t.__anitaPendingWriteToken=i,$.Schedule(2,function(){t&&t.__anitaPendingWriteToken===i&&v.persistConfig(t,!1)}),(this.hasVisibilityDependents(t,e.setting_id)||a)&&this.queueRenderRefresh(t)}}}},handleBootstrapRequest:function(e){if(e&&e.mod_title){var t=this.findRegisteredMod(e.mod_title);t?("HP Colors"===e.mod_title&&(t.__anitaPortableSyncLoopStarted||(t.__anitaPortableSyncReason=String(t.__anitaPortableSyncReason||"bootstrap_request"),this.startPortableSyncLoop(t))),this.emitCurrentValues(t,{update_source:"bridge_bootstrap",skip_bridge_persist:!0,bootstrap_reason:String(e.reason||"request")})):e.mod_title}},updateWindowWidth:function(){if(!g.mainWindow)return;const e=this.registeredMods.length;let t=null;1===e&&this.registeredMods[0].isDummy?t=500:e<=4&&(t=300*e),g.mainWindow.style.minWidth=t?t+"px":"90%"},setupEventListener:function(){try{$.RegisterForUnhandledEvent("ClientUI_FireOutput",e=>{try{let t="string"==typeof e?JSON.parse(e):e;t&&"ANITA_REGISTER"===t.magic_word?(t.config&&t.config.title,this.registerMod(t.config)):t&&"ANITA_REQUEST_BOOTSTRAP"===t.magic_word?(t.mod_title,this.handleBootstrapRequest(t)):t&&"ANITA_UPDATE"===t.magic_word&&this.handleUpdateEvent(t)}catch(e){}})}catch(e){}},createOverlayButton:function(e){const t=e.FindChildTraverse(r);t&&t.DeleteAsync(0);const n=$.CreatePanel("Button",e,r);n.AddClass("AnitaOverlayBtn"),this._overlayBtn=n,n.SetPanelEvent("onmouseover",()=>$.DispatchEvent("UIShowTextTooltip",n,"Anita-UI Settings")),n.SetPanelEvent("onmouseout",()=>$.DispatchEvent("UIHideTextTooltip",n)),n.SetPanelEvent("onactivate",()=>g.toggle())},monitorEscapeMenu:function(e){let t=this._hudPanel,n=this._overlayBtn,a=d;if(!t||!t.IsValid||!t.IsValid()){if(t=e.FindChildTraverse(i),!t){let e=$.GetContextPanel();for(;e;){if(e.id===i){t=e;break}e=e.GetParent()}}this._hudPanel=t||null}if(n&&n.IsValid&&n.IsValid()||(n=e.FindChildTraverse(r),this._overlayBtn=n||null),"boolean"!=typeof this._lastEscapeState&&(this._lastEscapeState=!1),t&&n){const e=t.BHasClass(o),r=this._lastEscapeState!==e;if(!r&&n.BHasClass(s)||n.SetHasClass(s,e),!!n.hittest!==e&&(n.hittest=e),r)if(e){const e=(this._attentionToken||0)+1;this._attentionToken=e,n.AddClass(u),$.Schedule(4,()=>{this._attentionToken===e&&n&&n.IsValid&&n.IsValid()&&n.RemoveClass(u)})}else n.RemoveClass(u);this._lastEscapeState=e,!e&&g.isOpen&&g.toggle(!1),a=e?d:Math.max(8*d,.25)}else a=Math.max(4*d,.2);$.Schedule(a,()=>this.monitorEscapeMenu(e))},getRoot:function(e){for(;e.GetParent&&e.GetParent();)e=e.GetParent();return e}};m.init()}();
\ No newline at end of file
+!function(){"use strict";const e="AnitaUI_Window",t="AnitaUI_Backdrop",n="AnitaOverlayBtn",a="Open",r="Visible",i="Attention",o=.05,l={hp_enabled:"e",hp_mode:"m",hp_low_threshold:"l",hp_high_threshold:"h",hp_bg_visible:"b",hp_team_colors:"t",hp_color_low:"cl",hp_color_mid:"cm",hp_color_high:"ch",hp_counter_size:"s",hp_counter_position:"p",hp_text_color_mode:"tm",hp_level_number_visible:"lnv",hp_text_color_low:"tl",hp_text_color_mid:"ti",hp_text_color_high:"th",hp_pulse_bpm:"bp",hp_pulse_intensity:"pi",hp_pulse_enabled:"pe",hp_pulse_text_enabled:"pte",hp_pulse_text_scale:"pts",hp_pulse_text_position:"ptp",hp_pulse_hide_bar:"phb",hp_skip_buildings:"sb",hp_pulse_threshold:"pt",hp_friend_enabled:"fe",hp_friend_pulse_enabled:"fpe",hp_friend_pulse_bpm:"fpb",hp_friend_pulse_intensity:"fpi",hp_friend_pulse_threshold:"fpt",hp_friend_color_low:"fcl",hp_friend_color_mid:"fcm",hp_friend_color_high:"fch",hp_friend_pulse_color_enabled:"fpce",hp_friend_pulse_color:"fpc",hp_kill_zone_enabled:"kze",hp_kill_zone_threshold:"kzt",hp_kill_zone_color:"kzc",hp_kill_zone_width:"kzw"},s=function(){var e={};for(var t in l)Object.prototype.hasOwnProperty.call(l,t)&&(e[l[t]]=t);return e}();var u,d=(u="ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789-_",{encode:function(e){for(var t=[],n=0;n<e.length;n++){var a=e.charCodeAt(n);a<128?t.push(a):a<2048?(t.push(192|a>>6),t.push(128|63&a)):(t.push(224|a>>12),t.push(128|a>>6&63),t.push(128|63&a))}for(var r="",i=0;i<t.length;i+=3){var o=t[i],l=t[i+1]||0,s=t[i+2]||0;r+=u[o>>2],r+=u[(3&o)<<4|l>>4],r+=i+1<t.length?u[(15&l)<<2|s>>6]:"",r+=i+2<t.length?u[63&s]:""}return r},decode:function(e){for(var t={},n=0;n<64;n++)t[u[n]]=n;function getVal(e){if(void 0===e)return 0;if(!Object.prototype.hasOwnProperty.call(t,e))throw new Error("Invalid base64url char: "+e);return t[e]}for(var a=[],r=0;r<e.length;r+=4){var i=getVal(e[r]),o=getVal(e[r+1]),l=void 0!==e[r+2]?getVal(e[r+2]):0,s=void 0!==e[r+3]?getVal(e[r+3]):0;a.push(i<<2|o>>4),void 0!==e[r+2]&&a.push((15&o)<<4|l>>2),void 0!==e[r+3]&&a.push((3&l)<<6|s)}for(var d="",c=0;c<a.length;c++){var h=a[c];if(h<128)d+=String.fromCharCode(h);else if(h<224)d+=String.fromCharCode((31&h)<<6|63&a[++c]);else{var f=a[++c],p=a[++c];d+=String.fromCharCode((15&h)<<12|(63&f)<<6|63&p)}}return d}});function emitUpdate(e,t,n,a){var r={magic_word:"ANITA_UPDATE",mod_title:e,setting_id:t,value:n};if(a&&"object"==typeof a)for(var i in a)Object.prototype.hasOwnProperty.call(a,i)&&(r[i]=a[i]);$.DispatchEvent("ClientUI_FireOutput",JSON.stringify(r))}const c={normalizeNamespace:function(e){return String(e||"").toLowerCase().replace(/[^a-z0-9_]+/g,"_").replace(/^_+|_+$/g,"")},getVersion:function(e){var t=Number(e&&e.storageVersion);return!isFinite(t)||t<1?1:Math.floor(t)},hasPersistentConfig:function(e){return this.normalizeNamespace(e&&e.storageNamespace).length>0},isHpColorsConfig:function(e){return!!e&&"HP Colors"===String(e.title||"")&&"hp_colors"===this.normalizeNamespace(e.storageNamespace)},CONVAR_KEY:"deadlock_hero_debuts_seen",TOKEN_PREFIX:"ANITA-v1-",getTokenRegex:function(e){return new RegExp("\\["+this.TOKEN_PREFIX+e+"\\]:[A-Za-z0-9_-]+")},getCleanupRegex:function(e){return new RegExp("\\["+this.TOKEN_PREFIX+e+"\\]:[A-Za-z0-9_-]*","g")},canWriteConvarDirect:function(){return"undefined"!=typeof GameInterfaceAPI&&!!GameInterfaceAPI&&("function"==typeof GameInterfaceAPI.ConsoleCommand||"function"==typeof GameInterfaceAPI.SetSettingString)},getStorageKey:function(e){var t=this.normalizeNamespace(e&&e.storageNamespace);return t?"anita_v1_"+t:""},getRootPanel:function(){for(var e=$.GetContextPanel();e&&e.GetParent&&e.GetParent();)e=e.GetParent();return e||null},getSessionEncoded:function(e){var t=this.getStorageKey(e);if(!t)return"";var n=this.getRootPanel(),a="",r="";try{n&&n.GetAttributeString&&(a=String(n.GetAttributeString(t,"")||""))}catch(e){}try{var i=n&&n.FindChildTraverse?n.FindChildTraverse("Hud"):null;i&&i.GetAttributeString&&(r=String(i.GetAttributeString(t,"")||""))}catch(e){}return a||r||""},hasPersistentStorage:function(){try{return!(!$||!$.persistentStorage||"function"!=typeof $.persistentStorage.getItem||"function"!=typeof $.persistentStorage.setItem)}catch(e){return!1}},writeSessionMirror:function(e,t){var n=this.getStorageKey(e);if(n)try{var a=this.getRootPanel();a&&a.SetAttributeString&&a.SetAttributeString(n,t);var r=a&&a.FindChildTraverse?a.FindChildTraverse("Hud"):null;r&&r.SetAttributeString&&r.SetAttributeString(n,t)}catch(e){}},writeHpSharedSnapshot:function(e){if(this.isHpColorsConfig(e)&&Array.isArray(e.elements))try{for(var t={},n=0;n<e.elements.length;n++){var a=e.elements[n];a&&a.id&&this.shouldPersistElement(a)&&(t[a.id]=this.sanitizeValue(a,void 0!==a.currentValue?a.currentValue:a.defaultValue))}"undefined"!=typeof GameUI&&GameUI&&GameUI.CustomUIConfig&&(GameUI.CustomUIConfig().__hpColorsCfgRaw=JSON.stringify(t))}catch(e){}},writeConvar:function(e,t){"undefined"!=typeof GameInterfaceAPI&&GameInterfaceAPI&&"function"==typeof GameInterfaceAPI.SetSettingString?GameInterfaceAPI.SetSettingString(e,t):"undefined"!=typeof GameInterfaceAPI&&GameInterfaceAPI&&"function"==typeof GameInterfaceAPI.ConsoleCommand&&GameInterfaceAPI.ConsoleCommand(e+' "'+t+'"')},writeConvarBestEffort:function(e,t){var n=String(e||"")+' "'+String(t||"")+'"';if(this.canWriteConvarDirect())try{return this.writeConvar(e,t),!0}catch(e){}if(function(e){var t=String(e||"").trim(),n=!1;if(!t)return!1;try{$.DispatchEvent("CitadelConCommand",t),n=!0}catch(e){}try{"undefined"!=typeof GameInterfaceAPI&&GameInterfaceAPI&&"function"==typeof GameInterfaceAPI.ConsoleCommand&&(GameInterfaceAPI.ConsoleCommand(t),n=!0)}catch(e){}try{$.DispatchEvent("ConsoleCommand",t),n=!0}catch(e){}try{$.DispatchEvent("GameUIRunCommand",t),n=!0}catch(e){}return n}(n)){try{"undefined"!=typeof GameInterfaceAPI&&GameInterfaceAPI&&"function"==typeof GameInterfaceAPI.SetSettingString&&GameInterfaceAPI.SetSettingString(e,t)}catch(e){}return!0}return!1},getElements:function(e){return e&&Array.isArray(e.elements)?e.elements:[]},warnPersistence:function(e,t){e&&e.title&&String(e.title),e&&e.__anitaLastPersistWarning===t||e&&(e.__anitaLastPersistWarning=t)},shouldPersistElement:function(e){return!(!e||!e.id||"button"===e.type)},sanitizeValue:function(e,t){if(!e)return t;var n=e.defaultValue,a=String(e.type||"");if("toggle"===a){if(!0===t||!1===t)return t;if(1===t||"1"===t)return!0;if(0===t||"0"===t)return!1;if("string"==typeof t){var r=t.toLowerCase();if("true"===r)return!0;if("false"===r)return!1}return!!n}if("cycler"===a){var i=Array.isArray(e.options)?e.options.length:0,o=Number(t);if(isFinite(o)||(o=Number(n)),isFinite(o)||(o=0),(o=Math.round(o))<0&&(o=0),i>0&&o>=i){var l=Number(n);(!isFinite(l)||l<0||l>=i)&&(l=0),o=l}return o}if("stepper"===a||"slider"===a){var s=Number(t);isFinite(s)||(s=Number(n)),isFinite(s)||(s=0);var u=Number(e.min),d=Number(e.max);isFinite(u)&&s<u&&(s=u),isFinite(d)&&s>d&&(s=d);var c=Number(e.step);return isFinite(c)&&0!==c||(c=1),s=Math.round(c)===c?Math.round(s):parseFloat(s.toFixed(2)),isFinite(u)&&s<u&&(s=u),isFinite(d)&&s>d&&(s=d),s}return"colorpicker"===a?"string"==typeof t&&t.length>0?t:"string"==typeof n&&n.length>0?n:"#FFFFFF":void 0!==t?t:n},ensureDefaults:function(e){for(var t=this.getElements(e),n=0;n<t.length;n++){var a=t[n];if(this.shouldPersistElement(a)){var r=void 0!==a.currentValue?a.currentValue:a.defaultValue;a.currentValue=this.sanitizeValue(a,r)}else void 0===a.currentValue&&void 0!==a.defaultValue&&(a.currentValue=a.defaultValue)}},expandStoredValues:function(e,t){if(!t||"object"!=typeof t||!t.values||"object"!=typeof t.values)return null;var n=t.values,a={};if(!this.isHpColorsConfig(e)||1!==t.c&&!0!==t.compact){for(var r in n)Object.prototype.hasOwnProperty.call(n,r)&&(a[r]=n[r]);return a}for(var i in n)if(Object.prototype.hasOwnProperty.call(n,i)){var o=s[i];o&&(a[o]=n[i])}return a},parseStoredPayload:function(e,t,n){var a=String(t||"");if(!a)return null;var r=null;try{r=JSON.parse(a)}catch(e){return null}if(!r||"object"!=typeof r)return null;var i=this.expandStoredValues(e,r);if(!i)return null;for(var o={},l=this.getElements(e),s=0;s<l.length;s++){var u=l[s];this.shouldPersistElement(u)&&Object.prototype.hasOwnProperty.call(i,u.id)&&(o[u.id]=this.sanitizeValue(u,i[u.id]))}return{raw:a,values:o}},readPersistentStoragePayload:function(e){var t=this.getStorageKey(e),n=this.hasPersistentStorage();if(!t||!n)return null;var a="";try{a=String($.persistentStorage.getItem(t)||"")}catch(e){return null}if(!a)return null;var r="";try{r=d.decode(a)}catch(e){return null}var i=this.parseStoredPayload(e,r,"persistentStorage");return i?{raw:i.raw,encoded:a,values:i.values,source:"persistentStorage"}:null},readConvarPayload:function(e){var t=this.normalizeNamespace(e&&e.storageNamespace);if(!t)return null;if("undefined"==typeof GameInterfaceAPI||!GameInterfaceAPI||"function"!=typeof GameInterfaceAPI.GetSettingString)return null;var n="";try{n=String(GameInterfaceAPI.GetSettingString(this.CONVAR_KEY)||"")}catch(e){return null}var a=n.match(new RegExp("\\["+this.TOKEN_PREFIX+t+"\\]:([A-Za-z0-9_-]+)"));if(!a)return null;var r=String(a[1]||"");if(!r)return null;var i="";try{i=d.decode(r)}catch(e){return null}var o=this.parseStoredPayload(e,i,"convar");return o?{raw:o.raw,encoded:r,values:o.values,source:"convar"}:null},readStoredPayload:function(e){var t=null;try{var n=this.getSessionEncoded(e);if(n){var a=d.decode(n);if(t=this.parseStoredPayload(e,a,"session"))return{raw:t.raw,encoded:n,values:t.values,source:"session"}}}catch(e){}return(t=this.readPersistentStoragePayload(e))||(t=this.readConvarPayload(e))?(e&&e.title,t):null},applyResolvedValues:function(e,t){for(var n=this.getElements(e),a=0;a<n.length;a++){var r=n[a];if(this.shouldPersistElement(r)){var i=Object.prototype.hasOwnProperty.call(t||{},r.id)?t[r.id]:r.defaultValue;r.currentValue=this.sanitizeValue(r,i)}else void 0===r.currentValue&&void 0!==r.defaultValue&&(r.currentValue=r.defaultValue)}},hydrateConfig:function(e){this.ensureDefaults(e);var t="defaults";if(this.normalizeNamespace(e&&e.storageNamespace),!this.hasPersistentConfig(e))return e.__anitaLastPersistedRaw="",void this.applyResolvedValues(e,{});var n=this.readStoredPayload(e);n&&(t=n.source),n?(this.applyResolvedValues(e,n.values),n.encoded&&"session"!==t&&this.writeSessionMirror(e,n.encoded)):this.applyResolvedValues(e,{}),e.__anitaLastPersistedRaw=n?n.raw:"",this.writeHpSharedSnapshot(e),e&&e.title},buildStoredPayload:function(e){for(var t=this.getElements(e),n={},a=this.isHpColorsConfig(e),r=0;r<t.length;r++){var i=t[r];if(this.shouldPersistElement(i)){var o=this.sanitizeValue(i,void 0!==i.currentValue?i.currentValue:i.defaultValue);i.currentValue=o,a&&o===this.sanitizeValue(i,i.defaultValue)||(n[a&&l[i.id]||i.id]=o)}}return a?JSON.stringify({v:this.getVersion(e),c:1,values:n}):JSON.stringify({version:this.getVersion(e),values:n})},persistConfig:function(e,t){if(!this.hasPersistentConfig(e))return!1;var n=this.buildStoredPayload(e);if(!n)return!1;if(!t&&n===String(e.__anitaLastPersistedRaw||""))return!1;var a=this.normalizeNamespace(e.storageNamespace),r="";try{r=d.encode(n)}catch(e){return!1}var i="["+this.TOKEN_PREFIX+a+"]:"+r;try{var o="";"undefined"!=typeof GameInterfaceAPI&&GameInterfaceAPI&&"function"==typeof GameInterfaceAPI.GetSettingString&&(o=String(GameInterfaceAPI.GetSettingString(this.CONVAR_KEY)||""));var l=o.replace(this.getCleanupRegex(a),"").replace(/,,+/g,",").replace(/^,|,$/,""),s=(l?l+",":"")+i;s.length>1900?this.warnPersistence(e,"convar skipped (payload_too_large)"):this.writeConvarBestEffort(this.CONVAR_KEY,s)||this.warnPersistence(e,"convar unavailable")}catch(e){}try{if(this.hasPersistentStorage()){var u=this.getStorageKey(e);u&&$.persistentStorage.setItem(u,r)}}catch(e){}return this.writeSessionMirror(e,r),this.writeHpSharedSnapshot(e),e.__anitaLastPersistedRaw=n,!0},requestBootstrap:function(e,t){if(this.hasPersistentConfig(e)){var n={magic_word:"ANITA_REQUEST_BOOTSTRAP",mod_title:e.title,storageNamespace:this.normalizeNamespace(e.storageNamespace),reason:String(t||"request")};$.DispatchEvent("ClientUI_FireOutput",JSON.stringify(n))}},applyUpdate:function(e,t,n){for(var a=this.getElements(e),r=0;r<a.length;r++){var i=a[r];if(i&&i.id===t)return i.currentValue=this.sanitizeValue(i,n),this.writeHpSharedSnapshot(e),!0}return!1}},h={mainWindow:null,backdrop:null,navBar:null,menuArea:null,contentArea:null,activeModTitle:"",isOpen:!1,activeColorPickerClose:null,findElementById:function(e,t){if(!e||!Array.isArray(e.elements)||!t)return null;for(var n=0;n<e.elements.length;n++){var a=e.elements[n];if(a&&a.id===t)return a}return null},isElementVisible:function(e,t){if(!t||!t.visibleWhen)return!0;var n=t.visibleWhen,a=this.findElementById(e,n.id);if(!a)return!0;var r=a.currentValue;if(Array.isArray(n.equals)){for(var i=0;i<n.equals.length;i++)if(r===n.equals[i])return!0;return!1}return Object.prototype.hasOwnProperty.call(n,"equals")?r===n.equals:!!r},applyElementVisibility:function(e,t){if(t&&t.__anitaRowPanel&&t.__anitaRowPanel.IsValid&&t.__anitaRowPanel.IsValid()){var n=this.isElementVisible(e,t);t.__anitaRowPanel.style.visibility=n?"visible":"collapse",t.__anitaRowPanel.hittest=n}},refreshConditionalVisibility:function(e){if(e&&Array.isArray(e.elements))for(var t=0;t<e.elements.length;t++)this.applyElementVisibility(e,e.elements[t])},hasVisibilityDependents:function(e,t){if(!e||!Array.isArray(e.elements)||!t)return!1;for(var n=0;n<e.elements.length;n++){var a=e.elements[n];if(a&&a.visibleWhen&&a.visibleWhen.id===t)return!0}return!1},getElementCategory:function(e){return String(e&&e.category||"General").trim()||"General"},getCategoryList:function(e){var t=[],n={};if(!e||!Array.isArray(e.elements))return t;for(var a=0;a<e.elements.length;a++){var r=this.getElementCategory(e.elements[a]);n[r]||(n[r]=!0,t.push(r))}return t},getCategoryElements:function(e,t){var n=[];if(!e||!Array.isArray(e.elements))return n;for(var a=0;a<e.elements.length;a++){var r=e.elements[a];this.getElementCategory(r)===t&&n.push(r)}return n},ensureActiveCategory:function(e){var t=this.getCategoryList(e);if(0===t.length)return e.__anitaActiveCategory="","";for(var n=String(e.__anitaActiveCategory||""),a=0;a<t.length;a++)if(t[a]===n)return n;return e.__anitaActiveCategory=t[0],t[0]},getSaveCodeToken:function(e){if(!e||!e.storageNamespace)return"";var t=c.buildStoredPayload(e);if(!t)return"";var n=c.normalizeNamespace(e.storageNamespace);return n?"["+c.TOKEN_PREFIX+n+"]:"+d.encode(t):""},extractSaveCodeToken:function(e,t){if(!e||!e.storageNamespace)return"";var n=String(t||"").trim();if(!n)return"";var a=c.normalizeNamespace(e.storageNamespace);if(!a)return"";var r=n.match(c.getTokenRegex(a));if(r&&r[0])return r[0];var i=n.match(/\[ANITA-v1-[a-z0-9_]+\]:[A-Za-z0-9_-]+/i);return i&&i[0]?i[0]:/^[A-Za-z0-9_-]+$/.test(n)?"["+c.TOKEN_PREFIX+a+"]:"+n:""},syncSaveCodeInput:function(e){if(e&&e.__anitaSaveCodeInput&&e.__anitaSaveCodeInput.IsValid&&e.__anitaSaveCodeInput.IsValid()){var t=this.getSaveCodeToken(e);String(e.__anitaSaveCodeInput.text||"")!==t&&(e.__anitaSaveCodeInput.text=t)}},initWindow:function(n){n.FindChildTraverse(e)&&n.FindChildTraverse(e).DeleteAsync(0),n.FindChildTraverse(t)&&n.FindChildTraverse(t).DeleteAsync(0),this.backdrop=$.CreatePanel("Panel",n,t),this.backdrop.AddClass("AnitaBackdrop"),this.backdrop.SetPanelEvent("onactivate",()=>this.toggle(!1)),this.mainWindow=$.CreatePanel("Panel",n,e),this.mainWindow.AddClass("AnitaWindow"),this.mainWindow.canfocus=!0,this.mainWindow.SetPanelEvent("oncancel",()=>this.toggle(!1)),this.mainWindow.SetPanelEvent("onactivate",()=>{this.mainWindow.SetFocus()}),this.navBar=$.CreatePanel("Panel",this.mainWindow,"AnitaUI_NavBar"),this.navBar.AddClass("AnitaNavBar");const a=$.CreatePanel("Button",this.navBar,"");a.AddClass("AnitaCloseBtn"),a.SetPanelEvent("onactivate",()=>this.toggle(!1));const r=$.CreatePanel("Label",this.navBar,"");r.text="/",r.AddClass("AnitaTabSeparator"),this.menuArea=$.CreatePanel("Panel",this.navBar,"AnitaTabContainer"),this.menuArea.AddClass("AnitaTabContainer"),this.contentArea=$.CreatePanel("Panel",this.mainWindow,"AnitaUI_ContentArea"),this.contentArea.AddClass("AnitaContentArea")},toggle:function(e){if(this.mainWindow&&this.backdrop)if(this.isOpen=void 0!==e?e:!this.isOpen,this.mainWindow.SetHasClass(a,this.isOpen),this.mainWindow.hittest=this.isOpen,this.backdrop.SetHasClass(a,this.isOpen),this.backdrop.hittest=this.isOpen,this.isOpen)this.mainWindow.SetFocus();else{if(this.activeColorPickerClose){try{this.activeColorPickerClose()}catch(e){}this.activeColorPickerClose=null}$.DispatchEvent("DropInputFocus",this.mainWindow);let e=$.GetContextPanel();for(;e.GetParent();)e=e.GetParent();e.SetFocus()}},addTab:function(e,t){let n=e;n.length>17&&(n=n.substring(0,17)+"...");const a=$.CreatePanel("Button",this.menuArea,"");a.AddClass("AnitaTabBtn"),$.CreatePanel("Label",a,"").text=n;const r=$.CreatePanel("Label",this.menuArea,"");r.text="/",r.AddClass("AnitaTabSeparator"),a.SetPanelEvent("onactivate",()=>{this.menuArea.Children().forEach(e=>{"Button"!==e.paneltype||e.BHasClass("AnitaCloseBtn")||e.RemoveClass("Active")}),a.AddClass("Active"),this.activeModTitle=e,t()}),this.menuArea.GetChildCount()<=4&&(a.AddClass("Active"),this.activeModTitle=e,t())},renderModSettings:function(e){this.contentArea.RemoveAndDeleteChildren(),this.contentArea.canfocus=!0,this.contentArea.SetPanelEvent("onactivate",()=>this.contentArea.SetFocus());const t=$.CreatePanel("Panel",this.contentArea,"");t.AddClass("ModContainer"),t.canfocus=!0;const n=$.CreatePanel("Panel",t,"BackgroundShield");n.style.width="100%",n.style.height="100%",n.style.ignoreParentFlow="true",n.style.zIndex="-1",n.hittest=!0,n.SetPanelEvent("onactivate",()=>{t.SetFocus()});const a=$.CreatePanel("Label",t,"");if(a.text=e.title,a.AddClass("SectionHeader"),$.CreatePanel("Panel",t,"").AddClass("SectionHeaderLine"),e.description){const M=$.CreatePanel("Label",t,"");M.text=e.description,M.AddClass("ModDescription")}const r=$.CreatePanel("Panel",t,"");r.AddClass("AnitaSettingsShell");const i=$.CreatePanel("Panel",r,"");i.AddClass("AnitaTreePanel");const o=$.CreatePanel("Label",i,"");o.text="Settings",o.AddClass("AnitaTreeHeader");const l=$.CreatePanel("Panel",i,"");l.AddClass("AnitaTreeList");const s=$.CreatePanel("Panel",r,"");s.AddClass("AnitaDetailPanel");const u=this.ensureActiveCategory(e),p=this.getCategoryList(e);for(var C=0;C<p.length;C++){var v=p[C],g=$.CreatePanel("Button",l,"");g.AddClass("AnitaTreeNode"),g.SetHasClass("Active",v===u);var m=$.CreatePanel("Label",g,"");m.AddClass("AnitaTreeNodeGlyph"),m.text=v===u?"v":">";var y=$.CreatePanel("Label",g,"");y.AddClass("AnitaTreeNodeLabel"),y.text=v;var P=$.CreatePanel("Label",g,"");P.AddClass("AnitaTreeNodeCount"),P.text=String(this.getCategoryElements(e,v).length),g.SetPanelEvent("onactivate",function(t){return function(){e.__anitaActiveCategory=t,h.renderModSettings(e)}}(v))}const A=$.CreatePanel("Panel",s,"");A.AddClass("AnitaDetailHeaderRow");const S=$.CreatePanel("Label",A,"");S.text=u||e.title,S.AddClass("AnitaDetailHeader");const b=$.CreatePanel("Label",s,"");b.text="Select a setting group from the tree on the left.",b.AddClass("AnitaDetailHint");const _=$.CreatePanel("Panel",s,"");if(_.AddClass("AnitaSettingsList"),e.elements&&(e.elements.forEach(e=>{e.__anitaRowPanel=null}),this.getCategoryElements(e,u).forEach(t=>{var n=null;switch(t.type){case"toggle":n=function(e,t,n){const a=$.CreatePanel("Panel",e,"");a.AddClass("AnitaToggleRow");const r=$.CreatePanel("Button",a,"");r.AddClass("AnitaToggleBtn");const i=$.CreatePanel("Label",a,"");i.text=t.label||"Option",i.AddClass("AnitaLabel");const o=$.CreatePanel("Panel",a,"");o.AddClass("AnitaCheckBox"),$.CreatePanel("Panel",o,"").AddClass("AnitaCheckMark");let l=void 0!==t.currentValue?t.currentValue:t.defaultValue||!1;const updateState=e=>a.SetHasClass("Checked",e);return updateState(l),r.SetPanelEvent("onactivate",()=>{l=!l,updateState(l),t.currentValue=l,t.id&&emitUpdate(n,t.id,l),t.onChange&&t.onChange(l);var e=f.findRegisteredMod(n);e&&h.hasVisibilityDependents(e,t.id)&&f.queueRenderRefresh(e)}),a}(_,t,e.title);break;case"stepper":n=function(e,t,n){const a=$.CreatePanel("Panel",e,"");a.AddClass("AnitaRow");const r=$.CreatePanel("Label",a,"");r.text=t.label||"Value",r.AddClass("AnitaLabel");const i=$.CreatePanel("Panel",a,"");i.AddClass("AnitaStepperControls");const o=$.CreatePanel("Button",i,"");o.AddClass("AnitaStepBtn"),$.CreatePanel("Label",o,"less").text="-";const l=$.CreatePanel("TextEntry",i,"");l.AddClass("AnitaStepInput");const s=$.CreatePanel("Button",i,"");s.AddClass("AnitaStepBtn"),$.CreatePanel("Label",s,"").text="+";let u=void 0!==t.currentValue?t.currentValue:t.defaultValue||0;const d=t.step||1,c=!Number.isInteger(d);function update(e){e=c?parseFloat(e.toFixed(2)):Math.round(e),u=e,t.currentValue=u,l.text=u.toString(),t.onChange&&t.onChange(u),t.id&&n&&emitUpdate(n,t.id,u)}return l.text=c?u.toFixed(2):u,l.SetPanelEvent("ontextentrychange",()=>{let e=parseFloat(l.text);isNaN(e)||(u=e,t.currentValue=e)}),l.SetPanelEvent("oncancel",()=>{h.toggle(!1)}),o.SetPanelEvent("onactivate",()=>update(u-d)),s.SetPanelEvent("onactivate",()=>update(u+d)),l.SetPanelEvent("oninputsubmit",()=>{update(u),$.DispatchEvent("DropInputFocus",l),h.mainWindow.SetFocus()}),l.SetPanelEvent("onfocusout",()=>{update(u)}),a}(_,t,e.title);break;case"slider":n=function(e,t,n){const a=$.CreatePanel("Panel",e,"");a.AddClass("AnitaRow"),a.AddClass("AnitaSliderRow"),a.style.width="100%";const r=$.CreatePanel("Label",a,"");r.text=t.label||"Value",r.AddClass("AnitaLabel");const i=$.CreatePanel("Panel",a,"");i.AddClass("AnitaSliderValueGroup"),i.AddClass("SliderValueGroup"),i.style.flowChildren="right",i.style.verticalAlign="center",i.style.horizontalAlign="left",i.style.width="296px";const o=$.CreatePanel("Panel",i,"");o.AddClass("AnitaSliderContainer"),o.AddClass("SliderContainer"),o.style.width="230px",o.style.height="26px",o.style.padding="0px",o.style.verticalAlign="center",o.style.overflow="noclip";const l=$.CreatePanel("Slider",o,"",{direction:"horizontal"});l.AddClass("AnitaSlider"),l.AddClass("HorizontalSlider"),l.style.width="100%",l.style.height="100%",l.style.verticalAlign="center",l.style.overflow="noclip";const s=$.CreatePanel("Label",i,"");s.AddClass("AnitaSliderValue");const u=Number(t.min),d=Number(t.max),c=Number(t.step),f=isFinite(u)?u:0,p=isFinite(d)?d:100,C=isFinite(c)&&c>0?c:1;let v=void 0!==t.currentValue?t.currentValue:void 0!==t.defaultValue?t.defaultValue:f,g=!1;function normalize(e){let n=Number(e);return isFinite(n)||(n=Number(t.defaultValue)),isFinite(n)||(n=f),n<f&&(n=f),n>p&&(n=p),n=Math.round(C)===C?Math.round(n):parseFloat(n.toFixed(2)),n}function syncVisuals(e,a){const r=normalize(e);if(v=r,t.currentValue=r,s.text=r.toString()+"%",l&&l.IsValid&&l.IsValid()&&Number(l.value)!==r){g=!0;try{"function"==typeof l.SetValueNoEvents?l.SetValueNoEvents(r):l.value=r}finally{g=!1}}a&&(t.onChange&&t.onChange(r),t.id&&n&&emitUpdate(n,t.id,r))}return l.min=f,l.max=p,l.increment=C,l.value=normalize(v),"function"==typeof l.SetShowDefaultValue&&l.SetShowDefaultValue(!1),"function"==typeof l.SetRequiresSelection&&l.SetRequiresSelection(!1),syncVisuals(v,!1),l.SetPanelEvent("onvaluechanged",function(){g||syncVisuals(l.value,!0)}),l.SetPanelEvent("oncancel",()=>{h.toggle(!1)}),a}(_,t,e.title);break;case"button":n=function(e,t,n){const a=$.CreatePanel("Button",e,"");return a.AddClass("AnitaActionBtn"),$.CreatePanel("Label",a,"").text=t.label||"Action",a.SetPanelEvent("onactivate",()=>{t.onClick&&t.onClick(),t.id&&n&&emitUpdate(n,t.id,!0),a.AddClass("Activated"),$.Schedule(.1,()=>a.RemoveClass("Activated"))}),a}(_,t,e.title);break;case"cycler":n=function(e,t,n){const a=$.CreatePanel("Panel",e,"");a.AddClass("AnitaRow");const r=$.CreatePanel("Label",a,"");r.text=t.label||"Cycle",r.AddClass("AnitaLabel");const i=$.CreatePanel("Button",a,"");i.AddClass("AnitaCyclerBtn");const o=$.CreatePanel("Label",i,""),l=t.options||["OFF","ON"];let s=void 0!==t.currentValue?t.currentValue:t.defaultValue||0;(s<0||s>=l.length)&&(s=0);const updateVisuals=()=>{o.text=l[s]};return updateVisuals(),i.SetPanelEvent("onactivate",()=>{s=(s+1)%l.length,updateVisuals(),t.currentValue=s;var e=f.findRegisteredMod(n);e&&h.hasVisibilityDependents(e,t.id)&&f.queueRenderRefresh(e),t.id&&n&&emitUpdate(n,t.id,s),t.onChange&&t.onChange(s,l[s])}),a}(_,t,e.title);break;case"positionpicker":n=function(e,t,n){const a=$.CreatePanel("Panel",e,"");a.AddClass("AnitaRow"),a.AddClass("AnitaSliderRow"),a.style.overflow="noclip",a.style.width="100%";const r=$.CreatePanel("Label",a,"");function posPickerClamp(e){var t=Number(e);return isFinite(t)||(t=0),t<0&&(t=0),t>400&&(t=400),Math.round(t)}function posPickerNormalize(e){var t=function(e){var t=0,n=200,a=e;if(a&&"object"==typeof a)return Array.isArray(a)?(a.length>0&&(t=posPickerClamp(a[0])),a.length>1&&(n=posPickerClamp(a[1]))):(Object.prototype.hasOwnProperty.call(a,"x")&&(t=posPickerClamp(a.x)),Object.prototype.hasOwnProperty.call(a,"y")&&(n=posPickerClamp(a.y))),{x:t,y:n};if("string"==typeof a){var r=a.match(/-?\d+(?:\.\d+)?/g);if(r&&r.length>0)return t=posPickerClamp(r[0]),r.length>1&&(n=posPickerClamp(r[1])),{x:t,y:n}}return"number"==typeof a?{x:t,y:n=posPickerClamp(a)}:{x:t,y:n}}(e);return{x:posPickerClamp(t.x),y:posPickerClamp(t.y)}}r.text=t.label||"Position",r.AddClass("AnitaLabel");let i=posPickerNormalize(void 0!==t.currentValue?t.currentValue:t.defaultValue||"0,200"),o=!1;const l=$.CreatePanel("Panel",a,"");l.AddClass("AnitaSliderValueGroup"),l.AddClass("SliderValueGroup"),l.AddClass("AnitaPositionPickerGroup"),l.style.flowChildren="down",l.style.verticalAlign="center",l.style.horizontalAlign="left",l.style.width="332px",l.style.overflow="noclip";const s=$.CreatePanel("Panel",l,"");s.AddClass("AnitaHueSliderGroup"),s.AddClass("AnitaPositionSliderRow"),s.style.overflow="noclip";const u=$.CreatePanel("Label",s,"");u.text="L/R",u.AddClass("AnitaHueValue"),u.AddClass("AnitaPositionAxisLabel");const d=$.CreatePanel("Panel",s,"");d.AddClass("AnitaSliderContainer"),d.AddClass("SliderContainer"),d.AddClass("AnitaPositionSliderContainer"),d.style.width="230px",d.style.height="26px",d.style.padding="0px",d.style.verticalAlign="center",d.style.overflow="noclip";const c=$.CreatePanel("Slider",d,"",{direction:"horizontal"});if(c.AddClass("AnitaSlider"),c.AddClass("HorizontalSlider"),c.style.width="100%",c.style.height="100%",c.style.verticalAlign="center",c.style.overflow="noclip",c.min=0,c.max=400,c.increment=1,"function"==typeof c.SetShowDefaultValue)try{c.SetShowDefaultValue(!1)}catch(e){}if("function"==typeof c.SetRequiresSelection)try{c.SetRequiresSelection(!1)}catch(e){}const f=$.CreatePanel("Label",s,"");f.AddClass("AnitaSliderValue"),f.AddClass("AnitaPositionReadout"),f.style.textOverflow="clip",f.style.overflow="noclip";const p=$.CreatePanel("Panel",l,"");p.AddClass("AnitaHueSliderGroup"),p.AddClass("AnitaPositionSliderRow"),p.style.overflow="noclip";const C=$.CreatePanel("Label",p,"");C.text="T/B",C.AddClass("AnitaHueValue"),C.AddClass("AnitaPositionAxisLabel");const v=$.CreatePanel("Panel",p,"");v.AddClass("AnitaSliderContainer"),v.AddClass("SliderContainer"),v.AddClass("AnitaPositionSliderContainer"),v.style.width="230px",v.style.height="26px",v.style.padding="0px",v.style.verticalAlign="center",v.style.overflow="noclip";const g=$.CreatePanel("Slider",v,"",{direction:"horizontal"});if(g.AddClass("AnitaSlider"),g.AddClass("HorizontalSlider"),g.style.width="100%",g.style.height="100%",g.style.verticalAlign="center",g.style.overflow="noclip",g.min=0,g.max=400,g.increment=1,"function"==typeof g.SetShowDefaultValue)try{g.SetShowDefaultValue(!1)}catch(e){}if("function"==typeof g.SetRequiresSelection)try{g.SetRequiresSelection(!1)}catch(e){}const m=$.CreatePanel("Label",p,"");function syncPosition(e,a){var r,l=posPickerNormalize(e),s=(r=posPickerNormalize(l),Math.round(r.x)+","+Math.round(r.y)),u=String(t.currentValue||"")!==s;if(i=l,t.currentValue=s,c&&c.IsValid&&c.IsValid()&&Number(c.value)!==l.x){o=!0;try{"function"==typeof c.SetValueNoEvents?c.SetValueNoEvents(l.x):c.value=l.x}finally{o=!1}}if(g&&g.IsValid&&g.IsValid()&&Number(g.value)!==l.y){o=!0;try{"function"==typeof g.SetValueNoEvents?g.SetValueNoEvents(l.y):g.value=l.y}finally{o=!1}}f.text=function(e){var t=posPickerNormalize(e);return Math.round(t.x/4)}(l)+"%",m.text=Math.round(l.y/4)+"%",a&&u&&(t.onChange&&t.onChange(s),t.id&&n&&emitUpdate(n,t.id,s))}return m.AddClass("AnitaSliderValue"),m.AddClass("AnitaPositionReadout"),m.style.textOverflow="clip",m.style.overflow="noclip",syncPosition(i,!1),c.SetPanelEvent("onvaluechanged",function(){o||syncPosition({x:c.value,y:i.y},!0)}),g.SetPanelEvent("onvaluechanged",function(){o||syncPosition({x:i.x,y:g.value},!0)}),c.SetPanelEvent("oncancel",()=>{h.toggle(!1)}),g.SetPanelEvent("oncancel",()=>{h.toggle(!1)}),a}(_,t,e.title);break;case"colorpicker":n=function(e,t,n){const a=$.CreatePanel("Panel",e,"");a.AddClass("AnitaRow"),a.AddClass("AnitaSliderRow"),a.style.overflow="noclip",a.style.width="100%";const r=$.CreatePanel("Label",a,"");r.text=t.label||"Color",r.AddClass("AnitaLabel");let i=normalizeHexColor(void 0!==t.currentValue?t.currentValue:t.defaultValue||"#FF0000"),o=0,l=1,s=null,u=null,d=null,c=null,f=!1,p=0,C=!1;const v="undefined"!=typeof GameUI&&null!==GameUI;let g=-1,m=-1,y="",P=null,A=null,S=null,b=null,_=null,x=null,V=null,I=null,F=null,w=null;function clampByte(e){let t=Number(e);return isFinite(t)||(t=0),t<0&&(t=0),t>255&&(t=255),Math.round(t)}function byteToHex(e){const t=clampByte(e).toString(16).toUpperCase();return t.length<2?"0"+t:t}function rgbToHex(e,t,n){return"#"+byteToHex(e)+byteToHex(t)+byteToHex(n)}function hsvToHex(e,t,n){var a=function(e,t,n){var a=Number(e),r=Number(t),i=Number(n);isFinite(a)||(a=0),isFinite(r)||(r=0),isFinite(i)||(i=0),a=(a%360+360)%360,r=Math.max(0,Math.min(1,r));var o=(i=Math.max(0,Math.min(1,i)))*r,l=o*(1-Math.abs(a/60%2-1)),s=i-o,u=0,d=0,c=0;return a<60?(u=o,d=l,c=0):a<120?(u=l,d=o,c=0):a<180?(u=0,d=o,c=l):a<240?(u=0,d=l,c=o):a<300?(u=l,d=0,c=o):(u=o,d=0,c=l),[clampByte(255*(u+s)),clampByte(255*(d+s)),clampByte(255*(c+s))]}(e,t,n);return rgbToHex(a[0],a[1],a[2])}function clamp01(e){var t=Number(e);return isFinite(t)||(t=0),t<0&&(t=0),t>1&&(t=1),t}function isValidPanel(e){return!!(e&&e.IsValid&&e.IsValid())}function parsePoint(e){if(!e)return null;if("number"==typeof e.length&&e.length>=2){var t=Number(e[0]),n=Number(e[1]);if(isFinite(t)&&isFinite(n))return{x:t,y:n}}if("object"==typeof e){var a=Number(void 0!==e.x?e.x:e[0]),r=Number(void 0!==e.y?e.y:e[1]);if(isFinite(a)&&isFinite(r))return{x:a,y:r}}return null}function getCursorPosition(){try{return v&&"function"==typeof GameUI.GetCursorPosition?parsePoint(GameUI.GetCursorPosition()):null}catch(e){}return null}function getPanelWindowPosition(e){if(!isValidPanel(e))return null;try{if("function"==typeof e.GetPositionWithinWindow){var t=parsePoint(e.GetPositionWithinWindow());if(t)return t}}catch(e){}var n=Number(e.actualxoffset||e.actualx||0),a=Number(e.actualyoffset||e.actualy||0);return isFinite(n)||(n=0),isFinite(a)||(a=0),{x:n,y:a}}function getPanelBounds(e){if(!isValidPanel(e))return{left:0,top:0,width:1,height:1};var t=getPanelWindowPosition(e),n=t?t.x:Number(e.actualxoffset||e.actualx||0),a=t?t.y:Number(e.actualyoffset||e.actualy||0),r=Number(e.actuallayoutwidth||e.contentwidth||e.width||1),i=Number(e.actuallayoutheight||e.contentheight||e.height||1);return isFinite(n)||(n=0),isFinite(a)||(a=0),(!isFinite(r)||r<=0)&&(r=1),(!isFinite(i)||i<=0)&&(i=1),{left:n,top:a,width:r,height:i}}function getPanelLocalCursorPosition(e){if(!isValidPanel(e))return null;var t=getPanelBounds(e),n=getPanelWindowPosition(e);try{if("function"==typeof e.GetCursorPosition){var a=parsePoint(e.GetCursorPosition());if(a)return a.x>=0&&a.x<=t.width&&a.y>=0&&a.y<=t.height?a:n?{x:a.x-n.x,y:a.y-n.y}:a}}catch(e){}var r=getCursorPosition();return r&&n?{x:r.x-n.x,y:r.y-n.y}:null}function getBoxStateFromColor(e){var t,n,a,r,i,o,l,s,u,d=function(e){if(!e)return[255,255,255];const t=String(e).trim();if("#"===t.charAt(0)){let e=t.slice(1);if(3===e.length&&(e=e[0]+e[0]+e[1]+e[1]+e[2]+e[2]),e.length>=6)return[parseInt(e.slice(0,2),16)||0,parseInt(e.slice(2,4),16)||0,parseInt(e.slice(4,6),16)||0]}const n=t.match(/rgb\((\d+),\s*(\d+),\s*(\d+)\)/i);return n?[clampByte(n[1]),clampByte(n[2]),clampByte(n[3])]:[255,255,255]}(e),c=(t=d[0],n=d[1],a=d[2],r=clampByte(t)/255,i=clampByte(n)/255,o=clampByte(a)/255,u=0,0!==(s=(l=Math.max(r,i,o))-Math.min(r,i,o))&&(u=l===r?(i-o)/s%6*60:l===i?60*((o-r)/s+2):60*((r-i)/s+4)),u<0&&(u+=360),[u,0===l?0:s/l,l]);return{hue:Math.round(c[0])%360,sat:clamp01(c[1])}}function setPickerBoxState(e,t){var n=function(e,t){var n=getBoxStateFromColor(t||i);if(!e)return n;var a=Number(e.hue),r=Number(e.sat);return isFinite(a)||(a=n.hue),isFinite(r)||(r=n.sat),(a=Math.round(a)%360)<0&&(a+=360),{hue:a,sat:r=clamp01(r)}}(e,t);return o=n.hue,l=n.sat,n}function colorFromBoxState(e,t){return hsvToHex(e,t,1)}function hueFromRelX(e){var t=clamp01(e);return Math.max(0,Math.min(359,Math.round(359*t)))}function normalizeHexColor(e){if(!e)return"#FFFFFF";if("number"==typeof e){for(var t=Math.max(0,Math.floor(e)).toString(16).toUpperCase();t.length<6;)t="0"+t;return"#"+t.slice(-6)}if("string"==typeof e){var n=String(e).trim();if(!n)return"#FFFFFF";if("#"===n.charAt(0)){var a=n.slice(1);if(3===a.length&&(a=a[0]+a[0]+a[1]+a[1]+a[2]+a[2]),a.length>=6)return"#"+a.slice(0,6).toUpperCase()}var r=n.match(/rgb\((\d+),\s*(\d+),\s*(\d+)\)/i);if(r)return rgbToHex(r[1],r[2],r[3]).toUpperCase()}if("object"==typeof e){if(void 0!==e.hex)return normalizeHexColor(e.hex);if(void 0!==e.h&&void 0!==e.s&&void 0!==e.v)return hsvToHex(e.h,e.s,e.v);if(void 0!==e.r&&void 0!==e.g&&void 0!==e.b)return rgbToHex(e.r,e.g,e.b).toUpperCase();if(void 0!==e.red&&void 0!==e.green&&void 0!==e.blue)return rgbToHex(e.red,e.green,e.blue).toUpperCase()}return"#FFFFFF"}function getColorBoxMetrics(){var e=isValidPanel(u)?u:d;if(!isValidPanel(e))return null;var t=getPanelBounds(e),n=Number(e.actuallayoutwidth||t.width||240),a=Number(e.actuallayoutheight||t.height||240),r=Number(isValidPanel(c)?c.actuallayoutwidth||c.contentwidth||c.width:18),i=Number(isValidPanel(c)?c.actuallayoutheight||c.contentheight||c.height:18);return(!isFinite(n)||n<=1)&&(n=240),(!isFinite(a)||a<=1)&&(a=240),(!isFinite(r)||r<=0)&&(r=18),(!isFinite(i)||i<=0)&&(i=18),{panel:e,bounds:{left:t.left,top:t.top,width:n,height:a},width:n,height:a,cursorWidth:r,cursorHeight:i,maxCursorX:Math.max(0,n-r),maxCursorY:Math.max(0,a-i)}}function applyColorBoxCursorPosition(e,t){if(!isValidPanel(c))return!1;var n=getColorBoxMetrics();if(!n)return!1;var a=Number(e),r=Number(t);return isFinite(a)||(a=0),isFinite(r)||(r=0),a<0&&(a=0),r<0&&(r=0),a>n.maxCursorX&&(a=n.maxCursorX),r>n.maxCursorY&&(r=n.maxCursorY),g=a,m=r,c.style.x=a+"px",c.style.y=r+"px",c.style.transform="none",c.style.zIndex="20",c.style.opacity="1",c.style.visibility="visible",c.style.backgroundColor="#FFFFFF",c.style.washColor="#FFFFFF",!0}function updateBoxCursorVisual(e){if(d&&d.IsValid&&d.IsValid()){var t=getColorBoxMetrics();t&&(isFinite(o)&&isFinite(l)||setPickerBoxState(null,e),applyColorBoxCursorPosition(t.maxCursorX>0?o/359*t.maxCursorX:0,t.maxCursorY>0?l*t.maxCursorY:0))}}function syncFromCursorPanelPosition(e){if(!isValidPanel(d)||!isValidPanel(c))return!1;var t=function(){if(!isValidPanel(c))return null;var e=getColorBoxMetrics();if(!e)return null;function chooseLocalAxis(e,t,n,a){var r=!!a&&isFinite(e)&&e>=-1&&e<=n+1,i=isFinite(t)&&t>=-1&&t<=n+1;return r&&i?Math.abs(e-t)<=12?e:t:i?t:r?e:isFinite(t)?t:isFinite(e)?e:0}var t=Number(c.actuallayoutx),n=Number(c.actuallayouty),a=!(!c.GetParent||c.GetParent()!==e.panel),r=getPanelBounds(c),i=r.left-e.bounds.left,o=r.top-e.bounds.top,l=chooseLocalAxis(t,i,e.maxCursorX,a),s=chooseLocalAxis(n,o,e.maxCursorY,a);return l<0&&(l=0),s<0&&(s=0),l>e.maxCursorX&&(l=e.maxCursorX),s>e.maxCursorY&&(s=e.maxCursorY),function(e,t){var n=getColorBoxMetrics();if(!n)return!1;var a=Number(e),r=Number(t);!isFinite(a)||!isFinite(r)||(a<0&&(a=0),r<0&&(r=0),a>n.maxCursorX&&(a=n.maxCursorX),r>n.maxCursorY&&(r=n.maxCursorY),g=a,m=r)}(l,s),{metrics:e,x:l,y:s}}();if(!t)return!1;const n=t.metrics,a=n.maxCursorX>0?clamp01(t.x/n.maxCursorX):0,r=n.maxCursorY>0?clamp01(t.y/n.maxCursorY):0,i=hueFromRelX(a),o=clamp01(r);return syncColorVisuals(colorFromBoxState(i,o),e,!1,{hue:i,sat:o}),!0}function syncFromBoxPosition(e,t,n){if(!isValidPanel(d))return!1;const a=getColorBoxMetrics();if(!a)return!1;const r=clamp01(Number(e)/a.width),i=clamp01(Number(t)/a.height),o=hueFromRelX(r),l=clamp01(i);return syncColorVisuals(colorFromBoxState(o,l),n,!1,{hue:o,sat:l}),!0}function syncFromScreenCursorPosition(e){var t=getCursorPosition();if(!t||!isValidPanel(d))return!1;const n=getColorBoxMetrics();if(!n)return!1;const a=n.bounds,r=clamp01((t.x-a.left)/a.width),i=clamp01((t.y-a.top)/a.height),o=hueFromRelX(r),l=clamp01(i);return syncColorVisuals(colorFromBoxState(o,l),e,!1,{hue:o,sat:l}),!0}function syncFromLocalBoxCursor(e){var t=getPanelLocalCursorPosition(d);return!!t&&syncFromBoxPosition(t.x,t.y,e)}function syncFromBestDragSource(e){return v?"cursor"!==y&&"drag_event"!==y||!syncFromCursorPanelPosition(e)?"box"!==y&&"gameui"!==y||!syncFromScreenCursorPosition(e)?syncFromLocalBoxCursor(e)?"panel_cursor":syncFromScreenCursorPosition(e)?"screen_cursor":syncFromCursorPanelPosition(e)?"drag_panel":"":"screen_cursor":"drag_panel":"cursor"!==y&&"drag_event"!==y||!syncFromCursorPanelPosition(e)?syncFromLocalBoxCursor(e)?"panel_cursor":syncFromCursorPanelPosition(e)?"cursor_panel":"":"cursor_panel"}function setMouseCaptureState(e){var t=!!e;if(isValidPanel(d)&&"function"==typeof d.SetMouseCapture)try{d.SetMouseCapture(t)}catch(e){}if(v&&isValidPanel(c)&&"function"==typeof c.SetMouseCapture)try{c.SetMouseCapture(t)}catch(e){}}function beginColorDrag(e,t){C=!0,y=String(e||""),syncFromCursorPanelPosition(!1)||updateBoxCursorVisual(i),t&&("cursor"===y||"drag_event"===y?syncFromCursorPanelPosition(!0)||syncFromBestDragSource(!0):syncFromScreenCursorPosition(!0)||syncFromLocalBoxCursor(!0)||syncFromCursorPanelPosition(!0)),setMouseCaptureState(!0),function(){if(!s||!s.IsValid||!s.IsValid())return;const e=++p;$.Schedule(.016,function tick(){if(e===p&&s&&s.IsValid&&s.IsValid())return C?(syncFromBestDragSource(!0),v&&"function"==typeof GameUI.IsMouseDown&&!GameUI.IsMouseDown(0)?void endColorDrag():void $.Schedule(.016,tick)):void $.Schedule(.05,tick)})}()}function endColorDrag(){C=!1,y="",setMouseCaptureState(!1)}function closePalette(){if(p++,endColorDrag(),v&&"function"==typeof GameUI.SetMouseCallback)try{GameUI.SetMouseCallback(null)}catch(e){}s&&s.IsValid&&s.IsValid()&&s.DeleteAsync(0),s=null,u=null,d=null,c=null,P=null,A=null,S=null,f=!1,x=null,V=null,I=null,F=null,w=null,y="",h.activeColorPickerClose===closePalette&&(h.activeColorPickerClose=null)}function syncColorVisuals(e,r,o,l){i=normalizeHexColor(e),t.currentValue=i;var s=setPickerBoxState(l,i);b&&b.IsValid&&b.IsValid()&&(b.style.backgroundColor=i),_&&(_.text=i),P&&P.IsValid&&P.IsValid()&&(P.style.backgroundColor=i),A&&(A.text=i),S&&(S.text="Hue "+s.hue+"° | Sat "+Math.round(100*s.sat)+"%");const u=a.FindChildTraverse("ColorPreviewBtn");u&&(u.style.backgroundColor=i),updateBoxCursorVisual(i),f=!0;try{if(x&&x.IsValid&&x.IsValid()){var d=(h=s.hue,p=Number(h),isFinite(p)||(p=0),p=Math.max(0,Math.min(359,Math.round(p))),Math.round(p/359*100));if(Math.round(Number(x.value))!==d)try{x.value=d}catch(e){}F&&(F.text=s.hue+"°")}if(V&&V.IsValid&&V.IsValid()){var c=Math.round(100*s.sat);if(Math.round(Number(V.value))!==c)try{V.value=c}catch(e){}w&&(w.text=c+"%"),I&&I.IsValid&&I.IsValid()&&(I.style.backgroundColor="gradient( linear, 0% 0%, 100% 0%, from( #ffffff ), to( "+hsvToHex(s.hue,1,1)+" ) )")}}finally{f=!1}var h,p;r&&(t.id&&n&&emitUpdate(n,t.id,i),t.onChange&&t.onChange(i)),o&&closePalette()}const E=$.CreatePanel("Panel",a,"ColorPreviewBtn");return E.AddClass("AnitaColorPickerPreview"),E.style.backgroundColor=i,E.style.marginRight="6px",E.SetPanelEvent("onactivate",()=>function(){if(s)return void closePalette();if(h.activeColorPickerClose&&h.activeColorPickerClose!==closePalette)try{h.activeColorPickerClose()}catch(e){}var t=$.GetContextPanel();s=$.CreatePanel("Panel",t,""),h.activeColorPickerClose=closePalette,s.AddClass("AnitaColorPopup");var n=24,a=24;try{var r=getPanelBounds(E&&E.IsValid&&E.IsValid()?E:e);n=Math.max(8,Math.round(r.left+r.width+12)),a=Math.max(8,Math.round(r.top))}catch(e){}s.style.align="left top",s.style.transform="translate3d("+n+"px, "+a+"px, 0px)",s.style.uiScale="100%",s.style.flowChildren="down",s.style.ignoreParentFlow=!0,s.SetPanelEvent("oncancel",closePalette);const p=$.CreatePanel("Panel",s,"");p.AddClass("AnitaColorPopupHeader"),P=$.CreatePanel("Panel",p,"ColorPreviewBtn"),P.AddClass("AnitaColorPickerPreview"),P.AddClass("AnitaColorPopupPreview"),P.style.backgroundColor=i,A=$.CreatePanel("Label",p,""),A.AddClass("AnitaColorPopupHex"),A.text=i,S=$.CreatePanel("Label",s,""),S.AddClass("AnitaColorPopupMeta");const y=$.CreatePanel("Label",s,"");y.AddClass("AnitaColorPopupHint"),y.text="Drag the marker or use the sliders below to change hue and saturation.";const b=$.CreatePanel("Panel",s,"");if(b.AddClass("AnitaColorBoxWrap"),u=$.CreatePanel("Panel",b,""),u.AddClass("AnitaColorBoxFrame"),$.CreatePanel("Panel",u,"").AddClass("AnitaColorBoxHueLayer"),$.CreatePanel("Panel",u,"").AddClass("AnitaColorBoxSaturationLayer"),d=$.CreatePanel("Panel",u,""),d.AddClass("AnitaColorBox"),d.hittest=!0,d.hittestchildren=!0,d.style.ignoreParentFlow=!0,d.style.width="100%",d.style.height="100%",d.SetPanelEvent("onmouseactivate",function(){if(v)beginColorDrag("box",!0);else try{var e=getPanelLocalCursorPosition(d);e&&syncFromBoxPosition(e.x,e.y,!0)}catch(e){}}),d.SetPanelEvent("onactivate",function(){v&&beginColorDrag("box",!0)}),d.SetPanelEvent("onmousemove",function(){if(C)if(v)syncFromBestDragSource(!0);else{var e=getPanelLocalCursorPosition(d);e&&syncFromBoxPosition(e.x,e.y,!0)}}),d.SetPanelEvent("onmouseup",function(){endColorDrag()}),d.SetPanelEvent("onmouseout",function(){C&&!v&&endColorDrag()}),c=$.CreatePanel("Button",u,""),c.AddClass("AnitaColorBoxCursor"),c.hittest=!0,c.hittestchildren=!1,c.style.ignoreParentFlow=!0,c.style.align="left top",c.style.visibility="visible",c.style.opacity="1","function"==typeof c.SetDraggable)try{c.SetDraggable(!0)}catch(e){}if("function"==typeof c.SetDisableFocusOnMouseDown)try{c.SetDisableFocusOnMouseDown(!0)}catch(e){}if(c.SetPanelEvent("onmouseactivate",function(){beginColorDrag("cursor",!0)}),c.SetPanelEvent("onactivate",function(){beginColorDrag("cursor",!0)}),c.SetPanelEvent("onmousemove",function(){C&&syncFromBestDragSource(!0)}),c.SetPanelEvent("onmouseup",function(){endColorDrag()}),c.SetPanelEvent("onmouseout",function(){C&&!v&&endColorDrag()}),"function"==typeof $.RegisterEventHandler){try{$.RegisterEventHandler("DragStart",c,function(e,t){e&&e===c&&(t&&(t.displayPanel=c,t.removePositionBeforeDrop=!1),c.style.align="left top",beginColorDrag("drag_event",!1))})}catch(e){}try{$.RegisterEventHandler("DragEnd",c,function(e,t){t&&t.IsValid&&t.IsValid()&&t===c&&(u&&u.IsValid&&u.IsValid()&&t.GetParent&&t.GetParent()!==u&&t.SetParent(u),t.style.align="left top",function(){var e=getColorBoxMetrics();if(!e)return!1;if(!isFinite(g)||!isFinite(m)||g<0||m<0)return!1;var t=e.maxCursorX>0?clamp01(g/e.maxCursorX):0,n=e.maxCursorY>0?clamp01(m/e.maxCursorY):0,a=hueFromRelX(t),r=clamp01(n);return applyColorBoxCursorPosition(g,m),syncColorVisuals(colorFromBoxState(a,r),!0,!1,{hue:a,sat:r}),!0}()||(syncFromCursorPanelPosition(!0),updateBoxCursorVisual(i))),endColorDrag()})}catch(e){}}if(v&&"function"==typeof GameUI.SetMouseCallback)try{GameUI.SetMouseCallback(function(e,t){if(!s||!s.IsValid||!s.IsValid())return!1;if("pressed"===e&&0===t){const e=getCursorPosition();if(!e)return!1;const t=getColorBoxMetrics();if(!t)return!1;const a=t.bounds;if(e.x>=a.left&&e.x<=a.left+a.width&&e.y>=a.top&&e.y<=a.top+a.height)return syncFromScreenCursorPosition(n=!0)||syncFromLocalBoxCursor(n)||syncFromCursorPanelPosition(n),beginColorDrag("gameui",!1),!0}var n;return!("released"!==e||0!==t||!C||(endColorDrag(),0))})}catch(e){}var _=$.CreatePanel("Panel",s,"");_.AddClass("AnitaHueSliderGroup");var B=$.CreatePanel("Label",_,"");B.text="H",B.AddClass("AnitaHueValue"),B.AddClass("AnitaPickerAxisLabel");var N=$.CreatePanel("Panel",_,"");if(N.AddClass("AnitaHueSliderContainer"),N.AddClass("AnitaPickerSliderTrack"),x=$.CreatePanel("Slider",N,"",{direction:"horizontal"}),x.AddClass("AnitaHueSlider"),x.AddClass("HorizontalSlider"),x.min=0,x.max=100,x.increment=1,"function"==typeof x.SetShowDefaultValue)try{x.SetShowDefaultValue(!1)}catch(e){}if("function"==typeof x.SetRequiresSelection)try{x.SetRequiresSelection(!1)}catch(e){}F=$.CreatePanel("Label",_,""),F.AddClass("AnitaHueValue"),F.AddClass("AnitaPickerReadout"),x.SetPanelEvent("onvaluechanged",function(){if(!f){var e=hueFromRelX(clamp01(Number(x.value)/100)),t=colorFromBoxState(e,l);F&&(F.text=e+"°"),I&&I.IsValid&&I.IsValid()&&(I.style.backgroundColor="gradient( linear, 0% 0%, 100% 0%, from( #ffffff ), to( "+hsvToHex(e,1,1)+" ) )"),syncColorVisuals(t,!0,!1,{hue:e,sat:l})}});var R=$.CreatePanel("Panel",s,"");R.AddClass("AnitaHueSliderGroup");var k=$.CreatePanel("Label",R,"");if(k.text="S",k.AddClass("AnitaHueValue"),k.AddClass("AnitaPickerAxisLabel"),I=$.CreatePanel("Panel",R,""),I.AddClass("AnitaSatSliderContainer"),I.AddClass("AnitaPickerSliderTrack"),V=$.CreatePanel("Slider",I,"",{direction:"horizontal"}),V.AddClass("AnitaHueSlider"),V.AddClass("HorizontalSlider"),V.min=0,V.max=100,V.increment=1,"function"==typeof V.SetShowDefaultValue)try{V.SetShowDefaultValue(!1)}catch(e){}if("function"==typeof V.SetRequiresSelection)try{V.SetRequiresSelection(!1)}catch(e){}w=$.CreatePanel("Label",R,""),w.AddClass("AnitaHueValue"),w.AddClass("AnitaPickerReadout"),V.SetPanelEvent("onvaluechanged",function(){if(!f){var e=clamp01(V.value/100),t=colorFromBoxState(o,e);w&&(w.text=Math.round(100*e)+"%"),syncColorVisuals(t,!0,!1,{hue:o,sat:e})}});const M=$.CreatePanel("Panel",s,"");M.AddClass("AnitaColorPopupFooter");const T=$.CreatePanel("Button",M,"");T.AddClass("AnitaColorPopupBtn"),$.CreatePanel("Label",T,"").text="Close",T.SetPanelEvent("onactivate",closePalette),syncColorVisuals(i,!1,!1),$.Schedule(0,function(){s&&s.IsValid&&s.IsValid()&&syncColorVisuals(i,!1,!1)}),getBoxStateFromColor(i)}()),b=E,_=$.CreatePanel("Label",a,""),_.AddClass("AnitaColorHexValue"),_.text=i,a}(_,t,e.title)}t.__anitaRowPanel=n||null}),this.refreshConditionalVisibility(e)),e.storageNamespace){var x=$.CreatePanel("Panel",s,"");x.AddClass("AnitaFooterWrap");var V=$.CreatePanel("Panel",x,"");function makeFooterBtn(e,t,n){var a=$.CreatePanel("Button",e,n||"");a.AddClass("AnitaFooterBtn");var r=$.CreatePanel("Label",a,"");return r.text=t,{btn:a,lbl:r}}function flashLabel(e,t,n,a){if(e&&e.IsValid&&e.IsValid()&&t&&t.IsValid&&t.IsValid()){var r=t.text;t.text=n,e.AddClass("AnitaFooterBtnSuccess"),$.Schedule(a,function(){t&&t.IsValid()&&(t.text=r),e&&e.IsValid()&&e.RemoveClass("AnitaFooterBtnSuccess")})}}V.AddClass("AnitaFooterRow");var I=makeFooterBtn(V,"Copy","");I.btn.SetPanelEvent("onactivate",function(){h.syncSaveCodeInput(e);var t=h.getSaveCodeToken(e);if(t)try{$.DispatchEvent("CopyStringToClipboard",t,$.GetContextPanel()),flashLabel(I.btn,I.lbl,"Copied!",1.5)}catch(e){flashLabel(I.btn,I.lbl,"Failed",1.5)}else flashLabel(I.btn,I.lbl,"Empty",1.5)});var F=makeFooterBtn(V,"Reset","");F.btn.SetPanelEvent("onactivate",function(){c.applyResolvedValues(e,{}),c.persistConfig(e,!0),h.syncSaveCodeInput(e),flashLabel(F.btn,F.lbl,"Reset",1.5),h.renderModSettings(e),f.emitCurrentValues(e,{update_source:"ui_reset",force_persist:!0})});var w=!1,E=makeFooterBtn(V,"Import",""),B=$.CreatePanel("Panel",x,"");B.AddClass("AnitaPasteRow"),B.AddClass("AnitaPasteRowCompact"),B.hittest=!0,B.style.visibility="collapse",B.style.height="0px",B.style.marginTop="0px";var N=$.CreatePanel("Label",B,"");N.AddClass("AnitaLabel"),N.text="Import Code",N.style.width="84px",N.style.fontSize="12px",N.style.marginRight="6px";var R=$.CreatePanel("TextEntry",B,"");R.AddClass("AnitaPasteInput"),R.placeholder="Paste a code to apply...",R.style.width="320px",R.style.height="24px",R.style.fontSize="9px",e.__anitaImportCodeInput=R;var k=makeFooterBtn(B,"Apply","");function setImportRowVisible(e){w=!!e,B&&B.IsValid&&B.IsValid()&&(B.style.visibility=w?"visible":"collapse",B.style.height=w?"30px":"0px",B.style.marginTop=w?"6px":"0px"),E&&E.lbl&&(E.lbl.text=w?"Hide":"Import")}function applySaveCodeInput(){var t=String(R.text||"").trim();if(t){var n=h.extractSaveCodeToken(e,t);if(n){var a=n.split("]:")[1]||"";try{var r=d.decode(a),i=c.parseStoredPayload(e,r,"code");if(!i)return void flashLabel(k.btn,k.lbl,"Invalid",1.5);if(!i.values||!Object.keys(i.values).length)return void flashLabel(k.btn,k.lbl,"No IDs",1.5);c.applyResolvedValues(e,i.values),c.persistConfig(e,!0),h.syncSaveCodeInput(e),flashLabel(k.btn,k.lbl,"Applied",1.5),h.renderModSettings(e),e.__anitaImportCodeInput&&e.__anitaImportCodeInput.IsValid&&e.__anitaImportCodeInput.IsValid()&&(e.__anitaImportCodeInput.text=""),f.emitCurrentValues(e,{update_source:"ui_code_apply",force_persist:!0})}catch(e){flashLabel(k.btn,k.lbl,"Invalid",1.5)}}else flashLabel(k.btn,k.lbl,"Invalid",1.5)}else flashLabel(k.btn,k.lbl,"Empty",1.5)}E.btn.SetPanelEvent("onactivate",function(){setImportRowVisible(!w)}),k.btn.SetPanelEvent("onactivate",applySaveCodeInput),R.SetPanelEvent("ontextentrysubmit",applySaveCodeInput),setImportRowVisible(!1)}}},f={registeredMods:[],init:function(){const e=this.getRoot($.GetContextPanel());h.initWindow(e),e.AnitaUI={GetVersion:()=>"2.2.3",Register:e=>this.registerMod(e),Toggle:()=>h.toggle(),IsReady:()=>!0},this.setupEventListener(),this.createOverlayButton(e),this.monitorEscapeMenu(e),0===this.registeredMods.length&&this.registerMod({title:"Anita-UI",description:"No detected mods. Check your installed mods.",isDummy:!0,elements:[]}),$.DispatchEvent("ClientUI_FireOutput",JSON.stringify({magic_word:"ANITA_ALIVE"}))},registerMod:function(e){e&&e.title,1===this.registeredMods.length&&this.registeredMods[0].isDummy&&(this.registeredMods=[],h.menuArea.RemoveAndDeleteChildren(),h.contentArea.RemoveAndDeleteChildren()),c.hydrateConfig(e);for(let t=0;t<this.registeredMods.length;t++)if(this.registeredMods[t].title===e.title)return;this.registeredMods.push(e),h.addTab(e.title,()=>{h.renderModSettings(e)}),this.updateWindowWidth(),$.DispatchEvent("ClientUI_FireOutput",JSON.stringify({magic_word:"ANITA_HANDSHAKE",mod_title:e.title})),c.requestBootstrap(e,"core_handshake")},emitCurrentValues:function(e,t){if(e&&Array.isArray(e.elements))for(var n=0;n<e.elements.length;n++){var a=e.elements[n];a&&a.id&&void 0!==a.currentValue&&emitUpdate(e.title,a.id,a.currentValue,t)}},findRegisteredMod:function(e){for(var t=0;t<this.registeredMods.length;t++)if(this.registeredMods[t]&&this.registeredMods[t].title===e)return this.registeredMods[t];return null},queueRenderRefresh:function(e){if(e){var t=(e.__anitaUiRefreshToken||0)+1;e.__anitaUiRefreshToken=t,$.Schedule(.05,function(){e&&e.__anitaUiRefreshToken===t&&h.activeModTitle===e.title&&h.renderModSettings(e)})}},handleUpdateEvent:function(e){if(e&&e.mod_title&&e.setting_id){var t=this.findRegisteredMod(e.mod_title);if(t){var n=String(e.update_source||""),a="bridge_bootstrap"===n,r=a||"ui_resync"===n||"ui_reset"===n||"ui_code_apply"===n||"core_auto_resync"===n||"ui_refresh_after_apply"===n;if(c.applyUpdate(t,e.setting_id,e.value)){if(h.syncSaveCodeInput(t),a&&(t.__anitaBootstrapReceived=!0),!r&&"HP Colors"===t.title){var i=(t.__anitaHpReplayToken||0)+1;t.__anitaHpReplayToken=i,$.Schedule(.35,function(){t&&t.__anitaHpReplayToken===i&&f.emitCurrentValues(t,{update_source:"ui_refresh_after_apply",skip_bridge_persist:!0})})}var o=(t.__anitaPendingWriteToken||0)+1;t.__anitaPendingWriteToken=o,$.Schedule(2,function(){t&&t.__anitaPendingWriteToken===o&&c.persistConfig(t,!1)}),(this.hasVisibilityDependents(t,e.setting_id)||a)&&this.queueRenderRefresh(t)}}}},handleBootstrapRequest:function(e){if(e&&e.mod_title){var t=this.findRegisteredMod(e.mod_title);t&&this.emitCurrentValues(t,{update_source:"bridge_bootstrap",skip_bridge_persist:!0,bootstrap_reason:String(e.reason||"request")})}},updateWindowWidth:function(){if(!h.mainWindow)return;const e=this.registeredMods.length;let t=null;1===e&&this.registeredMods[0].isDummy?t=500:e<=4&&(t=300*e),h.mainWindow.style.minWidth=t?t+"px":"90%"},setupEventListener:function(){try{$.RegisterForUnhandledEvent("ClientUI_FireOutput",e=>{try{let t="string"==typeof e?JSON.parse(e):e;t&&"ANITA_REGISTER"===t.magic_word?(t.config&&t.config.title,this.registerMod(t.config)):t&&"ANITA_REQUEST_BOOTSTRAP"===t.magic_word?(t.mod_title,this.handleBootstrapRequest(t)):t&&"ANITA_UPDATE"===t.magic_word&&this.handleUpdateEvent(t)}catch(e){}})}catch(e){}},createOverlayButton:function(e){const t=e.FindChildTraverse(n);t&&t.DeleteAsync(0);const a=$.CreatePanel("Button",e,n);a.AddClass("AnitaOverlayBtn"),this._overlayBtn=a,a.SetPanelEvent("onmouseover",()=>$.DispatchEvent("UIShowTextTooltip",a,"Anita-UI Settings")),a.SetPanelEvent("onmouseout",()=>$.DispatchEvent("UIHideTextTooltip",a)),a.SetPanelEvent("onactivate",()=>h.toggle())},monitorEscapeMenu:function(e){let t=this._hudPanel,a=this._overlayBtn,l=o;if(!t||!t.IsValid||!t.IsValid()){if(t=e.FindChildTraverse("Hud"),!t){let e=$.GetContextPanel();for(;e;){if("Hud"===e.id){t=e;break}e=e.GetParent()}}this._hudPanel=t||null}if(a&&a.IsValid&&a.IsValid()||(a=e.FindChildTraverse(n),this._overlayBtn=a||null),"boolean"!=typeof this._lastEscapeState&&(this._lastEscapeState=!1),t&&a){const e=t.BHasClass("ShowEscapeMenu"),n=this._lastEscapeState!==e;if(!n&&a.BHasClass(r)||a.SetHasClass(r,e),!!a.hittest!==e&&(a.hittest=e),n)if(e){const e=(this._attentionToken||0)+1;this._attentionToken=e,a.AddClass(i),$.Schedule(4,()=>{this._attentionToken===e&&a&&a.IsValid&&a.IsValid()&&a.RemoveClass(i)})}else a.RemoveClass(i);this._lastEscapeState=e,!e&&h.isOpen&&h.toggle(!1),l=e?o:Math.max(.4,.25)}else l=Math.max(.2,.2);$.Schedule(l,()=>this.monitorEscapeMenu(e))},getRoot:function(e){for(;e.GetParent&&e.GetParent();)e=e.GetParent();return e}};f.init()}();
\ No newline at end of file
diff --git a/hp_colors_terser/panorama/scripts/healthbar_logic.js b/hp_colors_terser/panorama/scripts/healthbar_logic.js
index c19706f..0e53113 100644
--- a/hp_colors_terser/panorama/scripts/healthbar_logic.js
+++ b/hp_colors_terser/panorama/scripts/healthbar_logic.js
@@ -1 +1 @@
-"use strict";!function(){var e="HP Colors",t={hp_enabled:!0,hp_mode:1,hp_low_threshold:25,hp_high_threshold:65,hp_bg_visible:!0,hp_team_colors:!1,hp_color_low:"#E16161",hp_color_mid:"#FF7B00",hp_color_high:"#00FF00",hp_counter_size:120,hp_counter_position:"20,196",hp_text_color_mode:0,hp_text_color_low:"#E16161",hp_text_color_mid:"#FF7B00",hp_text_color_high:"#FFFFFF",hp_pulse_bpm:75,hp_pulse_intensity:1,hp_pulse_enabled:!0,hp_pulse_text_enabled:!0,hp_pulse_text_scale:120,hp_pulse_text_position:"20,196",hp_pulse_hide_bar:!1,hp_skip_buildings:!1,hp_pulse_threshold:25,hp_counter_format:0,hp_friend_enabled:!1,hp_friend_pulse_enabled:!1,hp_friend_pulse_bpm:75,hp_friend_pulse_intensity:1,hp_friend_pulse_threshold:25,hp_friend_color_low:"#E16161",hp_friend_color_mid:"#FF7B00",hp_friend_color_high:"#00FF00",hp_friend_pulse_color_enabled:!1,hp_friend_pulse_color:"#FF2222",hp_level_number_visible:!0,hp_kill_zone_enabled:!1,hp_kill_zone_threshold:25,hp_kill_zone_color:"#FF2222",hp_kill_zone_width:3},l={},r="#FFEFD7",a="low_hp_pulsing",i=["pulse_subtle","","pulse_intense"],n=!1,o=!1,s=0,u=null,_=-1,h=null,p=null,c=null,d=null,f=-1,y=-1,v=0,g=0,b=-1,m=-1,C=0,x=0;function applyPulseTextState(){if(H)try{s&&l.hp_pulse_text_enabled||(H.style&&(H.style.animationDuration=""),H.style&&(H.style.brightness=""),h=null)}catch(e){}}function applyPulseDuration(){var e=Number(l.hp_pulse_bpm)||75;e<30&&(e=30),e>300&&(e=300);var t=(60/e).toFixed(3)+"s";u!==t&&(u=t,R&&(R.style.animationDuration=t),V&&(V.style.animationDuration=t))}function applyPulseIntensity(){var e=0|Number(l.hp_pulse_intensity);if((e<0||e>2)&&(e=1),_!==e){var t=_>=0?i[_]:"",r=i[e];if(_=e,t){try{R&&R.RemoveClass(t)}catch(e){}try{V&&V.RemoveClass(t)}catch(e){}}if(r){try{R&&R.AddClass(r)}catch(e){}try{V&&V.AddClass(r)}catch(e){}}s&&applyPulseTextState()}}function clearPulsePanel(e,t){if(e)try{e.RemoveClass(a),t&&e.RemoveClass(t),e.style&&(e.style.animationDuration="",e.style.brightness="")}catch(e){}}function clearPulse(){var e=_>=0?i[_]:"";s=0,u=null,_=-1,h=null,J=q=Q=null,clearPulsePanel(R,e),clearPulsePanel(H,e),clearPulsePanel(V,e)}function clearAllyPulse(){var e=b>=0?i[b]:"";g=0,b=-1,d=null,clearPulsePanel(p,e)}!function(){for(var e in t)Object.prototype.hasOwnProperty.call(t,e)&&(l[e]=t[e])}();var P=!1,w=0,T=!1,F=!0;function getRootPanel(){for(var e=$.GetContextPanel();e&&e.GetParent&&e.GetParent();)e=e.GetParent();return e||null}function hexToRgb(e){if(!e)return[255,255,255];if("#"===e.charAt(0)){var t=e.slice(1);return 3===t.length&&(t=t[0]+t[0]+t[1]+t[1]+t[2]+t[2]),[parseInt(t.slice(0,2),16),parseInt(t.slice(2,4),16),parseInt(t.slice(4,6),16)]}var l=e.match(/rgb\((\d+),\s*(\d+),\s*(\d+)\)/);return l?[+l[1],+l[2],+l[3]]:[255,255,255]}function byteHex(e){var t=function(e){return e=Number(e),isFinite(e)?(e|=0)<0?0:e>255?255:e:0}(e).toString(16);return 1===t.length?"0"+t:t}function rgbToHex(e){return"#"+byteHex(e[0])+byteHex(e[1])+byteHex(e[2])}function normalizeWashColor(e){if("string"!=typeof e)return"";var t=e.trim();if(!t)return"";if("#"===t.charAt(0))return 4===t.length?("#"+t.charAt(1)+t.charAt(1)+t.charAt(2)+t.charAt(2)+t.charAt(3)+t.charAt(3)).toLowerCase():t.length>=7?t.slice(0,7).toLowerCase():t;var l=t.match(/^rgba?\(\s*(\d+)\s*,\s*(\d+)\s*,\s*(\d+)/i);return l?rgbToHex([l[1],l[2],l[3]]):t}function lerp(e,t,l){return e+(t-e)*l|0}function ip(e,t,l){return[lerp(e[0],t[0],l),lerp(e[1],t[1],l),lerp(e[2],t[2],l)]}function getHighColor(){return l.hp_team_colors?2===Z?"#6485FC":"#FFC961":l.hp_color_high}function getTextColor(e,t,r){return l.hp_text_color_mode?e<=t?l.hp_text_color_low:e<=r?l.hp_text_color_mid:l.hp_text_color_high:e<=t?l.hp_color_low:e<=r?l.hp_color_mid:getHighColor()}function getGradientTextColor(e,t,r){var a=Math.max(1,r-t),i=Math.max(1,100-r);if(l.hp_text_color_mode){if(e<=t)return l.hp_text_color_low;if(e<=r){var n=(e-t)/a;return rgbToHex(ip(hexToRgb(l.hp_text_color_low),hexToRgb(l.hp_text_color_mid),n))}var o=(e-r)/i;return rgbToHex(ip(hexToRgb(l.hp_text_color_mid),hexToRgb(l.hp_text_color_high),o))}if(e<=t)return l.hp_color_low;if(e<=r){var s=(e-t)/a;return rgbToHex(ip(hexToRgb(l.hp_color_low),hexToRgb(l.hp_color_mid),s))}var u=(e-r)/i,_=getHighColor();return rgbToHex(ip(hexToRgb(l.hp_color_mid),hexToRgb(_),u))}$.RegisterForUnhandledEvent("ClientUI_FireOutput",function(r){try{var a="string"==typeof r?JSON.parse(r):r;if(!a||a.mod_title!==e)return;if("ANITA_UPDATE"===a.magic_word){if(Object.prototype.hasOwnProperty.call(t,a.setting_id)){if("hp_counter_position"===a.setting_id&&"hp_counter_autoposition"===a.update_source)return;l[a.setting_id]=function(e,l){if(!Object.prototype.hasOwnProperty.call(t,e))return l;var r=t[e];if("hp_counter_position"===e||"hp_pulse_text_position"===e){var a=0,i=200,n=l;if(n&&"object"==typeof n)return Array.isArray(n)?(n.length>0&&(a=clampNum(n[0],0,400,0)),n.length>1&&(i=clampNum(n[1],0,400,200))):(Object.prototype.hasOwnProperty.call(n,"x")&&(a=clampNum(n.x,0,400,0)),Object.prototype.hasOwnProperty.call(n,"y")&&(i=clampNum(n.y,0,400,200))),Math.round(a)+","+Math.round(i);if("string"==typeof n){var o=n.match(/-?\d+(?:\.\d+)?/g);if(o&&o.length>0)return a=clampNum(o[0],0,400,0),o.length>1&&(i=clampNum(o[1],0,400,200)),Math.round(a)+","+Math.round(i)}return"number"==typeof n&&(i=clampNum(n,0,400,200)),Math.round(a)+","+Math.round(i)}if("boolean"==typeof r){if(!0===l||!1===l)return l;if(1===l||"1"===l)return!0;if(0===l||"0"===l)return!1;if("string"==typeof l){var s=l.toLowerCase();if("true"===s)return!0;if("false"===s)return!1}return!!r}if("number"==typeof r){var u=Number(l);return isFinite(u)?Math.round(u):r}return"string"==typeof r?"string"==typeof l&&l.length>0?l:r:l}(a.setting_id,a.value),F=!0,"hp_level_number_visible"===a.setting_id&&(Fe=null),"hp_enabled"===a.setting_id&&l.hp_enabled&&!n&&(n=!0,$.Schedule(.05,gL)),"hp_friend_enabled"===a.setting_id&&l.hp_friend_enabled&&!o&&(o=!0,$.Schedule(.05,aL)),"hp_friend_enabled"!==a.setting_id||l.hp_friend_enabled||(isAllyPlayerFlags(m)?resetAllyBarColor(p,C,m):clearAllyPulse(),o=!1),!s||"hp_pulse_bpm"!==a.setting_id&&"hp_pulse_intensity"!==a.setting_id&&"hp_pulse_text_enabled"!==a.setting_id&&"hp_pulse_text_scale"!==a.setting_id||("hp_pulse_bpm"===a.setting_id&&(u=null,applyPulseDuration()),"hp_pulse_intensity"===a.setting_id&&applyPulseIntensity(),"hp_pulse_text_enabled"!==a.setting_id&&"hp_pulse_text_scale"!==a.setting_id||applyPulseTextState()),"hp_pulse_enabled"!==a.setting_id||l.hp_pulse_enabled||clearPulse()}if("bridge_bootstrap"===a.update_source){P=!0,T=!0;try{var i=getRootPanel();i&&(i.__hpColorsBootstrapAppliedAt=Date.now?Date.now():(new Date).getTime())}catch(e){}}return}}catch(e){}});var B=$.GetContextPanel(),A=null,H=null,S=null,N=null,M=null,L=null,R=null,I=null,V=null,G=null,O=0,D=0,z=null,k=null,U=null,E=null,j=null,K=null;function fRB(){return B.FindChildTraverse("unit_healthbar_lagging")||B.FindChildTraverse("health_bar")||B.FindChildTraverse("unit_health")}var Z=0,W=0,J=null,q=null,Q=null,X=-1,Y=-1,ee=null,te=null,le=0,re="",ae=!1,ie=null,ne=null,oe=null,se=null,ue=0;function sBC(e){var t=normalizeWashColor(e);if(J!==t&&R)try{R.style.washColor=t,J=t}catch(e){J=null}}function sUC(e){var t=normalizeWashColor(e);if(V&&V.IsValid()||(V=B.FindChildTraverse("unit_ult_ready_icon")||B.FindChildTraverse("ult_icon")),V&&V.style&&q!==t)try{V.style.washColor=t,q=t}catch(e){q=null}}function sTC(e){var t=normalizeWashColor(e);if(H&&H.style&&Q!==t)try{H.style.washColor=t,Q=t}catch(e){Q=null}}function isAllyPlayerFlags(e){return!!(1&e&&2&e)}function resetAllyBarColor(e,t,l){if(clearAllyPulse(),e&&e.style){var a=function(e,t){return 1&t?r:2===e?"#5B79E6":1===e?"#E7B659":r}(0|t,0|l);try{e.style.washColor=a,d=normalizeWashColor(a)}catch(e){d=null}}}function clampNum(e,t,l,r){var a=Number(e);return isFinite(a)||(a=Number(r)),isFinite(a)||(a=0),isFinite(t)&&a<t&&(a=t),isFinite(l)&&a>l&&(a=l),a}function sHBV(e){if(S&&S.style){"visible"!==z&&(S.style.visibility="visible",z="visible");var t=e?"1.0":"0.01";k!==t&&(S.style.opacity=t,k=t)}}var _e=null,he=null,pe=null,ce=null;function sKZ(e,t){if(G&&G.style)if(!e||!l.hp_kill_zone_enabled||t<=0){"collapse"!==_e&&(G.style.visibility="collapse",_e="collapse");try{G.style.opacity="0"}catch(e){}}else{var r=0|l.hp_kill_zone_threshold;r<0&&(r=0),r>100&&(r=100);var a=0|l.hp_kill_zone_width;a<1&&(a=1),a>20&&(a=20);var i=Math.round(t*r/100-a/2);i<0&&(i=0),i>t-a&&(i=Math.max(0,t-a));var n=i+"px",o=a+"px",s=normalizeWashColor(l.hp_kill_zone_color||"#FF2222");"visible"!==_e&&(G.style.visibility="visible",_e="visible");try{G.style.opacity="0.95"}catch(e){}try{G.style.zIndex="1000"}catch(e){}if(he!==n&&(G.style.marginLeft=n,he=n),pe!==o&&(G.style.width=o,pe=o),ce!==s)try{G.style.backgroundColor=s,ce=s}catch(e){ce=null}}}function parseCounterPositionValue(e){var t=0,l=200,r=e;if(r&&"object"==typeof r)return Array.isArray(r)?(r.length>0&&(t=clampNum(r[0],0,400,0)),r.length>1&&(l=clampNum(r[1],0,400,200))):(Object.prototype.hasOwnProperty.call(r,"x")&&(t=clampNum(r.x,0,400,0)),Object.prototype.hasOwnProperty.call(r,"y")&&(l=clampNum(r.y,0,400,200))),{x:t,y:l};if("string"==typeof r){var a=r.match(/-?\d+(?:\.\d+)?/g);if(a&&a.length>0)return t=clampNum(a[0],0,400,0),a.length>1&&(l=clampNum(a[1],0,400,200)),{x:t,y:l}}return"number"==typeof r?{x:t,y:l=clampNum(r,0,400,200)}:{x:t,y:l}}function sHCS(t,r){if(H&&H.style){var a,i=!!(t&&l.hp_pulse_enabled&&l.hp_pulse_text_enabled),n=clampNum(l.hp_counter_size,72,400,120),o=i?(a=n,clampNum(l.hp_pulse_text_scale,72,320,a)):n,s=parseCounterPositionValue(i?l.hp_pulse_text_position:l.hp_counter_position),u=String(void 0!==r?r:re||""),_=H&&H.actuallayoutwidth>0?H.actuallayoutwidth:A&&A.actuallayoutwidth>0?A.actuallayoutwidth:I&&I.actuallayoutwidth>0?I.actuallayoutwidth:R&&R.actuallayoutwidth>0?R.actuallayoutwidth:0,h=function(e){for(var t=String(e||""),l=0,r=0;r<t.length;r++){var a=t.charCodeAt(r);l+=a>=48&&a<=57?1:32===a?.35:47===a?.55:58===a||46===a?.4:.8}return l>0?l:1}(u),p=_>0?Math.floor((_-12)/(.45*h)):o;p=clampNum(p,72,400,o);var c=Math.min(o,p),d=clampNum(s.x,0,400,0),f=clampNum(s.y,0,400,200);l.hp_bg_visible||i?ie=null:function(t){var l,r=(l=parseCounterPositionValue(t),Math.round(l.x)+","+Math.round(l.y));if(ie!==r){ie=r,F=!0;try{$.DispatchEvent("ClientUI_FireOutput",JSON.stringify({magic_word:"ANITA_UPDATE",mod_title:e,setting_id:"hp_counter_position",value:r,update_source:"hp_counter_autoposition",skip_bridge_persist:!0}))}catch(e){}}}({x:d=Math.min(Math.round(.025*o),8),y:f=Math.min(Math.round(.5*o),150)});var y=!l.hp_bg_visible&&o>=320?"8%":Math.round(d)+"%",v=c+"px",g=130;try{var b=H.GetParent?H.GetParent():null;b&&b.actuallayoutheight>0&&(g=b.actuallayoutheight)}catch(e){}var m=i?Math.max(g,Math.round(1.85*c)):g,C=i?f*g/m:f,x="-"+Math.round(C)+"%",P=i?m+"px":"100%";U!==v&&(H.style.fontSize=v,U=v),E!==x&&(H.style.marginTop=x,E=x),j!==y&&(H.style.marginLeft=y,j=y),K!==P&&(H.style.height=P,K=P)}}var de=0,fe=-1,ye=-1,ve=-1,ge=-1,be=0;function gL(){n=!0;try{if(!l.hp_enabled)return clearPulse(),sKZ(!1,0),sBC(""),sUC(""),S&&S.style&&(S.style.visibility="collapse",S.style.opacity="0",z="collapse",k="0"),H&&H.style&&(H.style.fontSize="",H.style.marginTop="",H.style.marginLeft="",H.style.height="",sTC(""),U=null,E=null,j=null,K=null),void(n=!1);var e=Date.now();if(!R&&!(R=fRB()))return void $.Schedule(.15,gL);if(!O&&!(O||!(D>=10)&&(D++,A&&A.IsValid()||(A=B.FindChildTraverse("UnitStatus")),A&&(H&&H.IsValid()||(H=A.FindChildTraverse("hp_counter")),S&&S.IsValid()||(S=A.FindChildTraverse("unit_healthbar_bg")),N&&N.IsValid()||(N=A.FindChildTraverse("unit_healthbar_pip_label")),M&&M.IsValid()||(M=A.FindChildTraverse("unit_healthbar_lagging")),G&&G.IsValid()||(G=A.FindChildTraverse("hp_kill_zone_marker")),!M||L&&L.IsValid()||(L=M.GetParent()),N&&M&&L&&(O=1,1)))))return void $.Schedule(.15,gL);if(R===ne&&H===oe&&S===se||(ne=R,oe=H,se=S,ue=Date.now(),clearPulse(),z=k=U=E=j=K=null,_e=he=pe=ce=null,Fe=null,X=-1,Y=-1,ee=null,fe=-1,ye=-1,ve=-1,ge=-1,be=0,ae=!1,F=!0),R.GetParent){var t=R.GetParent();I!==t&&(I=t)}!function(e){for(var t=0,l=0,r=0,a=e;a&&r<10&&!(a.BHasClass&&(t||(a.BHasClass("team2")?t=2:a.BHasClass("team1")&&(t=1)),1&l||!a.BHasClass("enemy")||(l|=1),2&l||!a.BHasClass("team_neutral")&&!a.BHasClass("neutral")||(l|=2),4&l||!(a.BHasClass("building")||a.BHasClass("boss_tier1")||a.BHasClass("boss_tier2")||a.BHasClass("boss_barracks"))||(l|=4),8&l||!a.BHasClass("friend")||(l|=8),t&&15&l))&&a.GetParent;)a=a.GetParent(),r++;Z=t,W=l}(R);var r=!(!(1&W)||2&W);if(l.hp_skip_buildings&&4&W){if(clearPulse(),!r)return sBC(""),sUC(""),sTC(""),sHBV(!0),sKZ(!1,0),void $.Schedule(.5,gL);S&&S.style&&(S.style.visibility="collapse",S.style.opacity="0",z="collapse",k="0");var i="#e16161";return sBC(i),sUC(i),sTC(i),sHBV(!!l.hp_bg_visible),sKZ(!1,0),void $.Schedule(.5,gL)}if(F?function(e){sHBV(!e||!!l.hp_bg_visible),e&&l.hp_bg_visible||sKZ(!1,0),sHCS(ae,re),F=!1}(r):sHBV(r&&!!l.hp_bg_visible),2&W)return clearPulse(),sHBV(!0),sKZ(!1,0),sBC("#5BEFB5"),sTC("#ffffff"),de=e,void $.Schedule(1.5,gL);if(!(1&W))return sHBV(!0),sKZ(!1,0),sBC(""),sUC(""),de=e,void $.Schedule(.4,gL);var o=0|R.actuallayoutwidth,u=I&&void 0!==I.actuallayoutwidth?0|I.actuallayoutwidth:0;if(o===fe&&u===ye&&!s)return e-de>2e3?void $.Schedule(1,gL):void $.Schedule(.15,gL);if(fe=o,ye=u,de=e,u<=0)return void $.Schedule(.18,gL);sKZ(!0,u);var _=o/u*100|0,p=0|l.hp_low_threshold,c=0|l.hp_high_threshold;if(p<0&&(p=0),p>100&&(p=100),c<0&&(c=0),c>100&&(c=100),c<p&&(c=p),Math.abs(_-ve)<3&&_>p&&ve>p&&!s)return void $.Schedule(.15,gL);var d=ve;_===ge?be++:(be=0,ge=_),ve=_;var f="";if(N)try{f=N.text||N.GetAttributeString("text","")||""}catch(e){f=""}if(M&&L){var y=M.actuallayoutwidth||0,v=L.actuallayoutwidth||0,g=v>0?y/v:0,b=function(e){if(e===te)return le;te=e;for(var t=0,l=0,r=e.lastIndexOf("|"),a=0;a<e.length;a++){var i=e.charCodeAt(a);124===i?t++:34!==i&&39!==i||!(-1===r||a>r)||l++}return le=500*t+100*l}(f);!function(e,t,r){if(H&&(e!==X||t!==Y)){var a;"visible"!==ee&&(H.style.visibility="visible",ee="visible"),a=1==(0|l.hp_counter_format)?(t>0?Math.round(e/t*100):0)+"%":e+" / "+t;try{H.text!==a&&(H.text=a)}catch(e){try{H.SetAttributeString("text",a)}catch(e){}}sHCS(ae=!!r,re=a),X=e,Y=t}}(g>=.97?b:Math.round(b*g),b,_<=p)}var m,C,x=.15,P=0|l.hp_pulse_threshold;if(_<=p){if(ue&&e-ue<900&&(d<0||d<=p&&_>d)){var w=getHighColor();return clearPulse(),sBC(w),sUC(w),sTC(getTextColor(100,p,c)),void $.Schedule(.05,gL)}sHBV(!l.hp_pulse_hide_bar&&!!l.hp_bg_visible),1===l.hp_mode?(m=l.hp_color_low,C=l.hp_text_color_mode?l.hp_text_color_low:l.hp_color_low):(sBC(l.hp_color_low),C=getTextColor(_,p,c)),l.hp_pulse_enabled&&_<=P?(s||function(){if(!s){s=1,J=q=Q=null;try{R&&R.AddClass(a)}catch(e){}try{V&&V.AddClass(a)}catch(e){}applyPulseIntensity(),applyPulseDuration()}}(),function(e){if(H&&H.style&&s&&l.hp_pulse_text_enabled){var t=clampNum(l.hp_pulse_bpm,30,300,75),r=Math.max(1,6e4/t),a=e%r/r,i=a<.5?2*a:2*(1-a),n=0|clampNum(l.hp_pulse_intensity,0,2,1),o=2===n?.55:0===n?.85:.65,u=(o+((2===n?2:0===n?1.15:1.5)-o)*i).toFixed(2);if(h!==u)try{H.style.brightness=u,h=u}catch(e){h=null}}}(e),l.hp_pulse_text_enabled&&(x=.05)):s&&clearPulse(),sTC(C),sUC(l.hp_color_low),1===l.hp_mode&&(sBC(m),sUC(m))}else{clearPulse(),sHBV(!!l.hp_bg_visible);var T=Math.max(1,c-p),ie=Math.max(1,100-c),me=getHighColor();_<=c?1===l.hp_mode?(m=rgbToHex(ip(hexToRgb(l.hp_color_low),hexToRgb(l.hp_color_mid),(_-p)/T)),C=getGradientTextColor(_,p,c)):(m=l.hp_color_mid,C=getTextColor(_,p,c)):(1===l.hp_mode?(m=rgbToHex(ip(hexToRgb(l.hp_color_mid),hexToRgb(me),(_-c)/ie)),C=getGradientTextColor(_,p,c)):(m=me,C=getTextColor(_,p,c)),be>=5&&(x=Math.min(.15*Math.pow(2,Math.floor(be/5)),1))),sBC(m),sUC(m),sTC(C)}$.Schedule(x,gL)}catch(e){$.Schedule(.5,gL)}}var me=[11,19,27,35],Ce=["level_tier2","level_tier3","level_tier4","level_tier5"],xe=null,Pe=null,we=null,Te=-1,Fe=null;function aL(){o=!0;try{if(!l.hp_friend_enabled)return isAllyPlayerFlags(m)?resetAllyBarColor(p,C,m):clearAllyPulse(),void(o=!1);var e,t,r=Date.now();if(!(p&&p.IsValid()||(p=null,c=null,m=-1,x=0,p=fRB())))return void $.Schedule(.2,aL);if(p.GetParent){var n=p.GetParent();c!==n&&(c=n)}if(m<0||r-x>=2e3){t=0,e=0;for(var s=0,u=p;u&&s<10&&!(u.BHasClass&&(t||(u.BHasClass("team2")?t=2:u.BHasClass("team1")&&(t=1)),1&e||!u.BHasClass("friend")||(e|=1),2&e||!u.BHasClass("player")||(e|=2),t&&3&e))&&u.GetParent;)u=u.GetParent(),s++;m=e,C=t,x=r}else e=m,t=C;if(!isAllyPlayerFlags(e))return clearAllyPulse(),d=null,v=0,void $.Schedule(1.5,aL);var _=0|p.actuallayoutwidth,h=c&&void 0!==c.actuallayoutwidth?0|c.actuallayoutwidth:0;if(h<=0)return void $.Schedule(.2,aL);if(_===f&&h===y&&!g){v++;var P=Math.min(.35*Math.pow(2,Math.floor(v/3)),2);return void $.Schedule(P,aL)}v=0,f=_,y=h;var w=_/h*100|0,T=0|l.hp_low_threshold,F=0|l.hp_high_threshold;T<0&&(T=0),T>100&&(T=100),F<0&&(F=0),F>100&&(F=100),F<T&&(F=T);var B=0|l.hp_friend_pulse_threshold,A=l.hp_friend_pulse_enabled&&w<=B,H=normalizeWashColor(A&&l.hp_friend_pulse_color_enabled?l.hp_friend_pulse_color:1===l.hp_mode?w<=T?l.hp_friend_color_low:rgbToHex(w<=F?ip(hexToRgb(l.hp_friend_color_low),hexToRgb(l.hp_friend_color_mid),(w-T)/Math.max(1,F-T)):ip(hexToRgb(l.hp_friend_color_mid),hexToRgb(l.hp_friend_color_high),(w-F)/Math.max(1,100-F))):w<=T?l.hp_friend_color_low:w<=F?l.hp_friend_color_mid:l.hp_friend_color_high);if(d!==H&&p)try{p.style.washColor=H,d=H}catch(e){d=null}var S=.35;if(A){if(!g){g=1,b=-1,d=null;try{p&&p.AddClass(a)}catch(e){}var N=0|Number(l.hp_friend_pulse_intensity);(N<0||N>2)&&(N=1),b=N;var M=i[N];if(M)try{p.AddClass(M)}catch(e){}var L=Number(l.hp_friend_pulse_bpm)||75;L<30&&(L=30),L>300&&(L=300);var R=(60/L).toFixed(3)+"s";try{p&&(p.style.animationDuration=R)}catch(e){}}S=.15}else g&&(clearAllyPulse(),d=null);$.Schedule(S,aL)}catch(e){$.Schedule(.5,aL)}}n=!0,gL(),o=!0,aL(),function lL(){(function(){if(xe&&xe.IsValid()||(xe=B.FindChildTraverse("unit_level_label")),Pe&&Pe.IsValid()||(Pe=B.FindChildTraverse("LevelContainer")),!Pe||we&&we.IsValid()||(we=function(e){for(var t=e;t;){if(t.BHasClass&&t.BHasClass("enemy"))return t;if(!t.GetParent)break;t=t.GetParent()}return null}(Pe)),xe&&Pe&&we){!function(){if(xe&&xe.style){var e=l.hp_level_number_visible?"visible":"collapse";Fe!==e&&(xe.style.visibility=e,Fe=e)}}();var e="";try{e=xe.text||xe.GetAttributeString("text","")||""}catch(t){e=""}if(e&&123!==e.charCodeAt(0)){var t=function(e){for(var t=0,l=0;l<e.length;l++){var r=e.charCodeAt(l)-48;r>=0&&r<=9&&(t=10*t+r)}return t}(e);if(t!==Te&&t){Te=t;for(var r=0;r<4;r++)we.RemoveClass(Ce[r]);for(var a=3;a>=0;a--)if(t>=me[a]){we.AddClass(Ce[a]);break}}}}})(),$.Schedule(.5,lL)}(),$.Schedule(.05,function scheduleBootstrapRetry(){P||T||(w>=8?T=!0:(function(t){var l=getRootPanel(),r=Date.now?Date.now():(new Date).getTime();try{if(l){var a=Number(l.__hpColorsBootstrapNextAllowedAt||0);if(isFinite(a)&&a>r)return;l.__hpColorsBootstrapNextAllowedAt=r+Math.floor(400)}}catch(e){}try{$.DispatchEvent("ClientUI_FireOutput",JSON.stringify({magic_word:"ANITA_REQUEST_BOOTSTRAP",mod_title:e,storageNamespace:"hp_colors",reason:String(t||"overlay_request")}))}catch(e){}}(1===(w+=1)?"overlay_startup":"overlay_retry"),$.Schedule(.5,scheduleBootstrapRetry)))})}();
\ No newline at end of file
+!function(){"use strict";var e={hp_enabled:!0,hp_mode:1,hp_low_threshold:25,hp_high_threshold:65,hp_bg_visible:!0,hp_team_colors:!1,hp_color_low:"#E16161",hp_color_mid:"#FF7B00",hp_color_high:"#00FF00",hp_counter_size:120,hp_counter_position:"20,196",hp_text_color_mode:0,hp_text_color_low:"#E16161",hp_text_color_mid:"#FF7B00",hp_text_color_high:"#FFFFFF",hp_pulse_bpm:75,hp_pulse_intensity:1,hp_pulse_enabled:!0,hp_pulse_text_enabled:!0,hp_pulse_text_scale:120,hp_pulse_text_position:"20,196",hp_pulse_hide_bar:!1,hp_skip_buildings:!1,hp_pulse_threshold:25,hp_counter_format:0,hp_friend_enabled:!1,hp_friend_pulse_enabled:!1,hp_friend_pulse_bpm:75,hp_friend_pulse_intensity:1,hp_friend_pulse_threshold:25,hp_friend_color_low:"#E16161",hp_friend_color_mid:"#FF7B00",hp_friend_color_high:"#00FF00",hp_friend_pulse_color_enabled:!1,hp_friend_pulse_color:"#FF2222",hp_level_number_visible:!0,hp_kill_zone_enabled:!1,hp_kill_zone_threshold:25,hp_kill_zone_color:"#FF2222",hp_kill_zone_width:3},t={},l="low_hp_pulsing",r=["pulse_subtle","","pulse_intense"],i=!1,a=!1,n=0,o=null,s=-1,_=null,u=null,h=null,p=null,c=-1,d=-1,f=0,y=0,v=-1,g=-1,b=0,m=0;function applyPulseTextState(){if(O)try{n&&t.hp_pulse_text_enabled||(O.style&&(O.style.animationDuration=""),O.style&&(O.style.brightness=""),_=null)}catch(e){}}function applyPulseDuration(){var e=Number(t.hp_pulse_bpm)||75;e<30&&(e=30),e>300&&(e=300);var l=(60/e).toFixed(3)+"s";o!==l&&(o=l,R&&(R.style.animationDuration=l),z&&(z.style.animationDuration=l))}function applyPulseIntensity(){var e=0|Number(t.hp_pulse_intensity);if((e<0||e>2)&&(e=1),s!==e){var l=s>=0?r[s]:"",i=r[e];if(s=e,l){try{R&&R.RemoveClass(l)}catch(e){}try{z&&z.RemoveClass(l)}catch(e){}}if(i){try{R&&R.AddClass(i)}catch(e){}try{z&&z.AddClass(i)}catch(e){}}n&&applyPulseTextState()}}function clearPulsePanel(e,t){if(e)try{e.RemoveClass(l),t&&e.RemoveClass(t),e.style&&(e.style.animationDuration="",e.style.brightness="")}catch(e){}}function clearPulse(){var e=s>=0?r[s]:"";n=0,o=null,s=-1,_=null,Y=ee=te=null,clearPulsePanel(R,e),clearPulsePanel(O,e),clearPulsePanel(z,e)}function clearAllyPulse(){var e=v>=0?r[v]:"";y=0,v=-1,p=null,clearPulsePanel(u,e)}function coerceCfgValue(t,l){if(!Object.prototype.hasOwnProperty.call(e,t))return l;var r=e[t];if("hp_counter_position"===t||"hp_pulse_text_position"===t){var i=0,a=200,n=l;if(n&&"object"==typeof n)return Array.isArray(n)?(n.length>0&&(i=clampNum(n[0],0,400,0)),n.length>1&&(a=clampNum(n[1],0,400,200))):(Object.prototype.hasOwnProperty.call(n,"x")&&(i=clampNum(n.x,0,400,0)),Object.prototype.hasOwnProperty.call(n,"y")&&(a=clampNum(n.y,0,400,200))),Math.round(i)+","+Math.round(a);if("string"==typeof n){var o=n.match(/-?\d+(?:\.\d+)?/g);if(o&&o.length>0)return i=clampNum(o[0],0,400,0),o.length>1&&(a=clampNum(o[1],0,400,200)),Math.round(i)+","+Math.round(a)}return"number"==typeof n&&(a=clampNum(n,0,400,200)),Math.round(i)+","+Math.round(a)}if("boolean"==typeof r){if(!0===l||!1===l)return l;if(1===l||"1"===l)return!0;if(0===l||"0"===l)return!1;if("string"==typeof l){var s=l.toLowerCase();if("true"===s)return!0;if("false"===s)return!1}return!!r}if("number"==typeof r){var _=Number(l);return isFinite(_)?Math.round(_):r}return"string"==typeof r?"string"==typeof l&&l.length>0?l:r:l}!function(){for(var l in e)Object.prototype.hasOwnProperty.call(e,l)&&(t[l]=e[l])}();var C={e:"hp_enabled",m:"hp_mode",l:"hp_low_threshold",h:"hp_high_threshold",b:"hp_bg_visible",t:"hp_team_colors",cl:"hp_color_low",cm:"hp_color_mid",ch:"hp_color_high",s:"hp_counter_size",p:"hp_counter_position",tm:"hp_text_color_mode",lnv:"hp_level_number_visible",tl:"hp_text_color_low",ti:"hp_text_color_mid",th:"hp_text_color_high",bp:"hp_pulse_bpm",pi:"hp_pulse_intensity",pe:"hp_pulse_enabled",pte:"hp_pulse_text_enabled",pts:"hp_pulse_text_scale",ptp:"hp_pulse_text_position",phb:"hp_pulse_hide_bar",sb:"hp_skip_buildings",pt:"hp_pulse_threshold",fe:"hp_friend_enabled",fpe:"hp_friend_pulse_enabled",fpb:"hp_friend_pulse_bpm",fpi:"hp_friend_pulse_intensity",fpt:"hp_friend_pulse_threshold",fcl:"hp_friend_color_low",fcm:"hp_friend_color_mid",fch:"hp_friend_color_high",fpce:"hp_friend_pulse_color_enabled",fpc:"hp_friend_pulse_color",kze:"hp_kill_zone_enabled",kzt:"hp_kill_zone_threshold",kzc:"hp_kill_zone_color",kzw:"hp_kill_zone_width"},x=!1,w=!1,S=0,P=!1,T=!0,F=0,A=0,B=0,H="";function getSharedStore(){try{if("undefined"!=typeof GameUI&&GameUI&&GameUI.CustomUIConfig)return GameUI.CustomUIConfig()}catch(e){}return null}function writeSharedSnapshot(){var l=getSharedStore();if(l)try{H=JSON.stringify(function(){var l={};for(var r in e)Object.prototype.hasOwnProperty.call(e,r)&&(l[r]=t[r]);return l}()),l.__hpColorsCfgRaw=H}catch(e){}}function invalidateEnemyVisualState(){Y=ee=te=j=Z=K=W=J=q=null,de=fe=ye=ve=He=null,le=re=-1,ie=null,be=me=Ce=xe=-1,we=0,T=!0}function applyDirectSnapshot(l,r){if(!l)return!1;var i=0;for(var a in l)Object.prototype.hasOwnProperty.call(l,a)&&Object.prototype.hasOwnProperty.call(e,a)&&(t[a]=coerceCfgValue(a,l[a]),i+=1);return!!i&&(x=!0,w=!0,P=!0,"shared"!==r&&writeSharedSnapshot(),invalidateEnemyVisualState(),!0)}function tryDecodeDirectPayload(t,l){if(!t)return!1;try{return applyDirectSnapshot(function(t){var l=null;try{l=JSON.parse(String(t||""))}catch(e){return null}var r=l&&"object"==typeof l?l.values||l:null;if(!r||"object"!=typeof r)return null;var i={};for(var a in r)if(Object.prototype.hasOwnProperty.call(r,a)){var n=Object.prototype.hasOwnProperty.call(e,a)?a:C[a];n&&Object.prototype.hasOwnProperty.call(e,n)&&(i[n]=coerceCfgValue(n,r[a]))}return i}(function(e){for(var t={},l=0;l<64;l++)t["ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789-_"[l]]=l;function val(e){if(void 0===e)return 0;if(!Object.prototype.hasOwnProperty.call(t,e))throw new Error("bad base64url");return t[e]}for(var r=[],i=0;i<e.length;i+=4){var a=val(e[i]),n=val(e[i+1]),o=void 0!==e[i+2]?val(e[i+2]):0,s=void 0!==e[i+3]?val(e[i+3]):0;r.push(a<<2|n>>4),void 0!==e[i+2]&&r.push((15&n)<<4|o>>2),void 0!==e[i+3]&&r.push((3&o)<<6|s)}for(var _="",u=0;u<r.length;u++){var h=r[u];if(h<128)_+=String.fromCharCode(h);else if(h<224)_+=String.fromCharCode((31&h)<<6|63&r[++u]);else{var p=r[++u],c=r[++u];_+=String.fromCharCode((15&h)<<12|(63&p)<<6|63&c)}}return _}(String(t))),l)}catch(e){}return!1}function tryApplyDirectBootstrap(){return function(){var e=getSharedStore();if(!e)return!1;var t="";try{t=String(e.__hpColorsCfgRaw||"")}catch(e){}if(!t||t===H)return!1;H=t;try{return applyDirectSnapshot(JSON.parse(t),"shared")}catch(e){}return!1}()||tryDecodeDirectPayload(function(){var e=function(){for(var e=$.GetContextPanel();e&&e.GetParent&&e.GetParent();)e=e.GetParent();return e||null}();if(!e)return"";try{if(e.GetAttributeString){var t=String(e.GetAttributeString("anita_v1_hp_colors","")||"");if(t)return t}}catch(e){}try{var l=e.FindChildTraverse?e.FindChildTraverse("Hud"):null;if(l&&l.GetAttributeString)return String(l.GetAttributeString("anita_v1_hp_colors","")||"")}catch(e){}return""}(),"session")||tryDecodeDirectPayload(function(){try{if("undefined"==typeof GameInterfaceAPI||!GameInterfaceAPI||!GameInterfaceAPI.GetSettingString)return"";var e=String(GameInterfaceAPI.GetSettingString("deadlock_hero_debuts_seen")||"").match(/\[ANITA-v1-hp_colors\]:([A-Za-z0-9_-]+)/);return e?e[1]:""}catch(e){}return""}(),"convar")}function requestBootstrap(e){var t=Date.now?Date.now():(new Date).getTime();if(!(F&&t-F<400)){F=t;try{$.DispatchEvent("ClientUI_FireOutput",JSON.stringify({magic_word:"ANITA_REQUEST_BOOTSTRAP",mod_title:"HP Colors",storageNamespace:"hp_colors",reason:String(e||"overlay_request")}))}catch(e){}}}function hexToRgb(e){if(!e)return[255,255,255];if("#"===e.charAt(0)){var t=e.slice(1);return 3===t.length&&(t=t[0]+t[0]+t[1]+t[1]+t[2]+t[2]),[parseInt(t.slice(0,2),16),parseInt(t.slice(2,4),16),parseInt(t.slice(4,6),16)]}var l=e.match(/rgb\((\d+),\s*(\d+),\s*(\d+)\)/);return l?[+l[1],+l[2],+l[3]]:[255,255,255]}function byteHex(e){var t=function(e){return e=Number(e),isFinite(e)?(e|=0)<0?0:e>255?255:e:0}(e).toString(16);return 1===t.length?"0"+t:t}function rgbToHex(e){return"#"+byteHex(e[0])+byteHex(e[1])+byteHex(e[2])}function normalizeWashColor(e){if("string"!=typeof e)return"";var t=e.trim();if(!t)return"";if("#"===t.charAt(0))return 4===t.length?("#"+t.charAt(1)+t.charAt(1)+t.charAt(2)+t.charAt(2)+t.charAt(3)+t.charAt(3)).toLowerCase():t.length>=7?t.slice(0,7).toLowerCase():t;var l=t.match(/^rgba?\(\s*(\d+)\s*,\s*(\d+)\s*,\s*(\d+)/i);return l?rgbToHex([l[1],l[2],l[3]]):t}function lerp(e,t,l){return e+(t-e)*l|0}function ip(e,t,l){return[lerp(e[0],t[0],l),lerp(e[1],t[1],l),lerp(e[2],t[2],l)]}function getHighColor(){return t.hp_team_colors?2===Q?"#6485FC":"#FFC961":t.hp_color_high}function getTextColor(e,l,r){return t.hp_text_color_mode?e<=l?t.hp_text_color_low:e<=r?t.hp_text_color_mid:t.hp_text_color_high:e<=l?t.hp_color_low:e<=r?t.hp_color_mid:getHighColor()}function getGradientTextColor(e,l,r){var i=Math.max(1,r-l),a=Math.max(1,100-r);if(t.hp_text_color_mode){if(e<=l)return t.hp_text_color_low;if(e<=r){var n=(e-l)/i;return rgbToHex(ip(hexToRgb(t.hp_text_color_low),hexToRgb(t.hp_text_color_mid),n))}var o=(e-r)/a;return rgbToHex(ip(hexToRgb(t.hp_text_color_mid),hexToRgb(t.hp_text_color_high),o))}if(e<=l)return t.hp_color_low;if(e<=r){var s=(e-l)/i;return rgbToHex(ip(hexToRgb(t.hp_color_low),hexToRgb(t.hp_color_mid),s))}var _=(e-r)/a,u=getHighColor();return rgbToHex(ip(hexToRgb(t.hp_color_mid),hexToRgb(u),_))}$.RegisterForUnhandledEvent("ClientUI_FireOutput",function(l){try{var r="string"==typeof l?JSON.parse(l):l;if(!r||"HP Colors"!==r.mod_title)return;if("ANITA_UPDATE"===r.magic_word){if(Object.prototype.hasOwnProperty.call(e,r.setting_id)){if("hp_counter_position"===r.setting_id&&"hp_counter_autoposition"===r.update_source)return;t[r.setting_id]=coerceCfgValue(r.setting_id,r.value),T=!0,"hp_level_number_visible"===r.setting_id&&(He=null),"hp_enabled"===r.setting_id&&t.hp_enabled&&!i&&(i=!0,$.Schedule(.05,gL)),"hp_friend_enabled"===r.setting_id&&t.hp_friend_enabled&&!a&&(a=!0,$.Schedule(.05,aL)),"hp_friend_enabled"!==r.setting_id||t.hp_friend_enabled||(isAllyPlayerFlags(g)?resetAllyBarColor(u,b,g):clearAllyPulse(),a=!1),!n||"hp_pulse_bpm"!==r.setting_id&&"hp_pulse_intensity"!==r.setting_id&&"hp_pulse_text_enabled"!==r.setting_id&&"hp_pulse_text_scale"!==r.setting_id||("hp_pulse_bpm"===r.setting_id&&(o=null,applyPulseDuration()),"hp_pulse_intensity"===r.setting_id&&applyPulseIntensity(),"hp_pulse_text_enabled"!==r.setting_id&&"hp_pulse_text_scale"!==r.setting_id||applyPulseTextState()),"hp_pulse_enabled"!==r.setting_id||t.hp_pulse_enabled||clearPulse(),writeSharedSnapshot()}return void(("bridge_bootstrap"===(s=String(r.update_source||""))||"ui_resync"===s||"ui_reset"===s||"ui_code_apply"===s||"core_auto_resync"===s||"ui_refresh_after_apply"===s)&&(x=!0,P=!0,invalidateEnemyVisualState()))}}catch(e){}var s});var I=$.GetContextPanel(),N=null,O=null,G=null,M=null,L=null,D=null,R=null,V=null,z=null,k=null,U=0,E=0,j=null,Z=null,K=null,W=null,J=null,q=null;function fRB(){return I.FindChildTraverse("unit_healthbar_lagging")||I.FindChildTraverse("health_bar")||I.FindChildTraverse("unit_health")}var Q=0,X=0,Y=null,ee=null,te=null,le=-1,re=-1,ie=null,ae=null,ne=0,oe="",se=!1,_e=null,ue=null,he=null,pe=null,ce=0;function sBC(e){var t=normalizeWashColor(e);if(Y!==t&&R)try{R.style.washColor=t,Y=t}catch(e){Y=null}}function sUC(e){var t=normalizeWashColor(e);if(z&&z.IsValid()||(z=I.FindChildTraverse("unit_ult_ready_icon")||I.FindChildTraverse("ult_icon")),z&&z.style&&ee!==t)try{z.style.washColor=t,ee=t}catch(e){ee=null}}function sTC(e){var t=normalizeWashColor(e);if(O&&O.style&&te!==t)try{O.style.washColor=t,te=t}catch(e){te=null}}function isAllyPlayerFlags(e){return!!(1&e&&2&e)}function resetAllyBarColor(e,t,l){if(clearAllyPulse(),e&&e.style){var r=function(e,t){return 1&t?"#FFEFD7":2===e?"#5B79E6":1===e?"#E7B659":"#FFEFD7"}(0|t,0|l);try{e.style.washColor=r,p=normalizeWashColor(r)}catch(e){p=null}}}function clampNum(e,t,l,r){var i=Number(e);return isFinite(i)||(i=Number(r)),isFinite(i)||(i=0),isFinite(t)&&i<t&&(i=t),isFinite(l)&&i>l&&(i=l),i}function sHBV(e){if(G&&G.style){"visible"!==j&&(G.style.visibility="visible",j="visible");var t=e?"1.0":"0.01";Z!==t&&(G.style.opacity=t,Z=t)}}var de=null,fe=null,ye=null,ve=null;function sKZ(e,l){if(k&&k.style)if(!e||!t.hp_kill_zone_enabled||l<=0){"collapse"!==de&&(k.style.visibility="collapse",de="collapse");try{k.style.opacity="0"}catch(e){}}else{var r=0|t.hp_kill_zone_threshold;r<0&&(r=0),r>100&&(r=100);var i=0|t.hp_kill_zone_width;i<1&&(i=1),i>20&&(i=20);var a=Math.round(l*r/100-i/2);a<0&&(a=0),a>l-i&&(a=Math.max(0,l-i));var n=a+"px",o=i+"px",s=normalizeWashColor(t.hp_kill_zone_color||"#FF2222");"visible"!==de&&(k.style.visibility="visible",de="visible");try{k.style.opacity="0.95"}catch(e){}try{k.style.zIndex="1000"}catch(e){}if(fe!==n&&(k.style.marginLeft=n,fe=n),ye!==o&&(k.style.width=o,ye=o),ve!==s)try{k.style.backgroundColor=s,ve=s}catch(e){ve=null}}}function parseCounterPositionValue(e){var t=0,l=200,r=e;if(r&&"object"==typeof r)return Array.isArray(r)?(r.length>0&&(t=clampNum(r[0],0,400,0)),r.length>1&&(l=clampNum(r[1],0,400,200))):(Object.prototype.hasOwnProperty.call(r,"x")&&(t=clampNum(r.x,0,400,0)),Object.prototype.hasOwnProperty.call(r,"y")&&(l=clampNum(r.y,0,400,200))),{x:t,y:l};if("string"==typeof r){var i=r.match(/-?\d+(?:\.\d+)?/g);if(i&&i.length>0)return t=clampNum(i[0],0,400,0),i.length>1&&(l=clampNum(i[1],0,400,200)),{x:t,y:l}}return"number"==typeof r?{x:t,y:l=clampNum(r,0,400,200)}:{x:t,y:l}}function sHCS(e,l){if(O&&O.style){var r,i=!!(e&&t.hp_pulse_enabled&&t.hp_pulse_text_enabled),a=clampNum(t.hp_counter_size,72,400,120),n=i?(r=a,clampNum(t.hp_pulse_text_scale,72,320,r)):a,o=parseCounterPositionValue(i?t.hp_pulse_text_position:t.hp_counter_position),s=String(void 0!==l?l:oe||""),_=O&&O.actuallayoutwidth>0?O.actuallayoutwidth:N&&N.actuallayoutwidth>0?N.actuallayoutwidth:V&&V.actuallayoutwidth>0?V.actuallayoutwidth:R&&R.actuallayoutwidth>0?R.actuallayoutwidth:0,u=function(e){for(var t=String(e||""),l=0,r=0;r<t.length;r++){var i=t.charCodeAt(r);l+=i>=48&&i<=57?1:32===i?.35:47===i?.55:58===i||46===i?.4:.8}return l>0?l:1}(s),h=_>0?Math.floor((_-12)/(.45*u)):n;h=clampNum(h,72,400,n);var p=Math.min(n,h),c=clampNum(o.x,0,400,0),d=clampNum(o.y,0,400,200);t.hp_bg_visible||i?_e=null:function(e){var t,l=(t=parseCounterPositionValue(e),Math.round(t.x)+","+Math.round(t.y));if(_e!==l){_e=l,T=!0;try{$.DispatchEvent("ClientUI_FireOutput",JSON.stringify({magic_word:"ANITA_UPDATE",mod_title:"HP Colors",setting_id:"hp_counter_position",value:l,update_source:"hp_counter_autoposition",skip_bridge_persist:!0}))}catch(e){}}}({x:c=Math.min(Math.round(.025*n),8),y:d=Math.min(Math.round(.5*n),150)});var f=!t.hp_bg_visible&&n>=320?"8%":Math.round(c)+"%",y=p+"px",v=130;try{var g=O.GetParent?O.GetParent():null;g&&g.actuallayoutheight>0&&(v=g.actuallayoutheight)}catch(e){}var b=i?Math.max(v,Math.round(1.85*p)):v,m=i?d*v/b:d,C="-"+Math.round(m)+"%",x=i?b+"px":"100%";K!==y&&(O.style.fontSize=y,K=y),W!==C&&(O.style.marginTop=C,W=C),J!==f&&(O.style.marginLeft=f,J=f),q!==x&&(O.style.height=x,q=x)}}var ge=0,be=-1,me=-1,Ce=-1,xe=-1,we=0;function gL(){i=!0;try{if(!t.hp_enabled)return clearPulse(),sKZ(!1,0),sBC(""),sUC(""),G&&G.style&&(G.style.visibility="collapse",G.style.opacity="0",j="collapse",Z="0"),O&&O.style&&(O.style.fontSize="",O.style.marginTop="",O.style.marginLeft="",O.style.height="",sTC(""),K=null,W=null,J=null,q=null),void(i=!1);var e=Date.now();if(!R&&!(R=fRB()))return void $.Schedule(.15,gL);if(!U&&!(U||!(E>=10)&&(E++,N&&N.IsValid()||(N=I.FindChildTraverse("UnitStatus")),N&&(O&&O.IsValid()||(O=N.FindChildTraverse("hp_counter")),G&&G.IsValid()||(G=N.FindChildTraverse("unit_healthbar_bg")),M&&M.IsValid()||(M=N.FindChildTraverse("unit_healthbar_pip_label")),L&&L.IsValid()||(L=N.FindChildTraverse("unit_healthbar_lagging")),k&&k.IsValid()||(k=N.FindChildTraverse("hp_kill_zone_marker")),!L||D&&D.IsValid()||(D=L.GetParent()),M&&L&&D&&(U=1,1)))))return void $.Schedule(.15,gL);if(R===ue&&O===he&&G===pe||(ue=R,he=O,pe=G,ce=Date.now(),clearPulse(),j=Z=K=W=J=q=null,de=fe=ye=ve=null,He=null,le=-1,re=-1,ie=null,be=-1,me=-1,Ce=-1,xe=-1,we=0,se=!1,T=!0),R.GetParent){var r=R.GetParent();V!==r&&(V=r)}!function(e){for(var t=0,l=0,r=0,i=e;i&&r<10&&!(i.BHasClass&&(t||(i.BHasClass("team2")?t=2:i.BHasClass("team1")&&(t=1)),1&l||!i.BHasClass("enemy")||(l|=1),2&l||!i.BHasClass("team_neutral")&&!i.BHasClass("neutral")||(l|=2),4&l||!(i.BHasClass("building")||i.BHasClass("boss_tier1")||i.BHasClass("boss_tier2")||i.BHasClass("boss_barracks"))||(l|=4),8&l||!i.BHasClass("friend")||(l|=8),t&&15&l))&&i.GetParent;)i=i.GetParent(),r++;Q=t,X=l}(R);var a=!(!(1&X)||2&X);if(a&&!w&&e-A>=1e3&&(A=e,tryApplyDirectBootstrap()),t.hp_skip_buildings&&4&X)return clearPulse(),a?(G&&G.style&&(G.style.visibility="collapse",G.style.opacity="0",j="collapse",Z="0"),sBC("#e16161"),sUC("#e16161"),sTC("#e16161"),sHBV(!!t.hp_bg_visible),sKZ(!1,0),void $.Schedule(.5,gL)):(sBC(""),sUC(""),sTC(""),sHBV(!0),sKZ(!1,0),void $.Schedule(.5,gL));if(a&&e-B>=1e3&&(B=e,invalidateEnemyVisualState()),T?function(e){sHBV(!e||!!t.hp_bg_visible),e&&t.hp_bg_visible||sKZ(!1,0),sHCS(se,oe),T=!1}(a):sHBV(a&&!!t.hp_bg_visible),2&X)return clearPulse(),sHBV(!0),sKZ(!1,0),sBC("#5BEFB5"),sTC("#ffffff"),ge=e,void $.Schedule(1.5,gL);if(!(1&X))return sHBV(!0),sKZ(!1,0),sBC(""),sUC(""),ge=e,void $.Schedule(.4,gL);var o=0|R.actuallayoutwidth,s=V&&void 0!==V.actuallayoutwidth?0|V.actuallayoutwidth:0;if(o===be&&s===me&&!n)return e-ge>2e3?void $.Schedule(1,gL):void $.Schedule(.15,gL);if(be=o,me=s,ge=e,s<=0)return void $.Schedule(.18,gL);sKZ(!0,s);var u=o/s*100|0,h=0|t.hp_low_threshold,p=0|t.hp_high_threshold;if(h<0&&(h=0),h>100&&(h=100),p<0&&(p=0),p>100&&(p=100),p<h&&(p=h),Math.abs(u-Ce)<3&&u>h&&Ce>h&&!n)return void $.Schedule(.15,gL);var c=Ce;u===xe?we++:(we=0,xe=u),Ce=u;var d="";if(M)try{d=M.text||M.GetAttributeString("text","")||""}catch(e){d=""}if(L&&D){var f=L.actuallayoutwidth||0,y=D.actuallayoutwidth||0,v=y>0?f/y:0,g=function(e){if(e===ae)return ne;ae=e;for(var t=0,l=0,r=e.lastIndexOf("|"),i=0;i<e.length;i++){var a=e.charCodeAt(i);124===a?t++:34!==a&&39!==a||!(-1===r||i>r)||l++}return ne=500*t+100*l}(d);!function(e,l,r){if(O&&(e!==le||l!==re)){var i;"visible"!==ie&&(O.style.visibility="visible",ie="visible"),i=1==(0|t.hp_counter_format)?(l>0?Math.round(e/l*100):0)+"%":e+" / "+l;try{O.text!==i&&(O.text=i)}catch(e){try{O.SetAttributeString("text",i)}catch(e){}}sHCS(se=!!r,oe=i),le=e,re=l}}(v>=.97?g:Math.round(g*v),g,u<=h)}var b,m,C=.15,x=0|t.hp_pulse_threshold;if(u<=h){if(ce&&e-ce<900&&(c<0||c<=h&&u>c)){var S=getHighColor();return clearPulse(),sBC(S),sUC(S),sTC(getTextColor(100,h,p)),void $.Schedule(.05,gL)}sHBV(!t.hp_pulse_hide_bar&&!!t.hp_bg_visible),1===t.hp_mode?(b=t.hp_color_low,m=t.hp_text_color_mode?t.hp_text_color_low:t.hp_color_low):(sBC(t.hp_color_low),m=getTextColor(u,h,p)),t.hp_pulse_enabled&&u<=x?(n||function(){if(!n){n=1,Y=ee=te=null;try{R&&R.AddClass(l)}catch(e){}try{z&&z.AddClass(l)}catch(e){}applyPulseIntensity(),applyPulseDuration()}}(),function(e){if(O&&O.style&&n&&t.hp_pulse_text_enabled){var l=clampNum(t.hp_pulse_bpm,30,300,75),r=Math.max(1,6e4/l),i=e%r/r,a=i<.5?2*i:2*(1-i),o=0|clampNum(t.hp_pulse_intensity,0,2,1),s=2===o?.55:0===o?.85:.65,u=(s+((2===o?2:0===o?1.15:1.5)-s)*a).toFixed(2);if(_!==u)try{O.style.brightness=u,_=u}catch(e){_=null}}}(e),t.hp_pulse_text_enabled&&(C=.05)):n&&clearPulse(),sTC(m),sUC(t.hp_color_low),1===t.hp_mode&&(sBC(b),sUC(b))}else{clearPulse(),sHBV(!!t.hp_bg_visible);var P=Math.max(1,p-h),F=Math.max(1,100-p),H=getHighColor();u<=p?1===t.hp_mode?(b=rgbToHex(ip(hexToRgb(t.hp_color_low),hexToRgb(t.hp_color_mid),(u-h)/P)),m=getGradientTextColor(u,h,p)):(b=t.hp_color_mid,m=getTextColor(u,h,p)):(1===t.hp_mode?(b=rgbToHex(ip(hexToRgb(t.hp_color_mid),hexToRgb(H),(u-p)/F)),m=getGradientTextColor(u,h,p)):(b=H,m=getTextColor(u,h,p)),we>=5&&(C=Math.min(.15*Math.pow(2,Math.floor(we/5)),1))),sBC(b),sUC(b),sTC(m)}$.Schedule(C,gL)}catch(e){$.Schedule(.5,gL)}}var Se=[11,19,27,35],Pe=["level_tier2","level_tier3","level_tier4","level_tier5"],Te=null,Fe=null,Ae=null,Be=-1,He=null;function aL(){a=!0;try{if(!t.hp_friend_enabled)return isAllyPlayerFlags(g)?resetAllyBarColor(u,b,g):clearAllyPulse(),void(a=!1);var e,i,n=Date.now();if(!(u&&u.IsValid()||(u=null,h=null,g=-1,m=0,u=fRB())))return void $.Schedule(.2,aL);if(u.GetParent){var o=u.GetParent();h!==o&&(h=o)}if(g<0||n-m>=2e3){i=0,e=0;for(var s=0,_=u;_&&s<10&&!(_.BHasClass&&(i||(_.BHasClass("team2")?i=2:_.BHasClass("team1")&&(i=1)),1&e||!_.BHasClass("friend")||(e|=1),2&e||!_.BHasClass("player")||(e|=2),i&&3&e))&&_.GetParent;)_=_.GetParent(),s++;g=e,b=i,m=n}else e=g,i=b;if(!isAllyPlayerFlags(e))return clearAllyPulse(),p=null,f=0,void $.Schedule(1.5,aL);var C=0|u.actuallayoutwidth,x=h&&void 0!==h.actuallayoutwidth?0|h.actuallayoutwidth:0;if(x<=0)return void $.Schedule(.2,aL);if(C===c&&x===d&&!y){f++;var w=Math.min(.35*Math.pow(2,Math.floor(f/3)),2);return void $.Schedule(w,aL)}f=0,c=C,d=x;var S=C/x*100|0,P=0|t.hp_low_threshold,T=0|t.hp_high_threshold;P<0&&(P=0),P>100&&(P=100),T<0&&(T=0),T>100&&(T=100),T<P&&(T=P);var F=0|t.hp_friend_pulse_threshold,A=t.hp_friend_pulse_enabled&&S<=F,B=normalizeWashColor(A&&t.hp_friend_pulse_color_enabled?t.hp_friend_pulse_color:1===t.hp_mode?S<=P?t.hp_friend_color_low:rgbToHex(S<=T?ip(hexToRgb(t.hp_friend_color_low),hexToRgb(t.hp_friend_color_mid),(S-P)/Math.max(1,T-P)):ip(hexToRgb(t.hp_friend_color_mid),hexToRgb(t.hp_friend_color_high),(S-T)/Math.max(1,100-T))):S<=P?t.hp_friend_color_low:S<=T?t.hp_friend_color_mid:t.hp_friend_color_high);if(p!==B&&u)try{u.style.washColor=B,p=B}catch(e){p=null}var H=.35;if(A){if(!y){y=1,v=-1,p=null;try{u&&u.AddClass(l)}catch(e){}var I=0|Number(t.hp_friend_pulse_intensity);(I<0||I>2)&&(I=1),v=I;var N=r[I];if(N)try{u.AddClass(N)}catch(e){}var O=Number(t.hp_friend_pulse_bpm)||75;O<30&&(O=30),O>300&&(O=300);var G=(60/O).toFixed(3)+"s";try{u&&(u.style.animationDuration=G)}catch(e){}}H=.15}else y&&(clearAllyPulse(),p=null);$.Schedule(H,aL)}catch(e){$.Schedule(.5,aL)}}tryApplyDirectBootstrap(),i=!0,gL(),a=!0,aL(),function lL(){(function(){if(Te&&Te.IsValid()||(Te=I.FindChildTraverse("unit_level_label")),Fe&&Fe.IsValid()||(Fe=I.FindChildTraverse("LevelContainer")),!Fe||Ae&&Ae.IsValid()||(Ae=function(e){for(var t=e;t;){if(t.BHasClass&&t.BHasClass("enemy"))return t;if(!t.GetParent)break;t=t.GetParent()}return null}(Fe)),Te&&Fe&&Ae){!function(){if(Te&&Te.style){var e=t.hp_level_number_visible?"visible":"collapse";He!==e&&(Te.style.visibility=e,He=e)}}();var e="";try{e=Te.text||Te.GetAttributeString("text","")||""}catch(t){e=""}if(e&&123!==e.charCodeAt(0)){var l=function(e){for(var t=0,l=0;l<e.length;l++){var r=e.charCodeAt(l)-48;r>=0&&r<=9&&(t=10*t+r)}return t}(e);if(l!==Be&&l){Be=l;for(var r=0;r<4;r++)Ae.RemoveClass(Pe[r]);for(var i=3;i>=0;i--)if(l>=Se[i]){Ae.AddClass(Pe[i]);break}}}}})(),$.Schedule(.5,lL)}(),$.Schedule(.05,function scheduleBootstrapRetry(){if(!x&&!P&&!tryApplyDirectBootstrap()){if(S>=8)return requestBootstrap("overlay_slow_retry"),void $.Schedule(3,scheduleBootstrapRetry);requestBootstrap(1===(S+=1)?"overlay_startup":"overlay_retry"),$.Schedule(.5,scheduleBootstrapRetry)}})}();
\ No newline at end of file
